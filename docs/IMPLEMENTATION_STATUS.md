# BlueLink 实现状态

## 2026-09-21 0.2.18 发布基线

- VERSION升级到0.2.18，Android versionCode从12升至13；预览标签为v0.2.18-preview.1，构建内嵌完整标签，继续采用GitHub Actions正式Android签名及Windows Review预览渠道。
- 汇总此前消息搜索/选择/批量操作、日期范围、双端连续图片预览、输入与文件分享、传输恢复、设备备注、存储清理和应用内更新改动。收藏功能仅保留后续版本设计，不包含在本版。
- 发布前验证：协议45项；Android单元测试356项（0失败/0错误，2项既有跳过），lint 0错误/30警告，全部ABI Debug/Release构建通过；Windows完整回归通过；版本/UI/安装器契约、14项Release附件测试与三次包保留回归通过。
- 修复旧Windows综合回归自身与现有UI不符的问题：消息行不再固定留白，改验行不覆盖滚动条与气泡至少8 DIP间隔；搜索测试使用Dispatcher同步上下文等待异步查询，并验证现有分类、空态和独立日期范围，不再引用已移除的日期分类。
- 实际私有桌面UIA复验搜索选区与搜索命中叠加（明暗主题），日期重置/重新选择/确认/重开循环10次；查看本版截图，用户前台保持不变。证据：.acceptance/release-0218-*。
- 限制：ARM64原生硬件、全架构完整安装/升级/卸载矩阵及真实跨版本正式签名Android覆盖仍为 **Not verified**；Windows既有RuntimeDetector可空警告保留。发布附件继续由CI独立构建、签名和清单哈希校验。


## 2026-09-20 Windows 搜索结果交互、文本选区与 Android 预览提示

- 消息搜索左键文件打开/图片预览，纯文本支持只读原生选择；右键与更多打开菜单。选区复制优先选中片段，多选模式接管行操作，退出恢复文本选择。
- 聊天与搜索手动选区为浅青蓝底深色字；搜索命中保持浅金色。选区部分/完全/跨词覆盖命中时，选区颜色优先，取消恢复命中。聊天启用WPF官方非装饰层选区绘制；搜索富文本通过TextRange格式避免选区叠加遮字，未新增基础模板。
- 按用户更正，仅Android图片首尾短提示使用圆圈i信息图标，预览内主题化Material提示约2秒自动消失，宽度随内容自适应居中（最大360dp），位于图片区底部、距工具栏12dp，显示时暂时隐藏手势说明；Windows提示保持原样。Android离线lint0错误/30既有警告、APK构建和保留数据覆盖安装通过；浅/深真机首尾提示及消失8图已验，已恢复原会话，最新布局及首尾自动消失证据.acceptance/android-preview-notice-layout-phone。
- Release0错误、UI契约、最终2366项定向WPF/UIA检查/104图通过；独立非输入桌面24条断言/12图验证两主题选区叠加、复制与恢复，右键以路由事件打开真实原生菜单。证据.acceptance/search-selection-native、search-click-final-*、search-click-preview-restored。物理拖选/右键及外部文件打开端到端 **Not verified**；尚未重新打Windows包。

## 2026-09-20 消息与文件操作细化

- Windows聊天/文件管理菜单已按打开（图片预览）、复制文件、复制文件名、多选、在文件夹中显示、另存为、查看文件详情、删除本机消息统一；搜索文件最前保留定位原消息，搜索文本不再显示消息详情，状态专属传输操作保留。
- 本机文本搜索命中使用浅金底深色字，避免白字与正文无法区分；多选行统一布局外边距，UIA反复勾选/取消不会改变复选框位置或尺寸。图片预览右键提供复制文件/文件名、另存为、在文件夹中显示，操作跟随当前图片，失效文件禁用磁盘操作。
- Android消息搜索日期行去除上下各8dp外边距，64dp收至48dp，保留48dp点击高度；lint0错误/30既有警告、APK构建与保留数据覆盖安装通过。真机前后截图及UI层级、打开日历已验，已返回原会话（.acceptance/android-message-search-spacing-phone）。
- Windows Release0错误、UI契约及2664项定向回归/140图通过（.acceptance/message-refinements-*），验证项目存在6条既有RuntimeDetector可空警告。旧综合搜索场景的传输进度环/选中背景断言尚未通过；物理鼠标/资源管理器/另存为端到端及Android其他字号/深色 **Not verified**。本次Windows修改尚未重新打包。

## 2026-09-20 双端图片连续预览与 Android 间距

- Android 文件页搜索框已按会话页统一最小高度42dp、圆角10dp；其他共用搜索框保留默认参数。lint0错误/30警告、APK构建及保留数据覆盖更新通过，真机空值/输入/清空高度稳定、圆角对齐，已恢复原会话（.acceptance/android-search-field-match-phone）。其他字号和深色真机 **Not verified**。
- 图片预览标题栏关闭图标已替换为矢量交叉线，并按用户反馈将初版14×14收至12×12、笔画1.4收至1.3 DIP，以平衡完整交叉线与分段全屏图标的视觉重量，32×32点击区和行为保持不变。普通/全屏与明暗截图、Release0错误/0警告、UI契约及901项真实WPF/UIA回归通过（.acceptance/preview-header-icons-balanced，36图）；这项追加修改尚未重新打包。
- Android 会话/文件/设置主页内容顶距统一12dp，会话搜索框下方列表顶距缩至8dp。图片预览手指左滑下一张、右滑上一张；当前图与邻图跟手移动，松手220ms平滑落位，短拖回弹、首尾阻尼及短暂info提示、不循环。邻图预载复用避免切图闪跳；放大平移/多指不翻页，双击重置保留。
- Windows 图片预览靠近左右边缘才显示对应箭头，移开隐藏，无按钮底色。根据用户再次对照反馈，替换硬裁剪细线为渐淡的多层矢量边缘阴影，保留约70%不透明白色填充，并按参考修正轮廓比例。实际按钮同尺寸/同棋盘格位置对照及完整预览证据.acceptance/preview-arrow-rematch（替代preview-arrow-reference）；Release0错误、UI契约和901项真实WPF/UIA检查/32图通过，无前台窗口。
- 两端聊天、搜索、文件管理按当前已加载/筛选排序列表浏览本地可打开图片；失效文件跳过，不加载更早历史。Android前轮完整353项通过/2跳过；本次动画专项3项、lint0错误/30警告、APK构建及保留数据覆盖安装通过。真机中间帧两图跟手、落位、短拖/首尾回弹、方向、放大平移及双击重置已验；录屏与截图见.acceptance/image-slide-phone，构建日志image-slide-build-final.log。
- 三个主页间距及聊天/文件入口顺序前轮已真机验收。本次手机已恢复原会话。Windows原生物理鼠标/跨屏DPI、Android多指物理输入及搜索入口真机翻页 **Not verified**。Windows已纳入E:/BlueLink/artifacts/windows-latest-20260920-image-preview（0.2.17 x64 Review、内置.NET 8、未签名），实际发布程序集901项/32图及包校验通过；源产物和交付均只保留最近3套。未安装或重启用户客户端。

## 2026-09-20 Windows 日期筛选修复

- 消息搜索分类、日期与结果数合并为单行，窄窗口仍保持同行。日历日期按钮为32×32，官方今天/选中高亮为正圆；所有日期可见可选，范围冲突时保留新选择、另一端清空为不限。
- 文件日期筛选最新源码已修正按下日期即销毁日历的时序：等待鼠标释放及日历事件完成后只关闭子浮层，清理未执行关闭任务；父捕获协调、确认提交/重置清空保留。独立不可见桌面10轮重置/重选回归（20次选择、4图）及409项日期离屏回归通过；首轮原生窗口消息，其余为真实UIA+路由输入。用户当前桌面的物理鼠标端到端 **Not verified**。此补充修复已纳入 E:/BlueLink/artifacts/windows-latest-20260920-date-popup（0.2.17 x64 Review、内置.NET 8、未签名）；实际发布程序集日期409项/42图、独立桌面10轮重置重选及打包门禁通过。交付与源产物均仅保留最近3套；未运行安装/升级。
- Release、UI契约以及3790项离屏回归/176图通过。证据.acceptance/date-range-fix-final、date-range-file-regression、date-range-search-regression。未占用桌面前台；已纳入E:/BlueLink/artifacts/windows-latest-20260920-date-range（0.2.17 x64 Review、内置.NET 8、未签名），实际发布版本日期409项/42图、文件筛选多选2119项/78图及包校验通过。仅保留最近3套；安装/升级未实测。

## 2026-09-19 Windows 日期与文件操作界面细化

- 文件筛选与消息搜索共用仅选择日期的浮层日历，保留今天/匹配日期标记。文件日期面板为紧凑整行选项；搜索按开始/结束按钮展开，有日期才显示清除，清除保留关键词与消息类型。
- 文件搜索右侧更多入口已删除；批量确认不列文件名，文件删除复用消息删除的共用弹窗与取消/删除语义，只说明文件数量、跳过规则及不删除实体文件的影响。
- 搜索结果文件名和大小同行，去掉重复完成状态；单条底部状态/时间保留收发状态，以及异常或进行中的附件状态。
- Release、UI 静态门禁与 3646 项离屏回归/164 图通过，覆盖三语明暗与真实 UIA。证据 .acceptance/windows-date-file-polished、windows-date-search-polished、windows-date-polished。未创建前台窗口；原生浮层鼠标交互与跨屏放置 **Not verified**。本轮已进入 E:/BlueLink/artifacts/windows-latest-20260920（0.2.17 x64 Review，内置.NET 8，未签名）；实际发布程序集日期265项/30图、消息搜索多选1262项/56图及打包门禁通过。交付与源产物均仅保留最近3套；实际安装/升级未验证。

## 2026-09-19 Windows 多选与搜索/粘贴完善

- 聊天/搜索/文件多选已选行铺满浅灰背景，复选框间距缩小，官方复选框保留UIA和稳定勾显示。搜索采用本机右、对端左气泡；图片缩略图保留文件名，移除发送者标签，底部状态后显示时间，日期继续分组。
- 复制后蓝联输入框可按原顺序恢复文本和文件附件；普通文本粘贴后不保持选中。原生剪贴板格式仍供其他应用使用，其他应用是否同时消费两种内容取决于接收端；不保证外部应用保留蓝联私有顺序。
- 文件多选加入“选择到这里”，移除选择前100项，底部操作右对齐，右键不选行。文件日历标记今天与匹配文件日期；查询保留旧行至新结果就绪、固定滚动条留白，并移除加载后修补模板内边距，空/一/多行实际列宽保持一致。
- Release构建与UI规范通过；3740项针对性离屏检查、155图，含真实控件UIA和HWND为0断言。证据.acceptance/windows-refinements-final-message-batch、windows-refinements-file-final、windows-refinements-final-composer、windows-refinements-final-message-history及对应日志。
- 无前台操作，真实物理输入/跨进程剪贴板 **Not verified**；已纳入windows-latest-20260919-refinements安装包（0.2.17 x64、内置.NET 8、未签名预览）。实际安装/升级未运行；交付及源产物均仅保留最近3套。

## 2026-09-19 Windows 自动连接失败提示修正

- 启动自动连接和后台重连失败仅保留状态/诊断日志，不再弹出无法连接或超时Toast；手动连接失败/超时仍即时反馈，用户取消/销毁保护保留。删除原自动失败一分钟提醒冷却，不改变重连策略或信任判断。
- Release构建、43项定向回归和UI规范通过；两个设备共60次自动失败无提醒事件，手动反馈/日志/取消覆盖，6张实际WPF控件离屏三语明暗图。证据.acceptance/automatic-connection-notice.log及同名目录，原生HWND为0。
- 未运行前台或真实蓝牙重连验收（**Not verified**）；已纳入windows-latest-20260919-refinements安装包。

## 2026-09-19 Windows 范围选择方向稳定

- 聊天/搜索取消非起点消息不再重设范围起点；范围选到上方再取消上方端点时，“选择到这里”仍回到上方，下方同理。原起点取消/删除则选取最近剩余已选消息，零选择清空起点，UIA/框选/刷新采用相同校验。
- 消息/搜索1162项52图、历史70项2图、Release构建及UI规范通过；上下方向重复选择/取消、三语明暗搜索与起点边界均有回归。证据：.acceptance/range-anchor-verified、range-anchor-history及对应日志。
- 未使用Windows前台，真实物理输入 **Not verified**；本次及上一轮交互修复均已纳入windows-latest-20260919-selection安装包。

## 2026-09-19 Windows 框选与搜索多选反馈修正

- 聊天/搜索框选在松开鼠标时提交；拖动仅展示选框，不提前改已选数量。命中仅看消息内容行纵向覆盖，保留单行高度50%阈值，横向不必接触气泡；纯竖直、反向、取消选择和边缘滚动均覆盖。
- 搜索结果鼠标操作菜单从点击坐标展开，键盘入口保留结果锚点；复选框保留官方控件/UIA，组合稳定勾图标直接跟随IsChecked，避免官方裁剪动画使勾显示不全，官方尺寸和内边距已对齐。
- Release构建、UI规范通过；多选/搜索1072项50图、历史70项2图后台回归通过，实际查看浅深主题勾选截图。证据：.acceptance/selection-refinements-verified、selection-refinements-history及对应日志。
- 未操作Windows前台；真实物理输入、跨屏菜单与长时间虚拟化滚动 **Not verified**。已纳入windows-latest-20260919-selection安装包。

## 2026-09-19 Windows 聊天历史无按钮预加载

- 已移除加载更早消息按钮，向上接近顶部240 DIP时自动加载上一批200条，串行去重并缓存历史终点；打开会话仍从最新消息开始。
- 在插入前记录当前可见消息ID与像素偏移，插入后按实际布局恢复；历史补入不会被当作本机新发送而滚底，选中消息保持。框选活动期间暂缓插入，旧会话/A→B→A异步结果不会混入；状态更新不再误触发滚底，实时新消息保持原有阅读/发送行为。
- --message-history70项2图通过：变高文本、图片、文件、日期分组的锚点及首个Render回调偏移≤1 DIP，实际滚动UIA触发、终点、重入、实时消息及切会话验证；原多选/范围手势930项50图、Release构建与UI规范通过。证据：.acceptance/windows-history-first-frame、windows-history-selection-regression及对应日志。
- 原生鼠标滚动/真实显示器帧呈现 **Not verified**；未操作Windows前台、未重启客户端；已纳入下述 windows-latest-20260919-history 安装包。

## 2026-09-19 Windows 统一复制、紧凑多选与范围选择

- 聊天/搜索多选统一为复制和删除，复制同时封装Unicode文本及可用实际文件；不再单独显示复制文件。文件重复路径去重，当前传输状态优先；未完成、待恢复或缺失附件明确阻止复制。接收应用如何选用多格式由其自身决定，不保证外部应用保留文字与附件交错顺序。
- 收紧复选框左间距，聊天底栏满宽、按钮靠右；聊天/搜索均去掉全选及Ctrl+A。复制成功、删除有成功项后退出多选；取消、复制失败、全部删除被跳过时保留选择。文本右键菜单补齐多选图标，删除独立分组。
- 两处均新增选择到这里（仅完整可见气泡作为边界）和多选状态框选。框选纵向须覆盖至少单行气泡高度50%、横向实际相交，排除时间/头像；支持取消已有选择、反向拖动、缩框还原、边缘滚动及丢失捕获回滚。
- Release构建和UI规范通过；最终--message-batch930项、50张三语明暗/范围框选离屏截图通过，包括真实控件UI Automation、隔离数据库和可注入剪贴板发布路径，原生HWND为0。证据：.acceptance/windows-selection-gestures-final.log及同名目录。
- 实际系统剪贴板与外部应用粘贴、原生鼠标手势端到端 **Not verified**；未操作Windows前台、未重启客户端。上述源码修复已纳入下述 windows-latest-20260919-history 安装包。

## 2026-09-19 最新 Windows 安装包交付

- 最新候选E:/BlueLink/artifacts/windows-latest-20260919-selection，0.2.17 x64 Review EXE/MSI，内置.NET8、未签名预览，附SHA256SUMS/build-manifest。包含框选释放提交、纵向命中、搜索菜单点击定位与稳定勾显示、范围选择方向修复及历史自动加载。
- 产物结构、架构/运行库、MSI/Burn门禁通过；实际发布程序集与安装载荷哈希一致，多选/搜索1162项52图、历史70项2图通过；交付哈希匹配。证据.acceptance/windows-package-selection-refined-20260919.log、refined-package-message-batch.log、refined-package-message-history.log。
- 持续保留规则：Windows成功打包/校验后只保留最近3次；公共打包器自动执行，DeliveryDirectory支持交付根目录同步保留。当前另保留windows-latest-20260919-history和windows-latest-20260919；已删除128个旧包/副本约12.54GiB，Android/Data/Download/测试证据保留。隔离保留测试及再次预览待删除0项通过。
- 未使用Windows前台、未安装或重启；真实安装/升级矩阵 **Not verified**，未发布GitHub Release。新自动清理钩子未经重复完整构建，保留脚本已在隔离目录和实际产物目录执行验证。

## 2026-09-19 Windows 多选交互统一

- 聊天与搜索均支持顶部数量/取消、底部复制/纯附件复制文件/删除，Shift+单击连续选择，Ctrl+A/Ctrl+C/Esc；全选只覆盖已加载消息/结果。搜索结果菜单定位优先，新增多选，保留桌面复制文件和资源管理器入口，退出多选保留搜索条件。
- 文件多选改为顶部数量/取消、底部四项直接操作；无适用项目时禁用，结合设备连接、文件状态和可读性；保留操作确认、逐项复核和部分结果，最多100项，Shift范围超限不部分选择。列表刷新及同ID快照替换保留选择，批量执行时锁定操作，离开会话/文件视图清理选择。
- 搜索批量删除覆盖聊天窗口未加载的同会话历史记录，保留活动/待恢复消息和物理附件；成功删除不会被旧搜索快照恢复。
- Release构建、UI规范通过；消息/搜索746项42图、文件1981项60图、搜索动作143项通过。实际WPF控件/UI Automation Provider、隔离数据库与三语明暗离屏截图证据：.acceptance/windows-selection-message、windows-selection-file、windows-selection-search。没有创建原生窗口或触碰真实剪贴板。
- 按用户要求未使用Windows前台验收、未重启运行中客户端，未打新安装包；真实输入/系统剪贴板落地/生产传输联动/完整DPI矩阵 **Not verified**。

## 2026-09-19 搜索结果消息多选

- Android附件搜索菜单采用定位原消息、打开/预览图片、复制文件名、多选、分享、另存为、查看文件详情、删除本机消息顺序；纯文本提供对应的定位、复制、多选、分享、删除。
- 搜索页支持消息多选，上方数量/取消、下方复制/分享/删除；复用消息框批量操作与分享封装、删除校验，以及同一完整可见范围选择组件。范围仅覆盖当前搜索结果，日期标题不参与；退出后保留搜索条件。
- 搜索全量历史与实时记录后台合并；历史独有消息可参与批量操作，成功删除的项不会被旧加载快照恢复，失败项保留。
- 测试353项0失败（2项既有跳过）、最终构建/lint通过；USB真机验证八项菜单、文本/附件多选复制、系统分享选择器、隔离历史删除、连续选择/整行勾选、退出与深色英文，另核对真实会话入口及原消息框。证据.acceptance/search-multiselect-device。未向外部应用实际投递或删除用户数据；横屏/大字体/进程重建/超大历史性能矩阵 **Not verified**。

## 2026-09-19 独立文件管理多选导航收起

- 独立文件管理进入多选时隐藏会话/文件/设置底部导航，保留底部四项批量操作。取消勾选至0项仍隐藏；顶部取消或Android返回退出多选后恢复。仅独立文件管理接入，不改变会话内文件页导航。
- 构建/lint通过，USB真机保留数据安装；真实UIA/截图验证进入、0项、取消/返回恢复及设置/会话/文件切换，证据.acceptance/file-selection-nav-device。旋转/进程重建/完整DPI矩阵 **Not verified**。

## 2026-09-19 文件多选上下布局与连续选择

- Android文件多选顶部显示数量/取消，底部直接提供分享、重试、取消传输、删除记录；按选择实时更新状态，没有可重试的本机发送失败项时重试禁用，没有未结束传输时取消传输禁用，零选择全部禁用。保留原逐项检查、确认和执行规则。
- 新增与消息多选一致的“选择到这里”：仅完整可见文件作为终点，按当前展示顺序连续选取，跳过折叠分组，支持上下方向并保留原选择；超过100项整次拒绝并提示。重试改为环形箭头，长按文件菜单同步。
- 351项单测及最终构建/lint通过，USB真机保留数据安装。隔离真机场景验证布局、动态状态门禁、上下范围与部分露出排除、确认/退出、深色英文；证据.acceptance/file-batch-layout-device。未执行真实批量传输/分享/删除；横屏/大字体完整矩阵 **Not verified**。

## 2026-09-19 文件操作菜单与多选入口

- Android文件长按菜单统一打开/预览图片、复制文件名、多选、分享、另存为、查看文件详情、删除本机消息的顺序；仍根据文件可用性和传输状态提供适用操作。删除文案调整不改变原入口的记录删除范围。
- 筛选右侧单用途三点入口已移除，多选从文件长按菜单进入并勾选当前文件；保留原多选模式批量操作。
- 构建/lint通过，USB真机保留数据安装；真实图片/TXT七项菜单、三点移除、多选进入/退出及隔离深色英文传输操作菜单通过UIA/截图验证，证据.acceptance/file-menu-order-device。实际删除/分享/暂停取消及全状态/大字体/横屏矩阵 **Not verified**。

## 2026-09-19 紧凑文件卡片

- Android文件列表采用文件名、大小/设备方向、时间/状态三行布局；长名称单行摘录，保留扩展名、搜索高亮、完整名称无障碍及原操作。状态不再独占胶囊行，缩小图标与留白，字号不变；异常长状态可换行。
- 构建/lint及USB真机验收通过，已保留数据覆盖安装。同一示例卡片高度301px→214px，隔离长短文件名列表同屏7张完整卡片；菜单、搜索、百分比、浅深/英文状态验证通过，证据见.acceptance/compact-files-device。多选窄宽度、横屏/大字体/全DPI矩阵 **Not verified**。

## 2026-09-19 文件筛选浮层选择

- Android状态改为单选下拉框，“设备名称”改为“设备”；状态和设备均以独立浮层优先向上展开，不撑高筛选面板，长列表内部滚动。超过6个设备保留搜索，重开时清空搜索；选择/返回/点击外部收起。空间不足时定位回退并限制在窗口内，筛选草稿/计数/应用规则不变。
- 345项单测及最终构建/lint通过，USB真机已覆盖安装。状态与设备上浮、面板高度不变、7状态完整显示、应用/重置、深色英文以及13设备键盘搜索验证通过；截图/XML见.acceptance/filter-popups-device。横屏/所有DPI/大字体/繁体真机矩阵 **Not verified**。

## 2026-09-19 接收分享后进入会话

- Android外部分享确认后，整批文本/附件提交成功即自动打开目标会话消息页；文件后续传输可在会话查看。部分失败停留确认页重试，稍后发送/关闭维持原行为。复用已有主页面并移除完成接收页，返回不重新进入分享。
- 341项单测、lint和构建通过；USB真机以实际接收页、持久化收件箱和测试Sender验证未完成/部分失败留页、完成后正确会话与返回栈，保留数据安装；证据见.acceptance/share-conversation-device。真实网络传输未重复，进程被杀/冷启动/旋转/OEM矩阵 **Not verified**。

## 2026-09-19 消息多选整行反馈

- Android多选点击反馈覆盖整行及左右留白，文字、附件和图片均由行统一处理，取消多选后恢复原操作。消息内容仍保留16dp水平边距，不添加常驻选中背景。构建/lint通过，USB真机UI Automation验证整行边界、左右留白及三类内容点击仅切换一次、退出后的文件菜单；按住截图见.acceptance/message-row-feedback-device。所有DPI/横屏/大字体矩阵 **Not verified**。

## 2026-09-19 预览布局与当前章节

- 非图片文件在原页面后台进行有界内容检测，仅可识别文本进入预览；DOCX/PDF等直接调用系统打开方式，不再闪现不支持预览页面。读取失败留在原页提示。USB真机UI Automation与生命周期证据确认3类二进制均未创建预览Activity，取消选择器返回原页；错误后缀文本、Markdown正常进入，缺失文件不跳转。337项单测、lint（0错误/31警告）、APK构建通过并保留数据覆盖安装，证据见.acceptance/document-open-route-device。所有OEM/慢速提供器及探测期间文件变化 **Not verified**。
- 大文件范围说明改为预览标签旁的信息图标，点击悬浮展示，返回/点击外部关闭，不再额外占一行；仅实际截断文件显示，256 KiB预览与复制范围不变。普通文本/Markdown预览和原文、浅深主题及简体/英文/繁体浮层正文真机通过，截图见.acceptance/document-limit-info-device。
- 目录高亮跟随正文顶部阅读位置，支持小节、跳转与手动滚动；原文不显示当前章节。标题层级只以缩进区分，展开时定位当前目录项。
- 预览页使用紧凑文件头（返回/类型/文件名/编码/大小/目录）、图标操作栏及收紧的目录和正文排版。Markdown切换文案改为“预览/原文”（三语同步）；点击目录跳转保持面板展开，显式关闭/返回才收起。普通文本继续不显示Markdown控件。
- 336项单测通过，最终构建及lintDebug通过（0错误/31警告），覆盖安装USB真机，目录选择/滚动/源文切换与浅深UI Automation通过；截图见.acceptance/document-position-device。
- 阅读跟踪仅运行应用固定只读DOM测量，Markdown页面仍拒绝文档脚本（HTML转义+CSP）、网络和本地资源，无原生JS桥；不再以WebView JavaScript设置全禁用描述当前实现。所有OEM/横屏/大字体/缩放手势矩阵 **Not verified**。

## 2026-09-19 Markdown 目录与操作精简

- 普通文本直接阅读，不显示排版/原文/目录控件；Markdown独享排版与原文切换，以及标题栏目录按钮。目录按标题层级展示，点击后保持展开并定位，原文模式自动切回预览；支持无标题提示与返回键收起。
- Android testDebugUnitTest 333项通过（新增目录结构回归），lintDebug 0错误/31警告，assembleDebug通过；同签名保留数据覆盖安装至USB真机。实际打开入口、目录展开/分级、点击小节定位、原文模式经目录切换排版并定位、返回键仅关闭目录、普通文本无Markdown控件、无标题空态、深色英文目录均通过UI Automation；新截图位于.acceptance/markdown-toc-device。真机常亮设置在验收结束恢复原值15。Windows未做前台操作；所有OEM/横屏/大字体矩阵 **Not verified**。
- 目录只涵盖最多256 KiB预览中的标题。原有编码识别、远程图片说明、HTML转义及外部打开边界保持。

## 2026-09-18 Android 文本与 Markdown 预览

- 非图片文件打开入口统一进行内容识别：无后缀/错误后缀文本也可预览，伪装为TXT/MD的已识别二进制走其他应用。普通文本/代码/JSON等直接显示内容，只有Markdown默认排版并可切换原文、展开目录；支持复制预览内容、明暗主题和三语。
- 支持UTF-8、BOM UTF-16/UTF-32、明显无BOM UTF-16及保守GB18030；预览上限256 KiB，超出明确提示，源文件不改。Markdown支持常用段落/列表/代码/表格，图片仅显示说明、HTML转义；不执行脚本或加载外部资源，不支持所有编码与高级文档格式。
- Android testDebugUnitTest 全部332项通过（新增7项），lintDebug 0错误/31警告，assembleDebug通过；样式微调后再次assembleDebug通过并同签名保留数据覆盖安装。USB真机从正式FileInteraction入口打开Markdown，标题/强调/删除线/引用/列表/表格/代码及原文切换通过；复制按钮点击无异常。无后缀/错误后缀文本、UTF-16、256 KiB截断提示、伪装MD二进制外部回退通过；Android运行时7项编码/Markdown/二进制检查PASS。最终浅色中文和深色英文已做UI Automation及新截图（.acceptance/message-batch-android-device/text-preview-final-light.png、text-preview-final-dark-en.png）；三语资源构建通过，繁体单独真机界面与剪贴板内容逐字回读 **Not verified**。验收常亮保持，结束恢复原值15；Windows未做前台验收。
- 全OEM WebView/所有编码/DPI/横屏/大字体矩阵 **Not verified**。Windows本次未改动或操作前台。

## 2026-09-18 双端消息多选（Android交互与分享已修订）

- 双端支持消息多选、复制和仅删除本机记录，保留原文件，跳过发送中/活动传输/待恢复消息并报告结果。Windows保留全选已加载及纯附件复制文件，前轮358项WPF/UIA/数据库专项、18张三语明暗离屏图与UI契约通过，本次未改动Windows。
- Android多选改用勾选列表图标；聊天附件菜单按打开（图片为预览图片）、复制文件名、多选、分享、另存为、查看文件详情、删除本机消息排序。多选时标签栏替换为左侧已选数量、右侧取消；底部只保留复制、分享、删除，无整行选中背景。
- Android移除“全选已加载”，采用上/下悬浮“选择到这里”：按最近勾选锚点和完整可见边缘消息补选连续范围，保留其他选择；擦边/裁切项不作为端点。可见32dp、触控48dp；前轮320项单测、浅暗真机上下裁切端点检查通过。
- Android分享按用户最终确认调整：多条纯文本用粗分隔线合并，单条原样；混合内容的每段连续文字生成UTF-8 TXT，与原附件按原顺序一次分享。文件名为“01-内容摘要.txt”，含摘要截断、非法字符处理与重名保护。纯附件路径和复制格式保持；生成TXT计入100文件上限，临时快照自有标记、七天后于后续分享回收。已替代不兼容的文本数组与混合封装，不采用逐条返回蓝联流程。
- 接收页优化（本轮新增授权）：提取实际共用IncomingShareReview，使用独立会话选择卡片、文本/文件卡片、格式图标与文件大小、固定底部稍后发送/发送；纯文字不显示零文件/零大小，未选/离线禁用发送，完成态保留明确反馈并提供完成按钮。会话采用本机备注显示名；已提交内容不再计入文件页待发送数量，打开待发送入口优先展示未完成项。长内容滚动、明暗与三语资源保持；移除分享仍调用既有BlueLinkConfirmation，没有新增独立弹窗风格。
- 第三方边界与用户决定：本机微信实测提示“分享失败，多文件分享仅支持照片格式”，拒绝多TXT/普通文件；QQ能打开接收对象选择页，但实际对外发送 **Not verified**。用户知悉微信限制后明确保留当前多文件方式、不增加ZIP；不逐项返回、不改为ZIP、图片或其他自动降级。蓝联自收与双端实际文件交接通过不代表所有第三方接收应用兼容。
- 325项单元测试、lint（0错误/31项警告）与Debug构建通过；USB真机20项分组分享断言覆盖真实ShareInbox接收解析、文件内容、读取授权、重名和失效文件，均通过。实测系统面板展示三个文件；蓝联真实接收页可选择已连接会话并发送，Windows只读数据库与落盘文件确认一条合并文字和三个文件完整接收，两个TXT内容/顺序及原文件字节正确。最终APK保留数据覆盖安装；浅色中文/深色英文/繁体及实际选择与提交截图已核对。证据 `.acceptance/message-share-groups-final.log`、`.acceptance/share-review-verified.log`、手机cache `message-share-groups-result.txt`、`.acceptance/message-share-groups-received.json`、`.acceptance/message-batch-android-device/share-*`。手机常亮设置于验收后恢复。
- Windows本轮未改动、打包/安装或操作前台。向第三方联系人发送、其他接收应用兼容与其展示顺序 **Not verified**；不会从打开接收页面推断发送成功。Android全OEM/大字体/横屏矩阵 **Not verified**。

## 2026-09-18 图片预览全屏与精简工具栏（源码已完成）

- 全屏切换按钮位于关闭按钮左侧，与关闭等尺寸、8 DIP间距；图标/提示切换为退出全屏，支持F11切换和Esc先退出。退出恢复原窗口位置/大小/最大化状态；全屏尺寸不写入普通窗口偏好，全屏关闭再打开也不沿用屏幕尺寸。
- 已移除预览底部“复制文件名”按钮，前轮无图片外加描边保持。三个语言、明暗主题和宽窄布局均覆盖。
- 已验证：817项真实WPF专项及24张离屏图，包含UIA调用、全屏/退出/关闭重开与尺寸记忆保护；构建、UI契约和差异格式通过。**Not verified**：前台原生全屏及任务栏覆盖、混合DPI多屏、系统分辨率变化；未创建桌面窗口或操作用户前台。
- 本轮尚未打包。最新已交付windows-webp-preview-safe-20260918不含全屏、复制文件名移除和前轮图片描边移除。

## 2026-09-18 图片预览无外加边框（源码已修复）

- 已移除Windows图片四周额外叠加的描边及其缩放更新；透明棋盘格、缩放/旋转与导航视区保留。
- Release编译、649项真实WPF预览专项/18张离屏图及UI契约通过，未使用桌面窗口或前台输入。前台/完整DPI矩阵 **Not verified**。
- 本次未重新打包；下方windows-webp-preview-safe-20260918是当前已交付包，尚未包含此次描边移除。

## 2026-09-18 WebP 预览异常处理（后台验证通过）

- 实际闪退日志确认旧 WPF 解码失败（0x88982F8B）后，图片预览异常传出主界面。现使用独立WebP解码，并补齐预览创建及打开的异常边界；损坏/截断/文件占用等失败复用统一“无法打开文件”提示，不留下提前打开的遮罩。
- 已验证：新增17项预览回归，累计354项输入框/图片检查与31张离屏图；实际最终安装载荷同哈希组件再次通过，编译/UI契约及打包门禁通过。损坏图片失败后仍能打开正常WebP。
- 最新交付：E:/BlueLink/artifacts/windows-webp-preview-safe-20260918，0.2.17 x64 Review EXE/MSI、内置.NET 8、未签名，保留发送快捷键等前轮修复；替代windows-composer-shortcuts-20260918，交付SHA-256已核对。
- **Not verified**：用户原始图片、前台预览与错误弹窗、生产覆盖安装及跨架构实机；本轮未操作前台或安装到用户目录。

## 2026-09-18 Windows WebP、混排光标与发送快捷键（后台验证通过）

- WebP 使用随应用分发的 SkiaSharp 解码，不再依赖系统可选扩展；输入框/聊天/文件列表/图片预览和发送预览均覆盖。透明度、原始尺寸和极端比例裁剪已验证；动态 WebP 只显示首帧，损坏图片回退文件卡片，解码有分配上限。
- 附件按字体基线布局，两侧使用空文本 Run 保持原生字体高度的居中光标；不会发送额外零宽字符，退格和撤销回归通过。
- “设置 → 通用 → 消息输入 → 发送快捷键”支持 Enter 发送/Shift+Enter 换行或 Ctrl+Enter 发送/Enter 换行。PreviewKeyDown 修复 Enter 被富文本控件提前消费；输入法组合输入不发送，长按不重复提交。设置持久化、未保存状态、恢复默认、旧库缺省迁移及三语提示生效。
- 已验证：337项真实控件/数据回归与31张离屏图、56项设置检查、Release编译和UI契约；没有创建桌面窗口、注入系统输入或读取剪贴板。**Not verified**：真实前台输入法/键盘、多DPI、现有安装覆盖和x86/ARM64本轮变化。
- 前轮交付：E:/BlueLink/artifacts/windows-composer-shortcuts-20260918，0.2.17 x64 Review EXE/MSI，内置.NET 8、未签名。337项/31图在与安装载荷哈希一致的发布客户端和原生解码库上再次通过；确认x64 libSkiaSharp.dll已打包且无原生PDB，19项MSI结构、安装归属76项及构建器既有门禁通过。交付EXE/MSI大小及SHA-256逐项匹配清单；当时替代windows-installer-directory-20260918，现由上方预览异常修复包取代。

## 2026-09-18 安装器修复与独立 MSI 原生向导（后台验证通过）

- 已修复先独立安装准确 MSI 后，首次运行 EXE 因 execute=None 被拒绝的问题，保留实际载荷执行门禁；“打开日志文件夹”按钮使用自动宽度及现有样式，完整文字已在前台验收禁令前核对。
- MSI侧栏现参照EXE使用深蓝渐变、弧形装饰、蓝联LOGO与品牌文字，顶部横幅也使用蓝联LOGO，图片提升为4倍原生尺寸；七个侧栏页面正文恢复系统原生底色，完成页启动选项缩为单行并消除独立灰色块。最新包19项MSI表结构检查及嵌入图片哈希通过；依用户要求没有前台复验。
- 已安装目录默认回读同渠道HKCU记录，交互与静默安装均在目录计算前生效，用户/EXE/命令行显式选择优先。隔离真实MSI自定义目录安装→无目录参数升级→卸载通过，Download保留；新渠道默认值、中文空格路径及显式覆盖均已验证。
- 独立 MSI 采用原生 Windows Installer 向导，支持指定目录、运行环境说明/检查、桌面快捷方式选择和完成后运行。内置运行时包无需另装 .NET；框架依赖包检测匹配架构 .NET 8，缺失时阻止安装并提示。静默与 EXE 嵌入安装不触发完成页启动。
- 已验证：唯一隔离产品的 MSI 先装/旧 EXE 失败复现/新 EXE 修复/准确 DLL 恢复/重复修复/卸载与 Download 保留；运行时检查成功及不可满足版本阻断；指定目录与隔离快捷方式创建/卸载；此前 MSI 14项表结构检查、框架依赖夹具15项、安装归属76项、构建及静态门禁通过。
- 前轮安装器交付：E:/BlueLink/artifacts/windows-installer-directory-20260918，0.2.17 x64 Review EXE/MSI，未签名、内置.NET 8，包含此前输入框/拖放修复；交付SHA-256已核对。windows-latest-20260918、installer-fixed-20260918 与 installer-branding-20260918 已被此修正版取代，未覆盖用户实际安装，未发布。
- **Not verified**：按用户最新指示停止所有 Windows 前台验收，MSI 原生向导实际点击/完成后运行及多DPI暂未验证；完整生产升级矩阵、x86/ARM64、正式签名继续后置。运行时失败测试通过隔离 MSI 请求不可满足版本实现，没有卸载本机运行时。

## 2026-09-18 输入框对齐与拖放反馈修复（已实现并定向验收）

- Android最低高度使用原生垂直居中，hint、文字与光标同步；展开/附件高度仍顶部编辑。真机空输入/文字居中、展开、图片退格恢复及重建后高度记忆通过，同签名修复包已保留数据安装。
- Windows占位文字跟随当前富文本文档内边距和输入字体，消除5 DIP起点差；原生空框截图和13/18字号坐标通过。聊天文件拖出保留标准复制数据，同时屏蔽蓝联自身DragEnter/Over/Drop；实测拖回输入框没有重复附件。
- 输入框拖入提示改为横向28px图标配两行文字，适配最低高度；正常/超量三语边界、离线暂存及原生截图通过。Windows78项/13图、构建/UI契约及Android313项（0失败、2跳过）/构建/lint通过。
- 此轮输入框修复已纳入当时安装包：E:/BlueLink/artifacts/windows-installer-directory-20260918，0.2.17 x64 Review EXE/MSI，内含.NET 8运行时和本轮修复。包结构、实际载荷回归及交付SHA-256校验通过；尚未覆盖现有安装或发布。安装包未签名；实际安装/升级矩阵、外部目标应用实际接收、完整多DPI/字体倍率矩阵 **Not verified**。

## 2026-09-16 双端混排输入框与紧凑附件（已实现，定向验收）

- 输入框内粘贴/选择附件暂存，按文字/附件位置依次发送；Windows聊天区立即拖放发送、输入框拖放暂存，单批最多10文件。Windows高度120–360；Android空输入区恢复约40 dp，含拖动柄等首选高度68–280，附件临时撑高保证可读、删除后回到首选高度，偏好持久化。
- 缩略图复用聊天尺寸/极端比例裁剪算法，最大Windows96×64、Android88×64，方图/普通比例不再套固定横框；2000×10及反向长图保留自然短边并居中裁剪长边。两端用原生退格删除附件，Windows已移除×按钮并修复原生删除/撤销序列化元数据异常；Android光标独立按当前字体度量绘制。
- **已验证**：Windows构建、48项实际混排/退格/撤销/极端缩略图与5图、175项草稿6图、UI门禁和差异格式；此前3项加密内存会话顺序检查保持有效。Android274项单元（0失败、1跳过）、构建/lint（0错误23警告）；最终USB隔离真机完成最小高度、附件临时撑高/删除恢复、文件/图片退格、带附件重建、13/18sp光标和明暗极端图片验收。此前粘贴、分段提交、失败重试、设备草稿隔离已验收。
- 输入框专项验收包曾保留数据安装并核对SHA-256 `B1B9EC357F395338647D9DBAC58D65DD4C31AC01A840108C1C37F023ADA25528`。Windows没有创建桌面窗口或读取系统剪贴板，前台原生拖放/粘贴、DPI/完整键盘仍 **Not verified**；手机提交为隔离回调，真实双端链路、第三方URI来源和生产文件选择器未验收。本轮不作为发布验收。
- 草稿最多100附件，Android单文件导入2GiB上限；自有副本清理已在P6按归属及引用保护接入，有序附件草稿身份迁移已在P5增量补齐：合并保留两份草稿，墓碑保证中断重试不重复，导入/发送中禁止迁移。P5恢复与USB排队切换已补定向验证，P6/P7实现及P8候选交付已完成，完整硬件与发布验收仍未关闭。

## 2026-09-17 产品优化 P4–P8（实现与候选交付，完整发布门槛待验）

- Windows文件表头按参考图改为灰色标题、上下排序三角与三条渐短横线筛选图标；列菜单直接勾选条件并确认，关闭未确认菜单丢弃草稿。同列多状态/设备取并集，各列取交集。时间支持起止或单边日期范围，含首尾整天，倒置范围不可确认。重置仅清除当前列，清除全部保留关键词、排序与原范围。没有独立筛选面板或顶部摘要；分页和紧凑批量底栏保持。
- Windows最终构建通过（0错误/6项既有警告）；列筛选/日期/批量1975项与60图、组合查询/分页281项与9图、产品搜索427项与18图、展示381项与6图、UI规范通过。展示图在E:/BlueLink/.acceptance/windows-reference-screens-20260914；下拉编辑器使用实际组件在表头位置离屏合成。后续已补列筛选、日历选择、排序及多选的原生UIA/点击，见本节新增证据；完整焦点/键盘与DPI矩阵仍 **Not verified**，未覆盖Windows安装。
- Windows过滤面板视觉已补充：圆角/阴影、标题/分组留白、36px勾选项、左侧重置与右侧主确认按钮；Filter20图标在打开或生效时蓝色，日期面板一致。最终增量构建、1951项/60图与展示379项/6图、UI规范通过，实际组件离屏图位于E:/BlueLink/.acceptance/windows-filter-polish-20260914；原生鼠标交互已补验，完整键盘/DPI验收仍保留。
- Android文件页现为整行搜索＋结果数量行中的排序/筛选计数/更多；底部筛选面板直接选择类型/方向/状态、展开设备及编辑日期，确认草稿才应用，关闭丢弃，重置保留关键词/排序/会话范围。按追加反馈收紧行内布局、减轻字重与边框并统一蓝色。254项单元测试（1跳过、0失败）、Debug构建/lint（0错误22项既有警告）通过；隔离真机验证草稿取消、确认/重置保真、日期范围及倒置范围拦截。证据：E:/BlueLink/.acceptance/product-phone-20260914/filter-sheet-*。大字体/横屏、多设备搜索及生产传输期间更新仍 **Not verified**。
- Android16无线真机：搜索摘要/文件名高亮、完整原文/文件名复制、类型+日期组合及定位通过；隔离Room500条最近200条/更早页/按ID上下文与草稿重开通过。真实Compose搜索能定位未加载的第100条记录，离线输入能持久化。未读取或删除生产历史。
- 手机名称查询p95（含125ms防抖、不含渲染/冷索引）：1k164ms、10k269ms、100k1598ms。完整10k交互≤300ms仍未验证，100k保留压力限制。
- P4双端多选/批量状态检查、重试/取消/删除记录及部分结果已接线。Windows离屏568项/18图通过；Android246项单元测试无失败（1跳过），真机混合删除75成功/25活跃任务跳过通过，长按菜单保留并可进入多选。只删除记录，不批量删除真实文件。
- Android两文件系统分享的URI/读取授权/精确字节/混合MIME契约及实际系统Chooser打开、取消返回通过。QQ/微信的多文件接收、外部来源文件及真实传输控制尚未验收。Windows后续已补隔离原生文件交互，未覆盖生产安装。
- Android外部分享接收已实现：文本/PDF/多文件确认、独占快照、显式保留、内容去重与部分交接重试。修复同URI内容变化被错误合并、文件交接未等持久化完成、取消作用域回调悬挂及失败后显示旧分享。259项单元报告（1跳过、0失败）、最终构建/lint通过；37项隔离真机检查及真实Room交接事务通过。
- 独立QA来源应用跨UID真机验证：冷启动确认、单/多文件、重复去重、撤销来源URI授权后快照哈希保持、无授权拒绝、移除确认只清自有快照、重启保留且未自动提交。未向任何设备/联系人实际发送，QQ/微信入口和真实传输交接仍 **Not verified**。证据E:/BlueLink/.acceptance/p4-phone。
- Windows隔离原生UIA/鼠标通过：已完成筛选6→3，日期嵌套日历/单边范围，大小排序，两项多选/菜单/退出；截图和UIA树E:/BlueLink/.acceptance/windows-native-20260915。完整键盘/焦点/DPI与生产传输仍 **Not verified**。
- P5消息中断恢复已通过Windows隔离SQLite10项与Android真机Room验收：重开将旧发送中/排队消息归为失败，保留正文、时间、草稿、已送达和已读状态；双端已移除重连自动重放。
- P5阶段展示已通过双端定向回归及真机/原生UIA：校验、落盘等无可测进度阶段使用不定进度或“—”，不虚报0%/100%；设备显示队首与其余活动原文件数量。Windows28项语义、17项接收器、119项持久化/明暗4图通过；设备、文件与消息附件的原生截图在E:/BlueLink/.acceptance/windows-native-p5-20260915，手机p5-*在product-phone-20260914。
- P5源指纹已接入既有持久列及回读：重试Offer前重新核对稳定快照大小/SHA-256，变化或旧记录缺少指纹时要求重新选文件。Windows9项源身份、10项实际加密内存会话断连/恢复/取消及同长度替换拦截通过；Android264项单元（1跳过、0失败）、构建/lint0错误23警告、真实Room指纹持久化通过。旧任务不会自动补造原始指纹；重新选择按新发送处理。
- P5发送尝试保护已通过：跨重连保留任务所有权直到发送工作与清理退出，每次重试分配新ID和单调顺序；两级状态投影拒绝旧尝试、迟到首条队列事件和终态回退。重试重新检查同一设备、信任、连接与可重试终态；Android拒绝重入会反馈给批量结果。Windows29项所有权/重试权限及12项实际加密内存会话回归通过；Android269项单元（1跳过、0失败）、构建/lint通过（0错误23警告）。本轮Windows无窗口、无Computer Use或前台验收，未覆盖生产安装。
- Android本轮最新Debug已保留数据安装并核对手机APK SHA-256一致；真实Room及明暗阶段展示6项检查通过，截图/记录product-phone-20260914/p5-attempts-*。此真机回归验证数据库和阶段呈现，不代表尝试竞争已在真实Bluetooth/WPD链路复验。
- P5恢复增量（2026-09-16）已实现：Recovery/*.json独立于两类历史开关，首次公告前和阶段边界原子落盘，保存epoch/会话/尝试与任务最小信息；重启只显示待恢复，发送方手动重试并重查信任/peer/源指纹，接收方等待发送方。旧字节计数不作为恢复检查点，待恢复进度为“—”。清空历史保留活动/待恢复项，删除记录墓碑防复活；合法新Offer保留同连接接收重试。
- **已验证**：Windows恢复27项、实际主模型/明暗三语177项及6图、安全握手草稿迁移与事务中断110项、加密内存传输10项（包含初始落盘失败不公告、拒绝后同连接重试、字节一致），构建/UI门禁通过；原生Computer Use检查隔离文件页、菜单及恢复说明。Android284项单元（0失败、1跳过）、构建/lint通过（0错误23警告）；USB真机私有恢复文件重开、状态、方向性动作和中文明色/英文暗色界面通过。恢复说明沿用既有共用组件。草稿关联后超过100附件的合并结果保留，仅允许编辑/减少，不允许进一步增加。
- P5早期USB真机曾安装该增量Debug包，并核对安装包SHA-256 `7DFC0D6C8F13D771555EC9736ABAD884DCB8227F98A0D6C6CF1BD65E00B33C76`；手机原电源值已恢复，独立Windows QA进程已退出。该包仍是定向验收构建，不作为P8候选发布结论。
- P5继续实现并定向验证：消息附件恢复统一投影、恢复身份关联幂等迁移、认证尝试流ID与旧回调隔离、部分接收文件所有权、原指纹源文件重选、USB排队改用蓝牙（仅未开流且对端确认释放）。旧对端同任务重试要求新连接；切换拒绝保留原队列。Windows恢复56/尝试29/源9/阶段28/握手110及加密切换12项通过，共享协议45项通过；Android296项（0失败1跳过）、构建/lint通过。
- 原生Windows隔离文件页和菜单、Android USB真机隔离恢复及菜单已验收；候选APK哈希7EAB2394CCE446157BA6774D4492A6275A967CFB0DB071CFF3D8DE80FC5696CC。证据为E:/BlueLink/.acceptance/p5-route-native与p5-route-phone，均未发送生产数据；原手机电源值已恢复。
- P6已实现分类存储、引用保护、SAF对象归属、启动草稿回收、更新交接缓存保护、CPU唤醒锁与安全退出。Windows14项存储/32项尝试及Android313项测试（0失败、2跳过）与构建/lint通过。真机亮屏后台蓝牙30.03分钟、取消释放唤醒锁、384MiB USB显式重试及32MiB/16MiB/256MiB双向USB哈希已验。活动退出提示已验，但文件在确认前完成，不算在途中断验收。
- Android清理预览已修复：路径检查以系统dataDir为边界，避免越界检查系统挂载祖先；应用私有目录内软链接仍拒绝。最终真机预览21 MB/取消、QA软链接拒绝和canary保留均通过；未删除用户实际缓存。此真机反例补齐Windows宿主因权限跳过的软链接测试。
- P7本地置顶/备注已实现，Windows8项偏好持久化/身份边界回归、原生置顶/重开及备注弹窗展示取消通过，Android备注输入/保存/原名详情通过。Windows原生备注输入保存、完整字体/DPI/键盘及TalkBack矩阵仍 **Not verified**。
- P8 Windows x64自包含Review EXE/MSI与Portable通过包内架构/运行库/MSI/Burn/哈希及更新校验；三语明暗更新组件66项/12图通过。GitHub公开检查通过；9月16日及17日Windows完整公网下载均在超时/取消边界失败，未残留该次.part；Android公网下载进度/取消通过，完整下载未通过。Android最终同签名Debug包已从应用内经生产校验交给系统安装器、覆盖安装并返回蓝联，安装来源显示蓝联，APK SHA256为014E61F76E383B6C96F51739505D403CD3797E8489641859A3F212D1BD9758EE；既有数据库/身份/草稿文件仍在，未清数据，全量历史内容比对未做。本地QA载荷验收与公网完整下载分开记账。
- 当前候选：E:/BlueLink/artifacts/product-p5-p8-20260917（Windows x64 Review/Portable、Android universal Debug、build-manifest.json、SHA256SUMS.txt）。Android签名/zipalign通过；Windows未签名预览例外沿用用户授权，不标为正式签名发行。手机原电源设置已恢复，仅本次QA安装载荷按精确路径与哈希清理；未覆盖D:/BlueLink、未提交/推送/上传Release。
- **Not verified / 发布门槛**：P5实际Runtime+Room历史关闭组合、真实WPD占用/拔插/生产重启恢复、源选择器完整交接、强杀/掉电、真正传输途中退出、锁屏/低电量/通知禁用、Windows实际覆盖安装与跨架构实机。QQ/微信真实交接、完整键盘/DPI、手机冷索引/渲染性能原缺口继续保留。损坏/未知版本恢复记录当前失败关闭。


## 2026-09-14 产品优化 P3（查询与分段历史，本地验证）

- 双端独立日期范围、组合筛选、时间/名称/大小排序、条件清除；查询移到后台并丢弃过期结果，稳定ID排序和100条增量呈现。会话先读最近200条、显式加载更早、按ID取定位上下文；附件批量读取。文件选中仍无背景。
- **已验证**：Windows82项查询、221项数据库/分页/布局专项9图、1166项搜索回归98图、175项草稿回归6图及UI静态门禁；Android完整单元243项（1跳过，0失败）、构建/lint 0错误20既有警告；实际DAO SQL宿主SQLite10项。Windows10k文本查询含防抖及离屏布局p95约179ms；10k文件名称查询不含渲染约150ms。
- **Not verified**：手机真实性能/Room/UI、真实桌面交互、搜索冷索引构建耗时与内存。Android宿主JVM10k/100k约144/204ms不能作为手机结论；Windows100k文件名称压力值约343ms（不含渲染）。首次搜索仍按需加载完整内存索引。未发布、未覆盖生产安装。证据：.acceptance/product-optimization-p3-20260914。

## 2026-09-14 产品优化 P2（草稿，本地验证）

- 双端按设备身份保存草稿，250ms防抖、离开刷新、重启读取；离线可编辑且重连不会自动发送。发送成功仅清除对应版本，保存失败保留内容并提示。沿用现有数据库字段和身份关联边界。
- **已验证**：Windows175项实际绑定/SQLite/重启/并发清空/旧回调专项、6张离屏截图及143项搜索回归；Android完整单元报告239项（1跳过，0失败）、构建/lint通过（0错误20既有警告），包含5项草稿账本回归。
- **Not verified**：Android真实Room/UI及两端蓝牙发送/强制终止路径；实际磁盘满、真实桌面交互。本輪无在线ADB、不操作Windows桌面。草稿最末250ms遭强制终止可能丢失。尚未发布或覆盖生产安装。

## 2026-09-14 产品优化 P1（本地候选，实机后置）

- Android文件名支持两行、扩展名与命中片段；双端消息搜索显示命中上下文，复制/分享保持完整原文。附件说明命中显示说明，文件操作仍针对正确附件。
- 双端文件未完成分组下区分失败、拒绝和取消并可独立筛选；取消不标故障红色，发送方有可读源时仍可重试。Android目录底栏收纳至已有存储设置，缓存反馈改为短Toast，协议版本在详情查看。
- **已验证**：Android27项回归、构建/lint 0错误20既有警告；Windows构建、143项行为、367项新离屏布局18图、1166项共用搜索/菜单/预览回归98图及UI静态门禁。Unicode簇/长命中/清空/扩展名与状态权限有定向覆盖。
- **Not verified**：ADB无在线设备，Android新界面实机未验；Windows桌面交互未验。未运行传输/更新/安装矩阵，不作为正式发布通过。
- P1候选包：E:/BlueLink/artifacts/product-p1-20260914/BlueLink-0.2.17-android-universal-debug.apk及SHA256SUMS；未安装、推送或发布。证据：工作树.acceptance/product-optimization-p0-20260914和product-optimization-p1-20260914。后续阶段仍在实施。

## 2026-09-14 Android 文件搜索关键词高亮

- 全局与会话内文件页对文件名关键词着色，与消息搜索共用高亮组件；支持忽略大小写、多处匹配及中文，清空恢复普通文字，保留单行省略与文件操作。
- **已验证**：Debug构建、10项定向回归、lint 0错误/20项既有警告、APK签名/对齐；全局/会话内 × 明暗 × 匹配/清空共8项真机UIA、新截图与文件名像素检查通过。已保留数据覆盖安装并返回正式应用。
- 文件名高亮阶段Android本地包：E:/BlueLink/artifacts/android-file-highlight-20260914/BlueLink-0.2.17-android-universal-debug.apk及SHA256SUMS，包含此前气泡与文件操作调整，未提交、推送或发布。
- **Not verified**：完整传输与安装/升级矩阵本轮未重复运行。Windows本轮未改。证据：工作树及主工作区 .acceptance/android-file-highlight-20260914。

## 2026-09-14 双端会话气泡圆角

- Android文本及文件气泡与Windows方向一致：其余三角14dp，本机右下4dp、对端左下4dp；传输状态卡片、图片关闭缩略图后的文件卡片共用形状。Android图片缩略图继续四角12dp。
- Windows实际显示图片缩略图时，外层背景与内容裁剪统一四角14；关闭/缺失缩略图后恢复方向性文件气泡。
- **已验证**：Android构建/lint/签名/对齐、8个明暗真机场景UIA和新截图，已保留数据覆盖安装并返回首页；Windows x64构建、173项离屏检查与28次渲染、UI规范及差异格式检查通过。
- **Not verified**：用户要求不占用电脑，Windows实际桌面交互未验；未重复全传输、安装、升级矩阵。Windows安装版未替换，未发布新Release。
- 气泡阶段Android本地包：E:/BlueLink/artifacts/bubble-shapes-20260914/BlueLink-0.2.17-android-universal-debug.apk及SHA256SUMS，包含此前搜索操作与复制文件名修改。证据：工作树及主工作区 .acceptance/bubble-shapes-20260914。


## 2026-09-14 Windows 搜索操作与双端复制文件名（本地构建）

- Windows 搜索结果已接入文本复制、详情、显式定位和确认删除；文件结果复用按实时状态开放的文件/传输动作，点击/右键/更多及键盘菜单入口不再直接定位。文件列表、消息附件、图片预览两端均可复制原始文件名，缺失或未完成文件仍可复制名称；Android 搜索共用入口且不重复。
- **已验证**：Windows x64 Release构建、49项搜索行为回归、1166项专项布局/菜单检查及98图、UI规范静态检查。三语明暗结果/菜单和预览宽窄布局、选中态文本及深色标题对比度通过。按用户要求，搜索结果卡片悬停或选中时保持原背景；使用列表作用域内官方资源覆盖，三语明暗状态回归通过。Android Debug构建、lint 0错误/20既有警告、签名/对齐及真机文件名复制与搜索条件保留通过，已保留数据覆盖安装并返回首页。
- **Not verified**：用户正在使用电脑，要求减少computer-use，已停止Windows桌面自动操作；最终版本完整UIA与真实剪贴板/文件操作、删除、传输端到端未验，离屏结果不替代这些项。较广workspace套件有compact dropdown结果断言失败，本轮专项通过不代表全产品门禁通过。未重跑安装/升级矩阵。
- 文件操作阶段Android本地包：E:/BlueLink/artifacts/file-actions-20260914/BlueLink-0.2.17-android-universal-debug.apk及SHA256SUMS。Windows为本地x64构建，未替换用户正在使用的安装版；未提交、推送或发布。证据：工作树及主工作区 .acceptance/search-cross-platform-20260914。


## 2026-09-14 Android 搜索结果操作

- 搜索结果点击、长按或右侧更多可直接操作：文本完整复制、系统分享、定位和确认删除；文件复制文件名，并按当前状态提供预览/打开、分享、另存为、详情和传输控制。复用原生分享与共用操作/确认组件。多附件结果针对匹配文件，优先采用实时传输状态；操作后保留搜索条件。
- **已验证**：Debug构建、223项JVM测试0失败/0错误（6项新增，1项可选联网测试跳过）、lint 0错误/20既有警告、APK签名/对齐。真机UIA与新截图验证点击/长按/更多、完整文本和匹配文件名复制、文本/图片原生分享取消返回、预览返回、详情、定位、删除取消/确认及结果更新、关键词和类型保留、深色缺失文件/离线动作门禁。仅用生成文件和内存QA记录，未向联系人发送。
- **Not verified**：搜索入口另存为实际落盘、真实传输控制端到端、分享接收方实际发送、完整传输/升级矩阵；以上底层实现复用，未在本轮全量重验。
- 搜索阶段Android本地测试包：E:/BlueLink/artifacts/android-search-20260914/BlueLink-0.2.17-android-universal-debug.apk及SHA256SUMS，已保留数据覆盖安装并返回正式应用。含此前图标、关于页、系统分享、历史信任和应用内更新修改；未提交、推送或发布。证据 .acceptance/android-platform-20260914/search-*。

## 2026-09-14 Android 打开与分享图标

- 文件/附件菜单与图片预览统一使用矢量图标：分享为向右弯箭头，打开为方框与右上外开箭头。原生系统分享及打开行为不变。
- Debug构建、lint（0错误/20警告，含3条旧导出位图未使用）、签名/对齐通过；明暗主题下文件菜单、图片预览及附件菜单共5张真机UIA/截图通过。已覆盖安装手机。
- 图标阶段Android本地测试包：E:/BlueLink/artifacts/android-icons-20260914/BlueLink-0.2.17-android-universal-debug.apk及SHA256清单，包含下述关于页、系统分享和历史信任修改。未提交、推送或发布。

## 2026-09-14 关于页移除本地安装入口

- 已删除 Android 关于页“选择本地安装包”及无引用选择器、回调、三语文案；在线检查更新、下载与安装流程保留。
- Debug构建/lint、APK签名/对齐通过；三语AboutSettings与正式关于页真机UIA/截图通过，在线检查入口正常打开更新弹窗。完整更新升级矩阵未重复运行。
- 关于页阶段Android本地测试包：E:/BlueLink/artifacts/android-about-20260914/BlueLink-0.2.17-android-universal-debug.apk及SHA256清单，已保留数据覆盖安装；包含下述原生分享与历史信任修复。未提交、推送或发布。

## 2026-09-14 Android 历史信任兼容与系统分享（已安装本地候选）

- 修复旧版撤销信任后残留 UNKNOWN 历史设备：启动事务依据真实信任键及历史身份/连接证据一次性迁移为 REMOVED，保留会话、传输和文件；当前 TRUSTED 但密钥已撤销的中断状态也会修复。真机旧离线项3→0，已信任电脑保留；隔离 Room 的迁移、重启、迟到回调、重新信任与记录/文件保留通过。
- Android 文件/附件菜单移除“在文件夹中显示”和仅复制URI的“复制文件”。文件、附件、图片预览统一调用系统原生分享面板，原文件名、MIME、content URI与临时读取权限保留。用户已明确允许系统决定应用图标、排序和展开；自绘优先列表/折叠规则及分享包查询已移除，不限制微信、QQ、Telegram、WhatsApp之外的兼容接收应用。
- **已验证**：最终原生面板Debug构建、217项JVM测试0失败/0错误（1可选联网测试跳过）、lint 0错误/17警告、APK签名/对齐通过。已保留数据覆盖安装；PDF原生分享面板截图及content URI、原文件名、字节数、MIME、内外Intent读取权限断言通过。PDF与图片原生面板、取消返回通过；系统面板进入QQ ForwardRecentActivity和微信MvvmContactListUI通过，未实际发送；图片预览夹具移入既有共享目录后的原生分享和取消返回正式首页通过；最终仅有原受信任电脑（当时离线），旧已移除记录未复现。夹具修正后的增量构建/lint再次通过，最终包已安装。此前三个文件菜单9图与平台动作检查、隔离历史迁移回归保持有效。
- **Not verified**：WhatsApp、Telegram商店版未安装；本轮原生面板未复验Telegram接收；所有接收应用的实际发送/对方收取、全部文件类型及大文件未验。完整页面/状态UI覆盖仍以既有验收清单为准。
- 原生分享阶段Android本地测试包：E:/BlueLink/artifacts/android-native-share-20260914/BlueLink-0.2.17-android-universal-debug.apk及SHA256清单，已通过Debug签名/对齐，包含本次Android修复与此前应用内更新。旧双端目录中的Android包不含本次分享改动；Windows包保持此前交付。源码未提交、推送或发布；本地v0.2.17-preview.4标识不是公开标签。
- 证据：.acceptance/android-platform-20260914。

## 2026-09-14 GitHub 应用内更新（源码接入，尚未发布）

- 双端固定 GitHub Release 源，完整标签比较，包含预览版；应用内下载、进度、取消、重试、SHA-256 校验及安装前复验，不跳转浏览器。普通本地构建按 VERSION，流水线写入真实标签。
- Windows 安装版按架构选择完整包。用户已批准未签名 Review 预览包在固定源/大小/哈希/产品版本通过且明确提示后交给安装器；正式包仍要求可信签名和相同发布者。Portable ZIP 应用内下载，但退出解压替换仍需手动。
- Android 新增 INTERNET、REQUEST_INSTALL_PACKAGES 与专用更新缓存共享目录；验证包名、版本、同一签名并交给系统安装确认。首次来源授权由用户自行完成，数据不上传 GitHub。
- **已验证**：Windows Release 构建、29 项选择/32 项更新/6 项真实 PE 本地传输校验、66 项原生 UIA和12图、12 项 portable 数据/架构通过；正式版不获未签名例外。Android 214 项 JVM 测试0失败/0错误（1项可选网络探测跳过）、Debug/Release 构建、lint（0错误/16既有警告）、实际APK权限/Debug签名/对齐通过。REDMI K80 Pro 真机完成真实查询、下载进度与取消清理、三语明暗6图、失败重试与过期回调保护；同签名本地候选经生产校验后在系统安装器覆盖成功，用户自行完成首次安装来源授权，设备记录保留并重新连接电脑。最终候选正式设置入口的真实查询、缓存正式签名包拒绝覆盖均通过。
- **Not verified**：公网完整包下载（Windows超时、Android慢速下载至19%后取消）、正式签名 Android 跨版本覆盖、Windows 真实签名覆盖、Portable 自动替换、ARM64 实机、完整安装矩阵、新标签云端构建。局部网络测试及本地候选不等同公网端到端升级。当前公开旧包不包含本次源码，未发布新 Release。
- 最新双端本地测试包：E:/BlueLink/artifacts/dual-in-app-20260914（Windows x64内置运行库 Review EXE、Android universal Debug APK及SHA256清单），本地测试标识v0.2.17-preview.4，不代表已发布标签。完整固定目录发布门禁未运行，不能计为正式发布验收。
- 证据：.acceptance/github-updates-device-20260913；较早元数据/标签证据 .acceptance/github-updates-20260913。

## 2026-09-13 Windows NoRuntime 安装包

- 在现有 x86/x64/ARM64 自包含 EXE/MSI 和 portable 之外，增加各架构 NoRuntime EXE/MSI，默认矩阵为 15 个 Windows 产物；与 Android 五 APK 合计 20 个安装附件。
- 精简包由 framework-dependent publish 生成；构建检查无内置运行库、Burn 外部下载 URL/架构/大小/SHA512，沿用同一 Review 安装身份。
- 启动器补齐三架构检测，核对 dotnet/图形/基础运行库 PE 架构，避免将其他架构的 .NET 或不完整安装误认作满足条件；运行库提示页同步显示目标架构。
- 本地 x64 实测 EXE 约 86.0 MiB、MSI 约 65.1 MiB。27 项架构回归、23 项安装安全回归、14 项发布附件回归通过；界面回归 152 检查/42 图，真实桌面 UI Automation 18 检查/6 图通过。
- 新增 x86 安装安全回归发现并修复共享清理模块的 winsqlite3 调用约定（Cdecl 改为 StdCall）；真实 x86 进程 27 项架构和 23 项安装安全检查通过。
- 全架构云端构建与 [v0.2.17-preview.3 发布](https://github.com/shuuuuuang/BlueLink/releases/tag/v0.2.17-preview.3) 已通过（[流水线 34760521733](https://github.com/shuuuuuang/BlueLink/actions/runs/34760521733)，Android 失败任务重试后成功）。发布后独立核对 22 个公开附件、20 个软件包、SHA256/大小、源码提交 62c0129 与 Android 正式签名指纹一致。
- 实际执行 Microsoft 运行库安装、缺运行库系统的完整安装升级矩阵、ARM64 实机仍未验证。
- 发现已有 Android `cancelWhileWaitingNeverCommits` 测试的取消异常时序波动：有时返回 `JobCancellationException`，强制转换为 `TrustHandshakeException` 失败；当前未修改握手实现或放宽测试，一次失败任务重试后全部通过，作为后续稳定性问题记录。

## 2026-09-13 GitHub 预览版自动发布

- 新增标签触发和手动触发工作流：三种 Windows 架构、Android 四 ABI + 通用包，14 个安装附件及统一清单/哈希。
- Android 正式密钥保存在仓库外，GitHub Secrets 提供临时签名输入；本地五个 Release APK 已通过 apksigner 签名/验证与 zipalign 校验。
- 发布附件门禁 12 项回归、PowerShell 语法、actionlint 和现有版本/UI/安装器契约已通过；GitHub Actions 首次流水线 [34756587487](https://github.com/shuuuuuang/BlueLink/actions/runs/34756587487) 全部成功，Android 冷构建 9 分 14 秒，五 APK 签名/对齐通过；[v0.2.17-preview.1](https://github.com/shuuuuuang/BlueLink/releases/tag/v0.2.17-preview.1) 已公开。发布后独立核对 16 附件（14 安装包 + 清单 + SHA256）、大小/哈希、源码提交 04977ed 及正式签名指纹一致。
- 当前仅发布 preview 标签，Windows 无 Authenticode 签名、Review 安装身份和此前实机/完整安装矩阵限制保持明确。

## 2026-09-13：多架构构建与 Windows Portable

Windows 构建入口支持 x86、x64、ARM64，统一生成内置 .NET 8 运行库的 Review EXE/MSI 和 Portable ZIP。旧 Review 入口委托同一流程；正式 x64 发布锁和版本号未改。WiX 每个架构重建，并在发布产物前解包验证 MSI 平台/位数、Burn 目标架构、产品码、载荷指纹、内嵌 MSI 和恢复清单，避免参数切换后复用上一架构的缓存产物。ARM64 客户端/运行库为原生 ARM64，安装引导界面暂用 x86 .NET Framework 兼容层。

Portable 由 BlueLink.portable 标记启用，身份、数据库、设置、缓存及日志保存在程序内 Data，默认接收目录为 Download；退出后整体移动，下次启动事务内修正原程序目录内部的文件路径，外部路径保留。身份私钥继续使用 Windows 用户保护，跨账户/电脑需重新核验信任。Portable 更新入口提示手动更新对应 ZIP，阻止自动转为安装版；安装版按进程架构选择更新资产。

Android 默认一次构建 armeabi-v7a、arm64-v8a、x86、x86_64 及 universal 的 Debug/未签名 Release APK，保留旧通用包名；从输出元数据选择 APK，并检查对应 Conscrypt 原生库、记录 ABI 与 SHA-256。IDE 普通构建保持原路径，分包由 Gradle 属性显式开启。

**已验证**：Android 196 项单元测试、协议测试、Debug lint（0 错误/15 个既有警告）和双构建通过，10 个 APK 原生 ABI 校验通过。Windows x86 原生执行发现并修正 winsqlite3 的 Cdecl/StdCall 声明错误；x86/x64 各通过 12 项迁移/架构、32 项更新、2 项实际 portable 数据与更新门禁检查，以及 2337 项 WPF 原生 AutomationPeer/几何检查和 61 张截图。安装器真实组件通过 134 项检查/36 图，三架构标签无裁切。最终三架构各通过 66 项安装归属检查；9 个产物逐文件、PE、MSI 位数/升级顺序与 Burn 载荷核对通过。最终产物在 artifacts/windows/multiarch-20260913-final-packages、artifacts/android，证据在 .acceptance/build-architectures。

**Not verified**：Windows ARM64 实机、Android 各 ABI 设备逐一运行、蓝牙/USB 全硬件矩阵及分架构实际安装/升级/卸载。本轮未执行安装或上传发布；包未签名，构建成功不等于正式发布验收完成。此次局部 WPF 验证不替代其他条目记载的完整 UI 回归限制。

## 2026-09-13：Android 下拉刷新与一次启动扫描

Android 设备与会话页的“附近新设备”扫描按钮已移除，改为列表顶部下拉刷新。启动扫描遵循既有开关，每次进程启动最多一轮；等待持久设置和首次蓝牙可用，不再按 10 秒扫描/5 秒暂停循环，也不在连接结束、返回设置、前后台切换或身份操作后重启扫描。手动请求每轮最多 5 秒，重复下拉不叠加，结束/取消/失败会收起指示器，迟到回调不会污染新一轮。蓝牙广播与入站/可信设备连接策略仍保留，Windows 扫描未改。

已验证：190 项 JVM 测试通过，含 12 项新扫描/启动策略测试；Debug 构建与 lint 通过（0 错误/15 警告，其中 3 项为旧扫描按钮资源未使用）。REDMI K80 Pro 正常应用真机 UI Automator/新截图 12 项通过，包括短列表下拉启动真实扫描、刷新不挤动行、结束收起、闲置不循环和页面/前后台返回不重扫。已覆盖安装 Debug APK，初版核对手机 APK 与交付产物 SHA-256 一致，手机仍停留前台并常亮。产物 artifacts/android/BlueLink-0.2.17-android-debug-pull-refresh-5s-20260913.apk；证据 .acceptance/android-pull-refresh-20260913。

时长追加：按用户反馈从 10 秒缩短为 5 秒，12 项定向单元测试和 Debug 构建通过，真机 4 项 UIA/截图检查通过；5 秒版已覆盖安装。该追加未重跑全量/lint，前述 190 项和 15 个警告是改时长前的基线。

**Not verified**：其他机型、TalkBack 操作、深色及其他语言实机新截图、实际撤权/蓝牙关闭/系统杀进程注入；相关启动延迟与扫描清理逻辑有单元覆盖。本次不等同于全量双端通信或正式发布验收。

## 2026-09-13：最新 Windows 安装验收包

当前工作区已重新生成 0.2.17 / x64 Review 安装包：artifacts/windows/review-20260913-153502/BlueLink-Review-DO-NOT-DISTRIBUTE.exe。客户端、启动器、安装 UI、卸载器均重新构建；版本、安装器与 WPF UI 静态合约和 66 项安装文件归属检查通过。解包核对 21 项载荷、22 项 MSI/ZIP 文件以及内嵌安装 UI/MSI、恢复清单、Review 注册身份、同版本升级事务顺序均通过，565 个构建输入哈希保持一致。

此包未签名、不内置 .NET Desktop Runtime，正式发布锁未变；实际安装/升级/卸载与本包完整端到端验收 **Not verified**，未替换当前运行的客户端。NU1900 漏洞源不可达不算漏洞审计通过。下文 USB 冷切换因外部防病毒扫描占用导致长等待的限制仍有效。完整校验结果见产物目录 build-verification.json / SHA256SUMS.txt，过程证据 .acceptance/package-20260913-153502。

## 2026-09-13：USB 自动检测事件唤醒（响应验收未通过）

双端已加入 USB/目录/开关变化事件唤醒、短期快速检测、忙接口退避和取消/信号合并。Android 区分“仅充电”的 unlocked=false，明确不可用时撤销就绪；可用需稳定 1.2 秒。USB 变化保留同一认证会话目录，Windows 重新验证路径、标记与对象 ID；Android 手动重试已接入真实检测。

已验证：最终保留版 Windows 11 项调度检查、加密/篡改/队列/COM 回归；Android 178 项 JVM 测试，Debug 构建和 lint（0 错误/12 警告）。16 MiB 和三份 4 MiB 合成文件在通道恢复后经 WPD 完成，Android 独立 SHA-256 一致。最终 Windows 运行 client-wpd-responsive-checked，手机保持 MTP/前台/常亮，未制作安装包；Windows NU1900 漏洞源不可达警告仍在。

**未通过**：用户真实拔插后目录探测等待 226375 ms（74 次），期间 WPD 返回资源忙。试验跳过尚不可读的同级目录虽使 3 轮图标恢复约 7.8–8.3 秒，随后首次写入与重试仍返回资源忙；该提前点亮试验及其回归已撤回，不算最终性能。仅充电切换到 MTP 同样复现约 220–231 秒等待，原始文件名读取试验也已撤回。命令级跟踪测得一次 MTP 文件读取持续 169.492 秒；WPD API 头部跟踪直接捕获北信源防病毒托盘进程 vrvsdtray.exe 的 1013 次文件读取请求，同期蓝联收到资源忙。已定位这台电脑的外部扫描占用，调试时几百毫秒是扫描结束后的通道核验，不能代表冷切换性能。未修改安全软件策略，无法承诺该环境秒级就绪。

本轮在 228078 ms 后自行恢复，随后首次发送新的 4 MiB 文件完成，Android 独立 SHA-256 一致，真实双端截图已审阅。文件选择器就绪延迟使发送晚于闪电出现约 2 分 44 秒，未证明图标亮起瞬间可写；Offer 至完成确认约 14.30 秒，也不视为吞吐达标。

诊断跟踪已自动停止；API 头部样本有 878 条丢失，计数不代表全量。没有采集文件数据缓冲区，没有改驱动/服务/防病毒策略。

**Not verified**：安全策略调整后的因果 A/B、其他设备、Hub、多机、长期耐久、每次首次写入可靠性。真实拔插已经测试但响应目标未通过，不再列为未测试。详见 WORKLOG 与 .acceptance/usb-responsive-20260913；本条优先于较早候选的 USB 就绪结论。

## 2026-09-13：移除信任后移出设备列表

双端移除信任后断开该设备并从已连接/离线列表移出；仍存在于当前扫描结果的端点归入附近新设备，扫描消失则隐藏。历史消息、传输记录和接收文件保留；已移除标记在启动同步时保留，重新核验信任可恢复原会话。以显式 Removed 状态区分普通未信任历史，没有删除历史记录或变更数据库结构。2026-09-14 已补齐 Android 旧版 UNKNOWN 残留记录兼容迁移，见本文件顶部更新。

已验证：Windows 19 项隔离加密会话/数据库/原生 UI Automation 回归与两张截图，Android 173 项 JVM 测试及 Debug/lint（0 错误/12 个既有警告）；REDMI K80 Pro 上隔离 Room、密钥存储和生产设备界面验证移除、重新初始化、迟到回调、重新信任及记录保留，UI Automator 与新截图检查通过。双端已更新到最新验收版本；Windows NU1900 漏洞源不可达警告仍在。证据 .acceptance/forget-device-20260913。

**Not verified**：未撤销用户真实手机/电脑信任以测试硬件会话；强制杀进程/磁盘故障注入、多机长时验收未做。未打包安装器或提交代码。

## 2026-09-13：Windows 离线卡片时间

Windows 离线分组中的历史设备统一显示最后连接时间，包含被扫描发现但尚未建立会话的设备；无时间则显示本地历史提示。连接能力和按钮保持原逻辑，Android 未改动。Windows 构建及真实 UI Automation/截图核对通过，已切换运行最新验收候选；未生成安装程序。证据 .acceptance/offline-last-connected-20260913。

## 2026-09-13：断线任务可重试与 Android 滚动稳定

两端会话结束后，在途和暂停任务转为失败，不再展示无效的继续/取消入口；发送方可在重新连接且源文件可读时“重试”。原任务 ID 保留，新会话创建新控制器，终态及旧会话缓存不再覆盖新进度。蓝牙从真实连续检查点重试，WPD 重传当前完整文件；USB 单独失效会结束关联任务。关闭原因先记录再释放通道，主动取消重试不产生误导性错误提示。

Android 图片异步改变高度时跟随消息真实底部；阅读历史时不自动跳转。预览返回保留滚动追踪和缩略图尺寸，三类设备分组标题展开/收起高度一致。

已验证：171 项 Android JVM 测试，Windows 147 项任务生命周期/队列、30 项暂停、81 项 USB/路由和 8 项实际加密 PeerSession 重连检查；手机生产 UI 组件隔离场景 25 项检查。正常应用 WPD 实测 128 MiB 文件暂停→断开→重连→重试→取消→再重试完成，接收状态正确，独立 SHA-256 一致；暂停发生在发送暂存进度 100% 的在途阶段，不计为 WPD 密文断点续传。最终双端构建已更新供验收，未制作安装程序。Android lint 0 错误/12 警告；Windows 构建成功，NuGet 漏洞数据源不可达的 NU1900 警告仍在。证据 .acceptance/reconnect-scroll-20260913。

**Not verified**：物理拔插、锁屏、多机/Hub、其他机型、长期耐久和 Android 发送方的完整 WPD 断线链本轮真机复验；跨进程自动恢复队列及 WPD 密文断点恢复不属于已实现保证。以下较早条目为对应时点记录，当前候选运行状态以本条为准。

## 2026-09-13：短暂异常提示与确认弹窗统一入口

扫描、连接超时/失败已接入现有 ToastHost：可读的三语提示、5 秒消失、自动重试按设备去重，主动取消不报错。原附近设备下方常驻反馈区域保持移除。客户端确认统一用 ConfirmationWindow；安装/启动/卸载的通知和确认统一用 InstallerDialogWindow。两个运行时共同使用 WindowsDialogLayout 尺寸规范，产品源码不再使用默认 MessageBox 控件或加载后模板修补，静态门禁及 AGENTS.md 已同步。

构建无警告/错误；Toast 36 项、客户端 444 项/48 场景、安装器 122 项/33 图、启动通知 2 个布局与实际字体解析通过，两套静态合约通过。真实设置返回入口的继续/放弃/保存及非法输入处理均通过；会话回归累计通过 188 项后仍在桌面焦点保持检查失败，整套未通过。外部真实鼠标、蓝牙异常硬件复现、完整安装/卸载矩阵 **Not verified**。截图和失败证据 `.acceptance/prompt-system-20260913`；用户仍运行此前构建，未打包或覆盖安装。下方较早条目仅描述当时状态，安装默认模板路径现已被本次统一组件替代。

## 2026-09-13：Windows 附近设备下方不再显示常驻反馈

已删除“附近新设备”下方的扫描/连接反馈区域和专用状态绑定，所有状态均不再在该位置显示提示；扫描按钮、设备卡片状态及诊断日志保持。截图中的英文源于连接超时异常原文，本轮仅移除显示区域，未复现或修复连接超时。

Debug 构建无警告/错误；隔离原生会话回归两轮各通过 188 项并产出 35 张截图，末项桌面焦点保持检查均失败，整套验收未通过。已审阅更新截图；焦点保持 **Not verified**。未打包或替换当前运行版本。证据 `.acceptance/home-feedback-removal-20260913`，详见 WORKLOG。

## 2026-09-13：Windows 提示弹窗统一

客户端普通信息、发送/文件错误、确认和未保存设置提示统一使用取消传输的确认窗口布局；无操作的按钮不占位，短提示紧凑，正文超高单独滚动，按钮与图标按主题展示。启动器及卸载启动错误同步替换默认 MessageBox；共用弹窗正文/图标、标题/关闭按钮按中心线对齐，启动提示实际解析打包字体。既有安装/卸载确认原型保持。

48 个深浅主题/三语场景共 444 项检查、会话/设置 189 项、安装器 96 项及 2 个启动提示布局通过，相关模块构建无警告/错误。Computer Use 应用授权超时，外部真实鼠标/hover 复核 **Not verified**；已完成原生窗口/AutomationPeer 检查及渲染截图审阅。未制作安装包、未替换用户正在运行的旧版本。详见 WORKLOG 和 `.acceptance/dialog-unification-20260913`。

## 2026-09-13：生产 USB 文件通道改为 WPD/MTP

双端生产运行时已移除 AOA 自动接入，改为 Windows 系统 WPD + Android SAF 专用目录；保留 MTP，不替换驱动、不切 USB 用途、不依赖 USB 调试。原有蓝牙信任核验、消息、控制、接收确认/大小限制、重名策略及最终 SHA-256/原子发布保留。首次使用须在手机选择“传输文件”，并授权专用中转目录；未就绪或旧版本未协商 `0x20` 时，新文件仍使用蓝牙。

同一物理手机的双向文件共用 FIFO 队列，后续文件显示“排队等待”；暂停队首保留槽位，取消等待项跳过该项，取消队首须等本地流退出后接续。不同手机使用独立调度器；文件探测、清理与数据流共用访问门。外部设备忙仅在打开/创建流前有界退避，部分写入不自动重放。闪电由认证会话对应的目录证明和 WPD 就绪状态决定，名称/MAC 不建立信任。公共目录只保存 BLM1 加密中转文件，单次密钥经加密蓝牙发送。

**已验证**：指定 REDMI K80 Pro / Android 16，USB 调试关闭、无线 ADB 仅控制蓝联验收，使用原 MTP 驱动。真实系统 SAF 授权 `Download/BlueLinkUSB`、正常 Windows 文件选择器批量发送、Android 真实运行时反向发送、双向竞争时等待、队首暂停/恢复、取消等待项、取消队首后下一项完成均有实机依据。Windows 暂停超过 17 分钟后仍可恢复，后面的 Android 256 MiB/16 MiB 按顺序启动。关闭并保存 Windows USB 开关后 1 KiB 通过蓝牙，重新开启后闪电恢复，256 MiB/16 MiB 再走 WPD 完成。文件字节通过独立 SHA-256 核对，未以 ADB 在线代替数据通道证据。

完整应用链路 256 MiB 正向约 9.7–10.3 秒（约 25–26.5 MiB/s）、反向约 10.1–10.9 秒（约 23.4–25.3 MiB/s），从队首启动计至接收端校验/发布完成，包含加密中转、WPD、解密和整文件校验，不包含排队及发送前快照准备。暂停场景不参与吞吐计算；这不是资源管理器原始吞吐或多机性能承诺。

自动化：Android 168 项 JVM 测试通过；Windows 新 BLM1 空文件/跨记录/篡改/错误 UUID/尾部拒绝、接收检查点重开、队列取消/设备隔离回归通过，两种实现 7 组密文向量完全一致；81 项 USB/路由及 30 项暂停控制回归通过。接收/设置/安全握手/通知定向回归共 200 项通过；共享 Java 协议 43 项、Windows 协议 14 项通过，覆盖旧版能力协商回退。双端 Debug 构建成功；最新 APK 已覆盖安装，Windows 运行最新验收候选；未制作安装程序或提交代码。原始证据 `.acceptance/wpd-production-20260912`，含真实 UIA/截图、运行日志和独立哈希。

**限制 / Not verified**：多手机和 Hub 实机并行、物理拔插、锁屏、其他机型、外部资源管理器 UI 同时复制、长期耐久和全量发布矩阵未验。原生 COM 阻塞依赖驱动返回；WPD 任务重试重传整文件，没有密文断点续传或跨进程持久排队保证。完整中转增加临时磁盘需求；异常终止可能留下加密中转文件，跨进程残留回收尚未验收。测试中 APK 更新导致在途任务中断，失败记录保留；曾有旧测试客户端已销毁窗口和控制通道但未退出的情况，仅结束该已核对的测试进程，不将它记为正常退出通过。该生命周期问题与跨进程任务恢复仍需发布前复核。

历史 AOA/原始 WPD 实验的详细数字保留在 WORKLOG.md。之前“纯 MTP 接口无法自动进入 AOA”的结论属于已被替代的生产路径；新方案需要用户已开放文件传输及 SAF 授权，不能承诺仅充电时也自动加速，也不会自动改手机系统用途。

## 2026-09-12：Android 正常会话输入法避让与附件图标

正常会话入口明确采用 adjustResize，消费已应用的 Scaffold 内边距，避免键盘仅把光标平移露出而遮挡输入栏底部。附件图标由原 Figma 路径生成完整、居中的矢量资源，替换左侧描边被截断的旧 PNG；按钮和输入框的紧凑尺寸保持不变。

REDMI K80 Pro / Android 16 / 当前搜狗输入法，正常 MainActivity 的空输入、单行、六行草稿滚动、清空和反复开合键盘共 7 状态通过；输入框和两侧按钮完整位于键盘上方，标题栏不平移，收起后恢复位置。附件图标实际像素边界与居中核对通过。167 项 JVM 测试、最终 Debug 构建和 lint 通过（0 错误 / 9 个既有警告）。最终同签名 APK 已覆盖安装并再次复核，草稿已清空、未发送消息，应用窗口继续常亮。

证据 `.acceptance/composer-ime-20260912`；回归入口 `scripts/test-android-composer-ime.py`。**Not verified**：其他设备/输入法、横屏/分屏、浮动键盘、字体缩放和其他系统导航方式。本轮未使用模拟器、Computer Use 或生成 Windows 安装包。

## 2026-09-12：聊天缩略图按比例与下限自适应

双端聊天图片已采用自然比例与最小短边规则，约 48 作为视觉参考值：小图不继续缩小或强制放大，留白补足最小区域；大图缩至短边下限后仍超出最大范围时，显示源图中心部分。Windows 上限 240×144，Android 190×126，布局不再给每张图片相同宽高。完整预览和消息搜索保持原行为。

完成的图片优先使用原图尺寸，未完成的图片只读取已接收预览。两端缓存均隔离新裁切策略；Windows 缓存 DPI 归一化，避免重读后尺寸漂移；进度标记不撑大最小缩略图。

Windows Release 0 警告/0 错误、官方控件契约、77 项原生专项后台检查/25 图通过。Android 167 项 JVM 测试、Debug 构建与 lint 通过（0 错误/9 警告）；原签名更新真机，浅深色各 8 类图片的实际边界、居中裁切及暂停状态核对通过。证据 `.acceptance/thumbnail-geometry-20260912`。Windows 安装包及运行候选未更新，Android 已更新并返回正常应用常亮界面。

**Not verified**：全部编码格式、其他设备/DPI、巨幅图片压力及传输/安装矩阵。原图未完成且预览不含原始宽高时，使用预览尺寸计算；原图完成后使用原始尺寸。本轮整套历史会话运行的末尾焦点断言未通过，最终尺寸/缓存/裁切与焦点结论来自独立缩略图专项，未将整套运行记为通过。

## 2026-09-12：设置离开确认、暂停等待、缩略图及输入栏

Windows 返回时发现未保存设置会提供「保存并返回 / 放弃修改 / 继续编辑」，保存校验失败保留草稿，改回已保存值不提示。此前 USB 与缩略图表现相反的 Windows 个案确认为修改后没有保存：数据库仍是 USB 关闭、缩略图开启。功能仍采用显式保存，未改成自动保存。

Windows 已补齐双向暂停控制与独立的本端/对端暂停状态；接收方展示等待，迟到进度不会冲掉暂停。Android 开启缩略图后可显示传输中已收到的预览，同时保留暂停/进度状态；未完成原图不可打开，关闭开关显示文件卡片。Android 输入栏两侧按钮收紧至单行输入框高度（默认约 40dp），图标 20dp 居中，多行时底部对齐，触控命中范围保留。

验证：Windows Release 0 警告/0 错误，官方 UI 契约、30 项暂停与 81 项 USB/传输回归、189 项原生后台 UIA/35 图通过；Android 164 项 JVM 测试、Debug 构建及 lint 通过（0 错误/9 警告），原签名覆盖更新真机。浅深色预览开关/暂停叠层、单多行输入栏与禁用/发送清空已在真机核对。

真实 USB 使用生产传输代码的后台 Windows 宿主：双向各 16MiB 完整传输及完整性核对通过，双方发起暂停均使对端进入等待；Android 发起暂停使用实际会话操作菜单。初始 USB ADB 占用导致 AOA 初始化访问拒绝，临时释放并恢复 ADB 后可工作，AOA 建立后两者共存。此操作仅用于验收，应用未加入强制停止调试工具的行为；不能据此声称 USB ADB 持续占用下也能自动切换。

证据 `.acceptance/transfer-fixes-20260912`。Android 已更新并返回正常蓝联界面，应用窗口常亮；Windows 当前运行候选及安装包未更新，硬件验收宿主已停止。**Not verified**：其他设备/字体缩放、多个物理设备、更多 USB 配置、长时间稳定性、测速与安装升级矩阵。未使用模拟器或 Computer Use。

## 2026-09-12：关于页说明移除与 Android 设置标题统一

Windows 关于页已移除 Bluetooth Only 蓝底说明条及占位。Android 设置首页与会话/文件首页共用标题组件：18sp 加粗、26sp 行高、34dp LOGO，横向边距和标题栏高度相同；子页返回导航保持原逻辑。

Windows Release 0 警告/0 错误，原生后台 5 项检查及浅深色截图通过。Android Debug 构建、lint 通过，原签名覆盖更新真机；三个真实首页的 LOGO 边界、标题左边缘/顶部/底部一致，设置页前后截图已核对。证据 .acceptance/about-header-windows 与 .acceptance/about-header-android。手机返回正常设置页，应用窗口常亮。

Windows 尚未重新打安装包。其他手机、字体缩放及本轮之外的通信场景 **Not verified**。

## 2026-09-12：文件类型图标（双端已接入）

Windows 与 Android 已共用 Figma 1375:291 的 19 类文件图标和 177 个扩展名规则，覆盖附件、文件列表、消息搜索、文件详情或不可用提示；真实图片缩略图继续保留。专用 PDF、Word、SQL、配置及镜像等类别优先，未知扩展名按 MIME 回退。qcow/qcow2、ova/ovf、vdi 均归入镜像。图形采用用户微调后的当前主图，浅深色依照分类色板；WPF 已验证现有图标在主题切换后立即更新。

资源、配色和映射集中在 shared/file-icons；19 张透明 PNG 为 40×40 Figma 主图的 6.4 倍直接导出，SVG 源保留，双端使用同一透明度遮罩。生成器 --check 通过，原稿颜色、尺寸和透明边缘检查通过；Phosphor MIT 全文已进入两端开源许可。

Windows Release 构建、536 项分类与原生 WPF UIA/渲染检查、官方控件契约通过；4 张浅深色实际文件表格/附件模板/20–40 尺寸图标截图已核对。Android Debug 构建、161 项 JVM 测试通过，lint 0 错误/9 警告。证据 .acceptance/file-icons-windows 与 file-icons-*-build.log。

Android 真机补验已完成：与设置标题同一版 Debug APK 使用原签名覆盖安装后，19 类图标在浅深色 20/24/40dp 均完整显示。使用独立内存验收场景，不写聊天/传输样例，证据 .acceptance/file-icons-android-device。

**Not verified**：其他设备/DPI、通信及安装矩阵。未生成 Windows 安装包或更新 Windows 已安装客户端，未使用模拟器。

## 2026-09-12：Android 选区手柄越界拖动（真机修复）

单条消息正文改为官方只读 BasicTextField，替代多组件 SelectionContainer 在上下边界外直接选到全文起止的行为。手指移出气泡后按最近文本行/字符继续更新选区，返回气泡和跨行拖动保持连续；浅蓝手柄、浅深色选区底色、固定消息布局和外部点击清除保持有效。正文不可编辑，失焦清空实际选择范围。

Debug 构建及 159 项 JVM 测试通过，最新 APK 已按原签名覆盖安装真机。ADB 连续按压验收覆盖单行原路径、英文多行两个手柄、中文收到消息、上下左右越界及返回；浅深色截图通过，正文/状态边界一致，局部复制 kilo 并粘贴核对通过，测试草稿已清空。证据 `.acceptance/selection-boundary-20260912`；自定义 UI Automation 探针被自动审批拒绝且未执行，结果来自标准 ADB 触控、截图及系统 XML。

Windows 无改动、未打安装包。**Not verified**：其他手机/系统、超长消息跨屏自动滚动及量化触控延迟。本条补足上轮仅在气泡内部拖动的验收缺口。

## 2026-09-12：文本选择后续修复（浅蓝色手柄）

Windows 点击别处后清除实际文本选区，再右键复制会得到全文；当前有效局部选区仍可正常复制。Android 点击当前选中消息之外的会话区域取消选择；发出消息选区与蓝色气泡区分，收发方向的手柄统一为浅蓝色 `#A8D1FF`。选择工具栏不再增加消息 Column 子项，修复选择/拖动时额外间距和状态行移动。

Windows Release 零警告/错误、官方控件契约及 174 项原生 UIA/28 图通过，包含实际局部/全文剪贴板与焦点清理回归。Android 最终 Debug 构建、159 项 JVM 测试通过，真机已覆盖更新；浅色真实会话已验证收发选区清除与几何不变，拖动过程中选区在抬手前更新，最终浅蓝手柄已复核。深色使用同一真机的应用内存验收场景核对，未修改用户设置。

证据 `.acceptance/selection-followup-20260912`。当前 Windows 候选为 `.acceptance/discovery-20260911/client-selection/BlueLink.exe`，安装目录及已有安装包未更新。**Not verified**：其他手机/系统、极长消息压力及量化帧率/触控延迟。本轮不包含通信或安装矩阵复验。

## 2026-09-12：设备名与消息交互（已验收，尚未重新打包）

双端安全握手支持可选的应用显示名称，Android 默认名称来源与设置一致。真实手机更新后，Windows 已连接设备卡片和会话标题均显示「Navi的REDMI K80 Pro」，不再显示地址或硬件型号。名称不参与身份/信任判断，更改在下次握手同步；没有扩展的旧客户端继续使用原回退。

Windows 消息右键不再改变气泡尺寸，发送悬停在浅深色均可读；正文可拖选、Ctrl+C，右键复制选区。Android 长按可选词并拖动手柄，复制局部文本且保留更多操作入口；最终真机复核浮条位置和打开操作菜单时的焦点清理均正常。真机实际复制「20260912 Windows」并在空输入框粘贴核对，测试草稿已清空，未发送额外消息。

Windows Release/官方控件契约、106 项安全握手回归、共享名称向量、144 项原生 UIA/28 图通过；Android 最终 Debug 构建与 159 项 JVM 测试通过。证据 `.acceptance/message-polish-20260912`。手机已覆盖安装，Windows 当前为 `.acceptance/discovery-20260911/client-messages/BlueLink.exe` 工作区候选；D:/BlueLink 与 9/11 23:34 安装包尚未更新。

**Not verified**：本轮 USB 硬件名称、多实体设备、其他 Android 版本/输入法及安装升级矩阵。未使用模拟器、Computer Use、Fixture.exe 或系统启动项。下方历史发现任务中的地址名称缺陷已解决。

## 2026-09-11～12：蓝牙发现与连接竞争修复（真机验证）

双方硬件均能收到 BlueLink 广播。已修复 Windows 只计新设备造成的“0 台”误导及历史卡片缺失连接入口；Android 完整保留已知设备发现端点、修正附近可连接投影，并支持平台元数据尚未到达的有效 Rendezvous 端点。安全握手与信任核验保留。

进一步修复真机同时重连竞争：Android 主动连接/Windows 回连共用按设备的拨号锁，登记会话后才释放，连接完成或失败恢复扫描；Windows 已收到的 socket 优先交给安全握手，迟到的 GATT 请求失败不会将其丢失。Windows 广播格式保持完整 ServiceData；实测 StartedWithoutAllAdvertisementData 仍可收到有效扩展广播，未采用中间精简广播实验。

Windows Release 零警告/错误，官方控件契约、86 项原生 UIA/20 图、73 项回连所有权回归通过；Android 构建与 156 项 JVM 测试通过，已按原签名覆盖安装真机。Windows 与 Android 两种发起路径均观察到真实 BTX/1.1 握手；最新候选重启约 3 秒重连，稳定超过 4 分钟，双向 QA 消息均送达。手机数据与身份保留，Windows 新信任通过 USB 公钥指纹核对和正常确认界面建立。

证据 `.acceptance/discovery-20260911`。当前手机为修复 APK，Windows 运行其中 `client-final/BlueLink.exe` 工作区候选；已安装的 `D:/BlueLink` 和下面 23:34 安装包均未更新。本轮未执行安装/卸载、模拟器、Computer Use 或 Fixture.exe。

**Not verified**：全新未配对设备、多实体设备并发、长时间稳定性及本轮文件性能。当时观察到的手机主动连接后 Classic 地址名称显示问题，已在 9/12 设备名任务修复并真机复核；身份/历史仍是同一设备。

## 2026-09-11：最新 Windows 验收安装包（23:34）

最新安装程序为 `artifacts/windows/review-20260911-233411/BlueLink-Review-DO-NOT-DISTRIBUTE.exe`（0.2.17 / x64，90,070,307 字节）。已纳入下方三轮源码修复：输入控件/设置反馈、取消连接数上限与双端设置分组中的 Windows 修改、消息行悬停和选中背景透明。下文“未打包”保留为历史任务的当时状态；请使用本节的新包。

构建和安装器/版本契约通过，66 项文件归属断言通过；离线解包核对全部载荷、MSI/ZIP、嵌入安装界面、客户端、卸载器及 Review 注册/升级/恢复配置。280 个构建输入未变，17 个 Windows 源文件与最新相关后台验收证据一致，正式发布锁未改。EXE SHA-256 为 `81EC55A3322ACBB16D63DF79C0CE83A6FF1695F86F1D77BDF6E07E81585A77B2`；详见产物内 `build-verification.json`、`SHA256SUMS.txt` 及 `.acceptance/package-20260911-233411`。

包未签名且不内置 .NET 8 Desktop Runtime，供当前验收使用。保留旧包，未启动安装程序；实际安装、升级与卸载仍为 **Not verified**。未操作真机、模拟器、Computer Use 或系统启动项。

## 2026-09-11：Windows 消息行背景修复（源码已更新）

消息行在悬停、选中及二者叠加时均保持透明；消息气泡配色和选择操作正常。Release 构建零警告/错误，74 项原生后台会话断言及 18 张截图通过，覆盖浅深色与收发方向；官方控件契约通过。证据 `.acceptance/message-row-background-20260911`。本轮未打包、未使用 Computer Use 或真机。

## 2026-09-11：连接数量与设置追加调整（源码已更新）

Windows/Android 已取消最大同时连接数设置和应用数量拦截，旧配置上限被忽略，安全核验和重复会话检查继续生效。双端连接设置统一为“本机设备、连接偏好、信任管理”，USB 位于连接偏好；Windows 本机设备图标、Android USB 图标已移除。Windows 补齐连接/通知/接收设置的行分隔线，文件页“更改”直接打开文件与存储。

Windows Release 零警告/错误，安全后台回归（含 12 个独立身份加密内存会话与旧配置兼容）及 7,095 项原生 UIA 设置验收通过；浅深色、三语、1000/1260 宽度截图已复核。Android Debug/Release Kotlin 编译、153 项 JVM 单元测试全部通过；lint 零错误，仅有 ObsoleteLintCustomCheck、KaptUsageInsteadOfKsp、IconDuplicates 三项既有警告。新增旧连接数配置忽略/不再保存和 12 个会话的路由隔离回归通过。证据 `.acceptance/unlimited-connections-20260911`。

没有重新打包或操作真机；Android 新分组视觉/触控及真实多机上限、性能仍为 **Not verified**。先前 Figma 的数量选择及独立 USB 卡片设计已被上述用户新要求替代，本轮未更新 Figma 文件。

## 2026-09-11：输入控件与设置页反馈修复（源码已更新）

最新源码已修正安装路径/文件搜索/消息搜索的单行文字布局，以及下拉选项的高度和内边距；保留多行编辑器顶部对齐。身份详情直接展示，帮助提示图标不再被窄列裁剪；许可列表高亮、蓝色标题及选中标记与帮助主题统一。设置页操作反馈改用现有 ToastHost，信息/成功 3 秒、警告/错误 5 秒自动消失，不占位、不推移内容。

后台验收通过：输入与设置追加行为 4,049 项/430 图，安装界面 110 项/15 图，关于子页滚动 201 项，文件/搜索工作区离屏 1,303 项/91 图；Release 零警告/错误，相关控件与接线契约通过。浅深色、三语、最小/较宽窗口均纳入检查；四档图像采样不等于四档真实系统 DPI 验收。证据 `.acceptance/input-layout-20260911`。

**本轮没有重新打包**；下方 9/11 已生成安装包不含这些最新修复。未启动安装引擎、Fixture.exe、Computer Use 或模拟器，未操作手机。

## 2026-09-11：最新 Windows 验收安装包

最新产物为 `artifacts/windows/review-20260911-latest/BlueLink-Review-DO-NOT-DISTRIBUTE.exe`（0.2.17 / x64，90,050,633 字节），包含当前设置对齐、紧凑界面/安装向导及 USB 主动取消修复。此前包不含这些最新变更；下文“未打包”为对应历史任务的当时状态。

本轮构建与安装器/版本契约通过，66 项文件归属断言通过；离线核对 21 个载荷文件和 22 个 MSI/ZIP 文件，嵌入安装器、客户端、卸载器及 Review 注册/升级/恢复配置一致。298 个构建输入未发生变化，正式发布锁未改。EXE SHA-256 为 `34B7DF00ECF4727A8A66F60B75251461BD95B7B2AC09B4A4066BA1B946FD2A33`，详见同目录 `build-verification.json` 与 `SHA256SUMS.txt`，证据 `.acceptance/package-20260911-latest`。

该包未签名、不内置 .NET 8 Desktop Runtime，仅作未公开验收版；未运行安装引擎，实际安装/升级/卸载仍为 **Not verified**。用户计划完成整体验收后完全开源，尚未执行公开发布、许可证选择或签名配置。

## 2026-09-11：当前真机补验结果

USB 与无线 ADB 已确认连接同一台 REDMI K80 Pro（Android 16/API 36）。最新 Android APK 保留原签名覆盖安装；常亮已在实际前台窗口验证，并修复 USB 入口新建 MainActivity 后丢失常亮的问题，仅对 Debug 明确启用的验收会话生效。

当前新增证据：Windows 完整安全后台回归 409 项及聊天时间组通过，Release 零错误/警告；Android 151 项单测、Debug 构建与 lint 通过（3 项既有警告）。正式设置页的开关/选择、重启持久化、缓存清理及恢复默认已在真机操作；原数据、身份/信任、原设置和 6 个应用私有原文件哈希保持。Windows 主动取消被误记 Failed 的问题已修正，后台和真实 USB 均复验为已取消，后续排队接收正常。

单手机真实 USB 双向 0 B、1 KiB、64 KiB−1/64 KiB/64 KiB+1、16 MiB、256 MiB 共 14 个文件的完成状态、大小和 SHA-256 全部一致，双向聊天到达。256 MiB 传输阶段实测 Win→Android 12.55 MiB/s、Android→Win 6.22 MiB/s；是单次短测，不含源文件准备时间。接收询问的同意、拒绝、30 秒到期、远端取消与后续排队请求通过，3 个附加接收文件也校验一致。Windows 为后台生产 USB/会话组件，Android 反向发送为 Debug 入口调用生产运行时；不代表双端正式附件选择器或 Windows 原生弹窗链路已完成。

证据：`.acceptance/physical-settings-20260911` 下的 `windows-safe-summary.json`、`android-final-summary.json`、`final-verification.json`、`wake-multiple-activity.json`、`settings-events.jsonl`、真机截图/UIA XML、`usb-user/file-integrity.json`、`usb-user/prompt-integrity.json` 和 `usb-cancel-fixed/events.jsonl`。验收宿主已退出、临时 USB/接收设置已恢复、两种 ADB 正常、蓝联前台继续常亮。本轮没有模拟器、Computer Use、安装夹具、系统启动项操作或 Windows 打包。

以下清单保留审计基线，相关行已合并本次证据；未列为通过的条件继续视为 **Not verified**。尤其 Bluetooth 最新链路、双侧暂停/拔插续传、SAF/存储失败、系统通知/后台/锁屏、多手机、兼容/耐久与安装矩阵仍未闭环。

## 2026-09-11：双端测试缺口与验收顺序

以下为补验前的证据审计基线；当时只核对源码、测试入口、结果文件和既有原型台账，后续执行结果见上节及已更新的清单行。范围是 Windows、Android 和 Windows ↔ Android；README 尚未将同平台互通列为本轮交付目标，未把它扩成必测范围。

证据分三层：**后台已测但系统/真机未闭环**、**旧版本已测但最新代码待回归**、**未找到完整对应证据**。下列“待补”不等于功能没有实现，也不表示已确认有缺陷。

当前可确认的基线：

- 2026-09-10 设置专项：Windows 196 项行为断言、7,047 项后台原生 UIA 检查/86 图；Android 151 项 JVM 单测，Debug 构建和 lint（零错误/3 项既有警告）通过。7,047 大量来自尺寸、主题、语言及文字几何重复检查，不能换算成 7,047 个独立业务场景。Windows 专项入口不执行 USB、更新、反馈、全部历史/预览等完整回归，也不运行安装夹具。
- 2026-09-09 USB：Windows 生产传输组件后台宿主 → 一台真实 Android，16/256 MiB，完成 ACK 与手机文件大小/SHA-256 一致；正常退出、强制终止失联检测，以及手动关闭/开启应用 USB 开关后原接口重连已验证。Android → Windows、多台手机、无需操作自动重连、物理拔插续传未闭环；不是双方正式应用完整流程验收。
- 2026-09-09 身份关联：Windows 自动测试和 Android 真机隔离确认/取消、Room 2→3 实际迁移及原有数据一致性已核对；不是实际蓝牙/AOA 重置身份后完整跨端重关联。
- 安装有历史隔离执行证据：旧基线首次安装、故障复现、半安装恢复、同包修复、注入失败回滚、卸载保留 Download。健康旧版直接升级、新候选全新安装与完整矩阵当时因杀毒软件 RunOnce 拦截停止；最新紧凑向导只有界面/构建验收。
- 旧 Windows 台账为 148 节点/145 项有组件范围原生证据；旧 Android 应用台账为 118 场景，其中 85 partial、33 not_verified，另有 1 个对照板。后续专项和零散证据未完整回填，设置/USB/身份又已更新，**这不是最新版本剩余场景数或覆盖率**。某些反馈节点仍记未验，但已有真机 ZIP 实物证据，必须先对照合并，不能重复判为从未测过。

优先级：P0 为数据/连接核心闭环或相应发布阻断；P1 为正常交互、生命周期及系统集成；P2 为扩展兼容与长时间压力。安装/公开更新的 P0 以准备发布为前提。UI 宿主、模拟传输与注入失败不能替代其系统环境的成功路径。

### Windows 待补

| ID | 场景 | 已有证据边界及还需验证的内容 | 条件/优先级 |
| --- | --- | --- | --- |
| W01 | 最新版本完整回归 | 已补 `--safe-background-only` 并在取消修复后通过 409 项计数断言及聊天时间组；完整安全后台回归完成，安装执行明确排除。原生 UI/硬件缺口分别见后续行。 | 可后台，P0 |
| W02 | 接收弹窗完整生命周期 | 现有通信测试用替代决策回调，UIA 测 CreateWindow；缺真实 DecideAsync 接线、多请求排队、取消/断线/到期关闭窗口、点击与超时竞争、关闭应用时释放队列的闭环。 | 可后台，P0 |
| W03 | 连接偏好到系统服务 | 单纯策略与保存已测；启动不扫描、发现关闭不误断现有会话、四种自动连接/重连组合、人工断开抑制及手动连接恢复，尚需完整调用链/蓝牙适配器验证。 | 先后台替身，再真机，P0 |
| W04 | 桌面与输入 | 当前重点为 150% DPI 和有限逻辑窗口宽高；100%/125%/200%、跨屏动态 DPI、工作区小于最小尺寸、混合附件整页、真实鼠标命中/瞬时按下、键盘导航/输入法/辅助功能矩阵仍缺。历史已验证窗口重启恢复，不列为从未测。 | Windows 桌面/多屏，P1 |
| W05 | 托盘与通知 | 通知中心/原生控件宿主已有自动证据；系统托盘悬停、任务栏角标/闪烁、系统通知开关/免打扰、点击通知选正确会话、最小化/关闭到托盘/退出与未读保持未闭环，三类新通知开关需实测。 | Windows 系统外壳，P1 |
| W06 | 本地文件与数据异常 | 同名重命名和基础缓存保护已测；并发同名、覆盖授权后目标被占用/替换、目录只读/失效/空间不足、长路径/Unicode、预览原件被移走，及实际文件选择/保存/Explorer/剪贴板交接需补。记录开关/清理与活动任务并发、大历史量和跨进程恢复需系统化回归。 | 后台 + 文件系统/桌面，P0/P1 |
| W07 | 安装/卸载/运行时 | 最新候选全新安装、健康旧版直升、同版覆盖、正在运行时更新、保留/删除数据两分支、注册/文件/快捷方式一致性，以及缺 .NET 的下载校验/UAC/拒绝/重启恢复。保留已有隔离成功证据，不把局部测试当完整矩阵。 | 隔离 Windows 环境，发布 P0 |
| W08 | 更新与反馈交接 | 更新模拟 HTTP、损坏包/未签名拒绝和测试启动器已测；缺公开源真实下载、有效签名成功路径、退出→安装→重启。反馈 ZIP 及部分系统保存有历史证据，最新界面与路径失败/取消后的草稿保留需复验。 | 后台 + 实际发行/系统条件，P1/发布 P0 |

### Android 待补

| ID | 场景 | 已有证据边界及还需验证的内容 | 条件/优先级 |
| --- | --- | --- | --- |
| A01 | 最新设置的真实使用 | 9/11 较早版本已覆盖安装真机，正式入口操作三类通知、当时的连接偏好、禁用上限编辑、缩略图、历史/诊断、公共目录四选项、缓存清理、恢复默认和身份详情，关键值重启/Room 核对通过。其后取消连接上限及三组布局只完成后台检查，新布局真机复验待补。实际通知、后台/充电约束、公共目录发布与失败分支分别仍见 A02–A05。 | 一台手机，P0 |
| A02 | 权限全流程 | 附近设备/通知首次授予、拒绝、不再询问、系统设置返回；系统蓝牙关闭/开启失败；连接/传输中撤权及恢复。早期模拟器记录不作为当前真机结论。 | 手机系统权限，P0 |
| A03 | 前后台与电源 | 锁屏、息屏、省电/Doze、进程被回收、重新启动；后台连接开关两态，关闭可选通知后必要前台服务仍正确；100 MiB 充电限制及接电/拔电临界状态。前台/后台/锁屏新消息的未读与通知需真实事件验证。 | 手机 + 通信对端，P0/P1 |
| A04 | SAF/公共目录 | 已有发布事务纯逻辑测试；缺真实文档提供器权限持久化/撤销、目录不可写/消失/空间不足、自动保存四组合、同名三策略及覆盖失败回滚。需检查应用内文件与公共目录副本，不能只看完成提示。 | 手机存储提供器，P0 |
| A05 | 缓存与记录 | 已验证真机正式入口清缓存成功、6 个应用私有原件/接收预览哈希不变，新配置 Room 持久化和恢复默认不改原身份/历史。损坏缓存、清理与预览并发、清后重建、保留期限实际删除及活动任务记录竞争仍需补。 | 可先拆策略后台测，最终真机，P0 |
| A06 | 全页面与手势 | 原型逐节点、正式入口和状态闭环未完成；重点补搜索结果/空态/日期筛选、定位原消息、会话文件页、文本/附件菜单、拒收/取消/移除信任等确认、图片缩放/双击/拖动/旋转、系统打开/分享。三语/浅深主题、小屏/大字体/键盘遮挡及 TalkBack 仍非全覆盖。已有组件截图需复用并标版本。 | 手机，P1 |
| A07 | 迁移与本地更新 | Room 2→3 有旧版证据；最新同签名覆盖安装后原消息/附件/传输逐行、Peer/会话 ID/草稿、信任和身份已核对保持。本地 APK 更新的正确包、错误包/签名、取消、系统安装权限及升级后启动/数据保留尚未形成完整矩阵。 | 手机安装器，P1/发布 P0 |
| A08 | 机型/系统兼容 | 当前主要物理证据来自一台 API 36 手机；minSdk 33，Android 13/14/15/16、不同厂商蓝牙/USB/后台策略及屏宽/字体的最新真机矩阵未完成。 | 多设备环境，P2 |

Android 本地 APK 入口保留；GitHub Release 应用内检查、下载、校验及系统安装交接已实现，新增 INTERNET 与 REQUEST_INSTALL_PACKAGES。同签名候选真机覆盖成功，完整公网与正式签名跨版本升级仍未验证，见最新条目。

### 双端通信待补

| ID | 场景 | 待补的完整闭环 | 条件/优先级 |
| --- | --- | --- | --- |
| C01 | 发现与建立连接 | Windows 发起、Android 发起、同时发起；首次系统配对/已配对、取消/超时/对端离线；隐藏发现、蓝牙热切换、可信设备自动连接及人工断开后的行为。旧版本基本互通已有证据，新偏好尚未真机复验。 | PC + 1 手机，P0 |
| C02 | 安全与设备身份 | 双端显示相同安全码、任一方拒绝/超时、确认期间离线；真实重置身份后的唯一旧设备关联/歧义/旧设备仍在线、撤销信任立即影响两个通道，旧身份不能重新获得信任；历史、附件、未读和草稿不串设备。 | PC + 手机，歧义需额外 Peer，P0 |
| C03 | 消息与回执 | USB 双向单条聊天已到达；Bluetooth 两个方向、双向同时发送及突发消息；发送回执、乱序/重复保护、失败/排队补发；前台读当前会话、设置/文件页、其他会话、后台/锁屏时计数和提醒，以及重启回读一致。USB 会话并存时亦不得双计数或串路由。 | PC + 1 手机，P0 |
| C04 | 文件基本与边界 | USB 两个方向已补 0 B、1 KiB、64 KiB−1/64 KiB/64 KiB+1、16/256 MiB，共 14 文件全部完成/哈希一致（后台生产宿主与真机生产运行时）。剩余 Bluetooth 两方向、双方同时发送；0 B、1 KiB、64 KiB 块边界、10 MiB、500 MiB 上下边界、关闭上限后的大文件；图片/普通文件、多小文件、中文/长名。每次核对完成 ACK、接收路径、大小、SHA-256 和双端记录。 | PC + 1 手机，P0 |
| C05 | 接收与异常组合 | Android 真实 USB 询问开/关、同意/拒绝、30 秒到期关闭、取消当前并处理下一排队请求已过；Windows 取消终态修复后双端均为已取消。仍缺 Windows 原生询问链路、同名重命名/询问/覆盖与更多并发组合；超限提高上限后继续、拒绝及过期；Windows 磁盘占用/空间不足与 Android SAF 失败均需反馈到发送端并保护旧文件。 | PC + 1 手机，P0 |
| C06 | 控制、中断与续传 | 发送/接收双方分别暂停/继续、双方同时暂停、一方继续但另一方仍暂停、取消临界态；蓝牙中断、USB 真拔插、杀一端进程/重启后明确重试与持久断点；不丢字节、不重复完成、不复活取消任务。 | PC + 1 手机，P0 |
| C07 | USB 选路与恢复 | WPD/MTP 不可用、SAF 授权拒绝/撤销、外部占用、换接口/线/Hub；认证蓝牙与专用目录绑定，按设备串行双向队列，队首/等待项取消恢复、USB 开关及物理失联恢复；只在真实就绪时显示闪电。 | PC + 1 手机/接口环境，P0 |
| C08 | 多手机并发 | 至少 2 台手机同连，BT/USB 混合及多条 USB 大文件；A 断开/失败/拒收/重置不影响 B；选择会话、进度/速率/未读/接收路径、闪电均按 Peer 独立。1/2/4/8 上限、满额和并发连接竞争需要组合验证，可先后台模拟高数量。 | PC + ≥2 手机，P0 |
| C09 | 生命周期及耐久 | Windows 睡眠/唤醒、应用崩溃恢复、手机锁屏/后台回收、双端先后启动；重复连接/断开、大量历史/消息/文件、长时间持续运行、内存/句柄/CPU/耗电。USB 已有单向短测速度不能代表双向/多机/长时间性能。 | 硬件 + 持续时间，P1/P2 |
| C10 | 版本与硬件交叉 | 新 Windows + 旧 Android、旧 Windows + 新 Android、不同 Android/Windows 支持版本及蓝牙/USB 实现；兼容时协商正确，不支持时清楚拒绝，不错误继承身份或损坏记录。纯 MTP 初始识别等已知实现限制单列，不以“测试通过”掩盖。 | 多版本/硬件，P2 |

可在无手机环境先推进的顺序：W01 全量安全后台回归 → W02 弹窗决策队列/竞争 → W03 连接偏好调用链与通知事件替身 → W06/C05/C08 存储错误、并发与上限模拟 → A05 配置/缓存/通知策略测试补强 → 原型台账按最新设计与具体证据回填。真正 Android SDK、驱动、系统外壳的行为仍留系统/真机验证。

设备恢复后：先一台手机完成 C01–C07 和 A01–A07 的核心收发/安全/数据闭环，再进行 C08 多手机与 C09–C10 耐久兼容；安装/卸载/公开更新在独立、可恢复环境验收，沿用此前不再运行反复触发安全软件告警的夹具边界。后续已启动的单手机设置与 USB 子集见文首；其余仍待实施。

结案标准：每个场景记录代码/产物版本、系统/设备、主题/尺寸、链路/方向、前置设置、操作与断言、日志/截图；文件需双端哈希，迁移/身份需前后记录一致，通知需系统实际证据。旧版通过、部分组件通过和故障注入通过不得直接升级为最新完整场景通过。

证据索引：`.acceptance/settings-alignment-20260910/verification-summary.json`、对应 `native-ui/results.json` 与测试日志；`.acceptance/usb-liveness-20260909/checks.json`、`benchmark-final/results.json`；`.acceptance/usb-hardware-20260909/run3/results.json`；`.acceptance/peer-reassociation-20260909/phone-data-checks.json`；`.acceptance/install-repeat-20260907-03/results.json`；`.acceptance/prototype-cua/20260906/scenario-results.json`；`.acceptance/android-physical/20260907-refactor/prototype-inventory.json`、`20260908-stage1/*evidence.json` 与 `feedback-packages.json`。

双端设置功能对齐（2026-09-10，源码与后台验证完成）：Windows/Android 统一接收前询问、同名策略、500 MiB 可关闭上限、三类通知、三语、启动扫描/发现/自动连接/断线重连与五档记录期限；原 1–8 连接数已由 9/11 取消上限要求替代；Android 补缩略图、传输历史、诊断、清理缓存及恢复默认入口。移除旧面板死配置、额外首次连接确认和重复信任入口，首次安全码/身份变化核对继续强制执行。两端缓存仅清可再生成缩略图，保留原件及接收预览；保留托盘/移动后台、公共目录与发行方式特性。Figma 主页面/选择状态及新接收弹窗已同步，映射 `design/figma/settings-alignment.json`。Windows 构建零错误/警告，196 项专项行为回归、7,047 项原生后台 UIA/86 图、设置接线及官方控件契约通过；Android 151 项单测及 Debug 构建通过，lint 零错误/3 项既有警告。证据 `.acceptance/settings-alignment-20260910`。该设置对齐任务完成时没有真机；9/11 已补正式设置操作与 USB 子集，权限/系统通知与后台、最新 Bluetooth 完整链路、Windows 通知实际展示及其他 DPI 仍为 Not verified。未使用模拟器或 Computer Use，未启动安装夹具；未生成新 Windows 安装程序，既有检查包不含本轮修改。

安装向导紧凑化（2026-09-10）：固定 800×560 DIP，系统不提供缩放/最大化，官方标题栏保留最小化与关闭；同步调整安装、结果与运行时页面的文字/卡片/页脚。完成页左侧图标 18 DIP、背景仍为 36×36；右侧复制图标 14 DIP、24×24 点击区域，与路径行居中对齐。Release 构建零警告/错误，最终原生后台 99 项/15 图、离屏回归 96 项/33 图及安装器/WPF-UI 契约通过；未执行安装引擎或实际安装。证据 `.acceptance/installer-compact-20260910`。仅源码与后台验收，未打包；现有安装包不含本轮修改。

Windows 按钮与设置补充（2026-09-10）：修复紧凑按钮旧垂直内边距导致文字裁切，设置输入框同步居中，英文返回按钮按内容宽度布局、导航项外边距收紧；连接页重复扫描卡片及专用代码移除，主界面扫描保持。设置/阅读内容区与底栏外边距 12 DIP，滚动条仅溢出时在右侧占 14 DIP，几何检查无重叠。本机只读检查信任数量确实为 0、两条历史 peer 为 Unknown，当前记录不能确定此前清除原因；空态已说明历史会话不等于当前信任，未改写用户信任数据。隔离验证覆盖离线已信任设备显示、历史设备排除、撤销信任即时刷新和重载保留历史。原生后台 4603 项/50 图，设置离屏 400 项/52 图、共用交互 1283 项/28 图通过；构建零警告/错误、官方控件契约通过。证据 `.acceptance/windows-settings-controls-20260910`。本轮仅源码与后台验收，下面的 review-20260910-latest 安装包不含本轮修复。

最新 Windows 安装检查包（2026-09-10）：`artifacts/windows/review-20260910-latest/BlueLink-Review-DO-NOT-DISTRIBUTE.exe`，0.2.17 / x64，包含全客户端紧凑化、关于子页面独立滚动/许可悬停，以及离线卡片/会话切换、预览关闭/设备图标和此前 USB/身份关联修复。构建零警告/错误，66 项目录归属及安装器/版本契约通过；离线解包核对 21 项载荷和 MSI/ZIP 的 22 个文件、安装/卸载身份、同版本升级与事务移除顺序均通过，24 个密度调整源码文件与已验收版本哈希一致。EXE 90,081,495 字节，SHA-256 `273F10643E2E7D87002E1DF28A9D2D020A35430C26C169FB09C7345D394439DC`。未签名、不含 .NET 8 Desktop Runtime；实际安装/卸载仍为 Not verified，本轮未启动安装包或 Fixture.exe。校验记录 `artifacts/windows/review-20260910-latest/build-verification.json`，证据 `.acceptance/package-20260910-latest`。当前 Windows 交付状态以本条为准。

Windows 客户端紧凑化（2026-09-10）：按用户确认覆盖主界面、设置、搜索、预览、通知和客户端弹窗，统一较小字号/控件高度、卡片内边距与间距；设备卡片最小 60 DIP、文件行 46 DIP、主要标题 18–22 DIP、常规操作高度 32 DIP。帮助/许可/隐私/协议使用左右独立且仅溢出时显示的滚动区，标题/许可搜索固定，移除只读页空白底栏；许可列表悬停不再覆盖选中高亮，长名称保留提示。原生后台 201 项/16 图、设置离屏 400 项/52 图、文件不可用 128 项/10 图、会话切换 22 项及四个原生预览关闭窗口通过；构建零警告/错误、官方控件契约通过。最终完整离屏套件 4686 项/224 次渲染通过，几何失败 0。证据 `.acceptance/windows-density-20260910`。后台原生窗口保持透明、桌面范围外、不激活；物理输入及其他 DPI 为 Not verified。已纳入 review-20260910-latest 检查包。

Windows 离线设备与会话切换专项（2026-09-09）：按 Figma 9:63 / 623:16522 恢复离线图标、图标底色和状态点的灰色语义，附近可连接也不显示蓝点，选中背景/边框及深色主题已核对。修复可变 record 哈希破坏 WPF 选择索引、其他入口切换后列表选中项不同步，以及旧历史异步返回覆盖当前会话的问题；当前卡片可从文件页返回消息，支持 Enter/Space。原生后台 22 项/2 张深浅主题截图和共用交互 1315 项/28 次渲染通过，Release 构建零警告/错误、官方控件契约通过；使用隔离数据库和透明、不激活、桌面范围外的原生窗口，前台焦点未变化。完整离屏套件触发三分钟总超时，未计为通过；物理输入、其他 DPI 及安装版本复验仍 Not verified。证据 `.acceptance/windows-offline-conversations-20260909`。本项及预览关闭/图标修复已纳入 review-20260910-latest 检查包。

Windows 关闭按钮与通用设备图标专项（2026-09-09）：图片预览关闭按钮已放入官方 TitleBar.TrailingContent，修复原生点击被当作标题栏拖动的问题；按钮与 Esc 绑定明确的关闭目标，尺寸/位置保持原型。共用 generic.png 按 Figma 原始路径去掉手机屏幕的不透明填充，轮廓和底部圆点保留。Release 构建零警告/错误，WPF-UI 契约和 1315 项/28 次离屏渲染通过；四个透明、桌面范围外、不激活的原生预览窗口在本机 150% DPI 下通过三点命中、UI Automation 关闭及 Esc 绑定关闭，前台焦点不变。未发送物理输入；其他 DPI/物理鼠标键盘为 Not verified。证据 `.acceptance/windows-preview-close-icon-20260909`。这两项修复已纳入 review-20260910-latest 检查包。

历史 Windows 安装检查包（2026-09-09）：`artifacts/windows/review-20260909-latest/BlueLink-Review-DO-NOT-DISTRIBUTE.exe`，0.2.17 / x64，已包含当前 Windows 界面修复、USB 会话/重连和设备身份关联源码。构建、66 项目录归属、实际 MSI/Burn/ZIP 载荷与安装/卸载身份一致性校验通过；EXE 90,087,451 字节，SHA-256 `B95EA08F9724AFE19C7AE8EC79E0413625EADEDA1BDA052C7DCB6D00DA672A2B`。未签名、不含前置运行时，属于供用户检查的本地包；未实际安装/卸载，不代表正式发布或完整桌面验收。该包已由上方 review-20260910-latest 替代；较早条目保留其当时验收事实。

设备身份变化关联（2026-09-09）：双端源码已接入“已验证连接线索 → 唯一旧设备候选 → 新安全码及明确确认 → 保留会话并替换信任”。只用蓝牙 MAC/可用 USB 标识辅助候选判断，不按名称或型号合并，不继承旧身份信任；歧义、取消、超时、旧设备在线和信任撤销时拒绝关联。已确认日志支持进程中断后重放，迁移保留消息、附件关系、文件位置、草稿、未读和显示名，停止旧待发/未完成任务，阻止退休身份和迟到回调恢复记录。Windows Release 构建零警告/错误，105 项安全、39 项设置、63 项 USB 回归通过；79 项/8 图离屏 UI Automation 通过，无原生窗口。Android 最终构建及 148 项单元测试通过，lint 0 错误/3 项既有警告；真实手机上最终 APK 的深色中文确认、浅色英文取消流程通过，使用真实 Android 加密与内存传输/Room 隔离数据。正式 Room 2→3 升级、原会话/消息/附件/传输/设置逐行一致、身份公钥和安装 APK 哈希已核对，已退出验收页。证据 `.acceptance/peer-reassociation-20260909`。本项真机隔离验收不代替实际 Bluetooth/AOA 身份重置的完整跨端验证，该项及 Windows 原生桌面仍 Not verified；既有同名记录不会无依据批量合并。本次未打发布包，Windows 仅恢复身份关联及前述 USB 定向修改。

USB 定向实施（2026-09-09）：已接入双端“设置仅开关、设备卡片与消息/文件 Header 显示会话级闪电”，依据 [Windows 1303:15593](https://www.figma.com/design/Tna537kDQdpsVwDX0lXlCs?node-id=1303-15593) / [Android 1305:5172](https://www.figma.com/design/Tna537kDQdpsVwDX0lXlCs?node-id=1305-5172)。旧全局状态/详情界面及入口已移除，USB 说明移入帮助；仅本地启用且当前 Peer 已认证 USB 通道可用时显示闪电，关闭立即停止新发送选择 USB，保留原文件任务通道。Windows 主机按物理设备独立管理连接与失败状态；52 项后台 USB/安全路由测试及 159 项/14 PNG 离屏检查通过。Android Debug 构建及 139 项单元测试通过，lint 0 错误/3 项既有警告，已保留数据安装到真机。恢复后完成 55 项真机隔离组件断言/28 张截图，覆盖浅深主题、长名称、会话/文件 Header、开关/未核验/断线状态及三语设置；源码与手机 APK 校验一致，验收页已退出。真实硬件跟进：无线 ADB 已连通并完成 AOA 切换；Windows 接口枚举修复后 USB 回归为 56 项。用户授权后，仅手机 CCC032BC 的 AOA MI_00 绑定系统微软 WinUSB，PnP 状态正常、无需重启，再进入配件模式仍有效。生产 Windows 传输组件后台宿主与已安装 Android 应用完成真实安全码核对及 BTX/1.1 加密传输：16 MiB / 1.7766 秒 / 9.44 MB/s；256 MiB / 21.8493 秒 / 12.29 MB/s。两文件均收到校验落盘 ACK，手机大小及 SHA-256 读回一致；正式设备卡片和会话 Header 闪电已取得真机证据。证据 .acceptance/usb-hardware-20260909/run3。后续关闭/失联与原接口重试优化已完成：认证 USB 心跳、加密 GOAWAY、Android 可中断流、有界旧数据清理，以及 Windows 文件处理与心跳解耦。最终 Windows 63 项、Android 142 项单元测试通过，lint 0 错误/3 项既有警告，Android 已保留数据安装新版本。真机正常退出及强制终止后均移除闪电；关闭/重新开启蓝联 USB 开关后在原 MI_00 接口成功重连，无需拔插或 USB 模式重置。重连后 16 MiB / 1.78 秒 / 9.43 MB/s、256 MiB / 23.32 秒 / 11.51 MB/s，大小及 SHA-256 再次一致（.acceptance/usb-liveness-20260909）。无需操作的自动重连、反向、多机并发及物理拔插续传仍未验证。测试不代表 Windows 正式桌面全流程或驱动分发安装器通过。本次未打发布包；Windows 仅恢复 USB 本项修改。

当前进行中（2026-09-09）：已保留数据安装消息回执/新消息提示、附件菜单与圆形卡片、双侧独立暂停和历史进度恢复、关于/文档布局、设备菜单/状态搜索与文件状态细节。真机 138–181 覆盖 Room 时序/进度回读、消息六态、计数定位、附件菜单、暂停/继续；183–197 为三语关于及文档，198–223 为设备菜单/任务入口/状态搜索，224–241 为文件状态菜单/进度与主题语言。最新已安装 build-help-reading 成功（2m55s），最近完整单元测试 135 项全通过，lint 0 错误/3 项既有警告。242–248 的消息页可见性与未读计数、249–261 的连接设置三语及可信空态已通过；262–279 三篇帮助文章的三语阅读和表单入口通过；两张 Figma 帮助原型读取 522，尚未计逐图对照。280/281 最终版本文件六操作可见及删除取消通过。隔离事件不代表真实双端蓝牙/USB 通信通过。

此前真机已核对：文件首页汇总全设备历史并保留原始时间，125–130 包括正式入口两条真实历史及隔离筛选/空态。按用户决定保留默认 500 MiB，Figma 设置页补充可调整入口及正常/错误/深色弹窗；131–137 核对三语、无效值及键盘布局，正式配置 500→512 跨重启保持，已恢复原 500 MiB 与简中。USB 原型状态、开关持久化与未知速率已有证据（71–93），没有把 ADB 当 USB 传输证明。实际超限接收恢复、AOA/双设备蓝牙、SAF、Android Keystore 失败路径、完整 118 场景、更新和全回归仍未完成。本段记录的旧轮次未增加 INTERNET；当前更新联网基线已改为 GitHub 检查、应用内下载校验及系统安装确认，见本文最新条目。USB 备用无线调试和本机驱动已就绪，真实传输及失联检测问题见首段；未更改手机无线设置。详见 WORKLOG.md。

当前执行范围（2026-09-09 更新）：Android 原型重构与 USB 真机验收已恢复，Windows 仅恢复用户授权的 USB 会话及设备身份关联定向修改，其他重构暂停。真机 ADB 已连接并授权；初始调试构建和现有 Android 单元测试通过。118 个 Android 应用原型场景仍需逐项核对，尚不能认定整体已完成。设备操作限蓝联、蓝牙与必要权限、USB 应用安装；不启动模拟器、不清空用户数据。下文此前 Windows 专项成果保留为历史证据，旧 Android 暂缓说明不再代表当前授权。

更新日期：2026-09-09。本次执行分支基线为 a700bae / 0.2.17；下方历史能力表保留原 0.2.9 证据时间，不代表已按新 Figma 复验。本文只记录可由源码、测试、真机或发布产物证明的状态。

Android 设置基础与共用弹窗（2026-09-08）：源码已接入分类首页、通用页、主题/语言/通知持久化、静态字体字重、实际事件通知和确认/设备详情弹窗。31 项单元测试通过，Debug 构建成功；lint 无错误、3 项警告，其中 Compose 自定义检查因 API 不兼容跳过。最新 APK 已通过原调试签名保留数据覆盖安装和启动。已恢复真机验收，设置首页/通用页深浅主题、通用页三语切换、覆盖安装后主题及语言保持、设备详情和清除记录确认框已取得组件范围截图。尚未完成完整 115 场景验收，其他设置页、完整应用三语翻译、安全/更新与 USB 仍未完成；该段为早期基础证据，隐私等后续批次见本文首段。证据：`.acceptance/android-physical/20260907-refactor/foundation-checks.json`。

真机验收常亮（2026-09-08）：已修复普通启动指令清除验收常亮的问题，并保存 Activity 重建状态。真机验证普通启动保留、显式关闭/启用正常；静置 324.1 秒超过系统 300 秒自动锁屏阈值后仍亮屏。仅蓝联窗口可见时生效，不改变系统锁屏设置、不绕过锁屏；其他应用覆盖时不能保证常亮。验收期间时钟曾占据前台，守卫停止输入，仅重新打开蓝联后继续。守卫现按焦点窗口实际所属包识别 Compose 弹窗，6 项离线防误操作回归通过。

状态定义：`源码`＝代码已接入；`自动`＝自动验证通过；`真机`＝真实设备验收通过；`发布`＝已经进入同版本安装包/APK。四项不得互相替代。

## Windows 历史专项验收记录（以文首最新状态为准）

Windows交互与文件布局补充（2026-09-07）：额外焦点装饰框统一移除，保留输入框原有聚焦反馈、键盘操作和选中状态；普通/图标/标题栏/模板内按钮均配置手形光标。文件表格“打开/继续”、接收目录“更改”及同类文字链接无悬停背景；文件表头背景铺满，列与实际行视口同步；文件列表及聊天行不再无条件预留滚动条空白，仅内容溢出时显示滚动条并占位。设置侧栏底部空分隔线已移除。专项检查覆盖明暗主题、焦点/光标、滚动条出现/消失/筛选为空、输入框聚焦保留及标题栏正常/悬停/移开/实时主题切换。

标题栏黑图标排查：上一轮后台截图已存在该现象；离屏宿主遗漏官方TitleBarButton加载事件，未更新实际绘制色。应用原有主题色绑定未被本轮修改；已补齐离屏加载/卸载生命周期并配对释放订阅，新截图恢复主题色。此前深色截图中的黑色标题栏图标不能作为真实桌面色彩证据。本项专项1315 checks/28次渲染通过，真实窗口/DPI/物理输入仍Not verified。

最终后台证据 `.acceptance/windows-background/20260907-interactions-caption-final`：Release构建0 warning/0 error、4845 checks/239次渲染（237张唯一PNG）、几何失败0，Windows后台行为及官方控件契约通过；所有渲染无原生窗口、无物理输入。源码未打包，现有安装包不包含本轮交互与布局修复。

图片预览已按确认原型实施（2026-09-07）：[明暗与宽窄对照1227:15065](https://www.figma.com/design/Tna537kDQdpsVwDX0lXlCs?node-id=1227-15065)对应ImagePreviewWindow。移除预览内另存为/定位入口，900 DIP以下五项工具为40×40方形按钮，空间恢复显示文字，固定72 DIP底栏单行居中。关闭按钮32×32、右边距20、标题栏内垂直居中，移除Esc提示文字但保留同一关闭命令；主图与导航共享明暗棋盘格并支持实时切换。旋转图标使用用户指定Miku Desu的58:107/58:146原始矢量，旋转/关闭PNG精确导出且核验SHA-256。默认1000×700、最小720×480、尺寸记忆及图片操作保留，消息界面文件入口保留。专项后台305 checks/12 PNG；完整`.acceptance/windows-background/20260907-preview-final`为3331 checks/223 PNG，Release构建0 warning/0 error、既有后台行为及官方控件契约通过。原生句柄为0；桌面/DPI/实际鼠标拖动及物理键盘Not verified。本轮未打包，现有安装包不含本项；对照板仍不计入149个Windows应用画板。

文件不可用弹窗已按新原型实施（2026-09-07）：[完整场景 1209:17479](https://www.figma.com/design/Tna537kDQdpsVwDX0lXlCs?node-id=1209-17479) 与组件1205:1525对应新增 FileAvailabilityWindow.xaml(.cs)。使用官方 FluentWindow/Button、原有主题和Noto Sans SC、精确导出的关闭图标；显示实际文件名/类型图标，标准中文520×306，长文件名和英文文案自动换行增高，受限高度正文滚动且底栏固定。打开、定位、另存副本、复制文件共用完成状态与当前文件存在性检查，缺失/未完成时进入新弹窗；操作系统打开失败仍显示原始错误。仅保留“知道了”和关闭，Enter默认确认、Esc与按钮使用同一关闭命令。专项后台118 checks/10 PNG，最终完整离屏3027 checks/211 PNG，Release构建0 warning/0 error、官方控件契约通过；原生句柄为0，未发送桌面输入。证据`.acceptance/windows-background/20260907-file-availability-final`（专项目录`20260907-file-availability-focused`）。本项已接入源码并后台验证，实际桌面/DPI/物理键盘验收Not verified；按用户要求未打包，现有安装包不含本项。

验收方式更新（2026-09-07）：用户停用Computer Use，当前仅运行Windows后台回归、无原生窗口的离屏渲染与内容几何检查。此前原生证据保留为历史基线，不代表本次修改已完成桌面复验。Android和真实安装继续后置。

文件管理细节已修复并后台复验：行铺满内容视口、清除分组缩进和行间空隙、使用单底边框，操作和更改按钮无边框，折叠箭头与展开状态一致，接收目录/按钮/文件数垂直居中。宽度不足时四个状态按钮自动转为下拉框，首项改为“全部状态”；空间恢复后还原按钮，保留当前状态、关键词和结果，所有筛选与搜索同排。默认1180×720、最小1000×600、大尺寸及中英文/深浅主题通过；长名称/路径保留完整提示，文件列表可滚到底且底栏固定。共用样式同时修复设备侧栏与安全确认页的滚动条覆盖；78次滚动条边界、675次透明按钮实际边框检查通过，183个渲染场景均确认未创建原生窗口。完整逐项结果见最新套件的 `layout/layout-results.json`。

附近设备标题行已移除扫描按钮的固定左间距和叠放布局，标题占剩余空间，扫描和展开/收起箭头在右侧分列并保持8 DIP间隔。扫描按钮按文案宽度自适应，标题与箭头分别保留展开/收起操作，隐藏扫描按钮时不残留空列。最小1000×600、默认1180×720和1600×1000，以及中英文、扫描中/完成、折叠/恢复共169项相关后台检查通过。

设备行右侧计数与状态点已互斥显示并垂直居中：有未读计数时仅显示徽标，清零后恢复连接状态点；99+按内容撑开。在线/离线、默认/最小宽度、未读增加/清零，以及带传输进度的较高卡片共542项相关后台检查通过。

语言初始化与发送框细节（2026-09-07）：加载持久化语言后已刷新已有的视图模型绑定，扫描按钮及其辅助功能名称不再保留上一语言；中英文双向初始化及实时切换回归通过。消息输入框已移除官方模板独立底边和交互背景，保留外层圆角边框与多行编辑。67次扫描文案同步和48次实际模板边框/背景检查通过，含普通、模拟聚焦、禁用和深浅主题；均为离屏检查，未获取桌面焦点。

窗口与设置导航补充（2026-09-06）：已移除独立 SettingsWindow，将设置、帮助/法律/反馈和 USB 子页改由主窗口内 SettingsPage 承载。主界面与设置共用默认 1180×720、最小 1000×600（WPF 逻辑尺寸）；侧栏宽度随窗口调整，文件状态筛选在窄窗口改为下拉框，搜索、设备与方向筛选保持同排，正文保留滚动、设置底栏固定。进入/返回设置保留会话和窗口尺寸，设置页面覆盖会话时不把新消息标为已读。

尺寸持久化已接入：普通宽高与最大化状态写入原 SQLite 的独立 app_setting 键；缩放停止约 700ms 后异步保存，关闭/隐藏补充同步保存。最小化保留此前状态，保存设置草稿不会覆盖尺寸，异常/损坏数据按安全默认值恢复。启动与 DPI/工作区变化时校验有效范围并保留最小值。搜索窗口默认/最小为 860×640 / 620×480，图片预览为 1000×700 / 720×480，工具栏按900 DIP断点切换文字/方形图标且始终单行，各自记忆尺寸。

此前反向验收 `.acceptance/windows-background/20260907-reverse-audit-final`：Release 构建 0 warning/0 error、窗口尺寸 22 checks、离屏 2910 checks/201 PNG、既有后台回归与控件契约通过。包含禁用按钮文字、五种发送状态、拖入覆盖层、文件范围/搜索、设备统一空态、消息分类/日历标记、图片尺寸/文件丢失回退和原生句柄为 0 的回归；此前尺寸恢复、损坏/占用、异步旧写入竞争及设置草稿不覆盖的检查继续通过。

2026-09-06 用户曾授权 Computer Use，首轮真实桌面验收在 150% DPI 和独立 QA 数据下完成。默认主窗 1180×720、最小 1000×600，聊天/文件/同窗设置的实际布局及设置滚动/固定底栏通过；深色主题保存后生效并跨重启保留。普通 1000×650、最小 1000×600 与最大化/最小化/还原/重启通过；搜索 620×480 和预览 720×480 的最小值与进程重启恢复通过。搜索图片分类、缩略图与返回原消息，预览一比一/旋转/适应窗口通过。证据位于 `.acceptance/desktop-cua/20260906`。

实际验收修复两项：宽高恢复移到 HWND 创建前，避免 SourceInitialized 重入覆盖高度；退出请求延后到 OnClosing 回调返回后，避免再次 Close 的异常。早期 QA 夹具会话 ID 不符合 `peer:<id>` 约定导致空历史及唯一约束错误，已修正，仅影响独立测试数据；失败日志保留，修复后正常退出/重启未新增异常。最终 QA 实例已关闭。

按原型逐场景复验已开始，证据位于 `.acceptance/prototype-cua/20260906`，逐节点范围见 `scenario-results.json` 与路线图。清单目前记录 148 个 Windows 节点，其中 145 个有相应组件范围的原生 UIA/截图证据；不代表 148 个完整画板全部通过。已核对设备分组/空态、扫描/连接静态状态、首次使用、蓝牙关闭/离线历史、消息空态/五种状态、文件空记录与拖入覆盖层，以及当前/全局/其他设备文件范围、组合筛选、关键词结果与空态。

W07 关于与文档已补验（150% DPI）：关于页按原型缩小品牌区、显示真实版本并补齐箭头；隐私政策和协议的目录点击可在1000×600显示末节完整正文；开源许可支持实际键盘筛选、无结果和全文滚动到最后一行，滚动条不再遮字。证据214–229，台账103节点/100项组件范围证据。帮助反馈已补验，见下段。

W07 帮助与反馈13场景已完成组件范围复验（150% DPI）：问答可展开并进入反馈；空描述校验、45字计数、诊断开关、失败重试取消后保留草稿、1000×600滚动可达已核对。生成中使用静态隔离状态，已修正官方进度模板内部边距挤掉可见轨道的问题；实际系统保存对话框另行生成两种ZIP并核验诊断文件差异，复制显示成功提示、打开文件夹定位QA ZIP。证据233–261及feedback-native-packages.json；台账116节点/113项组件范围证据，不代表148完整画板全部通过。

W07 更新6场景已完成组件范围复验（150% DPI、主窗口1000×600）：检查中/失败、新版本、下载中/失败/就绪均有原生UIA和截图；实际取消、检查重试、下载重试、稍后保留就绪已核对。对话框统一540×350及全主窗口遮罩，失败区域红色、下载中禁用文字可读、可见进度4 DIP。验收专用HttpMessageHandler只供应本地非可执行样本，并禁止安装；生产路径仍使用公开HTTPS及签名验证。证据262–270，截至更新阶段台账122节点/119项组件范围证据；公开下载、真实签名和安装交接仍Not verified。

W07 USB 页面与12种状态已完成组件范围复验（150% DPI）：1000×600纵向滚动、最大化双列、固定底栏、原型手机图标及状态配色已核对；实际详情页/连接页开关共享草稿，取消后恢复原值。证据274–291；台账123节点/120项组件范围证据，另列12个USB子组件。状态文字保留实际通道与64 KiB块协议含义，未连接USB/蓝牙或安装驱动，独立772宽组件以离屏验证为补充。

W08 托盘卡片已用400×274独立原生组件宿主核对（293–294）：两会话/三条未读、红色数字、行底色、无多余滚动条，点击会话跳到对应QA设备。原生托盘悬停与任务栏闪烁仍Not verified：Computer Use未提供系统外壳可选窗口；不以组件宿主替代系统外壳验收。截至托盘阶段台账125节点/121项组件范围证据，另列12个USB子组件。

W09此前23个原型场景在SetupUI宿主中完成组件范围原生复验（独立卸载确认漏接已在本轮修复）（2026-09-07，150% DPI）：安装欢迎/位置/路径错误/空间不足/进度/失败/完成、取消与覆盖提示、运行库六态、卸载两种选择/两种确认/进度/两种结果/失败逐项留存证据298–337。修复官方MessageBox按内容缩到480×220、确认按钮顺序与字号；普通确认560×260、覆盖提示500×263。安装窗口默认1040×680、最小920×620，欢迎底栏分行、完成长路径省略并保留完整提示，卸载页面可滚动读完；主应用同窗设置仍为1180×720/1000×600且保存用户尺寸。后台 `.acceptance/windows-background/20260907-install-recovery` 为102 checks/36 PNG、0 warning/0 error、官方控件及WiX契约通过。验收宿主不连接Burn、运行库下载器或卸载执行器；该界面宿主验收没有执行安装/删除、UAC或重启；另行完成的隔离安装执行项见安装故障记录。保留当前版本、实际路径/错误原文、256MiB预留、不可取消卸载等产品语义，不按原型示例伪造运行结果。截至安装器阶段台账148节点/144项组件范围原生证据，另列12个USB子组件；不是148完整画板全部通过。

新消息入口补验（2026-09-07）：309:13490改为原型136×38、19圆角、15字号及阴影，保留官方ui:Button。新增仅启动时选择的message-arrival隔离场景，40秒后向实际消息集合加入一条固定内存消息，不操作UI或启动通信。CUA先上滚读历史，确认到达时原位置保持、提示出现，实际点击后滚到最新消息并隐藏入口（证据338–340，1180×720/150% DPI）。拖边未取得更小的新尺寸，本入口最小尺寸未验证；不扩大为硬件接收或混合附件整页通过。最新客户端后台 `.acceptance/windows-background/20260907-message-arrival` 构建0 warning/0 error、612 checks/157 PNG、既有回归及官方控件契约通过。当前148个Windows节点已全部登记，其中145个有组件范围原生证据；当时无原生截图为扫描按下120:5615、连接按下120:5997和任务栏新消息623:16107；前两项本轮已补后台图。Android继续暂停，未提交、发布或执行真实安装/删除。

本轮按图修复了全局文件入口被单选 Checked 回调复原为当前设备、文件页误显消息搜索、未读角标缺失、查询高亮和不同无结果状态混用。消息搜索改为左侧缩略图、悬停定位、真实图片像素尺寸及内嵌日期日历；修复日历窄窗裁切和当天蓝色背景遮住记录点。实际验证日期切换、空日期、图片结果返回原消息；日历默认 860×640 已复验，新版最小 620×480 仅离屏复验，侧栏说明可滚动访问，月份导航保留官方 WPF UI 样式。已完成图片气泡按原型限制为 240×144 上限并隐藏附加标题栏，文件卡片尺寸/方向圆角已调整；传输中和文件丢失仍显示必要信息。混合历史夹具有 10 条记录，未将其视为原型整页消息序列一致的证据。

新增真实输入证据：设备搜索输入 `Galaxy` 后实际过滤列表，`Galaxy Tab` 显示统一空态，清空后恢复列表；最小 1000×600 窗口内文件搜索成功输入中文与英文，切换为无结果提示。UIA 焦点缓存有时滞后，先点击目标输入框并核对实际光标后可输入；模态消息搜索输入、其余快捷键/无障碍矩阵仍 **Not verified**。文件空态、输入区、筛选区和固定底栏在此尺寸可见。

仍 **Not verified**：瞬时按下的真实物理输入操作（后台样式已验证）和混合附件整页的完整原型矩阵；剩余键盘输入/完整混合附件页面、托盘外壳/通知；100%/125% 与动态 DPI、多显示器和低于最小逻辑工作区的组合。连接/发送状态使用明确隔离的 QA 夹具，未建立实际蓝牙/USB 连接，不能证明真实传输。安装/UAC、签名/公开更新仍后置；Android 未启动。2026-09-07 最新Windows修复包 `artifacts/windows/review-20260907-install-recovery` 已包含界面细节和覆盖安装修复；已通过隔离半安装恢复/重复修复/回滚/卸载，完整安装矩阵因杀毒RunOnce提示停止，不能视为正式升级/发布验收通过。

用户于 2026-09-06 为观察卡死/重启原因，暂停 Android 重构、Gradle、ADB 与模拟器启动。现有改动保留；B3 消息/搜索/文件中间版本的单元测试和 APK 构建通过，最后接入全屏图片预览后的验证被主机重启中断，完整 Android B3 **Not verified**。恢复时机由用户决定。

Windows W0 独立恢复完成：80 个源码/配置文件完整性检查、单 MSBuild 节点 Release 构建通过。W1 已补时间分隔与搜索缩略图；本轮按实时原型组件 60:77 将同日重复时间标记阈值修正为 10 分钟，今日分组补“今天”，边界与跨日回归通过。历史查询 16 checks、预览 27 checks、真实 WPF 搜索/聊天滚动回归及官方控件契约通过。实际 UI 已检查文件范围/失败菜单、搜索分类/缩略图/定位、图片打开和 1:1 缩放；模态消息搜索输入、剩余 DPI 与传输实机仍需补验。

W2 设置功能已补齐至后台验证：反馈 23、设置/身份 39 checks；主题、语言、设备名、信任管理、身份重置与本地反馈/帮助法律已接入。保存后生效、重启恢复，重置保留聊天/接收文件/设置，源 Figma PNG 形状不变。真实桌面已补验当前 150% DPI 下通用页和深色主题保存/重启，其他 DPI、系统主题通知、完整键盘矩阵与蓝牙重连仍为 **Not verified**。

W3 安全/更新功能已实施：加密协议问候通过后才保存信任，等待/拒绝/超时/身份变化/撤销投影、信任写入串行与启动一致性修复；内存双工握手 26 checks。更新固定公开仓库源，校验正式版本、x64 包名、大小、SHA-256、Authenticode 及当前发布者公钥；限时下载/取消/失败重试/安装前重验，更新 32 checks。公开接口当前返回 404；开发产物未签名，无法验证公开新版本或实际安装交接，这两项仍是发布阻断项。Android 旧握手保存顺序未改，跨端确认和真实安装均 **Not verified**。

Windows W0–W5 实现及后台验证已依次完成，整体重构尚未完成实机验收或发布。此前完整后台套件 `.acceptance/windows-background/20260906-windows-final-layout` 构建 0 warning/0 error，既有文件/存储/身份/协议/查询/预览回归与官方 UI 契约通过；离屏 100 checks/45 PNG。安装套件已更新为 `20260907-install-recovery`，构建 0 warning/0 error、离屏 102 checks/36 PNG、WiX 契约通过。两者原生窗口 HWND 均为 0，不使用 Computer Use 或物理输入。

后续按顺序补验：补齐上述实际桌面/DPI/键盘/托盘余项；隔离环境安装/升级/修复/卸载、运行时下载/UAC/重启；驱动、蓝牙/USB 真机双端矩阵；新版本签名与公开更新/发布。Android 的后续重构继续暂停，恢复时机由用户决定。离屏渲染不能替代未执行的真实桌面项目。

稳定性观察尚无结论：异常关机和此前虚拟内存不足有日志，无蓝屏转储可归因。AEHD 仍加载，本轮只停止 Android 工作负载；不使用会触发 Android 的聚合构建脚本。

后台验收已建立：scripts/test-windows-background.ps1 串行运行纯后台回归和 WPF 离屏内容检查。20260906-resource-host 的最终构建 0 warning/0 error，领域/存储/反馈等既有回归通过，离屏 28 checks / 12 PNG 通过，设置原生窗口句柄始终为 0；已查看小窗口反馈底部、信任空态和隐私正文图片。证据位于 .acceptance/windows-background/20260906-resource-host。失败处理自测确认异常写日志并返回 1。初期验收宿主未捕获异常曾触发 dotnet.exe 错误弹窗，已修复宿主线程/资源初始化与错误边界；早期尝试不计验收通过，不作为整机重启归因。用户操作电脑期间停用 Computer Use，真实桌面 UIA/系统交互/控件加载后状态/DPI 验收后置。

W06 短暂提示与安全连接已完成组件复验：四种提示及叠放在原生窗口可读；真实复制后 0.546 秒可见、4.279 秒复采时已消失，实际复制内容可粘贴。3/5 秒过期、最多四条、长文案和最小窗口由后台验证。安全五态修正了状态行高、对端图标、指纹字号、等宽底部按钮、150% DPI 多余滚动条，以及等待按钮白底白字；确认→等待→取消、拒绝→新确认夹具、身份变化→同窗信任管理已实际点击。界面夹具不验证真实蓝牙信号、磁盘错误、双端握手/超时或信任写入。W07 基础设置十个节点已补充 187–212 原生组件证据：通用/连接/文件/隐私的 1000×600 滚动与固定底栏、主题/语言菜单、可信列表/空态、三个确认对话框已对照；实际最小尺寸关闭重启保持 1000×600 且仅一个主 HWND。USB 两入口同草稿已后台验证，未启动硬件；设备名新值保存、真实身份重置/蓝牙断开尚未本轮验证。关于/法律、帮助反馈和更新已补验；USB 页面与十二状态已补验，真实硬件仍后置。

## 2026-09-06：Windows 安装与系统集成阶段

W5 已使用统一的卸载视图接入 Burn 安装器与独立卸载器：选项默认保留数据，明确二次确认后才选择删除；实际卸载成功并验证注册/程序移除后才清理数据。重启待完成状态不提前删数据。清理限已知身份、SQLite、日志与缓存；先从数据库读取接收目录/附件路径并保护已接收文件，拒绝损坏索引、占用和重解析路径，失败可重试。索引已不存在时保守保留缓存，避免重试丢失自定义接收文件的保护信息。只用隔离夹具验证，从未执行实际卸载或清理日常数据。

安装器与 .NET Framework 启动器已覆盖运行时缺失、下载、校验、权限等待、安装、完成/失败；下载量来自实际事件。启动器限制 Microsoft HTTPS 来源、总大小与 SHA-512，限时/取消/重试，WinVerifyTrust 无 UI 校验并核对 Microsoft 发布者，安装期间持有只读文件租约防止校验后替换。实际 Microsoft 下载、签名链/UAC 与安装引擎事件组合均 **Not verified**；离屏状态夹具不证明系统提权行为。

托盘汇总按真实 Peer 聚合未读消息，防重复计数，预览更新不再次增加未读；只有前台正在阅读会话才标为已读。清空历史同步清空通知与数据库未读数。已接入托盘菜单/悬停会话卡、任务栏未读标记和提示闪烁，采用 Windows 原生接口；“暂不处理”保留未读。程序更新交接使用有序退出以释放会话/存储。实际任务栏位置/徽标、悬停、系统主题和后台消息矩阵仍 **Not verified**。

验证：安装安全 23、通知 12、设置 39 checks 通过；安装目录归属 57 checks 通过。安装/启动器/卸载器最新30张离屏输出及固定底栏、小尺寸保留/删除两分支已检查；精确安装素材来源记录于 `design/figma/windows-installer-assets.json`。原型中的版本/速率/进度示例没有替代真实产品数据。

本地产物入口为 `scripts/build-windows-review.ps1 -CompileInstaller`。最新 Windows 修复包（2026-09-07）：`artifacts/windows/review-20260907-install-recovery`；0.2.17 / x64 Review，EXE 90,054,697 字节，SHA-256 `5497CDFCE6F7B833C1A769684F898A3C1698EF524289C20DF577A2C60F805BD9`。保留全部既有Windows界面修复，修复同版本覆盖顺序和已注册半安装状态恢复；未签名、不含前置运行时。Release/MSI/Burn构建0 warning/0 error，目录归属66 assertions；解包核验21个payload、22个安装文件、嵌入恢复清单、卸载配置身份及真实MSI升级表/执行序列通过。证据为产物目录build-verification.json、SHA256SUMS.txt与 `.acceptance/package-20260907-install-recovery`。正式发布锁保持不变，未在实际D:\BlueLink运行安装或卸载。19:46的reverse-audit包已确认存在覆盖缺陷，仅保留为回归样本。

安装故障修复（2026-09-07，用户19:54实际安装失败）：Burn/MSI日志确认新MSI成功写入后，旧同版本MSI被旧bundle卸载，删除了共用根目录启动器、卸载器、清单及Software\BlueLink.Review注册。根因是Review每次生成新ProductCode/根组件GUID却沿用0.2.17，原MajorUpgrade未包含相同版本，且采用先写新文件后卸旧程序顺序。Review改为AllowSameVersionUpgrades=yes、afterInstallInitialize，在可回滚事务内先移除旧MSI再写新版；正式发布默认升级策略及发布锁未放宽。保持安装后精确ProductCode和全量payload哈希校验。

半安装恢复入口：将当前包的归属清单嵌入BA，安装位置页、静默入口和最终安装入口统一校验；仅当Windows Installer按本产品UpgradeCode返回的注册路径精确匹配，且现存文件均在清单中、无外来文件/重解析路径、产品身份有效时允许补齐缺失文件。未知目录、错误路径、无注册、损坏清单和异物仍拒绝。真实D:\BlueLink只读检查确认符合恢复条件（exit 0），未手动写注册或改动已装文件。

真实静默安装验证 `.acceptance/install-repeat-20260907-03/results.json`：使用独立InstallRepeat产品族、独立组件GUID、工作树内安装目录，禁用快捷方式/应用自启动、不启动客户端。已通过旧基线首次安装、原故障复现1603、半安装恢复、同包重复修复、注入MSI错误后的旧版回滚、卸载与Download保留；逐步检查真实MSI状态、注册、每个文件哈希及用户目录未变。杀毒软件拦截Burn临时RunOnce续作注册，首次阻断重试一次，后续再次阻断且用户反馈持续提示后停止所有安装执行；正常旧包直升/新版全新安装两组未完成，不计完整矩阵通过。只读收尾确认Fixture进程0、测试Burn注册0、测试产品注册/RunOnce残留0，测试目录仅保留Download，真实应用与用户数据未变；未关闭安全防护或修改权限。

## 2026-09-06：Windows USB 阶段

历史 Windows W4（AOA 文件主通道已被 2026-09-13 WPD 替代）曾接入默认关闭的 USB 开关、十二种状态、AOA/WinUSB 主机与经信任核验的双通道选路。单个 USB Peer 与蓝牙备用共享逻辑会话；正在进行的传输继续归属原通道，只有同一设备的真实蓝牙安全会话可用时才显示回退。设置分别保存两种地址，升级不覆盖旧地址。原协议保持 64 KiB 块确认，不宣称滑动窗口吞吐。当前初始设备识别限 AOA/Android ADB 标识，纯 MTP 初始接口暂不自动发现。

证据 `.acceptance/windows-background/20260906-w4-final`：USB 26、设置 37、离屏 97 checks/44 PNG，HWND=0，构建 0 warning/0 error；既有回归通过。内存双工验证真实加密消息、活动文件跨选路暂停/恢复/完整性、USB 断开回退与信任撤销。未访问真实 USB；驱动安装/签名/接口绑定、授权、重枚举、拔插、真实速率与 Android UsbAccessory 均 **Not verified**。

## 2026-09-05：Figma 重构首批证据

| 能力 | 本次证据 | 限制 |
|---|---|---|
| Android 设备首页、分组折叠、搜索与权限/蓝牙状态 | 源码；DeviceScreenStateTest 6 项通过；模拟器实际 UI XML/截图 | 已验证权限首次请求/拒绝、系统授权返回、蓝牙开关与扫描、空搜索及导航；真实连接/失败/传输状态和 TalkBack **Not verified** |
| 权限/蓝牙不可用时保留历史搜索 | 修复无权限冷启动时未启动本地数据观察；scripts/test-android-offline-history.ps1 的 Denied/Off 各 6 项通过 | 专用模拟器 Room 库中的标注 QA 记录，验证冷启动、折叠、筛选、阅读与返回；记录已清理，不证明蓝牙/USB 互通 |
| 设备数据与图标 | 真实连接时间/目标地址字段接线；18 个精确 Figma PNG、Logo 哈希与 Noto Sans SC 字体来源已记录；修复按钮文字颜色及选中图标 tint | 360/412/600dp × 100%/130% 字体六组页面/滚动截图与操作可达性通过；系统状态栏/平板任务栏跟随实际 Android 环境 |
| Android 构建及回归 | scripts/build-android.ps1 成功；23 个 app 单元测试（原 17 + 新 6）、protocol-core:test、lintDebug、Debug/Release assemble 成功 | Lint 0 error、2 warning；其中 ObsoleteLintCustomCheck 导致部分 Compose 自定义规则未执行，另为 KaptUsageInsteadOfKsp。不等于完整 UI 验收 |
| 共享 BTX 协议 | gradlew.bat --no-daemon verify：41 checks 通过 | 本批不改身份/加密/文件协议；跨设备与通道切换 **Not verified** |
| 本地 APK | artifacts/android/BlueLink-0.2.17-android-debug.apk 与 unsigned release 已生成；最终修复版在 emulator-5580 安装运行，aapt 无 INTERNET | 未发布；版本沿用基线，未覆盖主目录 VERSION |
| Windows 双栏首页与导航 | 官方 TitleBar 52px、设备栏 440px、38px 搜索；旧第三栏替换为右侧消息/文件工作区，全局文件可独立打开并返回 | B2 核心布局已落地，消息/文件内部内容仍待 B3 重构，连接/失败细分视觉待补 |
| Windows 离线历史、搜索与滚动 | 独立临时数据库中 18 条 QA 历史；实际 UIA/截图验证搜索命中/无结果、折叠、阅读、禁用发送、文件范围切换和列表尾部访问 | 实际显示 DPI=150%，1600×1000 与 1180×720 已检查；100%/125% DPI、蓝牙开关热切换、跨端连接 **Not verified** |
| Windows 构建与官方控件 | Release 0 error/0 warning，UI 契约通过；聊天滚动回归 SafetyGap=10px、ScrollToEnd=True；12 个原图标、3 个静态字体及哈希/许可证已归档 | 中间修复过测试数据 ID 格式和滚动样式资源名错误；最终运行验证不代表完整 B2 验收或安装包已发布 |

证据目录：`.acceptance/figma-batch1/` 的 11 张 `figma-*.png` 是 Figma 参考图。`runtime/` 内 PNG/XML 是真实客户端及系统权限页面，含 `regression-denied/result.json`、`regression-off/result.json`、`matrix-results.json`、最终构建 `build-offline-startup-fix.log`、APK 哈希/权限记录。自动测试 XML 与 Lint 报告位于 `android/app/build/`；既有协议 41 checks 证据仍为 `verify.log`，本次不改协议。

本日已按用户要求在 `D:/Develop/Android` 安装 Emulator 37.1.11 与 Android 14/API 34 x86_64 镜像，创建 `BlueLink_API_34`（4 核、4GB、1080×2400/420dpi）。AEHD 2.2 硬件加速、ADB 和系统启动均通过，主机意外重启后也已恢复并继续完成上述 BlueLink 验收。屏宽/字体测试后恢复模拟器默认显示，测试记录已清理。当前 Windows 缺少 WHPX 可选功能，使用官方签名 AEHD 驱动；该驱动官方支持截至 2026-12-31，后续需迁移 WHPX。

B2 证据位于 `.acceptance/figma-batch2/`：`figma-*.png/txt` 为参考；其余 UI PNG/UIA 文本为实际窗口。`geometry-standard-150.json`、`geometry-compact-150.json` 记录窗口真实 DPI/尺寸，`build-final.log`、`ui-contract.log`、`chat-scrollbar.log` 为构建及回归证据。字体来源见 `design/fonts/windows-noto-static.json`，图标来源见 `design/figma/windows-home-assets.json`。

B1 的模拟器可达场景及屏宽/字体矩阵已验收，真实连接/传输状态待补验；B2 双栏、搜索、离线历史和基础导航已实施并通过上述 150% DPI 检查，整体重构未完成。B2 其余 DPI/连接状态、B3 消息/文件实机、更新安装交接、USB/AOA/WinUSB 及安装矩阵尚未验收；后续范围与旧实现替代项统一见根 PROJECT_ROADMAP.md。

## 证据状态

| 模块 | 状态 | 说明 |
|---|---|---|
| BTX/1.1 framing / record | 源码、自动、发布 | 共享参考实现、Windows BTX/1.1 互操作 12 项于 0.2.9 发布前通过 |
| Identity / trust / E2EE | 源码、自动、单机真机 | 首次安全码和身份公钥固定已完成；仍需多设备和异常身份测试 |
| BLE Presence / GATT rendezvous | 源码、单机真机 | 双端互相发现已由用户验证；仍需多设备及系统恢复测试 |
| RFCOMM Android ↔ Windows | 源码、单机真机 | 单设备双向连接和基本自动重连已验证 |
| 多设备连接 | 源码、自动接线 | 默认最多 4 个活动 Peer；可信设备按顺序自动恢复并有 5 秒后台重试；尚未完成 2/4 台真实设备压力验收 |
| 会话排序与离线历史 | 源码、自动渲染 | 已连接、离线、附近新设备分组及离线历史入口存在；已按传输地址/身份键去重，待多会话真机验收 |
| Chat / receipts | 源码、自动、单机真机 | 基本文本已验收；未读、后台与排队补发仍需矩阵测试 |
| 图片与文件消息 | 源码、自动、单机真机、发布 | 聊天附件、传输中心、进度/速度/ETA、限制尺寸缩略图与图片预览已接入；新 0.2.9 Android 真机 UI 尚待复验 |
| 文件可靠传输 | 源码、自动、单机真机、发布 | 块/整文件校验、原子提交、不可变发送快照、持久化 `.part.resume` 与同 ID 重试均有自动回归 |
| 本地持久化 | 源码、Windows 自动 | SQLite/Room 结构存在；Android 记录开关刚接入，待重启行为测试 |
| 设置与数据管理 | 源码、静态接线验证 | Windows 4 项、Android 5 项运行时策略已接线；待双端行为测试 |
| Android UI | 源码、静态合约、Lint、发布 | 15 个页面入口/系统入口已核对；无在线真机，逐屏视觉及长按手势仍待复验 |
| Windows UI | 源码、自动渲染、发布 | 展开、收起、1180×720 与图片预览四种状态化渲染冒烟通过；人工逐页与 125%/150% DPI 仍待复验 |
| Android 发布产物 | 发布（内部 Debug） | `BlueLink-0.2.9-android-debug.apk` 已通过 assemble、Lint、badging、签名和无 INTERNET 检查 |
| Windows 安装程序 | 发布、实际升级/修复验收 | `BlueLink-Setup-0.2.9-win-x64.exe` 已验证 0.2.8→0.2.9 升级、Download、已安装启动、哈希和修复；五页 UI 自动截图通过 |
| Runtime 网络边界 | 源码、自动；新版本尚未发布 | 当前新增 INTERNET 用于用户触发的 GitHub 更新查询与应用内下载；REQUEST_INSTALL_PACKAGES 用于系统安装交接，来源权限由用户明确授权。消息/文件仍走设备直连；旧已发布 APK 无 INTERNET 属于历史基线。 |

## 已知限制

1. 当前 Android 产物使用 debug 签名，Windows EXE/安装包没有商业代码签名证书；仅适合内部验收。
2. 断点数据可跨进程恢复，但发起方仍需在重连后点击重试；同 ID 重试会从接收方连续已提交 extent 继续。超限 Offer 在活动会话内保留约 30 秒，过期需对端重试。
3. 流控仍为逐 64 KiB 块确认；大文件吞吐量受经典蓝牙和 stop-and-wait 往返影响。
4. 当前安全协议为项目自有 BTX/1.1；正式高安全等级发布前仍需独立密码协议审计。
5. Android 当前以 API 36 的 USB 真机验收；模拟器禁止运行。多设备、真实蓝牙中断、完整逐屏/语言矩阵仍未通过，部分 Debug 内存场景只证明组件布局。
6. Windows 使用 WiX Burn + WPF 安装 UI；23个原型场景已有150% DPI原生组件范围证据，100%/125% DPI和真实安装矩阵仍待验收。

## 发布前真机矩阵

- Windows 11 × Android 13/14/15/16，至少覆盖高通、联发科与三星蓝牙栈。
- 1、2、4 个并发 Peer：后台消息、文件接收、切换会话、自动重连和重复连接竞争。
- 0 B、1 KiB、10 MiB、500 MiB 文件；同名、长文件名、中文名、磁盘不足、文件占用、主动取消和蓝牙中断。
- Windows 睡眠/唤醒、Android 锁屏/省电、蓝牙开关、进程终止与重启后的历史恢复。
- 100%/125%/150% Windows DPI 与 Android 360–600 dp、字体缩放 1.0–1.3。

## 自动验证命令

常规回归使用以下 Windows 后台入口（串行运行，不显示原生窗口）；本轮已授权的 Computer Use 隔离桌面验收另见上方：

```powershell
.\scripts\test-windows-background.ps1
.\scripts\test-windows-installer-background.ps1
# 本地检查产物，仅编译，不执行安装器；不等于发布
.\scripts\build-windows-review.ps1 -CompileInstaller
```

以下保留旧验证入口作为历史参考；当前 Android 仅运行离线、单工作线程的专项构建及 USB 真机验收，Windows 完整套件暂停。

```powershell
# Android 与共享协议
$env:ANDROID_SDK_ROOT='D:\Tool\Android\Sdk'
$env:JAVA_HOME='D:\Tool\jdk-17.0.20'
D:\Tool\gradle-8.5\bin\gradle.bat --no-daemon -p .\android :protocol-core:verify :app:assembleDebug
D:\Tool\gradle-8.5\bin\gradle.bat --no-daemon -p .\android :app:lintDebug

# Windows 协议、SQLite 与文件接收器
$env:DOTNET_CLI_HOME="$PWD\.dotnet-home"
$env:NUGET_PACKAGES="$PWD\.nuget-mirror-test"
D:\Tool\dotnet-sdk-8\dotnet.exe build .\windows\BlueLink.TransferVerification\BlueLink.TransferVerification.csproj -c Release --no-restore
D:\Tool\dotnet-sdk-8\dotnet.exe .\windows\BlueLink.TransferVerification\bin\Release\net8.0-windows10.0.19041.0\BlueLink.TransferVerification.dll
```

本轮右键菜单复验已覆盖 25 个 W05 节点的菜单组件，传输中设备卡片也已补验。新证据为 `82`–`109`（`83`/`85` 为修复前对照，后续最终截图替代）；文件菜单按方向/状态显示，附近设备菜单已补齐，右键保持当前会话，选中行文字及分隔线已修正。菜单组件证据不代表附件卡片状态布局或混合记录整页通过；当前账本 79 个节点中 76 个有其所述范围的实际证据。

附件状态差异已继续修正：9 种文件卡片和 4 种图片状态获得 `111`–`123` 的原生截图/UIA；采用官方 ProgressRing 显示实际进度，补入 Figma `305:13238` 暂停接收图标及校验。当前仍只确认所述附件/菜单组件；整页消息顺序、传输中设备卡片等继续验收。最新后台为 523 checks/136 PNG。

传输中设备卡片与暂停状态已取得 `124`–`130` 的实际证据；修复“打开会话”仍停留文件页的问题，1000×600 下设备栏滚动可访问附近设备按钮。后台校验进度/暂停/完成/新增/删除记录及导航，最新 523 checks/136 PNG。当前 79 个登记节点中 76 个有对应组件证据，未将其扩展为完整画板通过。

W06 信息弹窗四个节点已完成组件范围的原生验收：设备、附件、图片、失败原因统一摘要卡片与字段行，只有右上角关闭，遮罩保留主标题栏。证据 `132`/`133`、`135`、`137`、`139`；长路径保留完整 UIA/ToolTip，完成时间取持久化终态更新时间，图片尺寸读取实际文件，未知信号、信任、校验和失败阶段不套用原型示例。后台新增详情数据真实性及四种尺寸可达性检查，当前 523 checks/136 PNG。仍是隔离静态记录，未验证真实设备传输；W06 预览、确认、安全和提示场景继续逐项核对。

图片预览 Fit/Zoomed 组件已完成本轮原生验收：`141`–`143`、`145`–`148` 覆盖实际缩放、拖动、旋转、重置、1:1、默认/最小尺寸和关闭重开保留尺寸。修正原型遮罩、棋盘格单元、标题/文件动作图标与适应状态背景；选中状态及边界/导航联动新增后台回归，523 checks/136 PNG 通过。窗口大小采用用户要求的 1000×700 / 720×480，工具栏窄屏换行；原型的图片尺寸/百分比仅是示例，真实数值来自文件。

W06 七种确认组件已取得 `152`–`161` 原生 UIA/截图证据：移除信任、单会话清除、本机记录删除、发送/接收取消、全部会话/文件记录清空。统一 460×250、红色具体操作、警告图标与影响提示，关闭/取消不接受操作。删除隔离传输记录后列表为零，原 PDF 哈希保持一致；全量清空只核对弹窗并取消，实际入口是主窗口隐私设置。取消任务文案遵循现有业务，不承诺原型示例中的失败标记和临时文件清理。后台 523 checks/136 PNG 通过；剩余 W06 安全五态、提示五态与真实设备行为继续验收。

本轮全界面反查（2026-09-07）：148项Windows原型均已读取并核对生产入口，131项映射到本轮后台组件图，其余17项沿用源码/此前限定范围证据；三个无独立原型的通用错误提示仍只声明官方控件符合性，不能声明148完整桌面画板全部通过。独立卸载确认漏接原型已用共享工厂修复；Review卸载配置与注册身份不一致已在打包阶段修正并经解包一致性检查。对真实D:\BlueLink安装记录只读复验：旧配置拒绝、新配置准确匹配、错误目录拒绝，退出码1/0/1；实际已装文件未改、未执行删除。扫描/连接按下与扫描悬停、文件搜索悬停的颜色/边框/文字尺寸也已补修。新包包含以上修复；隔离安装执行已验证范围及杀毒拦截限制见Windows产物记录，实际用户目录只读。完整逐项证据为 `.acceptance/windows-reverse-audit-20260907/audit-results.json`。
