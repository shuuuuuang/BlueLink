# WORKLOG

## 2026-09-13 — Android 启动一次与会话页下拉扫描

- 范围：Android DevicesScreen、蓝牙发现调度、运行时启动入口、必要的三语文案/Compose 依赖和回归验证；按用户要求移除“附近新设备”中的扫描按钮，列表下拉触发扫描。Windows、传输协议、用户设置值、历史数据及文件未改；未提交或推送。
- 根因与替代清单：原 BluetoothRepository 按扫描 10 秒/暂停 5 秒持续循环，并在连接结束后重启；后台停止和身份操作还会重置启动标记。现移除上述循环、连接结束重启与标记重置；StartupDiscovery 等待持久设置和首次蓝牙可用，遵循既有 scanOnStartup，每个进程最多领取一次启动扫描，手动刷新消费待处理的启动请求。后续页面/前后台切换和身份操作不再开启新轮次。蓝牙广播、入站连接和可信设备重连策略保持原样。
- 扫描与界面：SingleDiscoveryScan 每次最多执行一个 5 秒窗口，重复请求不叠加/不延长，连接或后台停止会结束当前扫描；失败、取消和运行时关闭均停止无线扫描并收起刷新态，旧请求迟到的结果/失败不能干扰新请求。使用官方 Compose PullRefresh 覆盖层与列表嵌套滚动，空态或分组全部收起时仍可下拉，提示器不挤动列表。扫描状态由真实 discoveryState 驱动，补充无障碍刷新操作和简中/繁中/英文引导。新增 material 依赖沿用当前 BOM 和已有缓存，无版本升级。
- 自动化：Android 190 项 JVM 单元测试通过（含新增 7 项单轮扫描生命周期与 5 项启动策略）；Debug assemble/lint 成功，0 错误/15 警告。15 项中 12 项属于已有提示，3 项是扫描按钮移除后保留的图标/两条文案资源未使用提示。首轮新增测试因 Kotlin last 的尾随 lambda 被识别为谓词而编译失败，改为显式 invoke 后整组通过；未绕过测试。变更检查和验收脚本语法检查通过。
- 真机：无线 ADB 连接 REDMI K80 Pro，同签名覆盖安装并以保留屏幕常亮方式启动正常 MainActivity；真实 UI Automator/截图 12 项检查通过，覆盖按钮移除、启动扫描结束、原周期之后仍不重扫、短列表下拉启动实际扫描、刷新前后分组坐标一致、扫描结束收起指示器、设置返回及 Home→应用前台不重扫。新截图已审阅，三组恢复展开，手机停留蓝联前台、已有安全连接/USB 就绪可见。没有清理历史或更改蓝牙/USB 系统设置。
- 交付：artifacts/android/BlueLink-0.2.17-android-debug-pull-refresh-5s-20260913.apk，SHA-256 8208FA6EB1D086E550BABA9DC0C615670C5AFB92EA8F4D017876BEECB1473A38；已再次同签名覆盖安装到手机。证据 .acceptance/android-pull-refresh-20260913，复验入口 scripts/test-android-device-refresh.py。本次没有重打 Windows 安装包。
- 时长追加：用户反馈 10 秒过长后，将唯一的扫描窗口默认值缩短为 5 秒；同步调整窗口边界/重复请求回归和 UI 复验脚本。12 项定向 JVM 测试与 Debug 构建通过；真机连续两次下拉后，真实 UIA/新截图 4 项检查验证扫描启动、收起并保持停止，已审阅。该轮只改时长，未重跑整组 190 项或 lint；此前全量结果作为原任务基线。追加证据在 shorter-window 子目录。
- 限制 / Not verified：其他手机/屏幕密度、TalkBack 实际操作、深色/繁中/英文的新鲜实机截图以及实际撤权/关闭蓝牙/进程被系统杀死的硬件注入未做；启动权限延迟与取消/失败由单元回归覆盖。未将本次界面与扫描检查扩大为全部双端通信或发布验收通过。PROJECT_ROADMAP 已更新 Android 发现策略。

## 2026-09-13 — 最新 Windows 安装验收包

- 范围：按用户要求对当前工作区的 Windows 客户端、启动器、安装 UI 和卸载器重新构建并打包；没有修改业务实现、Android、驱动或用户数据。沿用既有 Review 验收包流程和 0.2.17 版本，正式发布载荷锁保持原样，未提交或推送。
- 产物：artifacts/windows/review-20260913-153502/BlueLink-Review-DO-NOT-DISTRIBUTE.exe，x64，90241559 字节，SHA-256 CAA5CA18FDF4BD996F820F65F466EA4A1E9F6ADADD77C9C19F1AE942F7D0F66E；同目录保留 MSI、ZIP、SHA256SUMS.txt 和 build-verification.json。包含此前已完成的 Windows UI、文件图标、WPD 队列/重试、移除信任及 USB 事件唤醒改动。
- 验证：build-windows-review.ps1 -CompileInstaller 成功；版本、安装器及全量 WPF UI 静态合约通过，66 项安装文件归属检查通过。解包后逐个校验 21 项载荷和含清单的 22 项 MSI/ZIP 文件；内嵌 MSI、安装 UI、客户端、卸载器均与本次构建一致，恢复清单、Review 注册身份及同版本升级事务顺序通过。565 个构建输入前后哈希一致，正式发布锁未改变。取证在 .acceptance/package-20260913-153502。
- 边界：实际安装/升级/卸载及本次包完整端到端验收 **Not verified**；没有启动安装程序或替换正在运行的验收客户端。EXE/MSI 均为未签名验收包，不内置 .NET Desktop Runtime。NU1900 为漏洞数据源不可达，不能记作漏洞审计通过；不是正式发布门禁通过。上一任务已定位的北信源防病毒 WPD 扫描占用和冷切换长等待限制仍然存在，没有因本次打包而宣称解决。

## 2026-09-13 — USB 自动检测响应优化（响应验收未通过）

- 范围：双端 WPD 就绪检测、Windows USB 事件接线、Android USB 状态/手动重试和对应回归；保留 WPD/MTP、蓝牙认证及文件队列，不改驱动，不制作安装包。
- 保留实现：原先双端按 3 秒定时检测，手机目录到达、USB 变化和开关变化不能唤醒等待，Android 重试入口只更新展示。现用每会话单消费者、合并唤醒信号；Windows WM_DEVICECHANGE、手机 USB_STATE、新目录公告及开关/重试可立即唤醒。变化后最多 10 秒采用短周期重试，已就绪恢复 3 秒心跳；WPD 返回 0x800700AA 时退避到 3 秒。重复 hello/announce 不形成唤醒循环；取消后的探测不发布已销毁会话的就绪状态。诊断区分设备、存储、各层目录和标记读取阶段。
- USB 状态：这台手机“仅充电”仍可能报告 mtp=true，但 unlocked=false。Android 明确不可用时立即失效并通知 Windows 丢弃绑定；可用状态稳定 1.2 秒后才验证，覆盖先解锁数据、约 0.9 秒后再重配置的暂态。缺失厂商字段保留轮询兜底。USB 暂不可用时保留同一认证蓝牙会话的目录，恢复时重新核验路径/随机标记并取得新对象 ID；关闭功能、撤销权限、结束会话仍清理。
- 真实拔插：用户 14:05 确认重新插线并选择“传输文件”。Android 14:05:51.848 报告稳定配置，Windows 14:05:53.130 开始目录探测，持续返回资源忙，14:09:39.521 才验证就绪；本次目录探测耗时 226375 ms / 74 次。证实此前长等待并非仅由 adb 软件切换造成，不能用修改轮询间隔解释或宣称已解决。
- 定位与撤销：只读探针证明根目录 29 个项目可枚举，Download 名称已可读，但后续部分同级目录名称仍返回忙。曾精简属性读取并跳过忙的同级项，3 轮软件切换从命令发出至 UIA 闪电出现分别 8264 / 7916 / 7825 ms；随后首次写入及重试仍在 IStream.Write 返回 0x800700AA。说明可读目录不能证明此时已可写。该试验整体撤回：WpdDevice 恢复本任务基线，删除 WpdDirectoryLookup 与对应试验回归，继续严格查找目录，没有以降低检查换取提前点亮。调试中的 USB 变化重建目录方案也已撤销。上述 8 秒结果不属于最终交付性能。随后改用 WPD_OBJECT_ORIGINAL_FILE_NAME 的冷切换试验仍等待 223515 ms，也已撤回；WpdDevice 与本任务基线 SHA-256 完全一致。
- 继续定位（仅充电切换同样复现）：用户两次用途切换分别等待约 230.8/220.1 秒；15:04 本轮命令跟踪中，蓝联在收到公告后立即探测，持续收到 0x800700AA，226109 ms / 74 次后自行就绪。驱动层 MTP GetObject（0x1009）事务 2242 从 15:04:47.597 到 15:07:37.089 才成功返回（0x2001），单次读取 169.492 秒；不能把这段时间称为纯初始化或轮询延迟。
- 调用方证据：随后 60 秒 WPD API 实时头部跟踪直接定位 PID 16620（vrvsdtray.exe，安装文件版本为北信源防病毒系统 v3.0 托盘程序 / VRV Antivirus 3.0.4.1499）。抓到该进程 2845 次请求，其中 1013 次 OBJECT_RESOURCES_READ、90 次 OPEN、89 次 CLOSE；同期蓝联 PID 656036 为 75 次请求，返回中有 3 次 0x800700AA；StiSvc 只有 4 次请求。此前凭同时启动怀疑 vrvsdudisk 或 WIA 不足以归因，最终以 API 调用进程为准。该头部跟踪丢失 878 条事件，计数仅代表已捕获样本，不是全量比例；不影响已捕获读取请求的进程归属。当前电脑外部防病毒 MTP 扫描/读取占用是已定位的长等待来源，尚未通过改变安全软件策略做因果 A/B。
- 诊断边界：MTP 命令跟踪仅启用 2000/2003/2004（无文件内容），280 秒自动停止、零丢失。API 跟踪为内存实时会话，仅读取固定命令头并保存 PID/编号/HRESULT，不读取或落盘后续数据缓冲区，60 秒自动停止。两份跟踪和只读进程审计均已结束，没有修改持久日志配置、驱动、Windows 服务或防病毒策略。调试时几百毫秒的目录核验发生在外部扫描结束后的已可用通道，不能代表冷切换性能。
- 自动化：最终保留版本 Windows 11 项信号/合并/取消/忙退避检查、BLM1 密文/篡改/队列隔离及实际 COM 枚举通过；Android 178 项 JVM 测试、Debug assemble、lint 通过（0 错误/12 个既有警告），本次继续验证未再改 Android。Windows 构建与 publish 成功；NU1900 表示离线无法查询漏洞源，不算漏洞审计通过。试验期另有 17 项检查通过，但相关实现已撤回，不计入最终检查数。
- 文件证据：此前 16 MiB WPD 文件经重试完成，独立 SHA-256 为 80fcafb5a9c9b57fbf8fd04fa752010cc2338ca672388f063d0f82103c59dd72。真实拔插恢复后，4 MiB 文件 1a784854-c999-48a9-99f8-ec29f680b749 完成；试验失败的另一份 4 MiB 文件 e4abb050-8587-424d-8c48-d33e20e7d79a 在最终保留版本 14:36:37 重试完成，正常 UIA 显示已完成/100%，Android 独立 SHA-256 与源文件均为 a3b67bf5f73e02babdf17c2c74c334579025b258d85b49966792b7967fbcbd45。失败记录保留，未删除或操作用户文件。
- 本轮恢复验收：15:13 用途切换后未重启应用、未重开 USB 开关，15:17:16.883 自动就绪（228078 ms / 74 次）。UIA 在 15:17:17 观察到就绪；自动文件选择脚本因控件尚未就绪中止，未发送，随后在同一正式选择器内完成合成文件选择。15:20 首次发送 4 MiB c0fd9447-74c8-4eec-88ef-316b2a233e97，经 WPD 完成（Offer 至落盘确认约 14.30 秒），手机独立 SHA-256 与源文件一致，仍为 a3b67bf5f73e02babdf17c2c74c334579025b258d85b49966792b7967fbcbd45。该结果证明占用结束后自动恢复与实际完整传输；由于文件选择器的延迟，不证明闪电亮起瞬间即可写，也不作为吞吐性能达标证据。新 Windows UIA/截图显示该文件已完成/100%，手机新截图显示闪电；已审阅。
- 当前状态与限制：Windows 正常重启至 client-wpd-responsive-checked，Android 保留目录版已安装并保持前台/常亮，MTP 已恢复，双端闪电可见，合成传输均结束。自动检测事件链已改善，但重新插拔后及时可写的目标未通过；已定位这台电脑北信源防病毒进程批量读取 WPD 的占用，不能归咎于用户遗漏步骤，也不能仅靠缩短轮询承诺秒级恢复。未禁用、暂停或绕过安全软件；策略层排查应由设备管理员在现有安全要求内处理。其他手机/Hub、多机、长期耐久及每次首次写入可靠性 **Not verified**。没有提交、打包或更改驱动。证据 .acceptance/usb-responsive-20260913。

## 2026-09-13 — USB 插线后闪电延迟出现的只读排查

- 用户报告选择“传输文件”后闪电尚未出现，随后自行出现。此次只读取双端界面、USB 状态、蓝联设置及日志；未切换开关、USB 用途、驱动或重连，未发起文件传输。
- 当前证据：手机 dumpsys usb 为 MTP、configured=true、未锁屏；存在 Download/BlueLinkUSB 的已保存授权目录配置。Windows 保存的 usb_enabled=1，原生 UI Automation 的 ConversationUsbReady/HeaderUsbReady 与 Android UI Automator 均显示“USB 加速已就绪”。闪电在应用自动检测完成后出现，没有额外手动激活步骤。
- 代码确认：Windows 与 Android 的 MTP 协商循环均在每轮结束后延迟 3 秒，Windows 还要枚举 WPD 设备、匹配目录并核验会话标记，之后通知手机就绪；并非插线事件即时点亮。此次没有从插线开始采集分阶段耗时，不能将具体延迟归因于某一个步骤。插线到就绪耗时及本轮实际传输速率 **Not verified**。证据 .acceptance/usb-readiness-20260913；未改生产代码。

## 2026-09-13 — 双端移除信任后的设备分组

- 范围：调整双端设备列表、信任状态持久化和相应回归。移除信任后断开该设备，并从已连接/离线分组移出；只有当前扫描仍发现的端点进入附近新设备，扫描消失则不展示。聊天、传输记录和已接收文件保留，重新核验信任后恢复原会话。
- 根因与实现：原先撤销仅将持久化信任改为 Unknown，设备列表仍从全部历史设备投影，历史身份和地址也一直阻止其进入新设备分组。两端增加显式 Removed/REMOVED 状态；Android 会话投影携带 isRemoved，统一分组排除，Windows 历史列表和新设备排重采用可见设备。启动同步、身份重置保留已移除标记，重新信任恢复 Trusted；Android 忽略已移除身份的迟到连接持久化回调。没有删除 peer 行或关联记录，没有数据库结构迁移；已有普通未信任历史不做批量隐藏。
- 验证：Windows 隔离身份与实际加密内存会话的 19 项检查通过，涵盖连接中移除、离线移除、扫描出现/消失、数据重载、批量移除、重新信任和原消息恢复；原生 UI Automation 与两张新截图通过并审阅。Android 173 项 JVM 测试通过，含新分组回归；Debug 构建/lint 通过，lint 0 错误/12 个既有警告。Windows 构建与 publish 成功，NU1900 为 NuGet 漏洞源不可达，不代表漏洞审计通过。测试曾因合成 ID 格式和会话 ID 大小写不符合生产存储规则失败，已修正验收数据；未为通过测试改动生产读取规则。
- 交付与边界：Windows 已正常重启到 client-wpd-forget-device；Android 同签名覆盖安装最终 APK；REDMI K80 Pro 真机隔离 Room/密钥存储验收通过，验证移除、重新打开数据库和初始化、迟到连接回调、重新信任、历史/文件保留及批量移除；生产 DevicesScreen 的 UI Automator/新截图确认移除卡片消失、其余离线设备保留、扫描端点归入附近新设备。验收后回到 MainActivity 并保持手机唤醒。证据 .acceptance/forget-device-20260913。没有撤销真实手机/电脑的信任，没有删除用户历史及文件，没有打包安装器或提交代码。双端实际硬件会话上的撤信操作、强制杀进程/磁盘失败注入、多机长时验收 **Not verified**。

## 2026-09-13 — Windows 离线设备卡片显示最后连接时间

- 按用户追加说明，仅修改 Windows，Android 保持现状。根因是离线分组同时包含 Offline 和 Connectable，但卡片 LastSeenText 只为 Offline 读取 LastConnectedAt，后者退回“附近可连接”。现将所有非 Connected 历史设备统一展示最后连接时间；没有时间则保留“可查看本地历史记录”。扫描、连接入口、设备身份、在线及传输状态均未改动。
- 验证：Debug publish 成功；仅有既有 NuGet 漏洞源不可达 NU1900 警告。真实 UI Automation 确认原“附近可连接”卡片改为“上次连接 …”，截图 .acceptance/offline-last-connected-20260913/after.png。源代码相对本轮基线仅一处条件修改；未新增测试、未运行无关协议套件。
- 交付：已正常重启至最新 Windows 验收候选；未打包安装器、未更改或安装 Android、未修改历史数据。无连接时间的分支沿用现有文案，本轮未单独制造该类记录做真机截图。

## 2026-09-13 — 暂停传输断线恢复与 Android 会话位置稳定性

- 范围：修复双端暂停任务断线重连后操作失效、Android 图片到达后的底部跟随、预览返回位置和设备分组标题跳动。保留现有 WPD/MTP 方案、历史记录、统一弹窗及原有工作区改动；不改设备驱动，不使用模拟器，不制作安装程序。
- 根因与处理：暂停任务及其控制器属于原会话，断线后 UI/数据库仍保留可继续状态，而旧工作线程可能在清理时继续报告进度。两端增加会话任务账本：关闭时统一把在途任务标为失败并结束归属，关闭后的报告忽略；终态不再被进度回写复活。显式关闭先记录原因再释放通道，保留重复连接拒绝等具体原因。持久化的孤立活动记录恢复为可解释的失败记录，Windows 从最新全局投影取状态，避免旧会话缓存覆盖同 ID 的新重试。
- 恢复语义：双端发送方失败/拒绝/取消记录在原文件可用时显示“重试”；离线或原文件不可读有反馈。新会话沿用文件任务 ID，创建独立控制器和发送快照；Windows 接收端从真实连续检查点回复偏移，蓝牙发送进度从协商偏移重置。WPD 重试整份当前文件，不承诺密文断点恢复；USB 单独失效也清理对应任务，取消能够释放队首，后续队列可继续。主动取消重试不再误报连接失败提示。
- Android 布局：会话滚动追踪和缩略图尺寸随页面状态保存，预览返回不再触发首次进入的滚到底；跟随底部时观察实际布局变化并滚至最后一项真实底边，处理异步图片和超高多图消息。阅读历史时收到图片不抢位置，自己发送则定位底部。三类设备组的展开/收起标题使用同一最小高度。
- 自动化：Android 171 项 JVM 测试通过；Debug 构建与 lint 通过（0 错误、12 个警告，包含既有资源/工具警告和对 buffered().use 流包装的 Recycle 提示）。Windows 147 项断线归属/重试/取消/队列、30 项暂停和 81 项 USB/路由回归通过；真实加密 PeerSession 经内存双向连接的 8 项检查跑通 512 KiB 文件部分提交、暂停、断开、新会话检查点恢复、完整字节核对及重试后取消。初次完整路由回归发现清理先于具体关闭原因的问题，修正两端关闭顺序后整组通过。WPF UI 合约通过，工作区 diff 无空白错误；Windows 构建成功，NU1900 为离线环境无法查询 NuGet 漏洞源，不能记作漏洞审计通过。
- 真机：REDMI K80 Pro / Android 16，无线 ADB 只做控制与取证，保持唤醒，原 MTP 驱动。生产 Compose 组件的隔离数据场景 25 项 UI 检查通过，含异步收发图、预览前后可见消息坐标一致、阅读历史不跳、超高多图、三组标题/箭头坐标稳定。正常双端应用用自建 128 MiB 文件跑通 WPD 暂停→手机正常菜单断开→重连→Windows 行按钮重试→再次暂停→共享弹窗取消→再重试完成；Android 暂停时显示“等待接收·对端已暂停”。两端完成状态、WPD 日志和独立 SHA-256 一致（0573f1e347100a76030c797b2440c67c454ca38fefbc23f7c3e9b31f6144f38e）。WPD 暂停时发送侧暂存进度已到 100%，此项验证在途暂停，不作为半程吞吐或断点传输证明。
- 交付与证据：最终 APK 同签名覆盖安装，Windows 切换最新验收候选；没有在途 QA 任务，测试文件与历史保留。证据位于 .acceptance/reconnect-scroll-20260913；可重复入口为 scripts/test-android-conversation-scroll.py、--transfer-reconnect-only、--peer-reconnect、--usb-only。未提交代码。
- 限制 / Not verified：本轮未做物理拔线、锁屏、多手机/Hub、长时耐久、其他机型/输入法及全量发布矩阵；Android 作为发送方的完整 WPD 断线重试链未做本轮真机复验。跨进程持久排队、WPD 密文断点续传不在本轮保证内；手动重试仍要求原文件/快照可读。

## 2026-09-13 — 收口短暂异常提示与确认弹窗体系

- 范围与统一规则：按用户“异常使用短暂消息、未保存设置及同类问题一并修复”和对一致性的追加要求，收口 Windows 扫描/连接反馈、客户端确认探针及安装/卸载确认。操作结果和可恢复异常用 ToastHost，需要决策的流程用共用确认窗口；业务入口提供内容与操作，不再修补默认模板。保留此前删除的附近设备常驻反馈区域；不改 Android、传输协议、驱动，不安装、不替换用户在用进程。
- 根因：用户截图来自仍在运行的旧 client-wpd-final 构建，当前源码的 AskSaveSettings 已经走 ConfirmationWindow；此外安装取消/覆盖安装和卸载确认仍各自补丁式修改默认 MessageBox，旧静态门禁还要求该实现，客户端模板探针也残留默认类。这些是不同入口反复出现样式差异的结构性原因，不能只改截图上的一个窗口。
- 异常：MainViewModel 的扫描异常、连接超时/失败通过事件交给主窗口既有 ToastHost，呈现三语可读说明，5 秒自动消失；底层异常写诊断。自动重试按设备 1 分钟去重，不抑制手动重试或其他设备的失败，连接成功后重置；主动取消、蓝牙关闭、身份重置及关闭过程不额外提示连接失败。窗口销毁时解除事件，排队回调检查销毁状态。
- 确认体系：保留客户端 ConfirmationWindow 的统一信息/警告/错误/三选项及危险操作语义。安装器、启动器、卸载器改用同一个 InstallerDialogWindow；InstallerNoticeWindow 仅做通知入口封装，InstallerConfirmationDialog / InstallerPromptWindow 只做语义映射。移除两套默认 MessageBox、Loaded/ApplyTemplate 补丁和同步等待异步弹窗逻辑，覆盖安装仍必须点击“关闭并继续安装”，卸载仍保留/删除原选项指定的数据。两种运行时共同链接 shared/WindowsDialogLayout.cs，统一窗口宽度、标题/页脚、正文边距与按钮高度；危险操作使用红色，取消/关闭不执行动作。
- 替代清单：产品源文件中不再引用 MessageBox 控件类型；仅保留客户端旧调用兼容所需的 MessageBoxResult 枚举，不创建默认窗口。模板探针使用真实 ConfirmationWindow。静态门禁扩展到 installer/SharedUI，禁止默认类型、移除反向要求旧实现的断言；安装回归验证真实组件几何/内容/结果，不再调用样式修补器。主项目和工作树 AGENTS.md 同步强化禁用默认类、共享尺寸、禁止事后补模板的规则。原运行客户端未重启，未将旧进程冒认为当前修复。
- 验证：客户端与安装相关模块 Debug 构建均 0 错误/0 警告；Toast 36 项、6 张深浅主题/三语截图通过；客户端 48 场景 444 项原生窗口/AutomationPeer 检查通过；安装器 122 项纯视觉/操作语义检查、33 张图通过；2 个启动/卸载通知原生布局及实际打包字体解析通过；UI 与安装静态合约通过。安装首次运行发现宿主默认字体分支不一致，改为优先解析 InstallerFont 后重跑通过，失败记录保留。
- 真实返回路径：现有隔离会话回归先通过 188 项，包含继续编辑保留草稿、放弃不写配置、保存落盘再返回、非法输入阻止返回；三种选择分别保留新截图。末项“桌面焦点保持不变”仍失败，整套未通过，保留失败而不修改断言。此项及外部真实鼠标验收 **Not verified**；本轮无真实蓝牙超时/自动重试硬件复现，相关策略由合成故障验证。证据 `.acceptance/prompt-system-20260913`，已审阅正常/危险确认、未保存设置真实入口和短暂提示的新截图。所有隔离宿主均已退出。
- 交付边界：已生成当前 Debug 构建，尚未制作新安装包；用户正在使用的旧客户端保持原样。发布前仍需当前构建的交互验收及完整安装矩阵，不能以纯视觉检查代替实际安装/卸载。

## 2026-09-13 — 移除 Windows 附近设备下方的常驻提示

- 范围：按用户截图删除“附近新设备”下方整个反馈区域，覆盖扫描中、完成、失败和连接中、失败等所有状态；仅修改 MainWindow.xaml、MainViewModel.cs、MainViewModel.Home.cs 与原有 DesktopAcceptance 调用，不改 Android、传输协议、驱动或用户数据，不打包、不提交。
- 根因：连接尝试失败时将设备名与底层异常 detail 拼成 HomeFeedback；该区域同时显示扫描结果，且没有自动消失机制。截图英文是超时异常原文；本轮定位其显示路径，未复现或修复底层连接超时。
- 替代检查：删除该区域的 XAML、专用 HomeFeedback 属性、连接反馈字段及通知逻辑，清理 UpdateConnectionAttempt 中不再使用的名称参数和所有调用。代码检索确认无 HomeFeedback / _connectionFeedback 残留。扫描按钮与工具提示、附近设备卡片的连接状态/失败详情、诊断日志保持；没有将这些提示转成弹窗或另一个常驻区域。
- 验证：客户端及验证项目 Debug 构建 0 警告、0 错误，差异空白检查通过。现有隔离原生窗口会话回归两轮均先通过 188 项（含扫描已知设备、会话切换、深浅主题和未保存设置等），并各生成 35 张截图；末项“桌面焦点保持不变”两轮均失败，保留原失败记录，不宣称整套通过，也不归因于用户操作。截图复核确认附近设备下方提示已移除。未新增低影响删除的镜像测试，未更改既有验收断言。
- 限制与后续：桌面焦点保持验收 **Not verified**，本轮未定位该末项失败；未进行真实蓝牙连接或超时复现。证据 `.acceptance/home-feedback-removal-20260913`。未替换正在运行的客户端，需启动新构建或后续安装包才生效。

## 2026-09-13 — 统一 Windows 提示弹窗

- 规则固化：按用户要求，在主项目与当前工作树的 AGENTS.md 写入“禁止默认 MessageBox”的强制规范；覆盖客户端、启动器、安装与卸载、异常兜底，明确复用现有统一弹窗与短暂消息组件，同时约束空白按钮、裁剪、对齐和实际组件验收。保留原有规则，仅调整文档，不重新构建应用。
- 范围：按用户“所有界面检查并修复”要求，审查 Windows 客户端、启动器、安装/卸载的弹窗入口。修改客户端 BlueLinkDialog/ConfirmationWindow、启动器与卸载启动报错入口及共用 InstallerNoticeWindow、必要的回归/静态门禁；不改传输协议、Android、Figma、用户数据，不制作安装包或提交。
- 根因：普通错误/信息提示仍走默认 WPF UI MessageBox，而取消传输已使用原型确认窗口；两条路径的标题、图标、按钮列和间距不同。默认模板会拉伸按钮并保留空操作列，紧凑样式下文字也不协调。
- 替代检查：BlueLinkDialog 的信息、错误、确认、自定义内容和未保存设置三选项统一复用 ConfirmationWindow；不再实例化默认 MessageBox，移除原硬编码圆形文字图标和仅针对设置的模板补丁。保持官方 FluentWindow/Button，未新增基础控件模板。信息/错误确认按钮靠右、按内容取宽且保持 36 DIP 高度，隐藏不存在的操作，普通短提示最小高 220 DIP；危险行为保持原取消传输样式。正文超高才滚动，保留关闭/Escape/原有返回值，三选项映射仍为保存/继续/放弃。原生窗口标题也显式设置，避免只有内部标题文本。
- 其他入口：启动器“无法启动蓝联”、卸载器“无法启动卸载程序”改成同一布局的共用通知窗口；正文图标对齐、采用打包字体，且启动器不再同步阻塞异步 MessageBox 的 UI 消息循环。安装取消、覆盖安装、卸载确认本来已有原型样式，保留其真实操作语义；App 中唯一剩余默认 MessageBox 是显式控件探针，不属于用户流程。
- 验证：客户端和安装相关模块构建 0 错误/0 警告；客户端 48 个场景（深浅主题×三语×8 类提示）、444 项原生窗口/AutomationPeer/几何检查及截图通过；会话与未保存设置 189 项回归、35 张截图通过；启动/卸载错误的 2 个独立布局通过；安装器 96 项纯界面检查、33 张截图通过。静态 UI 合约通过，按新共用窗口更新旧 MessageBox 门禁与回归类型；查看实际渲染图修正了启动提示图标对齐，最终截图已复核。仅使用合成信息/隔离数据，没有实际删除文件、安装、卸载或连接真机。
- 追加修正：用户指出正文/图标未对齐；固定图标原先按顶对齐，文字按最小 44 DIP 行居中，导致中心线偏移。客户端共用确认窗口和启动通知的正文/图标、标题/关闭按钮均改为中心对齐，新增 96 项中心线检查。字体检查同时发现缺失程序集 BaseUri 导致回退字体，修正为明确的程序集资源目录后，两种启动程序均实际解析到打包的 NotoSansSC-Regular.ttf；不是只检查字体名称。最终新图为 notice-aligned.png、error-aligned.png。
- 验收限制：Computer Use 等待测试宿主的应用授权超时，真实鼠标操作/hover 的外部桌面复核 **Not verified**；不把原生 AutomationPeer 回归冒称真实鼠标通过。隔离测试宿主已结束，原正常蓝联进程保持。证据 `.acceptance/dialog-unification-20260913`，没有更换用户正在运行的旧客户端；修复需后续构建发布或启动新构建后生效。

## 2026-09-12～13 — WPD 文件通道替换与按设备排队

- 有效范围：Windows `Usb/` WPD 后端、设备 FIFO、Session/PeerSession、Supervisor、MainViewModel 和批量文件入口；Android `usb/` SAF 目录、MainActivity/Runtime/Session 接线；双端能力位/加密中转/接收导入、必要回归及 USB 使用指引。保留已有 UI、安装器和历史数据修改；本次不更改 Figma、不制作安装程序、不提交。仅用已授权真机，不使用模拟器、不改驱动或系统 USB 用途。
- 决策及根因：用户决定以 WPD/MTP 替代 AOA。先前原始 WPD 并发实验出现设备忙，因此同一物理手机的双向文件统一由 Windows FIFO 调度，后面的文件明确等待；不同手机独立。探测、清理和原生数据流也按设备互斥，避免自身竞争。外部设备忙只在流开始前退避，最长 45 秒，不重放部分写入。
- 实现：认证蓝牙保留消息/控制与原有 Offer、接收限制/询问、重名策略、最终确认。新能力 `0x20` 与控制类型 33 协调 SAF 会话目录随机证明、队列、进度及完成。共享目录使用每次随机密钥的 BLM1（1 MiB ChaCha20-Poly1305 记录），密钥仅经加密 BTX，UUID/大小/序号绑定于 AAD，最终仍校验 SHA-256。Windows/Android 保持本地文件准备顺序，Windows 多选即时提交为可见等待任务；取消单个文件不撤销整条 USB 就绪状态。等待项取消保持链条，取消当前读取后先等待原生流退出，再放行下项。主动取消不再进入批量发送失败弹窗。
- 替代检查清单已完成：Windows 不再构造 AOA UsbHost；Android 不再接入 UsbAccessoryController/Accessory 启动意图；闪电改为已认证 Bluetooth Peer 的 WPD 文件就绪能力；双端帮助改为 MTP/SAF 步骤，旧能力向量和路由测试同步更新。旧 AOA 类、专用硬件入口及历史原始证据保留作参考，不作为生产接入路径。最终范围包含必要的消息窗口批量提交/取消和三语 USB 指引；未回滚任何无关预存修改。
- 自动化：Windows BLM1 空文件、7 个跨端相同密文向量、篡改/截断/错误 ID/尾部/非法大小拒绝，导入检查点重开/最终落盘，FIFO 取消、暂停队首与不同设备隔离通过；USB/路由 81 项、暂停控制 30 项、接收/设置/安全握手/通知 200 项通过。Android 全量 168 项 JVM 测试、共享 Java 协议 43 项及 Windows 协议 14 项通过（包括旧 0x1f 客户端不协商 WPD 的回退）；双端 Debug 构建成功。首次补充代码有缺失 Kotlin 导入，扩大回归发现旧 greeting 0x1f 和旧 AOA 消息路由断言；均修正后重跑，失败证据保留。当前未运行本轮完整 lint/安装器发布矩阵。
- 真机：REDMI K80 Pro / Android 16，USB 调试关闭且维持原 MTP 驱动。通过系统选择器创建/授权专用 Download/BlueLinkUSB，普通 Windows 文件多选和 Android 生产发送入口完成 Windows→Android 1 KiB/16 MiB/256 MiB、Android→Windows 16 MiB/256 MiB；14 个完成文件的独立 SHA-256 全部匹配。256 MiB 正向约 9.7–10.3 秒、反向约 10.1–10.9 秒；计时从出队开始到接收端整文件校验/发布，含加密/中转/校验，不含排队和快照准备。256 MiB 先入队时 16 MiB 等约 9.3 秒才开始。窗口真实 UIA 和截图确认“已暂停”与“排队等待”；暂停超过 17 分钟、同时收到 Android 256 MiB/16 MiB 请求后，取消 Windows 等待项并恢复队首，三文件按序完成。取消活动队首后 16 MiB 接续完成；最终候选再验取消，真实 UIA 确认不出现“部分文件发送失败”弹窗。关闭并保存 USB 后 1 KiB 蓝牙完成，再开启后闪电和 WPD 批次恢复。
- 验收边界：部分手机 UIAutomator 受动态状态栏影响无法进入 idle，早期工具返回了旧树；改成每次唯一文件并明确检查 dump 成功，后续失败不读取旧树，改用限定蓝联前台的新截图/实际日志证据。APK 更新与一次发送交接重叠造成任务中断，该轮不算成功，之后重连重测。旧 Windows 测试实例曾在关闭窗口和控制句柄后残留进程，只结束此已核对的测试实例；不冒称客户端正常退出矩阵通过。原手机用户文件/设置/信任均保留，仅操作本轮合成文件。
- 收尾：逐个再次校验哈希后，只删除本轮 11 个 Android 接收副本、3 个 Windows 接收副本和 3 个工作区源样本，共 17 个合成文件；保留证据/哈希清单、所有目录和 SAF 授权、原有测试源及用户历史。原 Microsoft wpdmtp.inf 驱动未变；全部传输实测结束后手机进入休眠，收尾仅做 ADB 校验和合成文件清理，不将此作为锁屏传输通过。
- 风险与后续：多手机/Hub、真实拔插、锁屏、其他机型、外部资源管理器 UI 并发及长时耐久 **Not verified**。每设备独立队列软件验证不能替代多机实机。WPD 重试重传整文件，不保证跨进程持久队列；加密中转增加临时空间，异常终止残留的跨进程自动回收尚未验收。已更新 PROJECT_ROADMAP.md、docs/IMPLEMENTATION_STATUS.md、BTX/1.1 协议说明，原始证据在 `.acceptance/wpd-production-20260912`。

## 2026-09-12 — 保留 MTP 的 Windows WPD 文件通道测速与稳定性验证

- 范围：按用户要求验证“保留 MTP，通过 Windows WPD 传文件”的速度与稳定性。仅新增 `.acceptance/wpd-benchmark-20260912` 中的实验脚本和原始证据，更新本日志、实现状态及 USB 候选能力边界；不改双端生产代码、不安装应用/服务、不打包或提交。使用此前指定的 REDMI K80 Pro（Android 16），保持“传输文件”，Windows 直接调用系统 WPD COM / IStream；执行进程未提权，无 UAC、驱动替换、端口重连或 USB functions 修改，USB 调试保持关闭。
- 文件与计时：新建独占 QA 目录 `BlueLink-WPD-QA-20260912-9b7a`，仅收发随机合成的 `bluelink-qa-*` 文件。Windows 写入计时包含创建流、写数据及 Commit；读取包含打开流、写入本地文件和本地 fsync。样本生成、SHA-256、目录查找、连接建立及无线 ADB 观察不计入单文件吞吐。无线 ADB 只控制手机本地样本生成、读取状态及校验摘要，文件数据始终经 USB WPD 传输；本轮未接入 BTX 加密或蓝联 Android SAF 授权流程，不与此前加密 AOA 成绩作等价性能结论。
- 基线：1 KiB 冒烟通过；16/64/256/512 MiB 各往返 3 次，全部大小、手机独立 SHA-256 与回读 SHA-256 一致。三次中位数（Windows→Android / Android→Windows）分别为 33.35/34.03、35.09/34.87、36.20/36.58、35.74/34.97 MiB/s；512 MiB 写入约 14.3 秒、读取约 14.6 秒。见 baseline-summary.json 和逐文件 events.jsonl。
- 顺序稳定性：同一客户端连续完成 10 轮 256 MiB 往返（双向共 5 GiB，含校验/状态观察约 164 秒），写入 35.30–36.41、读取 35.43–36.73 MiB/s，无校验错误。32 个 64 KiB 小文件整批写入约 1.074 秒、回读约 0.372 秒，全部手机与回读哈希一致；空闲 45 秒再传、WPD 客户端关闭/重开 3 次均通过，没有 USB 重枚举。单流暂停 15 秒再继续完整通过；取消声明 256 MiB、已写 8 MiB 的流后 WPD 未留下可见对象，紧接着新文件正常收发。
- 并发限制及恢复：两个独立 WPD 客户端同时读写同一手机时，512 MiB 写入成功，读取端打开流返回 ERROR_BUSY / 0x800700AA；保留原失败结果，不宣称原生并发通过。写入结束后两个对象均可正常回读且校验一致。另建独立 backoff 实验，仅在未创建目标文件、尚未读取数据的 GetStream 阶段对该错误有界等待重试；17 次忙重试后完成 5 次 64 MiB 回读，写入文件手机哈希一致。读取客户端含等待共约 24.65 秒。证明可等待恢复，不代表并行传输能力；正式实现需按设备串行调度，并处理资源管理器等外部客户端争用，不能无限重试或重放部分写入。
- 手机源文件：在手机 QA 目录本地生成独立随机 64 MiB 文件，完成手机哈希后首次 WPD 枚举即找到，无须显式媒体扫描，回传约 35.24 MiB/s、SHA-256 一致。枚举调用约 2 ms 不是创建到可见的延迟测量，不能承诺毫秒级文件发布；样本生成受无线 ADB 控制，不是普通蓝联 APK 已能访问该共享目录的证明。
- 总量与收尾：累计完成 65 次写入、69 次读取，约 12.504 GiB（不含主动取消的 8 MiB），完整文件校验未发现损坏；有 1 次保留的原生并发忙失败，随后单独验证等待恢复，summary.json 中 allPlannedChecksPassed=false，避免将全部场景误写为通过。只删除本轮生成且归属已核对的 QA 文件和空目录，并经手机确认路径已不存在；清理本地约 848 MiB 合成源文件，保留摘要、日志和脚本。最终仍为 mtp / WUDFWpdMtp，普通蓝联前台、KEEP_SCREEN_ON、Awake；所有本轮受控传输子进程已结束。
- 结论与未验项：WPD 原始文件通道的吞吐及顺序短程稳定性有实机依据，可继续验证应用集成；这不是决定替换 AOA 或完成正式 USB 高速功能。**Not verified**：蓝联普通 APK 的 SAF 目录授权/读写、蓝牙认证与加密协调、物理拔插/断点续传、锁屏、多手机/Hub、资源管理器实际 UI 并行拷贝、其他机型及长时间耐久。证据 `.acceptance/wpd-benchmark-20260912/summary.json`、events.jsonl、concurrent-*-result.json、backoff-*-result.json。接口按 Microsoft 官方 WPD 读写示例及当前 Windows 类型库/SDK 头核对：https://learn.microsoft.com/en-us/windows/win32/wpd_sdk/transferring-an-image-or-music-file-to-the-device 、https://learn.microsoft.com/en-us/windows/win32/wpd_sdk/transferring-content-from-the-device-to-a-pc 。

## 2026-09-12 — 非调试 USB 批次复用 AOA 与返回 MTP 验证

- 范围：按用户要求验证“一批文件共用一次 AOA，空闲后再回 MTP”；用户明确允许临时将指定 REDMI K80 Pro 的 MTP 接口绑定 Windows 自带 WinUSB、重连该手机端口并恢复原驱动。只操作该手机对应的节点及 Hub 端口 2，不重启计算机、不操作其他 USB 设备、不启用手机 USB 调试或通过 ADB 修改 USB functions。没有更改应用生产代码、安装常驻服务、打包或提交。
- 权限方式：驱动安装和端口重连需要管理员令牌；改用一次 UAC 启动的临时管理员进程，在限定设备、限定动作和 20 分钟期限内处理后续请求，避免同次验证每步弹 UAC。进程已退出；这不是已经完成的生产提权助手。日常免重复 UAC 的受限助手、安装授权、请求认证与恢复机制仍需设计验证，不能以本脚本直接替代。
- 启动实证：普通 MTP 驱动下 WinUsb_Initialize 失败；绑定 Microsoft inbox WinUSB 后 GET_PROTOCOL 返回 AOA 2，START 成功重枚举为 AOA-only 2D00（无 USB ADB）。首次准备 AOA-only 驱动时出现脚本空 ClassGUID 处理错误，修正后绑定触发额外重枚举，首轮未取得会话；失败日志保留。AOA-only 的 WinUSB 绑定就绪后再次由普通接口启动成功，驱动绑定至 AOA 可用约 2.29 秒，不含安全码确认和传输宿主启动时间。
- 实际传输：Windows 使用生产 USB/BTX 传输组件后台宿主，Android 使用真实传输验收入口；独立 QA 身份经两端安全码及指纹核对。Windows→Android 1 KiB、16 MiB、64 MiB，Android→Windows 16 MiB，共 4 文件全部大小与 SHA-256 一致、同一 SessionId、Transport=Usb。文件间隔 2 秒，全部完成后继续空闲 15 秒，无断开或重连。按首个 Transferring 至 Completed 计，16/64 MiB 正向约 13.41/13.40 MiB/s，16 MiB 反向约 7.75 MiB/s；不含模式切换、初始化、信任交互和造样本时间。
- 返回实证：空闲验证完成并释放宿主后，显式请求仅重连目标端口，AOA 返回普通 MTP，再恢复 Microsoft wpdmtp.inf / MTP / WUDFWpdMtp，总计约 3.84 秒，无需系统重启。最终节点 problem=0，安装类、服务、INF/section/version、接口注册值与备份相符；原本不存在的自定义 DeviceInterfaceGUIDs 已移除。AOA-only 接口保留已建立的 Microsoft WinUSB 绑定，没有安装第三方内核驱动。注意本轮不是产品在第 15 秒自动切回，自动批次策略尚未实现。
- 验收前提更正：恢复后 Windows Shell 和独立 WPD EnumObjects(DEVICE) 均识别手机，但存储根目录数为 0；用户随后提供 21:06 手机截图，确认 USB 用途当前为“仅限充电”。该选择不开放普通文件浏览，可以解释存储为空；应撤回将该现象列为“恢复文件浏览未通过”的判断，不以此判定 MTP 驱动恢复失败。原驱动恢复、同一 AOA 批次双向收发的实证仍有效。截图不是切换前 USB 用途的记录，也未实际执行切换为“传输文件”的对照，不能反向宣称已证明文件访问恢复。
- 用户开启“传输文件”后继续验证：Windows WPD 两次读取均取得存储根 s10001，建立真实可访问基线。随后 MTP→WinUSB 绑定成功，但设备短暂重枚举，脚本立即读取 CM 状态失败，首次恢复检查也过早执行；该助手已退出。本次没有发送 START 或新文件，不能把该失败算成 AOA 往返失败。补充设备重新出现/驱动稳定的有界等待，再通过新的临时管理员进程恢复原驱动，确认 WUDFWpdMtp / wpdmtp.inf、problem=0、自定义接口值已清理。两次 UAC 来自脚本进程退出后重新启动，并非每次协议切换必然弹窗。
- 新的物理结论：原驱动恢复后 WPD 根目录仍为 0；用户明确确认手机已经从“传输文件”自动变回“仅限充电”。因此这台手机在本次驱动绑定/恢复涉及的重枚举后丢失了先前的文件传输选择，无法保证免操作恢复文件浏览。第二次脚本在检测到基线已丢失后停止，未执行 AOA 批次或两轮往返，不把计划写成完成。当前实验路径已确认不能保持该设置；这不证明 AOA 协议本身在所有机型上都无法恢复 MTP，也不否定上一批 4 文件传输的通过结果。
- 恢复与证据：Windows 原 USB 开关已恢复开启并读取数据库确认，两个临时管理员助手均已退出。用户重新选择“传输文件”后，只读 getFunctions 返回 mtp，WPD 再次取得 s10001，存储下的条目枚举调用成功；仅验证一个条目是否存在，没有读取用户文件名或内容。这证明手动重新选择后原文件访问恢复，不能计作自动恢复通过。没有通过 ADB 改变 USB functions 或默认用途。新增 `.acceptance/usb-filemode-20260912` 与 `.acceptance/usb-filemode-retry-20260912` 保存非空基线、失败堆栈、恢复快照、用途读取、wpd-after-user-file-transfer.json 与 final-verification.json；保留原始失败结果，Android 返回正常蓝联并维持应用窗口常亮。 收尾发现此机 svc usb getFunctions 将值写入 stderr，已改为读取两条输出流并确认 mtp；此前仅捕获 stdout 得到空值的派生判断已标记无效，早前仅充电的结论依据用户确认与 WPD 空目录，不依赖该空输出。
- **Not verified**：从文件传输基线完成整个 AOA 批次后保留文件访问、正式自动批次、多轮往返、多机/Hub、暂停队列、MTP 使用冲突和异常恢复。普通 USB 功能/用户授权状态与 Windows 驱动绑定分别验收；产品应明确区分“退出 AOA 并交还系统 USB 模式”与“恢复先前文件传输用途”，后者在当前实验中未成立，尚未决定或实施替代方案。依据用户截图及 Android 官方文件传输说明 https://support.google.com/android/answer/9064445?hl=en 。
- 清理与保留：后台传输宿主及临时管理员进程已结束，Windows 原 USB 开关恢复开启并确认数据库已保存；正常 Windows 进程及原蓝牙会话保持，Android 返回正常 MainActivity，核对 KEEP_SCREEN_ON 和未锁屏。未清理用户历史、信任或原文件；测试文件使用 bluelink-qa 名称。证据 `.acceptance/usb-batch-mtp-20260912`（batch-2/result.json、verification.json、native-events.jsonl、suite-final-state.json、wpd-root-result.json 与实际截图）。此前普通 USB 自动启动的产品阻断仍然成立；本轮只新增受控物理可行性证据。

## 2026-09-12 — 关闭 USB 调试后高速通道未触发的实机定位

- 用户报告关闭 USB 调试、双端开启 USB 后仍无闪电且文件未走高速通道。本轮只读核对双端配置、手机当前接口/驱动、正在运行的 Windows 候选及生产枚举器，不改手机 USB 模式、应用配置或驱动。
- 确认双端已保存开启：Windows app_setting.usb_enabled=1，Android app_setting.usb_transfer_enabled=1。Android 通过现有无线 ADB 读取；只提取配置字段，读取过程产生的本地数据库副本已删除。当前 Windows 进程仍为 .acceptance/discovery-20260911/client-selection/BlueLink.exe。
- 实机接口为 USB\VID_2717&PID_FF40（REDMI K80 Pro），WPD 类，WUDFWpdMtp 服务、wpdmtp.inf、USB\MS_COMP_MTP，状态 OK；手机物理 USB connected/configured=true，未进入 AOA。生产 --usb-inventory 在桌面只读权限下仍返回 []，不是沙箱导致的枚举空结果。
- 根因有两层：AoaProtocol.IsAndroidCandidate 只识别 AOA 数据接口或 ADB 的 FF/42/01 兼容标识，普通 MTP 被过滤；UsbHost/WinUsbConnection 又明确要求 WinUSB 绑定，即使仅放宽过滤也不能通过 MTP 功能驱动发送当前实现所需的 AOA 控制请求。没有已认证的同 Peer USB 会话，故闪电不出现，现有蓝牙会话不会自动变成 USB。
- 能力边界更正：此前 USB 收发/完整性/暂停/测速实机证明的是已取得 WinUSB 控制入口、并成功切换 AOA 后的数据链路；不代表“关闭调试，普通 USB 插线后自动启动”已经成立。当前非调试 MTP 启动路径由 Not verified 升级为**已确认不支持/待修复**。Android AOA 官方明确 USB 调试不是连接配件的必需条件；这是应用宿主初始化和驱动接入覆盖不足，不应要求普通用户以开启 USB 调试作为完成条件。
- 后续修复边界：需设计并验证普通 USB 状态下的 AOA 启动/Windows 驱动接入，以及未识别/驱动不适用时的可见原因。不能仅放宽设备过滤或伪造闪电就宣称完成；不将未认证 USB 设备误绑定到任意会话。本轮未替换 MTP 驱动（会影响系统正常文件访问），未触发 AOA 切换、重传文件、打包或更改应用代码。
- 证据 `.acceptance/usb-no-debug-20260912`：双端开关摘要与 diagnosis.json；参考 https://source.android.com/docs/core/interaction/accessories/aoa 及 https://learn.microsoft.com/en-us/windows-hardware/drivers/usbcon/using-winusb-api-to-communicate-with-a-usb-device 。该普通连接场景仍是发布前阻断项，不能以此前已成功的 AOA 数据测试替代验收。

## 2026-09-12 — Android 输入法避让与附件图标裁边修复

- 范围：Android 正常会话的键盘避让，以及用户追加的输入栏附件图标左缘裁切；保留紧凑输入框、按钮尺寸、缩略图和传输行为，不修改 Windows。
- 根因与修复：MainActivity 未声明键盘调整方式，真机实际选择 adjustPan；键盘顶端为 y=1614 时，按钮触控边界底部到了 y=1653，返回标题栏也被整体平移至屏幕之外。主入口明确采用 adjustResize；会话消费 Scaffold 已应用的系统栏内边距，子层只处理剩余 IME 边距。Debug 验收入口同步使用 resize，避免测试入口与正常入口模式不一致。
- 图标替换：从 Figma 文件 Tna537kDQdpsVwDX0lXlCs 的 422:123 读取原始路径，确认 20×20 导出框截断了左侧描边。使用同一回形针路径的 VectorDrawable 替换 figma_content_attachment.png，保留同名资源引用，恢复完整描边并居中，实际线宽约 1.42dp，四周至少约 1.37dp 留白；移除旧 PNG，未留双份运行时资源。未修改 Figma 原型或其他图标。
- 验证：Android 167 项 JVM 测试通过，无失败/错误；最终 Debug 构建与 lint 通过（0 错误、9 个既有警告）。新增 scripts/test-android-composer-ime.py，基于正常 MainActivity、现有真实会话及系统 UIA 验证，不以 Debug 展示页代替正常入口。验收覆盖空输入键盘关闭/打开、单行、六行草稿在四行视口内滚动、清空并收起、再次展开及最终收起，共 7 个状态。
- 真机结果：REDMI K80 Pro / Android 16 / 当前搜狗输入法，键盘打开时标题栏边界始终为 [11,144,146,279]，两侧按钮底部为 y=1602，完整位于 y=1614 的键盘上方；可见输入框/按钮底部对齐并保留约 8dp 间距，收起后恢复原始位置。附件图标实际描边距图标画布边缘最少 3.625 个物理像素，视觉中心偏差各 0.5 像素，无左侧切边。
- 最终交付：同开发签名覆盖安装最终 APK，安装后再次核对正常会话键盘展开/收起、主窗口 adjustResize 与 KEEP_SCREEN_ON；测试草稿已清空，没有发送消息、清理历史或下载文件。手机停留在正常蓝联会话页并沿用应用窗口常亮。证据 .acceptance/composer-ime-20260912/（before-ime、final-normal、installed、构建日志与图标边界数据）。未使用模拟器或 Computer Use，未修改手机系统设置，未生成 Windows 安装包。
- **Not verified**：其他手机、其他输入法、横屏/分屏、浮动键盘及不同字体缩放/系统导航方式；本次不扩展传输和安装矩阵回归。

## 2026-09-12 — 双端聊天缩略图自适应与居中裁切

- 范围：仅改变聊天消息内图片缩略图的尺寸、裁切及对应缓存，保留聊天图片开关、完整预览、消息搜索缩略图与文件操作；复用上一轮暂停状态及 Android 紧凑输入栏。用户补充 48px 为参考值，因此作为可继续调整的视觉下限，采用 Windows DIP / Android dp，未设为设备物理像素。
- 行为：保留现有最大展示范围（Windows 240×144、Android 190×126）。普通图片按比例缩小并保持自然尺寸；短边已小于或等于 48 时不再缩小，短边大于 48 时最小缩放比例以短边达到 48 为界；长边仍超限就展示源图中心区域。原本不足最小区域的小图不强制放大，以透明留白补足约 48×48 的最小显示区域。
- 实现：两端分别增加纯几何策略，使用同一份 16 例 TSV 验证缩放比例、视口和源裁切范围。完成后优先读取原图尺寸，原图不可读时回退预览；接收未完成只允许读取已收到预览，不读取半成品原图。尺寸由当前可用源图计算，避免对已经采样过的缩略图再次判断最小短边。
- 渲染：Android 移除聊天附件固定 190×126 容器，按计算尺寸显示；优先区域解码可见部分，不支持的格式走有界解码回退。Windows 转换器返回尺寸受限的中心裁切位图，Image 使用自然尺寸，进度叠层收至 40 避免将最小区域撑大。缓存加入策略版本；Windows 修复 PNG DPI 元数据量化造成首次和缓存重读尺寸不同的问题。未修改完整图片预览及原文件。
- 验证：Windows Release 0 警告/0 错误，官方控件契约通过；新增 --background-thumbnails 专项通过 77 项及 25 张原生截图，覆盖共用用例、缓存重读、真实聊天模板尺寸、中心颜色、透明留白、进度叠层和不读取半成品。尝试整套历史会话回归时末尾桌面焦点断言失败，之前所有缩略图断言通过；本轮最终使用不含其他设置弹窗流程的专项重新验证，桌面焦点保持不变，未将失败的整套运行记为通过。
- Android：167 项 JVM 测试通过（新增 3 项，含 16 例共用向量及 81 种宽高组合），Debug 构建、lint 通过（0 错误/9 个既有警告）。按原开发签名覆盖安装真机，浅深色各 8 类图片、共 16 场景检查完成：小图、短边原本不足下限、超长纵横图、普通照片、自然尺寸及正方形。逐张核对完成/暂停两种消息内图片边界与中心区域；暂停标记和输入栏正常。截图为 Display P3，颜色识别按色域下的青色/红色关系核对。验收仅在内存聊天中显示应用缓存样例，没有向另一台设备发送消息。
- 证据：`.acceptance/thumbnail-geometry-20260912` 的 `windows-thumbnails/`、`windows-build.log`、`windows-thumbnails.log`、`ui-contract.log`、`android-build.log`、`android-checks.json`、`android-device/`。真机已更新，验收结束后回到正常蓝联并验证应用窗口常亮；Windows 未重新打包或替换当前运行候选。
- **Not verified**：其他设备/真实 DPI 与字体缩放、全部图像编码格式、巨幅图片解码压力、网络传输与安装升级矩阵。旧的接收预览不携带原始宽高，原图未完成前只能依据该预览的实际像素；收到完整原图后再按原始尺寸计算。未使用模拟器、Computer Use 或 Fixture.exe，未清理用户数据。

## 2026-09-12 — 设置离开确认、USB/暂停/缩略图与 Android 输入栏

- 范围：处理双端 USB 未触发、发送方暂停后的接收状态、聊天缩略图开关，并纳入用户追加的 Windows 未保存返回提示和 Android 附件/发送按钮等高缩小。保留此前文件图标、标题、消息选择等修改。
- 设置根因：只读核对发现 Windows 持久化配置为 USB 关闭、聊天缩略图开启；用户确认修改开关后直接返回，没有保存。新增按实际控件值与已保存快照比较的离开检查，返回主界面、取消、Esc、全局文件入口统一经过确认，提供「保存并返回 / 放弃修改 / 继续编辑」。改回原值不提示；保存校验失败保留页面与草稿。继续使用官方 WPF-UI MessageBox，三按钮保留原模板，补齐紧凑高度、主题文字与主按钮配色。
- 传输暂停：Windows 原先只对发出任务应用收到的暂停控制，接收任务不会转入等待。现双向处理 Pause/Resume，分别保存本端与对端暂停状态；迟到的块确认不能覆盖暂停，一端继续不能解除另一端的暂停。新增 RemotePaused 状态，接收方显示等待及暂停标记，不显示过期速率。枚举追加以保留既有数值。
- 缩略图：Android 原先用「完整文件可打开」作为缩略图前提，忽略传输中已收到的预览。现在开启时可使用独立预览，原图未完整接收仍不可打开；关闭时回到文件卡片。传输/等待叠层和文字继续显示，消息搜索使用同一规则；失效预览可回退到已完成原图。Windows 保存后的 ON/OFF/ON 切换及传输中预览已验证，原模板绑定有效。
- 输入栏：Android 通用 IconButton 的最小布局空间连同外部背景一起放大。仅输入栏局部取消额外布局占位，可见按钮以单行输入框高度为边长，默认约 40dp；附件、发送图标 24dp→20dp 并居中。保留 Compose 最小触控命中范围，8dp 间隔/边距；多行输入时两侧按钮保持底部对齐，字号缩放时同步计算单行高度。
- 后台验证：Windows Release 0 警告/0 错误，官方控件契约通过；30 项暂停控制与 81 项 USB/传输回归通过。最终原生 WPF UI Automation 189 项、35 张截图通过，包含浅深色缩略图、接收等待、未保存三选项、改回原值、保存校验失败及桌面焦点不被抢占。Android Debug 构建、164 项 JVM 测试通过，lint 0 错误/9 个既有警告。
- 真机 USB：当前手机初始 USB ADB 接口占用会使 AOA 切换的 WinUSB 初始化返回访问拒绝。仅临时释放该调试连接并立即恢复 ADB 后，现有驱动成功进入 AOA，后续 USB ADB 与蓝联同时工作。未安装驱动、修改手机全局设置或在应用中加入强制停止 ADB 的行为。通过使用生产协议/会话代码的后台 Windows 硬件验收宿主，完成 Windows→Android 和 Android→Windows 各 16MiB 的真实 USB 文件及完整性核对；Windows 发起暂停使 Android RemotePaused，Android 正常会话中的「暂停传输」使 Windows RemotePaused。用于后一次暂停的 256MiB QA 任务已主动取消，不计为完整传输或测速结果。
- 真机 UI：按原签名覆盖更新 REDMI K80 Pro，保留数据。浅深色实际生产会话组件中核对预览 ON/OFF/暂停叠层，以及输入栏空内容、单行键盘、多行、点击发送清空的 6 项行为；输入栏与预览视觉样例仅存于验收场景内存，不发送样例消息。手机已返回正常 MainActivity，应用窗口 KEEP_SCREEN_ON 确认有效。
- 证据：`.acceptance/transfer-fixes-20260912` 下 `windows-final-ui/`、`windows-final-build.log`、`usb-regression.log`、`ui-contract.log`、`android-composer-build.log`、`android-final-checks.json`、`hardware-retry/`、`composer/`。硬件宿主已停止。本轮未生成 Windows 安装包、未替换已运行的旧 Windows 候选，Android 已更新；用户仍需保存 Windows USB 设置才能生效。
- **Not verified**：其他真机/字体缩放、多物理设备并发、不同 USB 驱动/线材、USB ADB 占用时的自动 AOA 切换、长时间稳定性、吞吐基准及 Windows 安装升级矩阵。未使用模拟器、Computer Use 或 Fixture.exe；没有清除用户身份、历史或下载文件。

## 2026-09-12 — Windows 关于页精简与 Android 首页标题统一

- 范围：删除 Windows 关于页红框内 Bluetooth Only 说明条及整个背景容器，移除随附占位；保留版权行。Android 仅调整设置首页标题，设置子页的返回按钮和居中标题保持原逻辑。
- 实现：DeviceHomeHeader 与 SettingsHeader 的首页分支共用 HomePageHeader，统一 18sp 加粗标题、26sp 行高、34dp 蓝联 LOGO、16dp 横向边距及最小 64dp 标题栏高度。会话和文件页原有连接状态区域继续由同一组件的 trailing 内容展示。
- 验证：Windows Release 构建 0 警告/0 错误；原生后台 --background-about 模式通过 5 项页面/焦点检查并输出浅深色截图，截图在官方导航过渡动画结束后生成，红框说明及占位均已移除。Android Debug 构建与 lint 通过（0 错误、9 个既有警告），使用已有开发调试密钥同签名覆盖安装 REDMI K80 Pro，不卸载应用。
- 真机：会话、文件、设置三页的 LOGO 坐标均为 [45,163,141,259]；标题左边缘、顶部和底部均为 [141,174,248]，即同一字号/行高和垂直位置。设置标题宽度自然使用余下空间。已对比修改前后设置页截图，未出现图标或文字裁剪。
- 补验：上一轮 19 类文件图标在真机浅深色主题中全部可见，20/24/40dp 同屏核对通过，样例只在 debug 场景内存中显示。退出验收场景并返回蓝联正常设置首页，沿用应用窗口 KEEP_SCREEN_ON。
- 证据：.acceptance/about-header-windows、.acceptance/about-header-android/header-geometry.json、settings-before.png 与三页 after.png；构建日志 about-header-*-build.log；图标补验证据 .acceptance/file-icons-android-device。
- 限制：Windows 安装目录与安装包未更新；本轮没有通信回归、其他手机/字体缩放验收。未运行模拟器或 Computer Use，未修改手机系统设置或操作其他应用。

## 2026-09-12 — Phosphor 文件图标 v2 与双端接入

- 范围：将确认后的 19 类文件图标用于 Windows/Android 的消息附件、文件列表、消息搜索和文件详情/不可用提示；保留真实图片缩略图，未改传输协议或用户数据。旧版分散的 PDF/图片/视频/通用文件规则由共享分类表替代。
- 原稿：Figma 1375:291，保留蓝色衬线 W、绿色 S、橙色 P、飘带单音符、四孔卷盘以及 PDF、SQL、配置、字体、镜像、设计源文件、3D/CAD、证书专用构图。SQL、音频、配置、镜像、设计源文件、证书采用用户微调后的主图；此前同步的 18 个实际尺寸样本、57 个主预览样本及六个 SVG 继续有效。电子书不新增类别。
- 共用资源：shared/file-icons/catalog.json 统一 177 个扩展名、MIME 回退和主题色；包含 java/py/go/js/ts/tsx/vue、conf/config/yaml/yml/properties/ini、qcow/qcow2/vdi/ova/ovf。扩展名大小写不敏感并优先于 MIME，未知类型兜底。19 张 256×256 PNG 由当前 Figma 主图直接导出，两端复用透明度遮罩，保留双层透明度及源 SVG；节点、SHA-256、透明边界见 export-manifest.json。
- 实现：scripts/generate-file-icons.py 生成双端分类器和资源映射；WPF 通过视图转换器解析图标，领域模型保持字符串属性，不序列化 WPF 对象。验收发现嵌套 DrawingImage 缓存旧调色板，改为视图层可变副本随主题更新，已显示的图标也立即变色；小尺寸启用高质量采样。Compose 使用应用当前主题着色，图标保持正方形。两端开源许可加入 Phosphor MIT 全文。
- 验证：Windows Release 构建通过，536 项分类/原生 WPF UIA/实际附件模板与文件表格检查通过，包含浅→深→浅切换、20/24/40 尺寸、源资源完整性，4 张最新浅深色截图已核对；官方 UI 控件契约通过。Android Debug 构建、161 项 JVM 测试（含共用分类用例及土耳其语区域设置回归）通过，lint 无错误、9 个警告。生成文件一致性通过；19 张源 PNG 的颜色、256×256 尺寸、四边透明边距均检查通过。
- 证据：.acceptance/file-icons-windows、file-icons-windows-build.log、file-icons-windows-test.log、file-icons-android-build.log、file-icons-ui-contract.log；Android 结果位于 app/build/test-results/testDebugUnitTest 及 app/build/reports/lint-results-debug.xml。
- 真机补验：手机解锁后，在设置标题调整这一轮使用原开发调试签名覆盖更新 Android，19 类图标在浅深色及 20/24/40dp 全部可见、无裁剪，截图已核对；证据 .acceptance/file-icons-android-device。此前锁屏造成的实机验收缺口已补齐。
- **Not verified**：其他实体设备、其他 DPI/字体缩放及通信/安装矩阵；未运行模拟器、Computer Use 或 Fixture.exe，未生成 Windows 安装包，Windows 已安装客户端未更新。

## 2026-09-12：Android 文字选择手柄越过气泡边界后仍连续更新

用户指出上轮仍遗漏手指移出气泡的情况。本轮仅修改 Android 消息文字选择组件、调用处和 debug 内存验收场景；Windows、传输和用户数据不变。真机复现：同一次按压中，手指在气泡下方左右移动，放大镜移动而选区一直到文本末尾。对照项目实际使用的 Compose Foundation 1.6.1 官方源码，SelectionContainer 的多组件选择将文字上方/下方坐标直接映射为全文起止；上轮气泡内部横移验收不能证明越界行为正常。

将单条正文的 SelectionContainer + Text 替换为官方只读 BasicTextField，使用显式 TextFieldValue 保存选区，并拒绝正文变更。官方文本布局可在边界外继续按最近行及横向字符位置计算选择。保留字体、行高、气泡内边距和自然宽度，光标透明且不弹出编辑键盘；失焦明确清空选区。继续保留浅蓝手柄 `#A8D1FF`、浅深色选区底色、单子项工具栏布局和外部点击取消选择。新增 conversation-selection debug 场景包含收到的中文三行、发出的英文四行及短消息，不写数据库、不发送消息。

验收：Android Debug 构建和 159 项 JVM 测试通过，使用原签名覆盖安装 REDMI K80 Pro 真机。使用标准 ADB motionevent 在保持 DOWN 到 UP 的过程中截取每个 MOVE 后的画面：原单行消息下方右移/左移/返回路径已出现不同选区；英文多行结束手柄覆盖下方横移、右侧跨行、返回和上方，开始手柄覆盖左侧跨行/返回/上方；中文收到消息覆盖下方横移、左侧跨行及返回。浅深色均复核，正文和状态行在选择前后/取消后的边界一致，局部复制 kilo 后粘贴到内存场景输入框核对并清空。手柄仍位于对应文字锚点，手指超出气泡时按最近文本位置更新；没有声称手柄会脱离文字跟到任意空白处。

证据 `.acceptance/selection-boundary-20260912`：官方版本源码、修复前后截图、连续路径 JSON/截图、`geometry.json`、`copy-check.json`、构建日志及范围差异。自动审批拒绝自定义 app_process UI Automation 探针，未部署或执行；实际验收沿用既有前台包校验的 ADB 触控/截图，未修改系统设置。最终手机返回真实会话、空输入框、无选区，蓝联验收窗口常亮；未使用模拟器、Computer Use 或生成 Windows 安装包。

**Not verified**：其他 Android 版本/实体设备、超长消息跨屏自动滚动、量化帧率/输入延迟及本轮之外的通信/安装矩阵。该条替代上一轮 Android 文字选择内部实现；之前 Windows 选区清理与浅蓝手柄要求继续生效。

## 2026-09-12：双端文本选区清理与 Android 手柄、布局修复

继续处理用户追加的选区问题，范围限定消息正文选择组件及相应验收；最新要求将 Android 选择手柄统一设为浅蓝色 `#A8D1FF`，替代中间试验的深色手柄。Windows 点击非焦点空白、切换键盘焦点或离开应用时清除真实选择范围，打开当前消息菜单仍保留有效选区用于复制；修复原先仅隐藏高亮而保留 SelectionLength、导致右键复制旧选区的问题。

Android 在会话容器观察外部按下并清除选择焦点，不消费事件、不拦截选择手柄拖动。发出消息的选区根据浅深主题使用与蓝色气泡有区别的底色，手柄均为浅蓝色。正文和选择工具栏统一包在一个 Box 中，避免工具栏弹出节点成为消息 Column 的额外子项，触发原有 6 dp 间距；真机原先状态行会移动 17 像素，修改后正文、状态和后续消息位置不变。消除拖动时工具栏显隐带来的布局跳动，保留原生选区手柄和放大镜。

Windows Release 构建零警告/错误、官方控件契约通过；原生后台 UIA 174 项/28 图通过。新增实际剪贴板断言覆盖当前局部复制、点击空白后右键复制全文、焦点切到输入框以及前后尺寸不变。Android 最终浅蓝手柄 Debug 构建与 159 项 JVM 测试通过，使用原签名覆盖安装真机并保留应用数据。

真机浅色真实会话已验证收发两方向的选择、点击外部清除以及正文/状态位置保持。连续拖动截图在手指抬起前观察到选区扩大及放大镜跟随，没有后续消息跳动；这些证据不作为帧率或输入延迟测量。浅蓝手柄最终 APK 的浅色配色已复核，深色配色通过应用内存验收场景检查，不更改用户主题设置。未发送新消息。

证据 `.acceptance/selection-followup-20260912`：最终构建日志、`native/`、几何对比 JSON、真机截图及拖动过程帧。Windows 运行 `client-selection/BlueLink.exe` 工作区候选，原 D:/BlueLink 安装目录及安装包未更新；本轮没有打包、模拟器、Computer Use、Fixture.exe 或系统启动项操作。手机仅通过蓝联窗口验收标记常亮。

**Not verified**：其他实体设备/Android 版本、极长消息下的持续拖动压力及量化触控延迟；没有重复通信吞吐或安装升级矩阵。

## 2026-09-12：双端设备名与消息交互修复（真机及原生后台验收）

本轮仅处理用户追加的四项问题：Windows 中 Android 名称不一致、右键后气泡变高、发送按钮悬停文字消失，以及双端文本不能局部选择。先前发现/回连修复继续保留；用户身份、历史消息及下载文件未删除。

设备名称不再只依赖扫描得到的名称或 RFCOMM 地址。双端在原有加密 PROTOCOL_HELLO 的八字节前缀后增加可选 BLDN 名称元数据，确认信任后才用于当前会话及历史显示；身份认证、合并与信任仍使用原公钥。旧版本忽略扩展，缺失/无效名称回退原显示逻辑。Android 默认名称与设置页统一取蓝牙名称，自定义名称优先；中间版本使用硬件型号的回退已被替换。名称更改在下次安全握手同步。真机覆盖安装最终 APK 后，Windows 已连接卡片及会话标题均为「Navi的REDMI K80 Pro」，与 Android 设置一致，真实 BTX/1.1 会话保持正常。

Windows 移除右键菜单打开时额外增大边框/内边距的触发器；发送按钮明确使用官方控件的悬停/按下背景与前景属性。消息正文改为官方只读 TextBox，允许拖选和 Ctrl+C，局部透明资源消除官方模板下划线，不覆盖模板；右键复制优先复制当前选区。Android 使用 SelectionContainer 与复制/更多操作工具栏，长按选择后仍可进入原消息操作；浮条按实际窗口坐标定位，进入消息菜单前清除选择焦点，最终真机截图确认不再残留于弹层后。

验收：Windows Release 零警告/错误，官方控件契约通过；共享中文/emoji 名称向量及旧版/无效扩展回退通过，安全握手回归 106 项通过。原生后台 UI Automation 144 项、28 张最终截图通过，包含单/多行局部选择、菜单前后尺寸、浅深色发送悬停与消息行透明。Android 最终 Debug 构建及 159 项 JVM 测试通过，以原签名覆盖安装真机并保留数据。真机已实际长按选词、拖动手柄扩大选区，将「20260912 Windows」复制后粘贴到空输入框核对，随后清空测试草稿，没有发送新消息；「更多操作」仍打开原有复制/删除本机记录菜单。

证据 `.acceptance/message-polish-20260912`：`native-reviewed/`、`native-reviewed.log`、`protocol-tests.log`、`android-toolbar-final-build.log`、`windows-connected-final.json`、真机选择/复制截图与 `android-copy-result.json`；当前 Windows 运行 `.acceptance/discovery-20260911/client-messages/BlueLink.exe` 候选，手机为本轮最终 APK。未改写 D:/BlueLink 安装文件、未生成安装包，未使用模拟器、Computer Use、Fixture.exe 或系统启动项。手机仅通过蓝联验收窗口常亮保持活跃。

**Not verified**：本轮未重新验证 USB 硬件名称同步、多实体设备、全部 Android 版本/输入法或新安装/升级矩阵；旧客户端兼容由编解码与握手回归覆盖，没有安装旧版 APK 实机回滚。下方发现任务记录的 Classic 地址显示问题已由本轮解决。

## 2026-09-11～12：双端发现、历史连接入口与同时重连修复（真机已验收）

用户反馈同时打开双端仍互相不可见。真机重新接入后继续同一任务，范围为 BLE 发现/广播、已知设备投影、手动连接入口及真实重连中发现的连接交接竞争。Windows 安装版与 9/11 23:34 新包一致，发现与启动扫描均开启；独立扫描和手机实际运行日志均证明双方能收到 BlueLink 广播，问题不只是蓝牙权限或系统扫描失败。

有效修改：Windows 扫描反馈原来只计新设备，历史设备附近可连接仍提示 0，现显示实际总数和新设备数；恢复历史卡片的“连接设备”菜单，普通点击继续打开历史。Windows 元数据读取暂时失败不再抹掉已经确认的可连接能力。Android 运行时过早过滤已信任设备，导致历史卡片无法取得发现端点；现在保留完整发现集，只在界面分组过滤，并依据真实可连接状态更新历史卡片。仅收到 Rendezvous UUID、平台尚 UNKNOWN 的有效可连接端点也可进入正常 GATT/安全握手，未降低身份核验。

真机重启复测发现双方同时自动连接的两个竞争：Android 本地主动拨号与 Windows 回连请求各建一个 RFCOMM socket，导致刚连接又被关闭；新增按 Classic 地址共享的拨号锁，持有到会话登记，活动会话拒绝重复拨号、不同设备仍可并行，连接结束恢复原先请求的扫描。Windows 在 GATT 请求尚未完成时先收到了手机连接，随后 GATT 失败使已收到 socket 被遗漏；新增回连交接器，socket 可立即进入握手，迟到的 GATT 错误不会覆盖成功连接，失败/取消/超时不再吞掉新入站连接，交接未完成的 socket 正确关闭。

验收：Windows Release 构建零警告/错误，官方控件契约通过；原生后台会话 UIA 86 项/20 图通过。新增不操作硬件的回连所有权回归 73 项通过，包含并发到达/失败、取消与超时。Android Debug 构建与 156 项 JVM 测试通过，包含同一设备并发拨号、会话登记后的重复请求及独立多设备锁；使用手机原签名覆盖安装，保留数据。真机实际收到 Windows 的 UUID-only 和携带 8 字节 ServiceData 两种广播；Windows 的 StartedWithoutAllAdvertisementData 在本机不代表广播全部丢失，UUID-only/关闭 discoverable 的实验均已撤销，最终保留原完整广播格式。新增仅 debug 的 20 秒只读诊断入口，生产包不包含它。

真实 Bluetooth 验收：Windows 发起回连、Android 主动连接均建立 BTX/1.1；首次 Windows 信任弹窗通过 USB 读取手机公钥并核对指纹后由正常 UI 确认，Android 已信任该 Windows，未声称两端同时显示并核对 SAS。四条带 QA 标记的消息为本轮新增，前后两轮双向收发均确认送达。最终候选 Windows 于 01:14:13 重启，01:14:16 安全会话建立，稳定保持超过 4 分钟，并完成最新双向消息。Android 真机日志已观察到重复回连被拨号锁阻止；Windows 的竞态由可控回归覆盖。旧身份、聊天/文件记录保留，未删除接收文件。

证据 `.acceptance/discovery-20260911`：最终 `windows-callback-build.log`、`windows-callback-tests.log`、`android-final-build.log`、`conversations-final/`、`physical/android-latest-messages.*`、`physical/windows-latest-messages.json`、`physical/windows-latest-session.log`、`verification-summary.json` 与逐文件任务差异。手机停在蓝联真实会话并保持该窗口常亮；Windows 当前运行 `client-final/BlueLink.exe` 工作区候选，`D:/BlueLink` 安装文件未替换，也未生成新安装包，9/11 23:34 包不含本轮修改。没有使用模拟器、Computer Use、安装器、Fixture.exe 或系统启动项操作。

**Not verified**：全新未配对设备、多台实体设备并发、长时间压力及本轮新文件吞吐。当时观察到 Android 主动连接后 Windows 名称退回 Classic 地址的问题，已在上方 9/12 设备名任务解决；身份/历史仍属于原设备，不能据单手机验收声称所有设备组合已覆盖。

## 2026-09-11：生成包含最新修复的 Windows 验收安装包（23:34）

按用户要求，从当前工作树重新生成 Windows 安装程序，输出到 `artifacts/windows/review-20260911-233411`，版本保持 0.2.17 / x64。包内包含输入框/下拉选项文字布局修复、安装路径框修复、许可列表高亮与帮助图标、身份详情直接展示、设置短暂提示、取消连接数上限、接收目录“更改”直达文件设置、连接偏好与 USB 分组/分隔线，以及最新消息行悬停和选中背景透明。保留既有源码、旧包和用户数据；下方各任务的“未打包”为当时状态，这些 Windows 修改现已纳入新包。

客户端发布、安装器组件及 MSI/Burn 构建通过，日志无警告/错误，66 项隔离文件归属断言通过。离线解包逐一验证 21 个载荷文件、22 个 MSI/ZIP 文件的大小和 SHA-256；嵌入 MSI、SetupUI、客户端及卸载器与新构建一致，Review 注册身份、卸载配置、同版本升级表、事务内先移除旧 MSI 以及恢复清单全部通过。安装器/版本契约通过；280 个构建输入前后哈希未变，17 个 Windows 源文件与最近后台验收留存哈希一致，正式发布锁未改。本轮只执行打包相关检查，不重复已通过的界面/通信回归。

EXE 为 `BlueLink-Review-DO-NOT-DISTRIBUTE.exe`，90,070,307 字节，SHA-256 `81EC55A3322ACBB16D63DF79C0CE83A6FF1695F86F1D77BDF6E07E81585A77B2`。产物目录保留 `build-verification.json`、`SHA256SUMS.txt` 和构建日志，源文件一致性及解包证据在 `.acceptance/package-20260911-233411`。仍为未签名验收包、不内置 .NET 8 Desktop Runtime；未执行安装、升级或卸载，实际安装验收仍为 **Not verified**。未启动 Fixture.exe、Computer Use、模拟器或手机调试，未操作系统启动项，也未提交或发布。

## 2026-09-11：Windows 消息行取消悬停与选中背景

按用户要求，仅调整 Windows 主会话消息列表的行容器。`MessageList` 虽继承透明容器样式，但局部的 `ListBoxItemSelectedBackgroundThemeBrush` 和 `MenuBarItemBackgroundSelected` 仍被覆盖为浅蓝色，现将这两个资源改为透明，继续沿用官方列表项模板及正常选择行为。收发消息气泡自身配色和设备/文件列表样式保持原样。

Release 构建零警告/错误，WPF-UI 契约通过。现有原生后台会话验收补充浅深色、收到/发出消息、正常/悬停/选中/选中并悬停的矩阵；通过 UIA 数据项执行选择，以官方模板渲染验证背景透明和气泡配色。74 项断言、18 张截图全部通过，并复核浅深色选中叠加悬停截图。测试使用桌面外透明、不激活窗口，未发送物理鼠标键盘输入。最初测试误取容器包装器的 UIA 模式，已改用数据项选择提供程序，最终验收通过。

证据 `.acceptance/message-row-background-20260911/final/`、`final-build.log`、`final-acceptance.log`、`ui-contract.log`；改动前备份及范围差异同目录保留。未使用 Computer Use、未操作手机或安装引擎，未重新打包；现有安装包不含此次修改。

## 2026-09-11：取消连接数量限制、修正接收目录入口与双端设置分组

按用户连续追加的要求实施，暂停后恢复完成收尾。范围限定 Windows/Android 连接设置、会话接纳、设置持久化、Windows 接收目录入口和设置行分隔；保留此前所有修改，未打包、未编辑 Figma、未操作手机、未运行模拟器或 Computer Use。

有效修改：

- 双端移除最大同时连接数设置、默认值和保存字段，取消握手前/认证后的应用数量拦截；旧 `max_connections` 值不再影响连接，也不再写入新配置。保留身份/信任核验、同一设备同一通道去重、生命周期暂停以及 Android 配件端同一 USB 资源的重复占用检查。实际能连接多少设备由系统、硬件通道和可用资源决定。
- Windows 文件页“更改”此前复用通用设置事件，现有独立处理函数直达“文件与存储”，导航选中正确页面并回到接收目录所在的顶部。
- 按用户最终确认，双端连接设置统一为“本机设备、连接偏好、信任管理”。USB 开关属于连接偏好，Android 蓝牙系统入口也在该组内；共通项顺序为 USB、启动扫描、允许发现、自动连接已信任设备、断线重连。Windows 本机设备名图标和 Android USB 图标已移除；Android USB 行复用原有整行开关语义，无独立嵌套卡片。
- Windows 连接偏好、三类通知与接收前询问/同名处理之间补齐 7 条行分隔线，使用主题颜色；设备名区移除原固定高度和图标占位，继续使用官方 WPF-UI 控件。

验证：Windows Release 构建零警告/错误；安全后台套件通过，其中 USB/通道专项 80 项，新增 12 个独立身份同时建立真实加密内存会话并逐一收发消息，验证超出旧上限仍可连接、重复通道被拒、断开一个不影响其他 11 个以及暂停期间仍拒绝新连接。SQLite 验证新配置不保存上限，并检查旧值 1/4/8/999/非法值在重载与保存后均失效。最新设置 UIA 7,095 项/110 图通过，覆盖浅深色、简中/繁中/英文、1000/1260 宽度，实际点击“更改”、设置切换与文字边界；连接页首尾、通知及目录跳转截图已复核。设置接线与官方控件契约通过。

Android Debug/Release Kotlin 编译、153 项 JVM 单元测试全部通过；lint 零错误，仅有 ObsoleteLintCustomCheck、KaptUsageInsteadOfKsp、IconDuplicates 三项既有警告。新增旧连接数配置忽略/不再保存和 12 个会话的路由隔离回归通过。

证据 `.acceptance/unlimited-connections-20260911`：最终 Windows 构建/设置验收日志及 `settings-final/`，安全后台套件日志，Android 首轮与最终后台验证日志，逐文件任务前备份和差异。最初 Android 构建使用错误缓存目录缺少离线插件，已切回既有 `.gradle-home`，未下载依赖。**Not verified**：真实多机同时连接的系统上限/吞吐，以及 Android 新分组的真机视觉与触控；内存通道和编译结果不替代这些验收。现有安装包未包含本轮修改。

## 2026-09-11：Windows 输入控件与设置页追加问题修复

用户在最新检查包验收中连续追加五项问题，本轮按顺序实施：输入框/下拉框文字居中与裁剪、移除“设备身份与安全”按钮、帮助提示图标不完整、许可列表高亮统一，以及设置操作反馈改用短暂消息。范围为 Windows 客户端、安装向导及对应后台验证；不改 Android、不使用 Computer Use、不运行安装引擎或 Fixture.exe，不重新打包。

有效修改与原因：

- 安装路径框缩到 32 DIP 后仍带官方默认 10,8,10,7 内边距，实际文字视口只有 15 DIP，但完整文字行需要约 18.8 DIP。补充紧凑内边距；客户端增加基于官方模板的单行输入样式，修正文件搜索框和消息搜索框，并保留多行编辑器顶部对齐和已有输入焦点反馈。
- 下拉选项仍带原来的大上下内边距，设置选项又固定 34 DIP，导致“每次询问”等行被内部模板裁剪。调整公共/设置选项内边距，移除设置选项固定高度、保留最小高度。选中值及选项文字按实际绘制背景居中；官方选项顶部间隔不被误当作内容偏移。
- 删除身份详情的展开按钮和处理函数，直接展示本机标识及重置入口；帮助提示的 24 DIP 图标原来被 16 DIP 固定列裁剪，改为按图标自适应列宽，三个帮助主题统一修复。
- 开源许可列表沿用帮助主题样式，调整高亮与分隔线之间的空间，选中标题变蓝并添加左侧标记；已选中项悬停仍保留高亮，切换条目会移除旧标记。
- 移除设置页 SaveInfoBar 及占位行，SetStatus 复用主窗口现有 ToastHost，信息/成功 3 秒、警告/错误 5 秒自动消失。页面和滚动位置不变，设置保存离页后反馈仍由主窗口承载；未另造消息组件。

后台验证：客户端与安装界面 Release 构建零警告/错误；最新 `--background-inputs` 为 4,049 项几何与行为断言、430 张组件/页面图，覆盖浅深色、简中/繁中/英文、1000/1260 宽度，输入空值/文本/长路径/禁用、下拉展开选项及追加设置行为。文字测试检查真实字符行、模板视口和布局裁剪；UIA 仅操作隔离后台窗口，无物理输入或前台激活。成功消息通过真实 DispatcherTimer 自动到期复验。安装界面 110 项/15 图、关于子页独立滚动 201 项、文件/搜索工作区离屏 1,303 项/91 图通过；WPF-UI、设置接线、安装器契约和改动空白检查通过。100/125/150/200% 图像是后台渲染采样，不代表切换了四档真实系统 DPI。

证据在 `.acceptance/input-layout-20260911`，最终 `acceptance/`、`installer-final/`、`about-regression/`、`workspace-regression/` 及对应日志有效。保留最初发现裁剪的失败记录；中间局部截图存在坐标与弹出动画采样问题，已改为等动画稳定后从视觉根节点渲染裁取，最终浅深色下拉选项、搜索框、帮助图标、高亮和 Toast 均人工阅图确认。未执行实际安装或改变用户手机/日常数据，最新已生成安装包仍为本轮修改前版本。

## 2026-09-11：生成最新 Windows 验收安装包

用户明确计划在完整验收后将项目完全开源，本轮仅要求最新 Windows 安装程序。使用现有 `scripts/build-windows-review.ps1 -CompileInstaller` 从当前工作树构建，版本保持 0.2.17 / x64，输出到 `artifacts/windows/review-20260911-latest`。包内包含最新双端设置对齐中的 Windows 修改、紧凑安装向导和完成页图标、按钮与设置边距修复，以及真实 USB 补测发现的主动取消误记失败修复。保留旧包与既有源码，不提交、推送、发布或选择开源许可证。

客户端发布、安装器各组件与 MSI/Burn 构建成功，构建日志零警告/错误；66 项隔离文件归属断言通过。离线解包检查 21 个载荷文件、22 个 MSI/ZIP 文件的大小和 SHA-256，确认嵌入 MSI、SetupUI、客户端及卸载器均为本轮构建；Review 注册身份、同版本升级表、事务内先移除旧 MSI、恢复清单及卸载配置一致。安装器/版本契约通过，298 个构建输入前后哈希未变，最新安全后台验收关联的 5 个 Windows 源文件也保持一致；本轮未重复执行此前已通过的 409 项回归。正式发布锁保持原哈希。

EXE 为 `BlueLink-Review-DO-NOT-DISTRIBUTE.exe`，90,050,633 字节，SHA-256 `34B7DF00ECF4727A8A66F60B75251461BD95B7B2AC09B4A4066BA1B946FD2A33`。校验结果与哈希分别在产物目录的 `build-verification.json`、`SHA256SUMS.txt`，原始证据在 `.acceptance/package-20260911-latest`。该包未签名且不内置 .NET 8 Desktop Runtime；仅供当前验收。未启动安装程序、Fixture.exe、Computer Use、模拟器或手机调试；实际安装、升级及卸载仍为 Not verified，留待用户检查及后续独立环境验收。

## 2026-09-11：恢复真机验收、保持常亮与 USB 双向补测

用户恢复 USB/无线调试并要求保持手机活跃。本轮范围为最新设置真机验收、安全后台回归、单手机真实 USB 收发与接收决策，以及验收直接发现的缺陷修复。仅操作蓝联及必要的 USB 通道；未使用模拟器、Computer Use、安装器/Fixture.exe 或系统启动项。Windows 使用后台生产传输组件宿主，Android 使用正式设置/接收界面；反向发文件通过 Debug 专用入口调用生产运行时，不能当作正式附件选择器的验收。

有效修复：USB 附件入口可能创建第二个 MainActivity，原先常亮只属于旧实例，新的前台窗口缺少 KEEP_SCREEN_ON。Debug 验收会话现在跨 MainActivity 实例同步明确设置的常亮状态；实际制造新的 USB 入口窗口，核对前台窗口标志和原生 mHoldScreenWindow 均指向新窗口。未修改系统自动锁屏时长、锁屏安全或 Release 常亮行为。另修正中/英/繁体接收上限说明：禁用时编辑只保存数值，需返回设置开启；不再误称保存即开启。

实际 USB 排队取消暴露 Windows 将 OperationCanceledException 一律映射为 Failed，并额外发送失败帧的问题。发送状态改为 Canceled，已有取消控制不再追加失败消息；新增发送/接收终态回归。修复后重建后台宿主并以同一可信身份重连真机，第一请求在双端均为 Canceled/CANCELED，第二个排队请求完成且哈希一致。

验收结果：

- Windows 新增 `--safe-background-only`，排除安装执行入口；最终 Release 零错误/警告，409 项计数断言及独立聊天时间组通过，覆盖传输、SQLite、设置/接收、安全、USB、反馈/更新、通知、历史、窗口与预览。接收组由 15 增至 17；不是 409 个独立真实硬件场景。
- Android 最新 APK 使用原 Debug 证书保留数据覆盖安装，151 项 JVM 测试、lint、Debug 构建通过；lint 只有 3 项既有警告。安装前识别旧后台构建证书不一致，使用原签名重新构建，没有卸载/清数据绕过。
- 真机正式设置入口验证三类通知、连接/发现偏好、1–8 最大连接数、接收询问/同名选择、上限禁用后编辑及 0 值无效、缩略图、四种公共目录保存选项、移动端后台/充电选项、历史/诊断、缓存清理、恢复默认确认与身份详情；关键值退出重启后从 Room 核对，最终恢复原值。这只证明设置操作/持久化，通知实际投递、后台电源限制和公共目录提供器行为仍须分别验证。
- Windows 生产 WinUSB/BTX ↔ 一台 API 36 真机完成双向聊天与双向 0 B、1 KiB、64 KiB−1/64 KiB/64 KiB+1、16 MiB、256 MiB，共 14 个文件；全部完成回执、大小与 SHA-256 一致。256 MiB 从首个 Transferring 事件到 Completed 的本机测量为 Win→Android 20.397 秒/12.55 MiB/s，Android→Win 41.134 秒/6.22 MiB/s；不包括准备快照/生成源文件，也不是长期吞吐指标。
- 真机接收前询问：同意落盘、拒绝、约 30 秒到期关闭、远端取消关闭并放行后续排队请求。正常接收、首次队列和修复后的队列共 3 个附加文件均核对哈希。等待期间聊天/附件元数据回执仍到达；未验 Windows 原生接收弹窗队列完整生命周期。
- 最终原有消息/附件/传输逐行一致，Peer ID、会话 ID/草稿、三组信任公钥和本机身份保持；6 个原应用私有文件/接收预览 SHA-256 保持。新增的是本轮 QA 消息/文件记录。原设置全部恢复（新增消息通知默认值为开），USB 模式已恢复原状态，两种 ADB 均指向同一手机，蓝联前台继续常亮。

证据统一在 `.acceptance/physical-settings-20260911`：`windows-safe-summary.json`/`windows-safe-final.log`、`android-final-summary.json`、`settings-events.jsonl`、34 张真机截图及 UIA XML、`settings-data-preservation.json`、`final-verification.json`、`wake-multiple-activity.json`、`usb-user/file-integrity.json`/`prompt-integrity.json`、`usb-cancel-fixed/events.jsonl` 与 `usb-restored-inventory.json`。最初在沙箱账户打开复制的旧 QA DPAPI 身份无法解密，握手按新身份等待确认并超时；没有确认该身份，随后在原用户上下文复用既有 QA 身份，最终 Peer/Trust 均仍 3 项。旧失败输出单独保留，不算成功证据。

剩余边界：Bluetooth 双向最新链路、双侧暂停/继续与真实拔插续传、500 MiB 上下边界、SAF/同名覆盖及存储故障、通知/后台/锁屏、多手机和多系统兼容尚未闭环；用户要求常亮，本轮不主动锁屏/进入 Doze。继续按规范场景清单补测，不将本次结果扩大成全矩阵完成。未生成 Windows 安装包、提交或推送。

## 2026-09-11：Windows、Android 与双端通信测试缺口审计

用户要求梳理尚未测试的全部场景。本轮只读核对应用/测试源码、最新专项结果、USB 真机传输/失联证据、身份关联与 Room 迁移证据、安装执行结果及两端原型台账，并更新规范记录；未修改应用代码，未运行构建/测试、ADB、模拟器、Computer Use、安装器或 Fixture.exe。

发现记录粒度混杂：7,047 项 Windows 设置 UIA 检查主要为重复布局/文字断言，不能折算独立业务覆盖率；旧 Windows 148 节点/145 项组件证据、Android 118 应用场景中 85 partial/33 not_verified 未完整合并后续补验及新原型。不能把旧状态机械判为当前剩余数，也不能把单元/隔离组件当真实链路成功。保留反馈 ZIP、Room 2→3、旧蓝牙基本互通及单机 USB 等有效历史证据，分别标明版本和边界。

重点缺口：最新 Windows 全量安全后台回归、真实接收弹窗决策队列/到期取消、连接偏好完整接线；Android 新设置与缓存/通知/SAF 的系统行为；双向四链路文件边界、双侧暂停/断线续传、真实身份变化关联、多手机隔离；系统托盘通知、多 DPI、后台/电源、兼容和耐久；最新安装/升级/卸载与有效签名更新的成功闭环。Windows 硬件 USB 仅证实主机到单手机 16/256 MiB 及手动开关重连，不扩大为反向、多机、物理拔插或自动恢复。历史隔离安装矩阵因安全软件告警停止，保留其已通过子项，不启动重试。

完整的 W01–W08、A01–A08、C01–C10 共 26 组验收清单、已有边界、优先级、环境和结案标准写入 `docs/IMPLEMENTATION_STATUS.md` 文首。它们是场景组，不是精确独立用例数。无手机可先补测试隔离入口与策略/文件系统/原生后台用例；一手机先闭合核心收发/安全/生命周期，随后多手机、兼容/耐久和独立安装环境。Android 本地 APK 更新按当前产品边界验收，旧在线更新原型不当作已实现功能。未更改长期路线或执行新任务；本次审计完成，列出的测试仍待实施。

## 2026-09-10：双端设置功能对齐与精简

用户在盘点后授权开始实施，当前不提供真机。本轮范围为双端设置原型、页面、配置持久化及其实际调用链，接收确认/同名处理、连接偏好、通知和缓存边界，以及对应后台验收。未使用模拟器、Computer Use、ADB 或用户手机；未运行安装器、Fixture.exe 或修改系统启动项，未生成 Windows 安装程序。

已统一的功能：

| 类别 | 当前行为 |
| --- | --- |
| 外观与通知 | 双端提供简体中文/繁体中文/英文，保留主题选择；消息、连接状态、传输完成通知分别开关。Windows 补齐事件提醒及繁体资源，Android 补消息通知；前台消息仍以会话展示，开关不清空未读计数。Android 必需的后台服务通知保持独立。 |
| 接收确认 | “接收前询问”与自动接收互为反向表达。Windows 原先关闭自动下载即拒收，现在可确认接收；等待期间加密聊天继续处理，拒绝、远端取消、断线及超时均取消请求，队列共用请求期限。预览附件不重复打扰。 |
| 同名文件 | 默认自动重命名，可选每次询问或覆盖；Windows 从直接覆盖改为显式策略，并处理并发落盘重名。未获覆盖授权时不会替换原件；确认后才出现的冲突安全失败或按重命名策略保留两份。 |
| 接收上限 | 默认 500 MiB，可开关并编辑数值。超限请求最多等待 30 秒，调整上限后仍遵守接收确认和原期限；Android 补禁用入口，编辑数值不会偷偷开启已禁用的上限。 |
| 连接与发现 | 启动扫描、允许附近设备发现、自动连接已信任设备、断线重连分别明确语义；Windows 修复服务启动隐式扫描，并抑制人工断开后的自动重连。最大同时连接数统一 1–8，默认 4；USB 开关及按会话显示高速能力保持既有设计。 |
| 历史与诊断 | 两类记录保存、缩略图显示及脱敏诊断均保留独立开关；Android 补缺失入口，保留期限统一 7 天/30 天/90 天/1 年/永久。 |
| 缓存 | 两端清理可重新生成的显示缩略图，明确排除接收文件、原始图片及已收到的预览附件；缓存有数量/空间上限，清理后按需生成，成功与失败均可见。 |
| 信任与身份 | 全部撤销信任集中于信任管理；移除 Android 隐私页的重复入口。身份指纹及重置收进详情入口。纠正此前盘点：Windows 当前代码已经有批量撤销，并非本轮新增；离线历史仍不等于有效信任。 |

精简了 Windows 无消费者的 `TransferPanelExpanded` 字段/读写/无调用方法，以及 Android `confirmNewConnections` 和首次安全码之前的额外确认。首次安全码核对、身份变化确认及撤销后的重新授权仍然强制执行，未弱化安全流程。旧数据库中的历史键可继续存在，但不再影响新逻辑。

保留平台特性：Windows 的关闭到托盘/退出、窗口记忆和更新分发；Android 的系统蓝牙入口、公共目录自动保存（全部/仅图片/仅其他附件/手动）、100 MiB 及以上新接收请求的充电限制、离开应用后保持连接和本地 APK 更新。Android 原先容易误解的“后台继续传输”已准确命名，公共目录自动保存不等于接收授权。Windows 维持保存/取消草稿，Android 维持即时保存并新增恢复默认确认；恢复默认只影响普通偏好，不重置设备身份、信任或历史。

Figma 已同步四组主设置页、相关选择状态，扩展 Android 最大连接数选择组件为 1–8，并补 Windows 接收/同名确认状态；沿用现有组件及语义颜色。映射见 `design/figma/settings-alignment.json`，文件 `Tna537kDQdpsVwDX0lXlCs`。Windows 接收界面复用现有确认窗口组合及官方 WPF-UI 控件，非新增系统 MessageBox 样式。

验证证据统一位于 `.acceptance/settings-alignment-20260910`：

- Windows Release 应用与验证宿主构建零错误/零警告；独立安全验收入口 `--settings-alignment-only` 通过 196 项（传输 17、设置/持久化/缓存 47、真实加密内存会话接收 15、安全握手 105、通知 12），不会执行安装器夹具。覆盖实际 30 秒到期、提高限制后仍确认、拒绝、取消、断线、同名保留原件、重连偏好及缓存不伤用户文件。
- 原生后台 UI Automation 通过 7,047 项/86 张新截图：浅深主题、三语、1000×600/1260×720、设置控件保存/取消、信任投影和接收弹窗。窗口在桌面范围外、透明且不激活，不使用物理输入。复查并修复英文同名选项裁切和深色接收图标；设置接线与 WPF-UI 契约通过。
- Android `testDebugUnitTest`、`lintDebug`、`assembleDebug` 通过；151 个单元测试零失败，包含旧配置迁移、新字段/1–8 数量持久化、安全码确认回归。lint 零错误/3 项既有警告（lint 检查器、kapt 建议及重复图标），本轮新增的无用资源已清除。

Not verified：Android 真机 UI/权限/系统通知与后台限制；真实蓝牙发现、重连、双端收发和 USB 多机行为；Windows 本轮系统托盘通知的桌面实际呈现与其他 DPI。后台测试不能替代这些验收。当前已完成源码、Figma 和可用的后台验证，真机恢复后按连接/接收/通知/存储顺序补验收；既有安装包不含本轮修改。

## 2026-09-10：安装向导固定紧凑尺寸与完成页图标

用户要求缩小安装向导，禁止用户调整尺寸和全屏，并补充要求完成页路径左侧图标缩小居中但保留背景大小，右侧复制图标缩小并对齐路径行。本轮范围为 InstallerWindow、现有安装 UI 验收宿主及文档；保留工作区此前修改，不修改安装引擎、注册身份、独立卸载器或运行时启动器。

安装向导由 1040×680 调整为固定 800×560 DIP，最小/最大尺寸相同，ResizeMode=CanMinimize，官方标题栏隐藏最大化；保留最小化与关闭，移除未接线的旧双击最大化处理。同步收紧页头、品牌图、正文、卡片、间距与页脚，普通按钮 32 DIP 高、垂直内边距为零，避免文字再次裁切。版本说明按实际内容宽度左对齐；长错误信息仍可在正文内滚动，底部操作保持可见。

完成页安装位置与默认接收文件夹图标由 24 缩为 18 DIP，36×36 DIP 背景块不变并居中；复制图标由 18 缩为 14 DIP、点击区域 24×24 DIP。路径与复制按钮改为同一行布局，按行中心对齐，长路径保留省略和完整提示；不更改复制行为。

验证：Release 构建零警告/错误；最终原生后台 UIA 99 项检查、15 张截图通过，核对系统无缩放边框/最大化能力、保留最小化、固定尺寸、常规页面不溢出、长内容可滚动、按钮文字与图标对齐、完成按钮退出。最终窗口使用不激活样式，透明且位于虚拟屏幕外，未激活或占据前台，安装/运行时安装/卸载请求均为零。早期试验发现调用系统 UIA Resize 即使失败也会激活目标窗口，已移除该动作；最终只读核对原生 UIA 能力及固定尺寸边界，不再模拟系统缩放操作。既有离屏安装/运行时/卸载/确认回归 96 项、33 张截图通过，全部 HWND=0；安装器契约与官方 WPF-UI 契约通过。已人工查看欢迎、路径、完成、失败及运行时页面的后台截图。证据 `.acceptance/installer-compact-20260910`。

未使用 Computer Use、物理输入、模拟器或 Fixture.exe，未执行安装引擎、真实安装/卸载、系统启动项或手机操作。物理拖拽/双击、其他 DPI 和已打包应用验收为 Not verified。未打包、提交或推送；现有 review-20260910-latest 安装包不含本轮修改。

## 2026-09-10：按钮文字裁切、设置扫描入口与边距修复

用户指出缩小按钮后文字显示不全、要求删除连接设置的附近设备扫描，询问信任列表为何为空，并补充设置四周边距仍过大。本轮范围为 Windows 共用按钮/设置样式、设置页面与扫描专用代码、空态中英文文案及定向后台验证；不修改 Android、安装器或安全信任规则。

按钮根因是高度缩为 32 DIP 后仍保留上下各 7 DIP 的旧内边距，扣除边框仅余约 16 DIP，低于 Noto Sans SC 13 DIP 文字约 18.8 DIP 的行高。已同步调整共用按钮、设置按钮、发送/工具栏按钮和设置单行输入框的垂直内边距与居中。补充横向检查时发现英文返回按钮固定宽度和设置导航项外边距导致截断；返回按钮改为内容自适应、12 DIP 字号，侧栏标题 18 DIP，导航左右外边距收至 8 DIP。原有选中/悬停、文字链接无背景、输入框焦点和官方控件模板保持。

连接设置中的扫描卡片、专用点击处理和 SettingsScanFeedback 投影已移除，主界面扫描与通用页启动扫描偏好保留。设置内容区、阅读页与底栏统一以 SettingsPageInset=12 DIP 对齐；主设置滚动区使用官方 ScrollViewer，根据实际溢出将右侧间距从 12 增至滚动条所需的 14 DIP，避免原 DynamicScrollViewer 在收窄边距后产生 2 DIP 覆盖，无滚动条时不额外占位。

信任排查：只读检查本机 identity.json 的安全信任数量为 0，历史 SQLite 中 trust 数量为 0、两条 peer 均为 Unknown；没有发现身份恢复备份。当前证据不能确定此前信任何时/为何被清除，不能把历史会话当作仍有安全授权。已补充空态说明“离线列表保留历史会话，不代表仍受信任”；未改写或恢复用户信任数据。隔离回归确认已信任的离线设备正常列出，仅历史设备排除；移除信任后打开中的设置立即刷新，离线记录与消息保留，重载后不自动重新信任。

验证：新增 --background-settings-controls 原生后台专项，先在旧代码复现“恢复默认值”内容高度不足，最终 4603 项检查、50 张截图通过，覆盖中英文、深浅主题、1000×600/1260×720、文字横纵向内容区域、模板裁切、导航、边距、扫描和信任生命周期；截图等待官方导航动画稳定。设置/帮助/安全弹窗离屏 400 项、52 次渲染通过，几何失败 0；共用交互回归 1283 项、28 次渲染通过。Release 构建零警告/错误，官方 WPF-UI 契约通过。证据 .acceptance/windows-settings-controls-20260910，包括基线失败、final-native、final-settings、interactions、只读信任数量摘要与最终源码哈希。

未使用 Computer Use 或物理输入；原生验收窗口透明、位于虚拟桌面之外、不激活，前台焦点保持。未启动模拟器、安装器或 Fixture.exe，未操作手机，未打包/提交/推送。物理输入、其他 DPI 和已安装版本复验为 Not verified；review-20260910-latest 安装包不含本轮修改。

## 2026-09-10：最新 Windows 检查版安装程序

用户要求打包当前 Windows 客户端。本轮仅构建并离线核对安装产物，使用 scripts/build-windows-review.ps1 -CompileInstaller 输出到 artifacts/windows/review-20260910-latest，保留旧包和正式发布载荷锁，不修改版本或应用功能。新包包含全客户端紧凑化、关于子页面左右独立滚动及许可悬停高亮、离线设备配色/会话切换、图片预览关闭按钮和通用设备图标修复，以及此前 USB 会话与设备身份关联代码。

结果：0.2.17 / x64 / Review 独立安装身份；EXE 90,081,495 字节，SHA-256 `273F10643E2E7D87002E1DF28A9D2D020A35430C26C169FB09C7345D394439DC`。客户端 Publish、Launcher、Uninstaller、Setup UI、MSI 与 Burn 构建成功，零警告/错误；66 项隔离目录归属断言、安装器及版本静态契约通过。WiX 离线解包逐项核对 21 项载荷及 MSI/ZIP 的 22 个文件，客户端/安装 UI/卸载器与当前构建一致；MSI/Burn/卸载配置的安装身份、嵌入恢复清单、同版本升级表及先事务移除旧 MSI 后写入新文件的顺序一致。24 个紧凑化源码文件哈希与已通过 201 项原生后台检查、4686 项完整离屏检查的验收版本一致。本轮不重复 UI 全套测试。

产物包含 EXE、MSI、ZIP、SHA256SUMS.txt、build-verification.json；证据 .acceptance/package-20260910-latest。未签名且不包含 .NET 8 Desktop Runtime，供本地检查使用；实际安装/卸载为 Not verified。未运行安装包、Fixture.exe、Computer Use 或模拟器，未操作用户已安装程序、手机、系统启动项；未提交或推送。用户可安装本包复核界面，正式发布与安装全流程验收仍需另行完成。

## 2026-09-10：Windows 客户端紧凑化与关于子页面独立滚动

用户要求修复关于子页面整页滚动、开源许可选中项悬停反而失去高亮，并明确将紧凑化覆盖整个 Windows 客户端。范围为客户端 15 个视图/窗口 XAML、4 个共用主题字典、设置/帮助/文档布局代码、主窗口尺寸与文件表头同步及后台验证；保留 Android、安装器及其他既有改动。用户的新密度要求优先于旧原型中的大字号/间距，本轮未修改 Figma。

根因与有效实现：帮助、开源许可、隐私政策和用户协议原先位于整页 SettingsScroll 内，并把双栏最小高度固定为 752–826 DIP；许可正文再套固定 449 DIP 的第三层滚动。现将阅读工作区移为受主窗口剩余高度约束的 Grid，标题固定，导航与右侧正文分别使用官方 ScrollViewer；许可搜索固定在左列表上方，取消固定大高度和正文内的嵌套滚动。仅在溢出时为滚动条让出 14 DIP，搜索结果减少后立即收回。只读关于/文档页的空白底栏移除，普通设置仍保留固定保存栏。开源许可和文档目录使用独立导航按钮样式，不再继承将 MouseOverBackground 强制透明的 LinkButtonStyle；非选中项悬停高亮，选中项悬停保持高亮，真正的文字链接仍无悬停底色。

密度：新增共用 ClientFont/ClientControlHeight 资源，正文主要采用 13 DIP、主要标题 18–22 DIP，常规按钮/输入框 32 DIP；减小卡片内边距、页边距与段落间距。设备卡片最小 74→60 DIP，传输中卡片 86→72，文件行 62→46，主会话 Header 96→72，输入区改为 104/120，设置侧栏 208/224；预览操作按钮保持方形并缩至 32，工具栏 72→56，关于品牌卡收紧。长许可名称/类型单行省略并保留完整提示。图标原始资源、位图像素比例、圆形进度、最小窗口尺寸和用户尺寸持久化保持。文件表头同步到新行内边距；图标按钮保留足够图形空间，搜索图标分隔线和滚动条通道不随装饰间距缩减。

验收：Release 构建零警告/错误，官方 WPF-UI 契约通过。原生后台 201 项/16 张截图通过，覆盖深浅主题、中英文、1000×600 与 1260×720、UI Automation 按钮/搜索/滚动、左右滚动互不影响、搜索释放滚动条及实际模板悬停高亮；悬停状态通过依赖属性模拟，未移动物理鼠标。另有设置/帮助/安全确认离屏 400 项/52 张、文件不可用弹窗 128 项/10 张通过；离线会话 22 项及四个原生预览窗口关闭/命中检测复验通过。最终完整离屏套件 4686 项检查、224 次渲染通过，几何失败 0。证据 `.acceptance/windows-density-20260910`，原生报告位于 final-native、整合报告位于 final-layout。原三分钟整合限时调整为十分钟，并增加可单独运行的设置/工作区分组，避免复验时重复无关场景。

未使用 Computer Use、物理鼠标/键盘、手机、模拟器、安装器或 Fixture.exe；透明且位于虚拟桌面外的原生宿主未抢前台焦点，全部数据为隔离验收数据。物理输入、多显示器 DPI 和已安装程序复验为 Not verified。未打包、提交或推送，现有 review-20260909-latest 不含本轮及前两轮 Windows 修复。

## 2026-09-09：Windows 离线设备配色与会话切换修复

用户指出离线卡片配色偏离原型，点击同名离线设备不能切换会话。本次只修改 Windows 卡片、会话选择/历史加载及定向后台验收，保留其他未提交改动。读取 Figma 离线组件 9:63 与离线历史选中实例 623:16522：离线图标采用灰色、图标底色 #F8FAFD、状态点 #AEB7C6；浅色选中背景 #ECF4FF、边框 #0771FE / 1.5 DIP。复用原始平台 PNG 的透明度作为灰色画笔遮罩，保留图标原始路径；深色使用对应主题画笔。附近可连接的离线设备也显示灰点，已有未读徽标/状态点互斥规则保持。未修改 Figma。

后台复现确认 ConversationSummary 的 record 自动哈希包含可变字段及 PropertyChanged 订阅，WPF 绑定订阅即可使哈希从 1671120599 变成 340403104，破坏 ListBox 的选择索引。现在仅以不可变 PeerId 计算哈希，保留 record 的其他语义；最终同一检查订阅前后均为 1299251835。两个分组通过实际 ActiveConnectedConversation / ActiveOfflineConversation 对象单向绑定 SelectedItem，非当前分组明确为空，避免其他入口切换后列表仍选中旧设备。点击当前卡片可从文件页返回会话，Enter/Space 同样可用；绑定刷新不重新打开会话或重置文件页。离线历史加载增加单调递增选择序号，只有最新请求可以写入消息/传输，在线选择也使旧离线请求失效，覆盖 A→B→A 的异步返回顺序。不会按相同名称或蓝牙地址自动合并历史身份。

验收：最终 Release 构建零警告/错误；官方 WPF-UI 契约通过。新增显式 --background-conversations 入口，独立临时数据库中准备同名、同蓝牙线索但不同 PeerId/历史的设备；透明、不激活、位于整个虚拟桌面之外的原生窗口完成 22 项检查和深浅主题 2 张截图，包括真实 UI Automation 选择、选中项同步、跨分组切换、当前会话返回、快速切换历史隔离与前台句柄不变。最终共用交互回归 1315 项、28 次渲染通过。完整 --offscreen-layout 套件触发既有三分钟总超时，未计为通过；物理鼠标/键盘、其他 DPI、已安装版本复验为 Not verified。证据 `.acceptance/windows-offline-conversations-20260909`，含修复前复现、哈希诊断、final 原生报告/截图、interactions 回归及构建日志。

未使用 Computer Use、手机、模拟器、安装器或 Fixture.exe，未修改真实用户数据；未重新打包，review-20260909-latest 不含本项及上一项预览关闭/图标修复。未提交或推送。

## 2026-09-09：Windows 图片预览关闭按钮与通用设备图标修复

用户明确两项均为 Windows 问题。本次仅调整 ImagePreviewWindow 的关闭入口、共用 generic.png 及来源记录、对应后台验收；Android 和其他既有改动保留。关闭按钮原先作为 TitleBar 外部的覆盖元素，WPF-UI 4.3.0 原生命中检测将其识别为 HTCAPTION，附加 IsHitTestVisibleInChrome 不能覆盖该控件的判定。修复前后台原生窗口实测按钮三个位置均返回 2。现在使用官方 TitleBar.TrailingContent 承载按钮，仍为 32×32、右边距 20、垂直居中；按钮和 Esc 明确指向 PreviewRoot 的既有关闭命令，不重写控件模板。

Windows Figma 148:103 的原始 SVG 同样含手机屏幕 #F6F8FC 不透明填充。保存原始源文件 windows-generic-device.svg，按原始四条路径/线宽生成 3 倍 PNG，仅省略手机屏幕填充；原资源 URI 和所有引用保留。已更新 windows-home-assets.json 的转换说明及哈希，未更改 Figma 文件。像素核验确认手机屏幕 alpha 从 255 变为 0，显示器屏幕仍透明，手机轮廓/底部圆点色值与不透明度保持一致。

验收：Release 构建零警告/错误；官方 WPF-UI 契约通过；交互/预览离屏回归 1315 checks、28 次渲染、几何失败 0，关闭检查改用 UI Automation Invoke。新增显式 --background-native-preview 入口，四个透明、不激活、位于整个虚拟桌面之外的真实预览窗口，在本机 150% DPI 下验证浅色 1000×700 / 深色 720×480 的三点 HTCLIENT 命中、标题空白 HTCAPTION、UI Automation 实际关闭和 Esc 绑定关闭；前台句柄始终未变化。离屏环境主动刷新 CommandManager 查询，避免无桌面焦点时将尚未重算的按钮状态当作验收结果。证据 .acceptance/windows-preview-close-icon-20260909，含修复前失败记录、最终原生报告、截图和图标像素/来源哈希。

未使用 Computer Use、物理鼠标/键盘输入、手机、模拟器、安装器或 Fixture.exe；其他显示器 DPI 与物理输入为 Not verified。未重新打包，review-20260909-latest 检查包仍是本次修复前版本。未提交或推送。

## 2026-09-09：最新 Windows 检查版安装程序

用户要求打包当前最新 Windows 安装程序。沿用既有 build-windows-review.ps1，0.2.17 / x64 / Review 独立安装标识，输出 artifacts/windows/review-20260909-latest。包含当前工作区的 Windows 界面、USB 多设备/关闭重连、指纹变化候选与确认关联等源码；保留历史安装包和正式发布载荷锁，未改版本或业务源码。Android 图标修正属于 Android 资源，不计入本 Windows 包。

构建与后台校验完成：客户端 Publish、启动器、卸载器、安装 UI、MSI、Burn 均通过，组件构建零警告/错误；目录归属 66 assertions、版本及安装器静态契约通过。用 WiX 离线解包核对 21 个载荷和 22 个 MSI/ZIP 文件、客户端/安装 UI 与当前构建一致、嵌入式恢复清单、MSI/Burn/卸载配置身份一致；检查同版本升级表和事务内先卸旧 MSI 再写新版的执行顺序。没有运行安装器、卸载器、Fixture.exe、启动项测试、Computer Use 或模拟器，也未操作手机；实际安装/卸载和本包原生桌面复验为 Not verified。

交付：BlueLink-Review-DO-NOT-DISTRIBUTE.exe，90,087,451 字节，SHA-256 `B95EA08F9724AFE19C7AE8EC79E0413625EADEDA1BDA052C7DCB6D00DA672A2B`。该包为未签名检查版，不捆绑 .NET 8 Windows Desktop Runtime；同目录保存 MSI、ZIP、SHA256SUMS.txt 和 build-verification.json。解包证据 .acceptance/package-20260909-latest。未提交、推送或公开发布。

## 2026-09-09：Android 通用设备图标透明屏幕修正

用户指出“电脑＋手机”图标的手机部分出现实心。范围仅限 Android 共用 figma_generic 资源与来源记录，保留其他未提交改动。读取 Figma 417:24 原始 SVG 确认手机路径包含不透明 #F6F8FC 填充；旧 PNG 在 tint=DeviceColors.Secondary 等位置将屏幕连同轮廓一起染色，未着色的深色页面也会出现浅色实心。

现以同名 VectorDrawable 替换 PNG，机械保留四条原始路径、尺寸、线宽和手机底部圆点，仅将手机屏幕填充改为透明；现有设备空态、未知设备、可信列表和 USB 提示等引用自动使用修复后的共用资源。原始 SVG 存入 design/figma/android-generic-device.svg，来源及转换哈希更新到 android-device-assets.json；未改动 Figma 文件或其他图标。

验收：Debug 资源构建通过（31 秒），保留数据覆盖安装到已授权 REDMI K80 Pro。已信任设备空态的浅深主题真机截图均确认仅轮廓着色，手机屏幕透明且底部圆点保留；设备列表未额外着色的同资源浅深主题也通过，共四张真机截图，退出验收后返回正式蓝联主界面；已安装 APK 哈希与最终构建一致。未使用模拟器、Computer Use、真实信任/设置变更或发布打包。本次为资源修正，没有新增镜像实现的单元测试。证据 .acceptance/generic-device-icon-20260909，源码路径一致性及 APK SHA-256 记录于 source-checks.json。

## 2026-09-09：指纹变化后的设备候选识别与会话关联

用户确认按“连接线索匹配原设备候选 → 重新核对安全码 → 明确关联原会话”的流程优化双端。范围限定 Windows/Android 身份与信任、蓝牙/USB 线索采集、会话/本地数据库、原有安全确认弹窗及相应回归；保留其他重构改动。未使用模拟器、Computer Use、安装器或系统启动项测试，未打发布包、提交或推送。

有效实现：
- 新增按 Peer 保存多条连接线索的 peer_hint 表（Android Room 2→3、Windows 3→4），只在身份验证成功后记住线索。接受规范化的对端蓝牙 MAC、Windows 读取的 USB 描述符序列号、Android 收到的 BlueLink USB 主机标识；名称、型号和 USB 端口实例号不作为归并依据。Windows AOA 主机标识改为单独持久的随机 ID，密钥重置/重载时保留；它属于应用标识，不宣称是主板硬件序列号。
- 仅对新身份且唯一匹配的候选显示“可能是原设备，身份已变化”；原有安全码弹窗展示旧/新指纹和“核对一致并关联”，支持双端原有主题与语言。原身份仍在连接、线索歧义、确认取消/超时、信任期间撤销等情况不会自动关联。已知身份照常复连；不会仅凭同名批量清理已有重复记录。
- 仅在新鲜确认及认证后的加密协议问候通过后，一次性持久化新信任、撤销旧信任和关联日志；旧身份标记 retired，无法静默恢复信任。随后以数据库事务迁移会话、消息、附件关系、传输、会话记录及连接线索，保留显示名、草稿、未读数、原始时间、文件 URI/路径和哈希。旧身份的待发消息/未完成传输转为失败，避免迁移后自动发送；原文件不移动或删除。启动时可幂等重放已确认日志；清除信任不会恢复已退休设备卡片。
- 真机隔离验收发现并修复 Android 的启动同步顺序：若信任先持久化而 peer 行尚未写入，先创建 peer 再同步 trust，避免外键错误；取消尚未验证的新身份时不写断开记录，避免引用不存在的 peer。隔离验收使用独立 SharedPreferences、内存 Room 和真实 Android 加密引擎，不改动正式会话或真实身份。Windows/Android 同时阻止旧身份的迟到回调恢复已迁移的待发/传输状态。

验收完成：Windows 最终 Release 构建 0 warning/0 error，安全握手及身份关联 105 checks、设置 39 checks、USB 63 checks 通过；安全弹窗后台 79 checks/8 图，包含 UI Automation 确认/取消、短窗口滚动、无原生窗口。Android 最终 Debug 构建成功，148 项单元测试通过，lint 0 错误/3 项既有警告。最终 APK 在真实 REDMI K80 Pro 上完成浅色英文取消、深色中文确认：核对新鲜安全码及加密握手，唯一设备卡片、显示名、历史/文件关系/草稿/未读保留，待发和未完成传输停止，日志重启重放及迟到回调保护通过；取消保留旧信任与记录。中途一次人工验收等待超时未建立信任，重新启动隔离场景后通过，超时证据保留。

收尾：已保留数据安装最终 APK（SHA-256 `a0c0b002095e34cf7281ba213531c646aba4a0cb38f169d4bdbcb430c0c52cda`），退出隔离验收返回蓝联主界面。正式数据库从 Room 2 升至 3；3 peer / 3 conversation / 7 message / 6 attachment / 6 transfer / 3 trust 数量及标识保持，conversation/message/attachment/transfer/app_setting 全行逐项一致，身份公钥哈希不变，隔离偏好已清理，手机已安装 APK 哈希与最终构建一致。证据目录 `.acceptance/peer-reassociation-20260909`，重点 `phone-data-checks.json`、两条 final/cancel result、构建/回归日志及 `windows-layout`。

能力边界：线索只用于候选判断，不能代替加密身份。历史记录未保存共同线索时不推断物理归属；本次不删除此前测试产生的同名设备。Windows 原生桌面、真实 Bluetooth/AOA 通道上重置物理设备身份的完整矩阵仍为 **Not verified**；将以真机隔离加密握手、数据库与界面结果分别记录，不能替代物理通道端到端证明。

## 2026-09-09：双端 USB 会话级标识与多设备管理

用户先确认双端 Figma 更新，随后授权“开始调整应用代码”。本次范围为 Windows/Android USB 设置、按 Peer 投影就绪状态、设备卡片与消息/文件 Header、会话提示、USB 主机管理和对应验证；保留其他未提交重构。Windows 仅恢复本项定向修改，其余既有暂停约定不变。未运行模拟器、Computer Use 或安装器/启动项测试，未打发布包、提交或推送。

设计基准：[Windows 1303:15593](https://www.figma.com/design/Tna537kDQdpsVwDX0lXlCs?node-id=1303-15593) / [Android 1305:5172](https://www.figma.com/design/Tna537kDQdpsVwDX0lXlCs?node-id=1305-5172)，状态规则 1307:16208 / 1312:5572。共用图标 1290:291 原始 SVG 已导出，转为 Android VectorDrawable 与 WPF DrawingImage，保留原始路径与主题颜色；来源记录 design/figma/usb-session-assets.json。旧详情 1034:17147 / 1034:22212 及状态板 1042:17178 / 1042:22491 已随旧 Section 隐藏归档，原 Figma 节点保留。设计证据 .acceptance/figma-usb-session-20260909。

有效实现：设置仅保留持久化“USB 高速传输”开关与静态说明。按本地开关、当前 Peer 和已认证 Connected USB 会话共同判断闪电，卡片 16、Windows Header 20、Android Header 16，名称后 6 间距、垂直居中；长名省略，保留未读徽标/状态点互斥规则。全局文件视图没有会话闪电。授权、核验、断线提示只投影到能确认身份归属的会话，无法识别的物理设备不强行关联现有聊天；详细指引移至帮助，说明无需 ADB/USB 调试。首次安全码核对保持。关闭开关立即阻止新发送选择 USB，即使底层连接仍在异步关闭；已有文件控制仍路由到其原通道。

根因与修正：Windows 旧 UsbHost 只有单一活动连接且等待其关闭才处理下一台设备；现按设备维护独立任务、连接、状态与取消源，一台权限失败或拔出不阻塞其他设备。失败设备只在重新插拔或开关重启后再试，避免持续授权循环。移除全局状态默认借用任意已连接 Peer 的旧逻辑。Android 更换 USB 配件时清除旧 Peer 身份，防止新授权状态落到旧会话。主题加载器为原始矢量单独应用品牌画笔，不再错误寻找 usb-ready.png；Ready 与未知物理速率分开。

替代清单：已删除 Windows UsbSettingsPage/UsbStatusCard 及入口；Android SETTINGS_USB、独立详情组件和旧详情文案已移除；旧全局状态演示改为开关/会话隔离场景；后台测试按新原型更新。实施前复制了 332 个源码文本文件，仅用于本轮差异核对，位于 .acceptance/usb-session-20260909/baseline。

验证（本项完成）：Windows 编译零警告/零错误，USB/安全路由 52 项通过，离屏 159 项/14 张图片通过，未创建桌面 HWND；并发回归修正完成态等待后连续三次通过。Android 最终 Debug 构建成功（4m22s），139 项单元测试通过，lint 0 错误/3 项既有警告，保留数据覆盖安装。恢复后核对源码及手机安装 APK 均与已验证构建一致（APK SHA-256 8abb66e3c8aae044f7e6bfdde5a8ea7046966d18c32a98b4d87a379cc84b6e1c）。真机隔离组件验收累计 55 项/28 张截图通过：浅深主题、长名与徽标、会话及文件 Header、关闭/未核验无闪电、同设备断线/核验提示、其他设备无提示、简中/英文/繁中设置静态文案与实际 checked 状态。已目视核对代表截图并退出隔离验收页，没有启用实际 USB 模式切换。最终证据 .acceptance/usb-session-20260909/android-physical/checks.json；暂停记录仅保留历史过程，已完成恢复收尾。

真实 USB 跟进（同日，用户开启无线 ADB 后）：发现并连接当前无线地址 10.33.252.13:43125，以 ro.serialno 验证仍为 ccc032bc；旧 mDNS 的 46457 端口拒绝连接，未复用过期地址。正式蓝联内临时将 USB 开关从关闭改为开启。Windows 实机暴露两处枚举问题：只读取复数 DeviceInterfaceGUIDs 而漏掉手机固件的单数 DeviceInterfaceGUID；在未装载接口的设备信息集中调用接口枚举。已合并两种注册形式，并改用 CM_Get_Device_Interface_List 按精确设备实例查询 present 接口，处理缓冲变化重试；同时排除 USB Composite 父节点，只接纳 AOA 数据子接口。旧枚举 P/Invoke 已删除，未放宽 ADB/非 Android 接口过滤。增加 --usb-inventory 只读命令和必须给出精确实例 ID 的 --usb-start-accessory 探测入口，30 秒控制请求期限，不修改驱动。

实机连接与驱动（同日继续）：新枚举得到正确 WinUSB 路径。沙箱内外打开均报 Win32 5；当前 ADB 后端不支持 detach，限制设备参数也未释放占用。短暂停止本次 ADB 服务后成功完成 AOA 51/52/53 控制协商，立即恢复服务及无线连接，证实占用来自有线 ADB。手机从 PID_4E11 重枚举为 PID_2D01，数据 MI_00（Android Accessory Interface）起初没有关联驱动，PnP Code 28；ADB MI_01 正常。用户随后明确“确认允许，继续吧”，授权指定数据接口绑定 Windows 自带 WinUSB。通过精确硬件 ID、父节点序列号 ccc032bc、INF 与 sys 的 SHA-256 及有效 Microsoft 签名核对后，使用 SetupAPI/DiInstallDevice 绑定 C:\Windows\INF\winusb.inf 的 WINUSB 节。仅在目标接口的 Device Parameters 注册专用 GUID；没有修改复合父节点、ADB MI_01、其他设备、签名策略、证书或启动项。首次全局驱动列表调用返回 87，临时 GUID 已回滚；改为目标设备专属列表后成功，PnP Started/Problem 0，无需重启。退出配件模式后再次进入，绑定仍有效。脚本和前后记录位于 .acceptance/usb-hardware-20260909/bind-winusb.py、binding-backup.json、binding-result.json，仅作为本机授权操作证据，未集成进发布安装器。

真实传输（Windows→Android）：新增显式 --usb-hardware/--hardware-output 后台验证入口，默认测试不触发硬件。仅打开参数指定的 AOA 数据接口，使用生产 WindowsUsbBackend、UsbHost、SessionSupervisor、WinUsbConnection、BTX 安全握手和文件协议；Windows 身份/缓存/日志隔离在 .acceptance，Android 为已安装的正式 MainActivity/runtime。首次核对因验收操作超时失效，未写信任；随后原接口重试出现 5 秒写入失败，记录后重建完整配件生命周期。第三次真实安全码两端一致并分别确认，建立 BTX/1.1 加密 USB 会话。16 MiB 随机文件耗时 1.7765986 秒，平均 9.4434477 MB/s（9.0059735 MiB/s）；256 MiB 随机文件耗时 21.8493331 秒，平均 12.2857505 MB/s（11.7166048 MiB/s）。时间从调用 SendFileAsync 到收到手机校验/发布完成 ACK，包含本地快照与哈希准备，不包含生成测试源文件或最初安全配对。数据只经 USB 发送，ADB 仅用于控制和读取接收端哈希。两文件在手机私有 received/<transferId>/<name> 中的大小与 SHA-256 均逐项读回一致。对应 run3/results.json、phone-file-checks.json、safety-match.json；正式设备卡片和会话 Header 的 USB 闪电以及两条已接收记录都有真机截图。

关闭与重连优化（用户继续授权“那就优化一下吧”）：此前只回复 PING，没有主动失联检测；Windows 先关物理宿主，未发送结束通知；Android 直接关闭共享描述符可能留下阻塞读，异常退出后 AOA 发送队列还有旧加密记录。现已增加仅针对认证 USB 会话的单调时钟心跳（3 秒检查、12 秒无入站记录判失联），有界未完成心跳队列；正常关闭先经串行加密写队列发送 GOAWAY（最多等待 1 秒），再停止会话及宿主。Windows 将 USB 认证读帧/心跳处理与应用处理分开，8 帧有界背压，防止文件哈希/落盘阻塞心跳。Android 使用可中断的 FileChannel，先结束读写再释放配件描述符，关闭幂等且拒绝新入队。Windows 新握手前最多 16 次、每次 100 ms 排空上一会话遗留的 AOA 入站数据；仅在本机发出 HELLO 前执行，不在活动 BTX 流中跳过数据，不放宽身份/重放校验。正常退出与身份重置、关闭 USB 的停止顺序同步修正。

优化验证：最终 Windows Release 编译零警告/错误，63 项 USB/安全路由回归通过，包含静默掉线、没有原生 EOF 的 GOAWAY、超过心跳期限的慢应用处理、多设备与同身份回退。Android 142 项单元测试通过，包含同时中断阻塞读/写；lint 0 错误/3 项既有警告，保留数据覆盖安装 APK（SHA-256 7017fbe9205fb013b8b1cb972f246bdd96a470ead65ab149a254986f9738940f）。真机及后台宿主验证正常退出、两次强制结束、原 AOA MI_00 接口重连；正常关闭后首次轮询约 3.96 秒确认手机闪电消失，最终异常退出轮约 17.19 秒确认离线（均为包含 ADB 采样延迟的观察上界，不是精确网络超时）。重连由关闭/重新开启蓝联 USB 开关触发，未拔插、未重置 USB 模式。首次异常退出后的复连仍受旧数据干扰，保留 recovered 失败记录；补上有界清理后，crash-final 与 benchmark-final 均首次成功握手，沿用本轮实际核验身份。

最终重连后实传：16 MiB / 1.7800152 秒 / 9.4253 MB/s；256 MiB / 23.3199395 秒 / 11.5110 MB/s。均收到完成 ACK，手机私有接收文件大小和 SHA-256 读回完全一致。计时口径同上，属于关闭/重连可靠性优化后的单次复测，不宣称吞吐提升。完整证据 .acceptance/usb-liveness-20260909（benchmark-final/results.json、phone-file-checks.json、disconnect.json，crash-final/termination.json、disconnect.json、真机截图及构建/回归日志）。未使用模拟器、Computer Use、安装器或启动项测试，未打发布包、提交或推送。

当前限制：无需操作的自动重连、Android→Windows、两台以上真机并发、物理拔插中断/续传仍 **Not verified**；原接口重试已通过上述显式开关操作验证。当前结果代表生产传输组件驱动的后台 Windows→真机链路，Windows 正式桌面操作全流程及安装器驱动配置尚未验证。

收尾：本机 WinUSB 关联按用户授权保留；验收结束恢复蓝联 USB 原关闭值、原仅充电/ADB 模式并结束本次 BlueLink 窗口常亮。原有历史/信任/Download 全部保留；当前验收进程无法解密旧隔离 DPAPI 身份，按生产恢复策略生成新的隔离 Windows PC，已再次逐项核对两端实时安全码后确认，未修改用户原设备身份。保留这一本轮测试身份及两条测试文件记录。后续优先补反向、多机并发和物理拔插续传；驱动分发属于后续发布范围。

## 2026-09-07：恢复 Android 原型重构，改用 USB 真机验收（进行中）

2026-09-09 继续实施：消息回执/新消息提示（138–151）、附件菜单与圆形卡片（152–172）、双侧独立暂停与历史进度回读（173–181）、关于/文档三语（183–197）、设备菜单与状态/搜索（198–223）、文件页状态菜单（224–241）已完成组件范围真机验收。USB 专项前已安装 build-help-reading 成功（2m55s），最近完整单元测试 135 项全通过，lint 0 错误/3 项既有警告。消息页可见性与未读计数（242–248）及连接设置（249–261）专项已通过，帮助文章阅读区（262–279）三语组件复验已通过。简中、500 MiB、USB 关闭等原配置保持，Windows 暂停。

范围及约束：仅 Android 应用相关实现、必要协议接入、Figma 原始资源、真机验收工具。只用已授权 USB 真机，不运行模拟器或 Computer Use；操作限蓝联、蓝牙/所需权限和 USB 安装权限，不改系统锁屏/分辨率/字体，不操作其他手机应用，不卸载或清空蓝联，不删除真实聊天、信任或已接收文件。外部分享/文件选择器的操作暂未验收。构建离线、单工作线程。Gradle 沙箱 UDP 锁绑定失败已确认，单条提升权限的构建获准并成功，不停止其他 Java 进程。原调试签名通过 BLUELINK_DEBUG_KEYSTORE 只读指定，不复制密钥。手机常亮仅使用蓝联窗口 FLAG_KEEP_SCREEN_ON；普通 Intent/Activity 重建保持状态已验证，离开蓝联后不改变系统锁屏行为。

阶段一已实施并在真机部分核对：统一设备、会话、文件、搜索、图片预览、菜单和确认/详情组件；使用 Figma 原图及 Noto Sans SC 原字体静态字重，主题取色。文件状态分组、失败筛选包含取消、空态、折叠图标、徽标与状态点互斥、操作状态及离线目标会话选择已有专项回归。图片预览改为 Activity 内页面，修复 ConfigurationContext 截断 ActivityResultRegistryOwner 的崩溃及 Dialog 系统栏颜色；SaveableStateHolder 保留主页面可保存状态，完整真实会话草稿恢复尚未实测。验收目录 `.acceptance/android-physical/20260908-stage1` 的 18/20/23/24/25 为最终日期/详情/预览证据，19/21/22 为失败尝试，不能算通过。

阶段二已实施：设置分类/通用页、主题/三语/通知持久化；帮助反馈表单、验证/生成/成功/失败、三语帮助及法律文章、关于页和开源许可。反馈包仅写应用缓存，诊断默认关闭且只导出结构化事件，排除自由文本中的设备名/路径/消息。真实 ZIP 已回读确认 opt-out 为 2 个条目、opt-in 为 3 个条目，未触发外部分享。许可清单从各构建实际运行时依赖及父 POM 生成，当前 Debug 66 项依赖加字体共 67 项，不伪造清单。27–31/33–36 为帮助/反馈证据，42/43 已核对深色许可列表与正文。其他语言全场景仍未完成。

连接设置已接入设备名称、发现、最大连接数、可信设备列表、三个连接行为开关及持久化。37–41 已核对正式入口连接页、名称弹窗、连接数选择、真实可信列表及移除确认；确认框只走取消。名称/信任按钮字重偏粗及连接数缺少单位已修正并包含在 storage-2 安装版本，专门截图复验待进行。用户明确“首次安全码核对始终保留”，新设备确认开关只控制额外确认；TrustConfirmationGate 用请求 ID 防止过期按钮接受下一请求。新增自动连接策略区分启动连接/断线重连，手动断开抑制自动重连。应用名称仅进入应用设备信息/GATT，不修改系统蓝牙名称，附近列表跨设备名称一致性尚未验证。

本轮连接底层修复：初始化 PeerEntity 保留既有 transportAddress 等信息；移除信任会规范化 ID、使已有验证失效并关闭相应会话；SessionSupervisor 取消握手任务，避免残留确认框。PeerTrustRegistry 在握手前记录信任版本，只在加密 PROTOCOL_HELLO 验证完成后持久化；撤销后的旧握手、并发不同身份均不能重新建立信任。JVM 专项通过，但未用真实设备执行移除信任或身份变化试验。记录连接时依据实际信任密钥生成状态，不无条件写 TRUSTED。

文件与存储已接入：按原型新增接收前确认、同名重命名/询问/覆盖、图片/其他附件自动保存、真实本地用量、管理已接收文件入口、100 MiB 以上充电条件、后台继续传输。私有接收目标改为按传输 ID 分目录，防止同名文件互相覆盖；未自动保存的附件仍留在蓝联内部。覆盖采用先写独立新文件再改名，失败删除本次新文件后恢复原名，提交成功前保留旧内容；成功/写失败/改名失败/提交失败的 4 项隔离测试通过。ReceivePolicy 的大小边界和自动保存独立性 2 项测试通过。正式真机 45–47 显示真实用量（截图时 5.1 MB），同名处理面板及管理入口正常；未实际接收/覆盖用户文件，SAF 提供方和进程中断恢复尚未验证。开关关闭态滑块偏小、统计条缺少圆角已修正，48/49 真机浅色中文及深色英文复验通过。50 接收确认曾混用中文正文，修复 Dialog 的 LocalContext/LocalConfiguration 传递后，52 全英文及拒绝回调通过；51 深色繁中同名面板通过。

隐私与数据已接入原型三组设置：消息保存、保留期限、清除记录、身份重置、移除全部信任和诊断导出。正式真机 53–58 已逐张核对页面/四类确认框/保留期限面板，破坏性确认均取消；59b/60/61b 深色英文页面与弹窗已核对，62 仅在内存中选择 7 天并验证显示，不改变真实保留期限。59 原尝试实际仍停留正式中文 MainActivity，不计通过；显式新任务启动隔离 AcceptanceActivity 后修正。诊断摘要改为仅时间/等级/许可组件名，排除自由文本，独立导出及反馈包共用。保留期限与旧值回退、会话准入代次、全局信任失效、诊断文件读回新增 7 项测试。身份重置一次提交新密钥和全部信任移除，失败恢复旧偏好；关闭旧握手/传输并刷新发现身份及 Room 投影。未在用户手机执行身份重置、信任删除或清空记录，实际 Android Keystore/文件系统失败路径仍 Not verified。

安全连接已接入 374:3 / 1004:4538、4661 / 1006:4693、4817：使用 Figma 原始盾牌与指纹 PNG（来源及 SHA-256 在 design/figma/android-security-assets.json），三语、深浅主题和窄屏布局。首次安全码确认独立于额外确认开关；握手并行观察既有 BTX 加密 GOAWAY/TRUST_REJECTED，EOF 单独作为连接关闭。只有加密 PROTOCOL_HELLO 与本机确认均完成才写信任；90 秒期限关闭阻塞读取；已知传输地址的身份变化会展示新旧指纹并阻止连接。全局代次覆盖 Hello 之前的身份快照，取消/过期请求不能再确认。新增 4 项请求状态测试及 6 项真实加密流握手测试通过，覆盖双方确认、拒绝打断尚未回答的弹窗、超时关闭阻塞读取、传输地址身份变化、保留首次核对、等待时取消，以及握手后的下一条加密记录。63/64 浅色中文确认/等待，65 深色英文拒绝，66 深色繁中超时，67 深色英文身份变化已逐张核对；按钮使用 debug 隔离回调，不证明实际双设备蓝牙互通。首次 UI 参数与测试访问内部方法错误已修正并重新通过构建。移除单个信任持久化失败改为可见错误，移除全部复用整体事务。

文件完成与取消修正已通过：将文件校验/发布移出会话读取循环，同名确认不再阻塞其他消息与取消请求；保存时每个数据块检查取消，最终提交与取消互斥，失败回滚原文件。SAF 仅在实际选择覆盖时检查重命名能力，选择保留两份不再提前被拦截。错误详情保留，发布失败不再一律称为校验失败。新增 5 项取消/提交事务测试通过；68 安全按钮字重修正、69 深色英文同名面板、70 返回正式主页面已核对。真实 SAF、断电/进程终止恢复仍 Not verified。

USB 已实施范围：377:3 连接页开关与独立详情入口、1034:22212 详情页、1042:22491 状态对照；复用原型通用设备图标和三语资源。仅通过 UsbManager 枚举 BlueLink/BlueLink 配件并申请访问，默认关闭，不操作其他配件；抽取 Bluetooth/USB 共用 PeerConnection，通过相同 BTX 身份握手后才选择 USB，未知协商速率保持未知。16 KiB 完整包读取避免 USB 短读丢失尾部；USB 地址不覆盖蓝牙重连地址；现有任务控制优先原持有会话，回退只允许同一身份的已加密蓝牙通道。同一设备身份的内容缓存由蓝牙/USB 共用，原通道进度与消息不会因前台选路被隔离；离线原任务标记失败，排队消息按身份串行派发。新增 11 项回归覆盖 USB 完整包读取、同身份选路/回退、蓝牙关闭下 USB 在线、蓝牙地址保留、任务归属和共用内容缓存。71–84/91–93 为状态组件与说明区证据，85–90 为正式入口与持久化证据。真机 AOA 与文件连续性尚未验证；已请求当前无线调试备用地址，获得前不切换唯一 USB 调试链路。

三语与正式入口回归：补齐 215 个资源项（含复数），文件筛选/分组改为稳定资源 ID，日期按应用语言显示月份/星期/日期；主菜单、确认框、详情和文件回调整体移入语言主题，修复仅子页面翻译而主入口仍取系统中文的问题。筛选按钮使用原型常规字重，英文权限徽标改为短文案，权限缺失不再错误提示先打开蓝牙。94–109/114–121 为独立内存场景，已逐张核对，114/115 覆盖旧字重及徽标截断；113/122/112/123/124 分别为正式文件首页、会话菜单、清空确认取消、设备详情、恢复原语言。110/111 是语言作用域失败证据，不计通过。首次自动英文残留检查将 Logo 品牌名“蓝联”误报，明确排除该品牌名后设备页继续通过。build-content-i18n-3 被缩进 lint 拦截，已修复并在 -4 通过，未跳过检查。未操作分享目标或系统文件选择器，也未清除真实记录。

USB 入口补漏：已认证 USB 连接允许在蓝牙关闭/权限缺失时发送，消息按钮、文件选择入口和返回回调共用 ConnectionSendPolicy；未握手/断开仍不能发送。安全确认失败的重试按 USB 地址回到 UsbAccessoryController，不再错误启动蓝牙重试。新增 3 项域回归通过，真实 USB 收发仍 Not verified。文件首页历史问题已在下述批次修复。

文件首页历史修复（2026-09-08）：新增全设备 TransferHistoryIndex，独立于当前会话；Room 全部历史与实时回调合并，数据库确认后释放进度覆盖，清除/删除/保留期限不会从旧内存恢复记录。保留原始创建与更新时间，持久化拒绝倒序事件，全局文件预览/详情按该文件取所属设备信息；快速切换离线会话防止旧查询覆盖新选择。9 项新增回归，共 107 项测试通过，build-file-history-3 构建成功（2m28s）、lint 0 错误/4 既有警告；已保留数据安装。125 正式入口直接显示原有两条记录及 9 月 1 日日期，126/127 隔离多设备与历史日期、128 筛选空、129 全局无历史、130 会话无历史均已在真机截图核对。初次构建改变存储统计所用 Entity 流导致编译失败，改为新增映射流后 -2/-3 通过。未清除真实记录。用户决定保留默认 500 MiB 接收上限，并在 Figma 补充可调整入口。

接收上限（2026-09-08）：原型 377:58 增加单文件接收上限行，复用原色彩/排版/图标，新增组件 1266:77、1270:95 及正常/错误/深色场景 1272:5247、5328、5410；三语实现正整数 MiB 编辑，防止空值/零/小数/溢出，保留旧配置，保存才生效。对旧版关闭上限的配置明确提示保存后启用。提高上限后重新走接收规则，仍保留接收前确认，未绕过安全核对。新增 3 项域测试，共 110 项通过；原型组件/页面及真机 131–137 已逐张核对，包括键盘弹出时错误信息及按钮可达、深色英文/繁中。正式入口 500→512，重启确认持久化，再恢复 500；未删除真实数据。超限等待后实际双端继续接收仍 Not verified。首次 UIA 检查误把 Compose 按钮限定为 android.widget.Button，改为核验实际可点击祖先及 enabled=false 后通过，未跳过验证。

消息状态时序修复（2026-09-08，后台与独立真机存储验收完成）：发送完成与回执不再直接相互覆盖，统一合并使 READ/DELIVERED 优先于晚到的本地发送/失败回调；数据库在事务内合并并限制所属设备与发出消息。初始记录与状态写入、删除/清空通过同一队列串行注册，避免状态先于插入或旧写入晚于删除；附件消息在写入通道前登记，写入完成/失败再更新状态。重复入库不重复增加未读数，既有附件完成状态/路径不被旧消息重置；尚未认证的发送明确返回失败避免卡住。新增 4 项状态合并、2 项队列时序/异常释放回归，116 项测试全部通过。真机 debug 专用内存 Room 回读与三语截图入口 conversation-receipts 已执行：确认晚到回调不降级 READ/DELIVERED、错误设备不更新、重复消息未读仅一次、附件路径保留、删除后不被晚到写入复活；138–140 已逐张复核，不接触真实数据库，也不作为双端收发证明。

新消息与文本六态：按 567:4319 / 1010:4906 调整提示尺寸与字重，增加按消息 ID 跟踪的新消息计数；读历史时保留位置，旧分页与回执更新不增加数量，自己发送/回到最新后清除。四项策略回归和真机 141–151 的三语显示、3→6 累计及三种清除路径均通过；仅隔离消息输入，真实蓝牙收发仍未验证。

附件操作菜单（2026-09-08 至 09-09）：对照 372:257/332/167、382:3/93/168/243/313、377:201 统一消息附件和文件列表入口，按状态提供暂停/继续/取消、失败原因与完成后的文件操作。使用原始图标，消息删除只删除拥有该附件的本机消息；文件列表删除仍针对传输记录。加入“在文件夹中显示”的目录解析及内部存储/不支持提供方的可见提示，未执行外部文件管理器操作。保存副本改为后台 IO。152–167 核对九类菜单、暂停→继续、取消/删除确认取消及三语；168–170 复核窄屏卡片和深色英文/繁中；171–172 正式历史入口核对六个选项和取消删除，真实消息仍在。build-attachment-actions-final 成功（4m17s），120 项测试通过、lint 0 错误/3 项既有警告。未把隔离状态当作真实文件传输证据。

暂停同步与历史进度（2026-09-09）：旧接收端忽略对端 PAUSE/RESUME，单一暂停标志也允许一方继续覆盖另一方的暂停。新增独立两侧暂停控制，等待对端时只提供详情和取消；迟到数据块保留暂停状态，校验/完成后不接受迟到暂停。历史附件回读对应传输记录的状态与进度，避免重新进入后退回 0%。六项暂停控制回归通过；首轮异常对象身份比较受协程异常栈恢复影响，改为验证异常类型和内容后 126 项全部通过（build-transfer-pause-final，3m43s）。173–179 核对暂停菜单、两侧继续顺序和三语等待状态；180 真机独立内存 Room 断言实际回读 7/10 进度及失败状态且拒绝旧事件，181 核对完成卡片。build-transfer-card-final 成功（3m2s）、lint 0 错误/3 项既有警告；已保留数据安装。此处为生产控制器加隔离事件，不是实际双设备 PAUSE/RESUME 传输证明。

关于与文档页面（2026-09-09）：对照 377:161、680:4366、680:4433，关于页品牌卡收紧为原型 170 dp、版本取真实 0.2.17 (12)、版权信息固定底部；隐私政策与用户协议保持完整正文，圆角阅读容器固定，只有正文滚动。调整标题、摘要、编号、章节间距和分隔线，不改条款内容。build-about-legal 成功（3m13s），保留数据覆盖安装；183–197 的简中浅色、英文/繁中深色页面及最后段落可达性通过，代表截图已核对。法律正文与在线更新/USB 能力的一致性仍待能力基线确定，不能据此宣称这些能力已完成。

设备菜单与设备页复验（2026-09-09）：对照 370:299、372:3、562:3888，补齐附近设备信息、传输任务入口与按状态暂停/继续。设备卡与菜单共用同设备最早活动任务，暂停后保留进度，菜单读取最新在线/任务状态，控制始终传入选定任务；附近连接保持原传输能力约束。连接图标由 372:85 原 SVG 精确转换为 VectorDrawable，原始字节和 SHA-256 已记录。四项任务归属/状态/附近连接能力回归通过，总计 130 项全通过。build-device-actions 成功（3m37s）、lint 0 错误/3 既有警告，已保留数据安装。198–213 核对八种菜单、附近信息、暂停/继续、任务入口、两类确认取消及深色英/繁中；确认框不执行真实删除或信任修改。初次脚本重复启动同一隔离场景未重置菜单，改为切换场景后只补验剩余确认和深色菜单。

设备扫描/连接/搜索对照：build-device-states 成功（2m40s），214–223 包括扫描中、连接中、失败、零连接、徽标、三组收起/恢复、输入搜索结果/空态和深色英/繁中。UIA 通过，代表截图已核对。输入框占位文字与外层描述同名导致脚本歧义；以实际唯一 content-desc 区域定位，未改变应用输入框或放宽前台守卫。所有扫描/失败由隔离事件驱动，不作为真实无线发现或重新连接的证据。

文件状态细节（2026-09-09，组件复验完成）：继续对照 380:215/315、564:3787 及失败菜单，补齐暂停/恢复百分比，按原型区分发送暂停黄色与接收暂停蓝色，限制长状态宽度避免挤没文件名；失败卡片使用红色边框，失败原因操作用原始警告图标及红色。新增独立文件页入口复用正式菜单，外部文件操作不会执行。build-file-actions-final 成功（2m31s），保留数据安装；224–241 的八种文件页状态菜单、进度徽标和深色英/繁中 UIA 通过，代表截图核对。280/281 在最终版本再次核对六个完整可见点击区域及删除确认取消，记录保留。仍未执行外部文件动作或真实双端传输。


会话未读可见性（2026-09-09）：发现旧版选中会话后，即使停留文件页、搜索或后台仍不增加该设备未读；选中传输也会直接清空未读。新增 ConversationReadTracker，区分路由选择与前台消息页可见性；切换/离开/预览时注销，Activity pause/resume 单独通知。消息写入与清除未读共用有序队列，在事件时捕获是否未读，避免延后读 UI 状态导致倒序。文件任务入口首帧直接展示文件页。五项新增回归通过，135 项全通过，build-message-visibility-2（3m27s）及 lint 0 错误/3 既有警告通过；首构建隔离消息未传 status 已修正。242–248 已通过真实页签/搜索/离开/后台覆盖与恢复回调加独立内存 Room，247 显示后台收到 1 条后恢复清零，248 显示文件页再收到消息后保留 1 条未读。原脚本搜索标签不完整、重新启动创建新验收 Activity 均已定位；改用蓝联 debug 内部覆盖页触发真实生命周期，保留原实例，不操作其他应用。专项 7 项计数断言均通过；关闭独立验收任务后回到桌面，前台守卫正确停止输入，随后显式启动蓝联继续下一项。

连接设置补漏（2026-09-09）：对照 571:4356、570:4278、567:3952/4034，可信设备空态原为普通蓝色提示，已改为 250 dp 虚线面板、原设备图标及居中文字，并调整说明区间距。build-trusted-empty 成功（2m57s），已保留数据安装；249–261 名称空值/33 字符禁用、有效名称保存、连接数四选项与选择、三设备列表过滤、移除全部确认取消及三语空态/菜单通过，代表截图已核对。长文本编辑首轮脚本的 MOVE_END 未在该输入法移动到末尾；改为当前 Android input 支持的 Ctrl+A 后替换，实际名称未改。隔离名称/连接数仅修改内存，真实信任不修改。


帮助阅读区（2026-09-09）：依据成功读取的 969:4443，统一共用文章排版与固定圆角阅读框，内部正文滚动；短文反馈操作靠近底部，长文向后排布且不覆盖正文。法律页复用同一标题/章节组件，布局参数保持前批约定。build-help-reading 成功（2m55s）、lint 0 错误/3 既有警告，已保留数据安装。262–279 三篇文章 × 三语的首屏/末段、反馈按钮可达和打开表单通过，代表截图已核对；未生成或分享反馈包。969:4394/4492 的 Figma 读取返回 HTTP 522，连接帮助单独重试仍失败，因此这两张只计共用组件/功能证据，不计本轮逐图对照通过。FAQ/法律中更新联网说明仍待用户选择能力基线后统一。


后续及限制：继续剩余原型逐图核对（含文本部分选择、反馈状态/分享、部分确认/搜索场景）、实际双端蓝牙/USB、更新及最终完整回归。当前关于只提供本地安装包，不宣称已在线检查最新版。更新原型的联网能力与 README 不联网基线冲突，已向用户询问采用在线更新还是继续仅本地包，回复前不添加互联网权限。USB 调试连接不代表 USB 数据通道已就绪；AOA 切换会影响唯一 USB 调试通道，实际切换前需备用调试连接。原型清单共 118 个应用场景 + 1 张 USB 对照板；partial 只说明组件证据，其余继续标记 Not verified，整体重构未完成。所有工作保留在未提交工作区，不提交或推送。

## 2026-09-07 — Windows 焦点框、链接按钮与按需滚动条（源码与后台验收完成）

- 按用户五项反馈统一 Windows 交互与布局：`ControlInteractionPolicy` 仅取消额外焦点装饰，并通过按钮元数据/光标查询覆盖普通、图标、标题栏及模板内按钮的手形光标；保留官方模板、键盘操作、选中状态。用户随后明确输入框焦点反馈可以保留，最终未修改输入框模板/焦点触发器；文件搜索焦点边框与消息输入框原有无内线约定继续保留。预览工具按钮显式焦点描边已移除。
- 新增共用 `LinkButtonStyle`，文件打开/继续、接收目录更改、打开设置、托盘文字操作及设置关于/文档链接不再添加悬停填充；复用官方按钮模板。文件表头外层背景铺满，内层列与实际列表视口同步；去掉文件列表固定14 DIP占位和聊天行固定24 DIP空白，共用滚动样式默认为0，仅滚动条可见时分配对应空间。设置左侧无内容页脚分隔线通过官方资源透明化移除。
- 后台验收：专项入口 `--offscreen-interactions` 覆盖明暗主题、滚动出现/消失/筛选为空、表头列对齐、链接实际模板背景、按钮光标、焦点装饰及输入框焦点保留。首次完整后台为4406 checks/235次渲染、构建0 warning/0 error，后台行为与官方控件契约通过。
- 用户补报深色标题栏三图标变黑：对比上一轮 `.acceptance/windows-background/20260907-preview-final/layout/file-table-long-content-dark.png` 确认旧离屏图已有此问题，非本次样式引入。WPF UI 4.3 `TitleBarButton.Loaded` 才将主题色复制到绘制色，离屏宿主未触发该生命周期而保留系统默认黑色。修正验收宿主，在离屏布局中配对触发加载/卸载并释放属性订阅，保留悬停状态；未改应用图标/颜色。修复后专项1315 checks/28次渲染，验证三按钮正常/悬停/移开及实时主题切换，已查看新截图。
- 最终 `.acceptance/windows-background/20260907-interactions-caption-final`：4845 checks/239次渲染（237张唯一PNG），几何失败0；Release构建0 warning/0 error，既有Windows后台行为与官方控件契约全部通过。全程无原生窗口、物理输入、Computer Use、Android或安装动作，未打包；真实桌面/DPI/物理鼠标验收仍Not verified。后续按用户新反馈继续处理。

## 2026-09-06 — 自动重启原因排查（现有证据检查完成，根因未锁定）

- 用户报告先卡死再自动重启；为避免重复触发，暂停启动模拟器和构建。当前没有 emulator/qemu/java/dotnet 进程。未改 BIOS、电源、分页文件或系统驱动配置，未运行压力测试。
- 系统事件：9 月 5 日 21:26、9 月 6 日 00:37、01:51 三次启动均伴随 EventLog 6008 异常关机；对应时段未发现 1074 计划重启、Kernel-Power 41、BugCheck/WER-SystemErrorReporting 或 WHEA 记录。6008 中的上次存活时间不可当作精确故障发生时刻；当前 LastBootUpTime 为 01:51:33，08:16 是待机/唤醒记录。
- 明确异常：9 月 6 日 01:17:35 Application Popup 26 报虚拟内存最小值太低、系统扩充分页文件且部分请求被拒；9 月 3 日 11:17 也有同类告警，早于本次模拟器安装。没有 Resource-Exhaustion-Detector 进程消耗名单，因此不能断言哪个进程耗尽内存，也不能把内存不足直接等同于整机重启根因。
- 转储与硬件：经只读管理员助手核验，Minidump 和 LiveKernelReports 中均无 dmp，MEMORY.DMP 不存在。当前开启小型转储与自动重启；8 月 30 日有一条历史 volmgr 161 转储失败，非本次直接证据。两块 SSD 基础健康状态 Healthy；32GB 物理内存，检查时提交约 14.4GiB/78.2GiB；C 盘分页文件系统管理、E 盘固定 32GiB，空闲空间足够。
- 候选与边界：AEHD、VMware vmx86、XTU 驱动同时加载，存在多个底层组件但没有冲突堆栈证据；不能据此直接归因。ACPI 两个热区一次读取约 87℃，不等同 CPU 核心温度，未发现本轮系统热保护事件；GPU 实测 51℃。系统还有早于本次任务的 vInject_x64.dll 签名/加载错误与缺失服务记录，未擅自删除或停用相关组件。
- 证据：.acceptance/restart-diagnostics/ 的 reboot-memory-events.json、system-snapshot.json、protected-results.json；只读管理员助手脚本也留在同目录。未上传系统转储或用户数据。
- 崩溃前操作核对：当前任务 rollout 原始记录显示 01:47:44 前一轮 Android 单元测试/Debug APK 构建成功；01:50:30–01:50:41 安装该 APK 并启动模拟器中的 BlueLink，随后新增全屏图片预览组件；01:51:18 修改 MainActivity 接入预览并添加 ImageViewportTest；01:51:19 启动 gradle.bat --no-daemon -p android :app:testDebugUnitTest :app:assembleDebug，尚未返回就中断。系统 LastBootUpTime 为 01:51:33。新构建日志为 232 个空字节，没有对应新 daemon 日志，无法确认 Gradle 已进入哪个具体任务。时序证据不等于因果结论；原始操作摘要在 last-codex-operations.json。
- 后续（按用户最新决策替代）：观察期间只重构 Windows；Android 代码、构建、ADB、模拟器全部暂停，已有改动保留，恢复由用户决定。依次完成 Windows 完整性检查、B2/B3 收尾、设置/反馈、安全/更新、USB、安装/整体回归，跨端实机后置。AEHD 仍加载，不修改系统驱动配置；不运行模拟器期间的稳定性不能直接证明根因。08:28 新出现的 Java 进程属于另一个项目 miku-desu，未擅自终止。

## 2026-09-05 — 依据最终 Figma 分批重构（执行中）

- 图片预览原型及实施（2026-09-07，已完成源码与后台验收）：按用户授权更新Figma Image Preview Dialog 174:253及Compact变体1223:252/1223:312，对照板1227:15065保留明暗宽窄四种效果，不增加149个Windows应用画板计数。用户追加删除预览内“另存为”和“在文件夹中显示”，并指定Miku Desu文件LAN7pHzjEV67Awz8fFhI4y的58:107（ri/anticlockwise-line）和58:146（ri/clockwise-line）旋转图标；精确复用原始矢量、等比24→20，并同步8个原型图标。设计截图/几何与素材来源记录于`.acceptance/figma-image-preview-20260907`。
- 用户确认后接入Windows：ImagePreviewWindow改为72 DIP固定底栏、单行居中操作组；900 DIP以下五个工具为40×40，1:1保留比例符号，空间恢复即显示文字，保留中文/英文提示和辅助功能名称。保留官方FluentWindow/TitleBar/Button，关闭按钮32×32、右边距20、在64高标题栏居中，隐藏系统右上角关闭控件和Esc文字，Esc与新按钮共用关闭命令。主图与176×120导航共用28 DIP棋盘格，Light为#F6F8FB/#E4E9F0，Dark为#232B38/#2B3544；导航视口为约12%透明填充。旋转/关闭PNG从已确认Figma节点精确3倍导出并登记SHA-256；选中适应图标改为前景蒙版以同步主题。缩放、旋转、重置、拖拽与默认1000×700/最小720×480及尺寸持久化保留；仅移除预览内重复文件入口，消息界面文件操作保留。
- 验证：新增专项离屏入口，使用独立透明PNG和独立偏好目录，覆盖中英文/明暗主题、720/899/900/1000/1320及反复缩放窗口、关闭按钮/长标题几何、旋转方向、一比一与重置、主题实时切换、主图/导航共用背景与关闭命令；305 checks/12 PNG通过。修正实现初稿中官方Button不支持的MouseOverForeground属性，以及验收代码调用Matrix而非MatrixTransform.TransformBounds的问题后构建成功；最终`.acceptance/windows-background/20260907-preview-final`构建0 warning/0 error、完整离屏3331 checks/223 PNG、既有后台行为与官方控件契约通过，已目视核对深色缩放、浅色宽版和英文窄版截图。所有相关窗口HWND为0，未发送物理输入；实际桌面/DPI/拖动及物理键盘为Not verified。旧原生预览脚本的过时入口/缩放列宽/浅色限定检查已同步，未执行该脚本。本轮未打包、未使用Computer Use、未启动安装器/Fixture/模拟器，现有安装包不含本项。

- 文件不可用弹窗（2026-09-07，设计及实施）：按用户截图补齐Figma组件1205:1525和W06画板1209:17479，W06计数24、Windows原型149；保持原Information Dialog四种纯信息变体。用户随后授权按图实施：新增FileAvailabilityWindow，复用官方FluentWindow/Button、主题、Noto Sans SC和现有文件类型图标；精确导出关闭图标并登记SHA-256。替代文件未完成/路径缺失时的通用MessageBox，消除空按钮；显示实际文件名、说明和单个“知道了”，关闭/Esc共用命令，Enter为默认按钮；标准520×306，长文件名/英文自动增高，受限高度正文滚动、按钮固定。打开/定位/另存/复制统一检查完成状态和当前文件存在性，不把局部文件交给外部操作；系统打开失败保留原始错误。专项118 checks/10 PNG，完整离屏3027 checks/211 PNG，构建0 warning/0 error、官方控件契约通过；已查看中英文/深浅主题和滚动底部截图。首次窄宽验收在绑定刷新前测量，导致验证画布高度不足；修正后台测量时机后复验通过，保留原失败日志。证据`.acceptance/windows-background/20260907-file-availability-final`；本轮不打包、不使用Computer Use、未启动Fixture/安装器/模拟器，实际桌面/DPI/物理键盘Not verified。

- 授权与方向：以当前 Figma 为最终 UI、交互和可见功能基准；本任务为实施。2026-09-06 用户将当前执行范围改为仅 Windows，Android 原样保留并暂缓，模拟器/Gradle/ADB 保持停用，恢复由用户决定。Windows 阶段沿用连续实施授权；重排后的 W0–W5、文件边界、全量索引及替代核对统一维护于 PROJECT_ROADMAP.md。
- 基线：工作树从默认 7c9d9c8 对齐到已经提交的 a700bae，分支 codex/figma-refactor；未纳入或改动 E:/BlueLink 未提交工作。读取主目录 AGENTS.md 与 WORKLOG.md，未伪称主目录存在路线图。
- 原型：实时枚举 Windows 148、Android 115 个应用画板；USB 对照单列。已读首批设计上下文、截图、组件及变量；图标从现有节点精确导出，不重新生成。
- 有效实现：首批 11 个 Android 首页/状态画板已接入独立 ui/devices 组件、DeviceScreenState 纯投影与 BluetoothAccess 系统状态观察。使用原型 64dp 顶栏/三项底栏、42dp 搜索、设备卡与虚线空卡、准确端类型与离线灰色、权限两态及蓝牙关闭展示。分组可折叠、搜索实际设备名称/标识/地址；权限不足和蓝牙关闭时保留历史查询及打开会话，限制发现、连接与发送。返回系统设置和蓝牙广播会重读系统状态，不以演示延时模拟授权或连接结果。
- 真实数据接线：从已有数据库连接时间投影 lastConnectedAt，避免误把消息活动时间显示为上次连接；连接进度/失败携带实际目标地址，避免同名设备混用反馈。仅新增领域/运行时投影字段，不改变持久化结构、NearbyDeviceTracker、安全与传输协议。
- 模拟器验收与修正：实际安装后发现权限按钮文字被外层 TextStyle 固定为深色、选中导航图标被统一 tint 抹掉白色细节，已分别改为遵循 LocalContentColor 和保留原图颜色。进一步用明确标注 QA 的离线历史记录发现无权限冷启动时 runtime 数据观察尚未启动；现进入前台即初始化本地历史/设置观察，蓝牙端点、启动扫描与自动连接仍受实际权限/开关控制，重新授权时恢复蓝牙工作。
- 替代清单：已移除旧首页深色摘要、旧分组/设备卡/未使用 NearbyScreen、四项底部导航及自动 PermissionGuideDialog；保留现有聊天、文件、设置目标作为后续 B3/B4 的迁移对象。失败文件重试、安全异常、反馈与 USB 等旧路径仍按路线图待替代，不宣称已完成。
- 素材：18 个 PNG 均从当前 Figma 源节点按 3 倍直接导出；精确选中/零连接变体、源节点与 SHA-256 记录于 design/figma/android-device-assets.json。Logo 图片填充 SHA-1 与仓库素材一致。Noto Sans SC 从 Google Fonts 官方来源下载并保留 OFL 许可证；未生成或重绘已确认图标。
- 自动验证：scripts/build-android.ps1 构建及复核成功，包含 protocol-core:test、23 个 app 单元测试（17 旧 + 6 新）、lintDebug、Debug/Release 构建。gradlew.bat --no-daemon verify 通过 41 checks。git diff --check 通过；aapt 核验最终 Debug APK 无 INTERNET。Lint 无 error、有 2 个工具链 warning：部分 Compose 自定义规则因 API 不匹配跳过，及 kapt 可迁移 KSP；未将其称为零警告或完整 UI 证明。
- 运行回归：新增 scripts/test-android-offline-history.ps1，仅允许专用 BlueLink_API_34 模拟器，写入自身 QA 记录并在 finally 中清除。权限拒绝、已授权但蓝牙关闭两种冷启动各通过 6 项检查：真实 Room 历史加载、折叠、搜索、消息阅读及返回等。360/412/600dp × 100%/130% 字体六组实际页面与滚动截图均通过标题/导航/权限按钮可达性检查并目视核验；系统状态栏、600dp 平板任务栏沿用 Android 实际行为，不伪造原型系统状态。
- 证据：.acceptance/figma-batch1/ 保存原型截图/上下文与首轮构建；runtime/ 保存真实客户端/系统授权 XML、PNG、两种冷启动回归结果、matrix-results.json、APK 哈希及权限记录。最终 build-offline-startup-fix.log 构建通过（23 个 app 测试、Lint、Debug/Release）；Lint 仍为 0 error、2 个既有工具链 warning。APK 0.2.17 已安装专用模拟器，未发布。电脑意外重启后已恢复模拟器，复查文件并重新完成中断的构建和权限流程。
- B2 有效实现：继续读取 Windows 297:12682、27:291、600:15457、27:616 的实时上下文，开始双栏首页。窗口使用 52px 官方 TitleBar、440px 设备栏和同一右侧消息/文件工作区，移除旧第三栏与收起轨道；加入实际设备搜索、三组折叠/空卡、真实 Radio 状态查询与回调、未选会话空态和全局文件入口。文件表格、消息气泡、菜单等内部内容仍由 B3 替代。未选会话稿没有可点击的全局文件入口，客户端在标题栏设置左侧保留精确文件图标与 Ctrl+Shift+F 快捷键，作为可达性补足；不改 Figma。
- B2 素材与修正：12 个 Windows 图标按 3 倍从源节点导出，来源见 design/figma/windows-home-assets.json；Noto Sans SC 从同一上游字体实例化为 400/500/700 静态字重，记录哈希并随输出附 OFL。真实 UI 暴露普通 Button 样式不适合 ui:Button 导致箭头叠字、列表选中背景露出方角及滚动条压住内容；改用官方 DefaultUiButtonStyle、局部官方资源覆盖与滚动留白，不引入基础模板。选中项按实际 peer ID 保持；上次连接取数据库 last_connected_at，不拿消息时间冒充。
- B2 验证：Windows Release 构建 0 error/0 warning；verify-ui-contract.ps1 通过；既有聊天滚动回归通过（SafetyGap=10px、ScrollToEnd=True），并将其数据目录改为独立临时目录。Computer Use 实际 UIA/截图检查了未选会话、全局文件/当前会话导航、18 条标注 QA 的离线记录、大小写无关搜索、折叠后继续阅读、发送禁用、长列表滚动和选中样式。当前实际 DPI 为 150%，1600×1000 与 1180×720 的几何报告均确认 TitleBar=52、设备栏=440、搜索框高=38。证据位于 .acceptance/figma-batch2/。
- B2 验收修复记录：首次 QA 消息 ID 使用带连字符格式，读取要求 N 格式，已修正测试数据；另一次滚动样式资源名误写导致启动异常，已按官方 UiScrollViewer 修复并重新启动验证。保留失败与后续成功证据，不把中间失败隐藏为全程零异常。独立 UI 测试使用受限名称的临时根目录、独立互斥与日志，未载入日常用户记录。
- 验收清理：通过实际标题栏关闭 B2 专用测试实例，确认进程退出且 BlueLinkHomeUI-b2-20260905 临时根目录自动删除，18 条 QA 设备/消息随之清除；截图与日志证据保留在工作树。Android 模拟器继续运行，收尾复查 sys.boot_completed=1。
- B2 补齐：真实连接阶段投影加载、取消与失败反馈；离线、扫描状态实际窗口已复核。连接计数只计入已连接会话，目标地址用于隔离不同设备的反馈。
- B3 进行中：Windows 消息/附件、当前设备与全局文件范围、状态/方向/文本筛选及分组、按状态提供的菜单、消息搜索窗口和图片预览已接线；移除旧 AllTransfersWindow 与可见重试入口。12 项历史查询回归、Release 构建、官方控件契约及聊天滚动/真实 WPF 搜索窗口回归通过。实际 UI 已验证当前设备 8 条文件、失败筛选 2 条及失败菜单、搜索打开与图片分类；查询/日期由 WPF 运行回归覆盖，尚未完成搜索输入的物理 UI 操作验收。修复搜索结果视图自动选择首项导致窗口构造中提前关闭的异常，最终回归通过。证据在 .acceptance/figma-batch3/。
- 2026-09-06 重启恢复：第二次主机意外重启后，原 AVD 的 userdata-qemu.img.qcow2 经只读 qemu-img 检查确认 L2 数据损坏；保留全部原始文件。在 D:/Develop/Android/avd 创建 BlueLink_API_34_Recovery，沿用官方 API 34 镜像与 AEHD；修改启动器并保留旧脚本备份。emulator-5580 开机完成、ADB 安装 0.2.17 Debug APK 与启动成功。离线历史脚本仅增加这个明确命名的专用 AVD 到允许列表。
- 风险与未验证：QA 历史只证明本地读取，不证明跨端通信。双端真实连接/失败/传输、TalkBack/屏幕朗读、Windows 100%/125% DPI、蓝牙热切换仍为 **Not verified**；B3 Android 已暂停。Windows W2–W5 后续完成的实现及后台证据见本条末尾，实机项不因此视为通过。
- 决策与下一步：依据最新仅 Windows 范围及连续实施授权，按 W0–W5 完成 Windows 完整性恢复、B2/B3 收尾、B4 设置反馈、B5 安全更新、B6 USB、B7 安装整体回归。Android 与跨端真机验收后置，用户另行决定恢复。未发布、提交或推送，未更改 Figma 或主目录未提交工作；保持本条为单一重构任务记录。
- W0/W1 Windows 独立恢复：80 个 Windows/安装器/共享源码配置文件无空文件或空字节；单 MSBuild 节点 Release 构建通过。新增同日五分钟消息时间分隔、按缩略图设置展示搜索结果图片、真实对端名称；历史查询回归扩至 16 checks，图片预览 27 checks、聊天滚动/搜索窗口回归及官方控件契约通过。观察时提交约 18.9GiB/78.2GiB，启动时间仍为 01:51:33；不是对重启根因的判定。
- W1 实际 UI 补验：用户修复沙箱后，普通命令、apply_patch 与 Computer Use 恢复。实际 UIA/截图确认时间分隔、搜索缩略图、搜索结果定位、真实图片打开及 1:1 缩放；截图保存于 .acceptance/windows-only/preview-actual-size.png。模态窗口键盘注入未写入搜索框，set_value 返回 CacheRequest 0x80070057，物理查询输入仍 **Not verified**。随后修正定位高亮过深和小图片大小显示为 0 MB，待新截图复查。
- W2 设置/反馈实施中：已接入帮助主题、隐私正文和本地反馈表单；1000 字/必填、诊断默认关闭、异步 ZIP 生成、失败保留草稿、生成结果复制路径/打开文件夹。诊断仅投影最近最多 1MiB 日志的时间、操作、异常类型与代码，不打包聊天内容、路径、设备名称、身份密钥或用户文件。设置写入成功后才应用运行参数；存储统计改为可取消后台估算，避免每次输入在 UI 线程递归遍历；缓存清理限预览目录，保留发送快照。QA 会话日志改用隔离根目录。
- W2 反馈验收：反馈 23 checks 通过，含诊断关闭时不读取锁定日志、启用分支脱敏、1000/1001 字、取消、失败保留已有 ZIP、临时文件清理及日志尾部上限。实际 UIA/截图验证空描述拦截、44 字输入/计数、原生保存对话框生成 .acceptance/windows-only/feedback-ui-off.zip；包内仅 feedback.json 和 README，无诊断文件。复制路径与剪贴板一致，打开文件夹确实选中该 ZIP；证据 feedback-ready-off.png。随后修正成功状态颜色、结果弹窗高度和输入后过期警告，已编译，尚待新截图。
- W2 设置与信任：信任列表改由 IdentityStore 的实际固定公钥记录投影，保留所有聊天会话；新增空态、逐台及批量移除，移除后断开对应会话，自动重连同时要求持久化信任与有效身份记录。IdentityStore 信任写入串行化，失败恢复内存快照。新增本机设备名持久化与编辑窗口，通过动态 GATT ReadRequested 和回连请求发送新名称；保留现有协议的 80 UTF-8 字节限制，不截断多字节字符。最大 32 字符仍受该协议字节上限约束，超限提示缩短。实现依据 Microsoft GATT Server 官方文档：https://learn.microsoft.com/en-us/windows/apps/develop/devices-sensors/gatt-server 。跨端名称更新、移除信任与在途握手竞态仍需 W3/真机补验。
- W2 法律与关于：按当前 Figma 重排关于页，新增隐私/用户协议可点击目录、开源组件搜索及完整随包许可证；清单来自实际 WPF-UI 4.3.0、其 Abstractions 与第三方声明、BouncyCastle 2.4.0 和 Noto Sans SC，未照搬原型示例依赖。精确信任空态 PNG 与来源哈希已归档。
- W2 最新验证与中断：build-settings-trust.log 的 Release 构建 0 error/0 warning；settings-checks.log 新增 23 checks，覆盖名称边界/持久化、信任写入失败回滚/重载/身份保留和完整嵌入许可；ui-contract-settings.log 通过，git diff --check 通过。重启隔离 QA 应用后，Computer Use 报告用户按下物理 Esc，已停止界面操作。上述新设置页面实际 UI **Not verified**；主题/语言、其余设置细节仍未完成，不能将 W2 视为完成。Android 保持暂停；未提交、推送或发布。

- 验收执行方式调整：用户要求避免 Computer Use 影响日常使用，已关闭此前明确命名的隔离 QA 实例；新增 scripts/test-windows-background.ps1、--background-only 与 OffscreenWpfVerification。使用真实 App 资源和 SettingsWindow XAML，在内存中测量/排列、软件渲染和触发页面内路由事件，测试中不显示设置窗口、不发送物理输入。App.CreateResourceOnlyHost 阻止验收 Dispatcher 执行正式启动路径；临时模型不启动蓝牙服务。系统对话框/拖拽/托盘/真实 UIA 与显示器 DPI 后置，离屏证据不冒充实际桌面截图。
- 后台宿主失败及修正：初期遗漏 DispatcherSynchronizationContext、触发正式 App.OnStartup、跨程序集/资源作用域加载失败；异常从验证 Main 逸出，连续重试导致用户看到 dotnet.exe / 0xe0434352 系统错误窗口。这是本次测试宿主问题，不能归因于 Android 或既往整机重启。补齐测试入口异常捕获/失败码/日志检查，失败自测返回 1 且日志为 Verification failed；未更改全局 Windows 错误报告设置。保留原正式应用资源声明，撤销过渡的 ApplicationResources.xaml/资源复制解析方案。最终 .acceptance/windows-background/20260906-resource-host 的后台套件和官方 UI 契约通过，离屏 28 checks / 12 PNG，Settings HWND=0；构建 0 warning/0 error。早期 first/fixed/isolated/resources/final/verified/no-dialog 输出均为失败或中间证据，不计验收通过。已查看反馈窄窗口滚动到底、信任空态、法律页；控件 Loaded/动画状态与真实桌面不等价，仍待最终实际 UI 验收。

- W2 连续实施补充（2026-09-06）：主题/语言由持久化设置驱动官方 ThemesDictionary 与应用资源，支持浅色/深色/系统、简中/English；页面/帮助法律/主要状态已迁至语言目录，动态颜色保持浅色设计值，单色图标保持原 PNG 形状。设置保存后生效，取消/默认值恢复只改草稿。新增无蓝牙初始化的本地状态加载入口供后台验收使用；运行时系统主题通知订阅会在退出时解绑。
- W2 身份重置：新增确认弹窗和真实本机身份重新生成，DPAPI 保护、原子写入失败回滚、信任撤销版本、会话暂停/恢复及蓝牙身份重建。隔离验证覆盖旧确认不得恢复已撤销信任、失败不改变身份、重置可重载、聊天/接收文件/设置保留。后续 W3 已补协议双向确认与在途持久化竞态，见下方安全记录。
- W2 最新后台证据：.acceptance/windows-background/20260906-w2-reset 构建 0 warning/0 error，反馈 23、设置/身份 33、离屏 44 checks / 17 PNG、其他 Windows 回归与官方 UI 契约通过；未创建原生设置窗口，未使用 Computer Use 或 Android。已查看英文深色通用窄窗、反馈和重置弹窗；重置背景/次按钮和文件处理标题已在 W3 套件复验。标题栏/部分官方控件在离屏未 Loaded 时与桌面渲染不同，真实交互、系统主题通知和 DPI 仍后置。

- W3 安全：独立内存双工流验证 26 checks 通过；本机确认后保持等待，只有收到有效加密 PROTOCOL_HELLO 并确认身份版本未失效才固定信任。使用 BTX 既有 GoAway 表达明确拒绝，普通关闭不误报拒绝；变更公钥导致 peer ID 变化时仍按原传输地址绑定的可信身份阻断。移除/重置与 SQLite 信任投影串行，启动修复中断撤销后的旧记录；取消/撤销后停止排队与收发。真实蓝牙双端状态仍后置，Android 旧保存顺序未改。
- W3 更新：关于页新增实际检查入口，固定仓库公开 Release、正式版本/x64 精确包名/大小/SHA-256 校验、限时流式下载、取消/重试、独立部分文件与完整包、安装前重验和传输占用保护。默认 WinVerifyTrust 无 UI 校验及与当前客户端相同发布者公钥校验，测试假下载/启动器仅在验证项目。公开 API 只读检查返回 404，当前开发产物无签名，公开更新和真实安装交接仍为发布阻断项；不伪造“最新版本”或“可安装”。
- W3 后台证据：20260906-w3-updates-fixed 串行构建 0 warning/0 error，安全 26、更新 32、设置 35，其他 Windows 回归和官方 UI 契约通过；20260906-w3-raster 离屏 64 checks / 28 PNG（无原生窗口/输入）。离屏首帧缺字由字体栅格化尚未完成引起，增加预渲染后清空重绘，完整版本/图标已核对；切换主题后重建未 Loaded 的夹具窗口，真实桌面动态切换仍单列补验。


- W4：新增默认关闭的 USB 设置/十二状态、SetupAPI 只读驱动/策略枚举、AOA 51/52/53 协商、WinUSB Bulk 流。经 BTX 身份核验后优先 USB，保留同一 Peer 的蓝牙备用，原文件任务继续由原通道接收控制；USB 地址独立持久化。实际枚举仅识别 AOA/Android ADB 标识接口，普通纯 MTP 初始接口尚不支持自动识别，不安装驱动。当前一次管理一个 USB 设备；协商速度不冒充实测速率，保持 64 KiB 分块协议。
- W4 验证：`.acceptance/windows-background/20260906-w4-final` 构建 0 warning/0 error，USB 内存双通道 26、设置 37、既有安全/更新/文件回归通过；离屏 97 checks/44 PNG，HWND=0。真实加密消息、传输中提升 USB 后继续暂停/恢复原蓝牙文件、文件哈希及回退消息通过；AOA/驱动策略用替身，不触碰硬件。已检查标准 USB 页、窄屏底部和高速状态；离屏旧主题残留通过重建测试页面纠正。真实 USB 授权/驱动/拔插、跨端吞吐与桌面补验后置。

- W5 安装/卸载（2026-09-06）：读取安装、卸载两分支、失败、运行时及托盘最新 Figma，复用七个直接导出的源图标和 Noto Sans SC，未改 Figma。Burn 与独立卸载器共享 1040×680、最小 920×620 的卸载视图，默认保留数据，删除操作需明确二次确认；忙碌、进度、保留/删除完成、失败重试、重启待完成均取真实状态。仅在程序实际移除并验证后清理已选数据，3010/重启待完成不提前清理。隔离清理策略读取 SQLite 接收路径，保护 Download/自定义接收文件，拒绝占用/坏索引/重解析路径；索引已不存在时保守保留缓存，以免重试误删接收文件。
- W5 运行时与更新退出：保留 net472 引导能力；实际字节进度、校验/提权/安装/完成状态接线，修复运行时检测失败后重试按钮仍被禁用。启动器下载限制 Microsoft HTTPS、大小、SHA-512、无 UI 签名验证和精确发布者，限时、取消、独立临时目录及只读租约防替换；失败可重试。更新安装交接改用有序退出释放会话和数据库。UAC、运行时正式下载/安装与引擎事件组合仍需隔离环境实测。
- W5 托盘/未读：按真实 Peer 聚合、消息去重、附件完成只更新预览；后台选择会话不清未读，前台实际阅读后再清。消息持久化与已读写入串行；清空会话/全部历史同时清零 SQLite 未读及通知状态，保留接收文件。托盘卡片可选择会话，“暂不处理”不清未读；系统任务栏使用原生徽标/提示接口，未伪造 Windows 外壳或其他应用内容。原生托盘服务没有在后台验证中创建。
- W5 最终后台验证：`.acceptance/windows-background/20260906-windows-final-layout` 构建 0 warning/0 error，反馈 23、设置 39、安全 26、更新 32、USB 26、安装安全 23、通知 12 checks，既有回归全部通过；客户端离屏 100 checks/45 PNG、官方 UI 契约通过。`20260906-installer-final` 构建 0 warning/0 error、安装离屏 57 checks/27 PNG、WiX 契约通过。原生 HWND 均为 0，失败处理自测均写日志并返回 1。已检查卸载两分支、窄窗固定底栏、运行时下载、安装路径、托盘卡片及英文深色 USB；修复英文标签重叠与托盘底部裁切后再次通过客户端套件。离屏不替代 Loaded、动画、DPI、焦点/输入、UAC 和系统界面验收。
- W5 产物：新增 Windows 专用 `scripts/build-windows-review.ps1`，框架依赖客户端 ZIP、独立 Review 标识 MSI/Burn 仅编译、不执行。安装目录归属 57 checks 通过，测试目录在递归清理前限定工作树 `.acceptance/install-ownership-*`；WiX 支持独立生成 payload，避免混入旧生成表。最终目录 `artifacts/windows/review-20260906-final`，版本仍 0.2.17、未签名、不含前置运行时，不分发、不用于正式升级，保留已锁定发布指纹。早期 v1 重复 payload 符号、v2 SDK 缓存解析失败均已修正，失败日志保留，不计通过。
- 当前结论与剩余：Windows W0–W5 实现及可独立后台验证已按序完成；接下来按实际桌面/DPI/托盘→隔离安装升级修复卸载/运行时→蓝牙/USB 驱动与双端矩阵→新版本签名/公开更新发布补验。Android 保持暂停、既有文件未动，本轮未启动模拟器/Gradle/ADB，也未操作用户桌面或日常数据。未提交、推送或发布；不能据本轮结果判定此前整机重启原因。

- 窗口尺寸与同窗设置补充（2026-09-06）：用户要求桌面验收加入缩放自适应、适中默认尺寸、最小值及重启恢复，并明确设置是主窗口内页面。检查发现旧 SettingsWindow.ShowDialog 仍残留，已移除并替换为 SettingsPage/SettingsHost；帮助/法律/反馈/USB 子页及安全异常的信任管理入口均走同窗导航，必要确认对话框以主窗口为 Owner。主界面/设置共用默认 1180×720、最小 1000×600，保留当前会话/文件工作区；覆盖会话时不误清未读，托盘打开会话先退出设置。
- 窗口自适应与持久化：侧栏随宽度收敛、窄文件工具栏分行、正文滚动与底栏可达；搜索默认 860×640、最小 620×480，图片预览默认 1000×700、最小 720×480，预览工具栏换行。正常宽高和最大化状态保存到独立 SQLite app_setting 键，700ms 防抖后台写入，关闭/隐藏补写；最小化不覆盖普通尺寸，旧设置草稿和迟到的后台写入不能回退新尺寸。只读故障/损坏值安全回退，工作区变化时裁定恢复尺寸但保留内容最小值。没有单独的设置窗口尺寸记录。
- 本轮验证：`.acceptance/windows-background/20260906-window-sizing-final` 构建 0 warning/0 error，尺寸状态/持久化 22 checks；离屏 159 checks/57 PNG，既有设置/安全/更新/USB/通知等回归及官方控件契约通过。已查看最小尺寸内嵌设置/文件/聊天、默认聊天及搜索/预览小窗；设置切换不增加 Window 数，消息和用户宽高保留，底栏按钮均在可视范围。旧桌面验收脚本已改为定位主窗口内 SettingsPage，未运行实际 UIA。原生拖动/启动恢复、DPI/多显示器和可用区小于最小尺寸仍 Not verified，已纳入路线图；不使用 Computer Use/Android，不改主目录或日常数据，未重新打安装包、提交、推送或发布。

- Computer Use 实际桌面验收（2026-09-06）：用户明确启用后，新增 App/DesktopAcceptance 隔离持久化入口 `--desktop-ui-test=<name>`，真实窗口/托盘服务可启动，不启动蓝牙/USB 运行时，不使用日常数据。全部原生操作通过 Computer Use；沙箱进程最初不在用户桌面可枚举列表中，确认停止该测试进程后正常桌面启动。一次自动审批将窗口 HWND 误作 PID 而拒绝重启，重新核实进程与窗口均退出后通过审批，没有重复启动。
- 实测修复：SourceInitialized 中恢复宽高会因原生布局重入使高度回到 720；移到创建 HWND 前恢复，以不可变局部值应用宽高，并在防抖保存时重新读取稳定尺寸。OnClosing 内同步发起退出可能在原回调未结束时再次 Close，改为 Dispatcher 延后请求退出。真实退出/重启复验后没有新增异常日志。补充离屏回归要求非默认宽高在 HWND=0 时已恢复。
- 首轮夹具问题：会话误用裸 peer ID，与正式 `peer:<id>` 约定不符，造成空历史及已读写入唯一约束异常；修正测试夹具，并在独立 QA 库修复编号、核验外键后继续。早期失败日志留存，不计为验收成功，也不作为此前整机重启归因。加入一张本轮截图作为图片附件测试文件，没有读取日常聊天或接收文件。
- 实际结果：当前 150% DPI 下，主窗默认 1180×720、实际最小 1000×600，聊天/文件/同窗设置窄布局、设置滚动/底栏、主题深色保存/重启通过；普通 1000×650 与最小尺寸重启恢复、最大化/最小化/还原及最大化重启通过。搜索 620×480 的限制/分类/缩略图/返回原消息，预览 720×480 的限制/两行底栏/一比一/旋转/适应窗口通过；二者进程重启后尺寸正确。证据 `.acceptance/desktop-cua/20260906`，原生几何、SQLite 快照与 UIA 树独立保留。
- 验证与余项：`.acceptance/windows-background/20260906-desktop-cua-regression` 构建 0 warning/0 error，窗口尺寸 22、离屏 161 checks/57 PNG、全部既有 Windows 后台回归和官方控件契约通过。Computer Use 输入控件返回 CacheRequest/缓存错误，查询输入及键盘矩阵未计通过；托盘外壳/通知、反馈/USB 完整桌面状态、其他 DPI/多屏、实机/安装/发布继续后置。最终正常关闭 QA 实例，Android 保持暂停；未重打安装包、提交、推送或发布。

- 逐原型场景验收（2026-09-06）：按用户要求读取实时 Figma，首先核对设备/连接、消息状态与文件空态。增加受限 `--desktop-ui-scene` 和串行启动脚本，仅在隔离桌面 QA 入口生效；连接/扫描状态来自显式夹具，不调用蓝牙/USB 运行时。新 QA 配置默认关闭后台驻留，关闭后退出，避免编译锁定。Android 源码、Gradle、ADB、模拟器保持停用。
- 本轮有效修正：附近设备空态文案、首次使用的浅蓝圆底、蓝牙关闭的灰色徽标和专用提示、离线历史说明；消息/文件导航精确选中图标保留白色细节。扫描完成改白底绿点并按新设备计数，连接失败提示收在对应红卡内，真实失败原因保留工具提示。官方按钮禁用资源与本地白色前景冲突导致“连接中”不可见，已通过局部官方资源修复；离线发送使用淡蓝“离线”，蓝牙关闭保留灰色“发送”，均仍禁用，没有添加基础控件模板。
- 消息/空态修正：五种发送状态使用新导出的原型图标与灰/蓝/红状态色；空会话补专用蓝色原图、24px 标题与当前设备名称引导。文件空记录补 72px 圆底、原图、24px 标题和说明，筛选无结果提供调整提示。消息时间标记按实时组件说明由此前 5 分钟改为 10 分钟，今日分组显示“今天”；修改边界测试。素材来源与 SHA-256 追加至原有 Windows home/content 清单。
- 实际证据：`.acceptance/prototype-cua/20260906` 保存原生截图与 UIA，`scenario-results.json` 映射节点及组件范围。默认 1180×720 与已保存最小 1000×600 的首次使用/蓝牙关闭/文件空态可读；各设备分组独立折叠保持右侧会话，五种消息状态可滚动且放大后完整显示。实际键盘输入设备搜索 `Galaxy` 并过滤、文件搜索中文/英文并显示无结果均通过；解决方法是先实际点击输入框，不能依赖滞后的 UIA 焦点缓存。没有把这些结果扩展为模态搜索/完整键盘矩阵通过。
- 夹具与回归修正：在内存清空/替换消息会被实际列表选择触发的数据库重载覆盖，改为初始化隔离 SQLite 时持久化空态/五态记录。新增 Run 文本最初沿用双向绑定，离屏宿主捕获只读属性错误，已设为 OneWay；校验器改读 Run 实际文本并在大视口验证五行，再单独检查小窗滚动布局。中间失败日志保留，未计成功。该轮 `.acceptance/windows-background/20260906-prototype-file-empty` 离屏 183 checks/63 PNG 通过；后续新增验证见下文。
- 边界与下一步：分组条目只证明对应组件；QA 文本历史不证明文件/图片混合整页，静态连接/发送状态不证明跨端通信。消息状态独立稿为 16px，共用气泡组件为 14px，暂沿用共用组件；扫描文案保留实际发现范围“运行蓝联的设备”。瞬时按下、混合附件/新消息提示及其余右键菜单、弹窗、设置等原型节点继续逐个核对；真实传输、其余 DPI/安装/发布后置。未重打安装包、提交、推送或修改原型。

- 继续逐图复验：补足拖入可发送/离线阻止的图标、虚线与底色；文件当前/全局/其他设备范围、无会话全局页、组合筛选、关键词高亮与两类无结果提示均留存实际证据。修复全局入口被文件单选事件重置，文件页隐藏消息搜索并显示真实未读数，回到消息后清除已读角标。QA 文件由隔离 SQLite 和真实微型 PDF/文本/原型图片组成，显示真实长度，不硬编码原型示例文件大小或总数。
- 搜索与消息细节：设备查询零匹配改为单个浅蓝原型提示卡，保持当前会话；文件输入框采用官方 TextBox 加外部边框组合，右侧图标/分隔线和悬停蓝色反馈已实际核对。消息搜索的全部/文本/图片/文件/日期分类、关键词显示和空态均复验；图片左侧缩略图、真实像素尺寸、点击定位原消息通过。已完成图片恢复紧凑气泡；文件卡片调整尺寸/图标/摘要/方向圆角，仍保留传输进度和文件不可用回退。
- 日期/夹具回归：消息场景等待真实 SQLite 历史加载完成后再打开搜索，避免异步选择回调尚未完成导致空结果。原有日期选择行改为官方 Calendar 与结果并排，新增真实记录日期圆点；通过缩放视口/滚动修复最小窗口右侧日期裁切，通过白色当天圆点避免与蓝色背景混色。后台新增全部日期按钮边界、实际圆点可见/对比/查询后清除、真实图片大小和不可用回退检查。单独附件离屏宿主最初未指定官方 ListBox 样式导致空视觉树，已修正；失败日志保留，未计通过。
- 最新验证：`.acceptance/windows-background/20260906-prototype-search-reviewed` Release 0 warning/0 error、全部后台套件与官方控件契约通过，离屏 241 checks/79 PNG，窗口尺寸 22 checks。`.acceptance/prototype-cua/20260906/scenario-results.json` 记录 41 节点，其中 38 节点有明确组件范围的实际证据；前后对照截图编号 01–78，不能等同于 148 个 Windows 画板全部通过。模态消息搜索键盘输入仍受 Computer Use 所有者窗口激活/缓存限制，预填查询只证明显示/筛选；其余原型节点、DPI/真机/安装/发布后置，Android 继续暂停。

- W05 本轮按原型补齐方向/状态菜单与附近设备菜单，修复文件行选中后白字、官方 Separator 重复 Margin 导致分隔线消失，以及右键默认选择导致整行高亮/意外切换会话。24 个 W05 节点取得菜单组件实际证据 `82`–`109`；传输中设备卡片态及附件状态布局另行补验。账本现为 66 节点/62 项范围证据，不能当作整页通过。最新 `20260906-prototype-menu-rightclick-final` 构建 0 warning/0 error，离屏 353 checks/101 PNG，UI 契约与串行后台套件通过；中间离屏画布裁掉末项和自动测量失败已修正，原失败日志保留。Android 继续暂停。

- 继续修正附件状态布局：文件卡片统一 388×76，大小＋方向状态摘要；实际传输百分比改为官方 ProgressRing，图片使用中央 52 圆形进度层，接收暂停使用原型 `305:13238` 导出图标。9 种文件与 4 种图片状态通过实际复验 `111`–`123`；最新 `20260906-prototype-attachment-cards-final` 构建 0 warning/0 error，离屏 405 checks/114 PNG、UI 契约与串行回归通过。

- 补齐传输中设备卡片与菜单 `95:3182`：从实际传输记录显示方向/百分比，暂停更新文案、完成/移除恢复连接信息；不重建列表或切换当前会话。实际菜单点击发现“打开会话”仍留文件页，已与设备选择共用消息导航流程并复验。`124`–`130` 补齐原生证据及 1000×600 设备栏滚动访问。最新 `20260906-prototype-device-navigation` 构建 0 warning/0 error，414 checks/116 PNG，串行回归/控件契约通过；账本 66 节点/63 项组件范围证据，继续 W06。

- W06 四类信息弹窗已替换旧通用 MessageBox：设备 520×400、附件/图片 520×370、失败 520×330，原始图标、摘要卡片、字段行与仅关闭按钮；主标题栏不受遮罩覆盖。修正长路径换行产生多余滚动条；真实读取信任、方向、图片尺寸、持久化完成/失败时间，未知字段明确标注，防止假校验/假失败阶段。原生 `132`/`133`、`135`、`137`、`139` 完成组件验收；后台 `20260906-prototype-information-final` 为 435 checks/120 PNG、构建与 UI 契约通过。清单 70 节点/67 项组件证据，尚不代表完整场景或真机传输通过。下一步修正预览遮罩、工具栏和适应状态，再继续确认、安全及提示。

- W06 预览 Fit/Zoomed 已按原型补齐遮罩、28 DIP 棋盘格、原始标题与工具栏图标、图片边界和适应/1:1 选中状态。实际发现布尔 Tag 的样式触发未着色，改用 DataTrigger 并检查真实背景色；离屏导航测试按实际 DPI 放大到溢出后验证。原生 `141`–`143`、`145`–`148` 覆盖拖动/旋转/重置与 720×480 关闭重开，后台 `20260906-prototype-preview-acceptance` 为 444 checks/122 PNG 全通过。清单 72 节点/69 项组件证据；下一步按原型验收七种确认场景。

- W06 七种确认场景接入独立官方 FluentWindow 组合布局，460×250、具体动作/取消顺序、红色主按钮与原始警告图标；替代主窗口记录/会话/传输操作及隐私页全量清空、单设备移除信任的旧消息框。原生 `152`–`161` 核对样式和取消路径；删除 QA 传输记录后列表归零且原 PDF SHA-256 不变。全量清空只取消，真实设备取消/信任撤销留待硬件验收；文案不虚称取消统一失败或临时文件已清理。后台 `20260906-prototype-confirmation-final` 474 checks/129 PNG、0 warning/0 error、UI 契约和 diff 空白检查通过；登记 79 节点/76 项组件证据。下一步为安全五态、提示五态，然后继续设置场景。

- 原型复验继续完成 W06 提示五态和安全五态：新增四种原型提示图标及自动过期/叠放组件，接入实际复制和本机记录操作反馈；修正安全窗口按钮列宽、状态行高、指纹/设备图标和缩放滚动条，修复等待按钮白底白字。真实 CUA 证据 162–183（163 为过期后画面，不计最小尺寸成功），89 条节点记录中 86 条有组件证据。最新 `20260906-prototype-security-buttons` 为 523 checks/136 PNG、0 warning/0 error、控件契约通过。安全/错误视觉夹具均不写信任或建立传输；后续继续 W07 设置页。

- W07 设置基础场景完成组件复验：按原型调整同窗侧栏、卡片/按钮、主题与语言选中菜单、USB 入口和实际可信列表；设备名、重置身份、批量移除使用对应尺寸的官方 FluentWindow 组合。通用/连接/文件/隐私在 1000×600 可滚动访问并保留固定底栏；实际拖动达到最小值后完整退出/重启仍为 1000×600、同窗设置仅一个 HWND。原生证据 187–212，账本 99 节点/96 项组件证据。设备名空值拦截和取消已验证，新名称键盘保存尚未验证；重置只取消；可信列表的实际移除仅在隔离身份库进行，不能代表蓝牙断开。官方开关几何和菜单字体保留控件适配差异，不宣称逐像素相同。后台 `20260906-prototype-about-documents` 550 checks/143 PNG、0 warning/0 error、串行套件与控件契约通过；关于/文档新增布局正在原生复验，目录定位和长文滚动边界继续修正。Android 保持暂停。

- W07 关于与法律文档实际复验：对照199:12698、700:16911、700:17032、700:16770，调整品牌区、行尾原型箭头、文档两栏与章节层级，收起只读页的保存操作。修复目录只露标题和许可证滚动条遮字；实际键盘筛选BouncyCastle/无结果、最小1000×600末节正文与MIT最后一行均验证。证据214–229；台账103节点/100项组件范围证据。帮助反馈状态正在继续复验，尚不算通过。

- W07 帮助与反馈13场景已完成组件范围复验（150% DPI）：问答可展开并进入反馈；空描述校验、45字计数、诊断开关、失败重试取消后保留草稿、1000×600滚动可达已核对。生成中使用静态隔离状态，已修正官方进度模板内部边距挤掉可见轨道的问题；实际系统保存对话框另行生成两种ZIP并核验诊断文件差异，复制显示成功提示、打开文件夹定位QA ZIP。证据233–261及feedback-native-packages.json；台账116节点/113项组件范围证据，不代表148完整画板全部通过。 最新后台 `.acceptance/windows-background/20260906-prototype-updates-final`：598 checks/157 PNG、构建0 warning/0 error及既有Windows回归通过。更新6场景继续复验，USB/托盘/安装界面在后，Android与真机仍后置。

- W07 更新6场景已完成组件范围复验（150% DPI、主窗口1000×600）：检查中/失败、新版本、下载中/失败/就绪均有原生UIA和截图；实际取消、检查重试、下载重试、稍后保留就绪已核对。对话框统一540×350及全主窗口遮罩，失败区域红色、下载中禁用文字可读、可见进度4 DIP。验收专用HttpMessageHandler只供应本地非可执行样本，并禁止安装；生产路径仍使用公开HTTPS及签名验证。证据262–270，截至更新阶段台账122节点/119项组件范围证据；公开下载、真实签名和安装交接仍Not verified。 USB 页面已完成首轮布局修正，后续继续逐状态原生验收。

- W07 USB 页面与12种状态已完成组件范围复验（150% DPI）：1000×600纵向滚动、最大化双列、固定底栏、原型手机图标及状态配色已核对；实际详情页/连接页开关共享草稿，取消后恢复原值。证据274–291；台账123节点/120项组件范围证据，另列12个USB子组件。状态文字保留实际通道与64 KiB块协议含义，未连接USB/蓝牙或安装驱动，独立772宽组件以离屏验证为补充。

- W08 托盘卡片已用400×274独立原生组件宿主核对（293–294）：两会话/三条未读、红色数字、行底色、无多余滚动条，点击会话跳到对应QA设备。原生托盘悬停与任务栏闪烁仍Not verified：Computer Use未提供系统外壳可选窗口；不以组件宿主替代系统外壳验收。当前台账125节点/121项组件范围证据，另列12个USB子组件。

- W09此前安装与卸载23个原型场景在SetupUI宿主中完成组件范围原生复验（不包含独立Uninstall.exe确认入口；该遗漏见本轮反查修复）（2026-09-07，150% DPI）：安装欢迎/位置/路径错误/空间不足/进度/失败/完成、取消与覆盖提示、运行库六态、卸载两种选择/两种确认/进度/两种结果/失败逐项留存证据298–337。修复官方MessageBox按内容缩到480×220、确认按钮顺序与字号；普通确认560×260、覆盖提示500×263。安装窗口默认1040×680、最小920×620，欢迎底栏分行、完成长路径省略并保留完整提示，卸载页面可滚动读完；主应用同窗设置仍为1180×720/1000×600且保存用户尺寸。后台 `.acceptance/windows-background/20260907-installer-final-acceptance` 为68 checks/30 PNG、0 warning/0 error、官方控件及WiX契约通过。验收宿主不连接Burn、运行库下载器或卸载执行器；实际安装/删除、UAC和重启未执行。保留当前版本、实际路径/错误原文、256MiB预留、不可取消卸载等产品语义，不按原型示例伪造运行结果。台账148节点/144项组件范围原生证据，另列12个USB子组件；不是148完整画板全部通过。

- 新消息入口补验（2026-09-07）：309:13490改为原型136×38、19圆角、15字号及阴影，保留官方ui:Button。新增仅启动时选择的message-arrival隔离场景，40秒后向实际消息集合加入一条固定内存消息，不操作UI或启动通信。CUA先上滚读历史，确认到达时原位置保持、提示出现，实际点击后滚到最新消息并隐藏入口（证据338–340，1180×720/150% DPI）。拖边未取得更小的新尺寸，本入口最小尺寸未验证；不扩大为硬件接收或混合附件整页通过。最新客户端后台 `.acceptance/windows-background/20260907-message-arrival` 构建0 warning/0 error、612 checks/157 PNG、既有回归及官方控件契约通过。当前148个Windows节点已全部登记，其中145个有组件范围原生证据；当时尚无原生截图为扫描按下120:5615、连接按下120:5997和任务栏新消息623:16107；本轮前两项已补后台图，仍不冒充原生操作。Android继续暂停，未提交、发布或执行真实安装/删除。

- 最新 Windows 修复包（2026-09-07）：`artifacts/windows/review-20260907-install-recovery`；0.2.17 / x64 Review，EXE 90,054,697 字节，SHA-256 `5497CDFCE6F7B833C1A769684F898A3C1698EF524289C20DF577A2C60F805BD9`。保留全部既有Windows界面修复，修复同版本覆盖顺序和已注册半安装状态恢复；未签名、不含前置运行时。Release/MSI/Burn构建0 warning/0 error，目录归属66 assertions；解包核验21个payload、22个安装文件、嵌入恢复清单、卸载配置身份及真实MSI升级表/执行序列通过。证据为产物目录build-verification.json、SHA256SUMS.txt与 `.acceptance/package-20260907-install-recovery`。正式发布锁保持不变，未在实际D:\BlueLink运行安装或卸载。19:46的reverse-audit包已确认存在覆盖缺陷，仅保留为回归样本。

- 文件管理细节与后台验收（2026-09-07）：按用户截图修正行宽、分隔线、操作/更改按钮边框、折叠箭头、接收目录居中、搜索位置和滚动条遮挡。保留官方控件模板，清除文件行模板固定内缩及分组缩进，用平直单底边框分隔；箭头随展开状态旋转。共用滚动样式仅在滚动条可见时预留14 DIP（无滚动条不占位，按2026-09-07最新反馈替代固定预留），补正设备侧栏旧12 DIP覆盖和安全确认页零Padding覆盖；状态文本省略并保留完整提示，长路径不挤占底栏操作。用户随后要求收窄时状态按钮自动变下拉框：根据按钮实际测量宽度切换，文案改为“全部状态”，宽度恢复时还原按钮；状态、关键词与结果同步保留，状态/搜索/设备/方向始终同排，兼顾英文文案。
- 附近设备标题行重叠修复（2026-09-07）：根因是扫描按钮固定左间距156 DIP并叠放在铺满标题行的分组按钮上，窄侧栏会覆盖右端箭头。改为标题、扫描、箭头三列，标题占剩余空间，扫描和箭头在右侧独立布局并保留8 DIP间隔；扫描按钮按文案自适应宽度，保留标题及箭头各自切换展开/收起的行为，补齐英文扫描中的Nearby标题。只修改MainWindow.xaml、对应英文资源和OffscreenWpfVerification.cs；新增最小/默认/大尺寸、中英文、扫描中/扫描完成、折叠/恢复的离屏回归，并将标题/扫描/箭头边界检查加入所有相关界面渲染；169项附近设备标题行检查通过。
- 设备行徽标与状态点（2026-09-07）：按用户反馈将ConversationCardTemplate右侧纵向堆叠改为同位居中的Grid，移除徽标顶部8 DIP间距；HasUnread为真时显示计数并收起状态点，清零后恢复对应连接状态点。仅修改MainWindow.xaml与后台验证代码；新增在线/离线设备0→7→100→0动态更新，验证默认和最小宽度、99+宽度以及带传输进度的较高卡片。相关互斥、垂直居中、文案容纳和状态恢复共542项检查通过，查看离屏图确认效果；本次最新Review安装包已包含该修改。
- 扫描语言刷新与发送框内线（2026-09-07）：截图混用中英文的根因是前一离屏场景遗留英文环境，MainViewModel.InitializeLocalStateAsync应用中文设置后仅通知UsbStatus，提前建立的ScanActionText绑定保留旧文案。补齐本地初始化的属性刷新通知，新增先绑定旧语言再加载相反语言设置的回归，修复前按预期失败，修复后双向初始化及实时切换通过。横线来自WPF-UI 4.3官方TextBox模板中独立的AccentBorder，普通BorderThickness=0不控制它；仅在MessageInput本地资源中透明化普通/聚焦/禁用底边和交互背景，保留外层圆角边框与官方模板。离屏模拟IsFocused触发器并检查多行输入、禁用状态、中英文/深浅主题；补正检查对设置页同名ScanButton的误匹配。本轮未使用Computer Use或获取原生键盘焦点；原生输入体验仍Not verified。
- 本次验收方式已按用户指示改回后台，停用Computer Use并确认关闭上一轮隔离QA进程。最新 `.acceptance/windows-background/20260907-language-composer-final`：Release 0 warning/0 error、2489 checks/183 PNG、既有Windows回归及官方控件契约通过；78次滚动条与内容边界检查、675次透明动作按钮实际边框检查，183个渲染场景均确认无原生窗口。新增默认/最小/大尺寸、反复缩放保持筛选、16条长文件名/设备名、长接收目录、中英文/深浅主题和滚到底部的检查，并查看对应离屏图。验收宿主补齐脱离窗口后的字体/前景继承、主题资源更新及分组容器Loaded生命周期；输出逐条passedChecks，原45秒限额此前调整为90秒；此前扩展验收两次触及总限时，保留三种宽度检查但仅增加5张关键截图，将后台总限时放宽到3分钟后完整通过，失败日志保留且不计通过。真实桌面/UIA、显示器DPI和实际安装仍Not verified；上述修复现已打入最新Review包，旧10:17包保留为历史产物。未启动Android、提交或公开发布。

## 2026-09-05 — 安装 Android 模拟器（完成）

- 授权与范围：用户明确要求将推荐的官方 Android 模拟器下载安装到 D:/Develop；本次包括运行工具、Android 14 镜像、虚拟设备、必要加速驱动及启动验证。
- 安装：D:/Develop/Android/Sdk 中安装官方稳定版 Emulator 37.1.11、Platform-Tools 37.0.1、Google APIs Android 14/API 34 x86_64 镜像 revision 14。复用已有官方 Command-line Tools 22.0 与已接受许可证；未改变原 D:/Tool/Android/Sdk 或全局 SDK 环境变量。
- 虚拟设备：BlueLink_API_34（Pixel 7）位于 D:/Develop/Android/avd，4 核、4096MB 内存、1080×2400/420dpi、6GB 数据分区；工具偏好与用户数据放在 D:/Develop/Android/user。双击 D:/Develop/Android Emulator.lnk 或 Android/Start-Android.cmd 启动。
- 加速处理：确认主机 i9-13900HX、32GB 内存、BIOS 虚拟化开启。Windows 不提供 HypervisorPlatform 可选功能，启用返回“功能名称未知”；没有修复或替换系统组件。改用 SDK 官方 AEHD 2.2，验证 Google LLC 有效签名，经 Windows 管理员授权安装后服务 RUNNING，emulator -accel-check 返回 0/installed and usable。未关闭系统安全功能、未更改启动配置或自动重启。
- 验证：实际启动窗口，emulator-5580 为 device，sys.boot_completed=1，Android 14/API 34 与屏幕配置均匹配；读取并目视核验真实 Android 主页截图。随后正常关闭并通过 D:/Develop/Android Emulator.lnk 再次启动，快照恢复、ADB 与开机完成再次通过。安装日志、驱动结果、版本/启动记录和 first-boot.png 位于 D:/Develop/Android/logs。模拟器保持打开。
- 限制与下一步：安装步骤本身仅验证 Android 主页启动；后续 BlueLink APK 安装与 B1 验收见上方重构记录，蓝牙/USB 跨端仍需实机。Google 公告 AEHD 于 2026-12-31 结束支持，后续需在提供 WHPX 的 Windows 环境迁移。官方说明：https://developer.android.com/studio/run/emulator-acceleration 。未提交、推送或发布；仅更新当前工作树记录。

- 全 Windows 原型反向核查（2026-09-07，用户明确范围为所有界面）：重新读取 Figma 的148个Windows画板（W01设备15、W02会话10、W03文件5、W04搜索11、W05菜单25、W06弹窗提示23、W07设置34、W08通知2、W09安装23），图片预览另读取两个子组件；逐项关联生产入口、现有实现和证据。检查范围包括页面/弹窗/菜单/空态/异常态/按钮状态，继续后台验收，Android暂停。证据 `.acceptance/windows-reverse-audit-20260907/audit-results.json`：131项关联本轮后台组件图，其余17项保留源码核对和此前组件证据；不将此计为148张完整原生画板全部通过。额外清点文件操作错误、启动器文件不完整、卸载临时助手启动失败等通用错误提示：Figma没有独立画板，沿用官方MessageBox，不宣称像素级原型验收。
- 本轮发现并修复：独立Uninstall.exe的确认入口绕过了SetupUI已调整的组件，故旧“安装器确认已验收”不能推及独立卸载器。新增共享 `SharedUI/InstallerConfirmationDialog.cs`，Burn和独立卸载实际入口都使用同一工厂；560×260、Noto Sans SC、22号标题、标题/页脚分隔线、左取消右确认，保留数据橙色#E88A1A，删除数据红色#F04444且按钮126 DIP。后台直接检查两条实际工厂的保留/删除确认，并覆盖取消安装与覆盖安装提示；仍使用WPF-UI官方模板。
- 卸载失败根因及复验：实际HKCU注册在 `Software\BlueLink.Review`、安装目录D:\BlueLink，旧Review的 `Uninstall.exe.config` 却指向正式版 `Software\BlueLink` 及正式BundleUpgradeCode。Review打包现先写入与MSI/Burn相同的身份，再生成文件清单和指纹；没有放宽MSI GUID和安装目录校验。只读调用生产ReadRegistration复现旧配置失败、修正配置成功、错误目录仍拒绝，退出码1/0/1；未写真实注册表、未关闭应用或执行卸载。已装旧版文件未自动改动，修复在新包中，真实覆盖安装/卸载仍待用户验收。
- 反查补修交互细节：扫描悬停/按下、连接按下此前只有默认样式；使用官方MouseOverBackground/PressedBackground/PressedBorderBrush/PressedForeground属性与明暗主题资源恢复原型反馈，保留布局自适应。文件搜索悬停恢复1.5 DIP、#1677FF边框、14号文字与22 DIP图标，并保证内容不越界。回归先复现旧扫描按下错误，再检查实际ContentBorder颜色；新分组/空态/连接成功失败/历史/按下夹具全部不创建HWND、不发送物理输入。
- 安装故障修复（2026-09-07，用户19:54实际安装失败）：Burn/MSI日志确认新MSI成功写入后，旧同版本MSI被旧bundle卸载，删除了共用根目录启动器、卸载器、清单及Software\BlueLink.Review注册。根因是Review每次生成新ProductCode/根组件GUID却沿用0.2.17，原MajorUpgrade未包含相同版本，且采用先写新文件后卸旧程序顺序。Review改为AllowSameVersionUpgrades=yes、afterInstallInitialize，在可回滚事务内先移除旧MSI再写新版；正式发布默认升级策略及发布锁未放宽。保持安装后精确ProductCode和全量payload哈希校验。
- 半安装恢复入口：将当前包的归属清单嵌入BA，安装位置页、静默入口和最终安装入口统一校验；仅当Windows Installer按本产品UpgradeCode返回的注册路径精确匹配，且现存文件均在清单中、无外来文件/重解析路径、产品身份有效时允许补齐缺失文件。未知目录、错误路径、无注册、损坏清单和异物仍拒绝。真实D:\BlueLink只读检查确认符合恢复条件（exit 0），未手动写注册或改动已装文件。
- 真实静默安装验证 `.acceptance/install-repeat-20260907-03/results.json`：使用独立InstallRepeat产品族、独立组件GUID、工作树内安装目录，禁用快捷方式/应用自启动、不启动客户端。已通过旧基线首次安装、原故障复现1603、半安装恢复、同包重复修复、注入MSI错误后的旧版回滚、卸载与Download保留；逐步检查真实MSI状态、注册、每个文件哈希及用户目录未变。杀毒软件拦截Burn临时RunOnce续作注册，首次阻断重试一次，后续再次阻断且用户反馈持续提示后停止所有安装执行；正常旧包直升/新版全新安装两组未完成，不计完整矩阵通过。只读收尾确认Fixture进程0、测试Burn注册0、测试产品注册/RunOnce残留0，测试目录仅保留Download，真实应用与用户数据未变；未关闭安全防护或修改权限。
- 升级语义参考：[WiX MajorUpgrade官方文档](https://docs.firegiant.com/wix/schema/wxs/majorupgrade/)。验证直接读取MSI InstallExecuteSequence，确认InstallInitialize=1500、RemoveExistingProducts=1501，避免WiX 4反编译错误显示afterInstallFinalize造成误判。
- 本轮最终后台验证：`.acceptance/windows-background/20260907-reverse-audit-final`，2910 checks / 201 PNG；安装界面 `.acceptance/windows-background/20260907-reverse-audit-installer-complete`，102 checks / 36 PNG；两套构建0 warning/0 error，既有行为回归和官方控件/安装器契约通过。实际托盘悬停、任务栏、显示器DPI、UAC和蓝牙/USB真机仍Not verified；隔离安装执行的已验证项与限制见上文。


### 2026-09-13 Android 图片预览读取原图修复
- 范围与根因：在用户指定的 `E:\.codex\worktrees\dbad\BlueLink` 最新工作目录修改，保留全部既有未提交改动。`ImagePreview` 原先调用缩略图加载函数 `FileInteraction.loadBitmap`，其来源顺序为 `previewUri` 优先，缩略图解码成功即返回，因此即使完成传输且原图存在，全屏仍展示缩略图。
- 有效改动：新增专用 `loadPreviewBitmap` 与 `ImagePreviewDecode` 策略，只从已完成图片附件的 `localUri` 读取原文件，绕开显示缩略图磁盘缓存；原图丢失/损坏显示不可读提示，不回退冒充原图。普通图片保留全部像素；大图按 2 的幂采样，最长边不超过 4096px、ARGB 位图不超过 8M 像素（32 MiB）。预览 bitmap/metadata 的加载键增加原图 URI 与传输状态。聊天缩略图的动态比例、最小边与中心裁切实现未改动。
- 回归：新增 6 项 JVM 策略回归及 debug 独立 QA 场景 `image-preview-original`，后者执行 8 项真实 Android 解码断言（旧缩略图缓存、file/content URI 原图尺寸与像素、原图缺失/未完成/损坏、大图内存、同路径文件替换）。`scripts/test-android-image-preview.py` 通过实际 ConversationScreen 点击进入 ImagePreview，以 UIA、新截图和真实像素检查来源；`original-final` 25 项通过。既有 `test-android-conversation-scroll.py` 的全部 25 项通过，覆盖收发滚底、多图、预览返回所有可见消息位置保持、阅读历史不跳走及设备分组标题稳定。证据位于 `.acceptance/android-image-preview-20260913/`。
- 验收修正：首轮截图断言未读取 Display P3 ICC，导致可见绿色原图误判；已按内嵌 ICC 转 sRGB 再比较。首轮重跑复用既有 QA 预览状态未返回初始页，随后增加 QA 场景初始化。两次失败原始证据分别保留在 `original`、`original-verified`，均未计成功。没有因此修改产品图片颜色或用户数据。
- 构建与边界：最终 `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug` 成功，196 测试通过、0 失败/跳过，lint 0 错误/15 个既有警告。REDMI K80 Pro / Android 16 / `10.33.252.13:36937` 同签名覆盖安装成功，无清数据。超大图采用受限采样，不承诺无限倍率逐像素；全部图片格式、真实跨端重传及独立文件页点击入口本轮 **Not verified**。共享预览加载路径和 file/content 原图解码已验证。未提交、推送、重置、清理；后续仅按新反馈继续。

### 2026-09-13 Android 预览旋转/重置按钮对齐 Windows
- 用户追加范围：仅对齐 Android 图片预览左旋、右旋、重置三个按钮。直接复用 Windows `preview-left.png`、`preview-right.png`、`preview-reset.png` 原有素材，三份 Android 文件与 Windows 来源逐字节/哈希一致；同步更新 `design/figma/android-content-assets.json` 的来源节点与哈希。
- 有效变化：三个按钮使用与 Windows 紧凑工具栏一致的 20dp 图标、8dp 圆角、无描边，以及浅色 `#F5F7FB` / 深色 `#293241` 背景；保留手机 52dp 触控区域、可访问标签和原图不可用时禁用状态。左右各转 90°，重置恢复初始方向、居中和适应视图，沿用既有 Android 交互。
- 验证：最终安装包在浅/深色各完成 19 项真机 UIA/截图/像素断言，真实点击右旋、左旋并检查图像方向与缩放保持，再从旋转且放大的状态重置检查 100% 与方向恢复。`controls-light`、`controls-dark` 保存证据；人工查看两个主题的新截图，无裁切/错色。UIA 显示三个按钮均为相同 146×146px 可点击区域，正常图可用、原图丢失时均禁用。最终全量 196 测试及 lint/build 通过。Windows 代码未变更，其运行时 UI 本轮 **Not verified**；对齐依据是实际现有 Windows XAML、主题与素材。
- 交付：两个修复合并在同签名覆盖安装包 `artifacts/android/BlueLink-0.2.17-android-debug-image-preview-original-controls-20260913.apk`，SHA256 `C2F13A3AA9FCA9F267D68FF54EF1095C178824DFECB6BA3C91AA6EDF5C8F3B3B`。保留之前 5 秒下拉扫描基线及全部既有改动。无提交/推送/清数据；无需更新长期路线图。


## 2026-09-13 Windows 附近设备扫描入口调整

- 范围与决策：按用户同意的方案移除常驻扫描按钮，保留启动扫描设置，设备栏空白处及附近分类标题右键菜单提供“扫描附近设备”，F5 同步触发；分类标题左键仍折叠。后续按用户要求将菜单最小宽度从 240 收至 190，并删除“右键扫描或按 F5 查找附近设备”提示及对应多语言资源，启动扫描关闭状态只说明已关闭。
- 实现：复用官方 ContextMenu/MenuItem 与现有 ScanAsync；扫描时标题旁显示 ProgressRing/“正在查找…”，禁用重复入口，重复 F5 不重启或延长扫描；保留设备卡片原有菜单、选中会话和消息历史。仅隔离验收场景注入 12 秒发现结果，不改变生产蓝牙扫描实现。
- 验证：198 项原生后台会话/UIA 检查、37 张截图通过，覆盖菜单/F5、重复请求、完成恢复、设置页和蓝牙不可用状态。桌面真实右键、点击扫描、F5、标题折叠及卡片菜单已核对；最终菜单文案/190 宽度和无提示状态见 `.acceptance/windows-device-spacing-20260913/desktop` 13–16，早期扫描交互证据见 `.acceptance/windows-device-refresh-20260913/desktop`。WPF UI 官方组件契约通过。
- 限制：全量离屏布局及独立 workspace 检查在未修改的文件筛选用例 `compact dropdown changes actual results and the hidden status tabs` 失败，未扩大范围修复；不代表全量检查通过，记录见 refresh 目录 `layout.log` / `workspace-isolated.log`。真实蓝牙发现及覆盖安装 **Not verified**；未替换 D:/BlueLink 运行实例。保留 dbad 工作区所有既有改动，无提交或推送。

## 2026-09-13 Windows 设备分类首尾留白与附近卡片尺寸统一

- 根因：每张设备卡片固定底部 Margin 8 与分类末尾 Margin 12/14 叠加，造成最后一张卡片下方留白偏大；附近卡片另用最小高度 74 和 42×42 图标底板，其他分类为 60 和 34×34，虽然 Padding 都为 10，视觉内留白仍不同。
- 修改：三个设备列表将 8 DIP 卡片间距放到非首项上方，移除卡片自身尾部 Margin；有设备且展开时分类底部 Margin 统一为 4，与内容顶部一致，空分类/折叠分类沿用原有间距。附近卡片最小高度改为 60、图标底板改为 34×34、文字起始位置与其他分类对齐；连接按钮和连接中状态保留。
- 验证：最终 Release 构建 0 错误，4 条 NU1900 警告来自无法访问 NuGet 漏洞信息源。最终设备专项离屏验收 2414 checks / 61 PNG 通过，覆盖简中/繁中/英文、浅深色、1000×600/1180×720/1600×1000、扫描/折叠/连接状态；实际几何检查确认三类卡片 60 DIP 高、上下留白相等、卡片之间保留原来的 10 DIP 可见距离（8 DIP 间距加官方容器内侧留白）。真实桌面 UIA 与截图确认两张已连接、一张离线、一张附近卡片均衡且连接按钮完整，最终证据 `desktop/17-unified-card-padding`。
- 交付与限制：证据与任务前备份位于 `.acceptance/windows-device-spacing-20260913`，最终日志 `build-card-final.log` / `layout-card-final.log`；验收隔离实例已关闭。只修改已授权设备栏组件及相应验收，不改业务连接协议；未打安装包、未覆盖安装、未提交/推送。上述文件筛选全量检查失败仍单独保留，后续仅在授权后处理；无长期方向变化，不更新路线图。


## 2026-09-13 Android 会话搜索入口迁移

- 用户要求：移除会话页顶部搜索按钮，将该页底部抽屉中的“打开会话”替换为“搜索聊天记录”。范围仅 Android 会话入口、共用设备抽屉的场景参数、三种语言资源与隔离验收接线。
- 实现：会话抽屉传入搜索回调，复用搜索图标，先关闭抽屉再进入既有 MessageSearchScreen；搜索请求按当前设备消费，搜索时消息可见性回调保持隐藏。设备列表长按抽屉仍保留“打开会话”；消息/文件会话页共用同一搜索入口，既有筛选与结果定位逻辑未变。
- 构建验证：`:app:assembleDebug :app:testDebugUnitTest :app:lintDebug` 使用已有缓存成功；196 项单元测试通过，lint 0 错误、15 警告。任务前备份、限定差异和日志在 `.acceptance/android-chat-search-menu-20260913`。
- 真机验收：用户明确允许安装后，在 Redmi 24122RKC7C（序列号 ccc032bc，无线 ADB 10.33.252.13:36937）同签名保留数据覆盖安装成功。简中浅色、英文深色、繁中浅色共 21 项 UIA/截图检查通过，覆盖列表保留打开会话、顶部搜索移除、抽屉替换、打开/关闭搜索、重复打开和系统返回。实际 MainActivity 的 Windows PC 离线会话另完成 4 项入口/搜索返回检查，已人工核对三个语言菜单及实际会话菜单截图，无裁切；当前返回正常会话页，未清消息或文件。
- 产物：`artifacts/android/BlueLink-0.2.17-android-debug-chat-search-menu-20260913.apk`，SHA256 `8f9e4710464811e1acad7812a488dce114a49a38b2d672980f9704d3d3addf41`，与此前图片预览安装包同签名。安装最初因自动审批要求明确设备及 APK 授权而暂停；用户授权后已完成，阻塞解除。
- 限制与决策：本次真机搜索使用空历史，不额外宣称非空搜索结果定位已复验；其实现未变。保留全部既有修改，无提交/推送，不更新长期路线图。


## 2026-09-13 Windows 文件管理入口移到常驻会话区域 header

- 用户要求：移除顶栏文件夹入口，未选中会话时也保留会话区域 header，可从其中“文件”进入文件管理。
- 修改：删除 TitleBar 的“全部文件”按钮及其无用点击处理器；WorkspaceHeader 不再随会话选择隐藏。空会话占位层由跨三行改为内容行，避免覆盖 header。沿用现有 FilesView_Click：无会话进入全部设备文件，有会话进入当前设备文件；Ctrl+Shift+F 全局入口保留。只涉及 MainWindow.xaml、MainWindow.Home.cs、MainViewModel.Home.cs 和原生验收文件，未改变文件筛选或传输逻辑。
- 验证：最终 Release 构建 0 错误、4 条 NuGet 漏洞信息源不可达 NU1900 警告。原生 UIA 验证中本次 4 项检查通过：文件按钮真实命中且未被占位层挡住、无会话 header/入口可见、点击进入全局文件、切回消息保留入口；已人工查看最终 `conversations-final/workspace-no-selection.png` 和文件页截图。最终运行在较后的剪贴板检查遇到 OpenClipboard 0x800401D0；两次重试又在原生窗口前台保护检查中断，因此不声明最终完整会话回归通过。早期 201 项通过发生于占位层修正前，仅作为中间记录。
- 交付与限制：限定差异、备份及检查输出在 `.acceptance/windows-workspace-header-20260913`。未覆盖 D:/BlueLink 已安装版本，未打安装包、未提交/推送；保留全部既有工作。后续完整后台回归需桌面焦点与剪贴板稳定时再跑，不扩大范围改动其他模块；无需更新路线图。


## 2026-09-13 最新 Windows 安装包打包（workspace-header）

- 用户要求打包最新 Windows 安装包；沿用当前 Review x64 安装身份和 0.2.17 版本，未修改正式发布锁。使用 `scripts/build-windows-review.ps1 -CompileInstaller` 完整发布最新客户端并编译启动器、卸载器、安装界面、MSI 和 Burn EXE；保留旧包与所有既有源码改动。
- 内容包含本轮 Windows 扫描按钮移除/扫描菜单、卡片首尾留白与附近卡片尺寸统一、扫描提示删除、顶部文件入口移除以及未选中会话时保留区域 header（含空状态遮挡修复）。
- 产物目录：`artifacts/windows/review-20260913-workspace-header`。安装 EXE `BlueLink-Review-DO-NOT-DISTRIBUTE.exe`，90,274,527 字节，SHA256 `07E7CDCDB32D924B83AAD8E02AF848EA236E2739469F8D95CC925C4E217C617B`；同时保留 MSI、框架依赖 ZIP、`SHA256SUMS.txt` 和 `build-verification.json`。
- 校验：客户端发布/安装组件/MSI/Burn 均成功，客户端发布有 2 条 NU1900 漏洞源不可达警告；安装目录归属 66 assertions 通过，版本及安装器契约通过。只解包核对 21 个 payload、22 个 MSI/ZIP 文件逐字节哈希、内嵌 MSI/SetupUI/恢复清单、Review 注册身份、卸载配置和实际 MSI 升级表及事务内卸旧顺序，全部通过；内嵌客户端与当前构建一致。证据 `.acceptance/package-20260913-workspace-header`。
- 边界：未签名 Review 包，不内置 .NET 运行库；未执行安装/升级/卸载，实际安装验收 **Not verified**。此处包完整性通过不替代上一任务受剪贴板/前台变化干扰的完整 UI 回归，也不宣称正式签名发布通过。未覆盖 D:/BlueLink、未提交/推送；无需更改长期路线图。

## 2026-09-13 提交并推送当前累计改动

- 用户明确授权将当前成果提交并推送至远程仓库主分支。范围为 dbad 工作树中此前已实施的 Windows、Android、协议、安装器及配套资源/验证脚本/文档；不重新修改业务逻辑，不清理或覆盖既有工作。
- 远程核对：origin 为 shuuuuuang/BlueLink；远程 HEAD 指向 feature/settings-ui-v1，且无 main/master。当前 codex/figma-refactor 与远程默认分支基线均为 a700bae，按默认主分支执行快进推送，不强推、不创建或改名远程分支。
- 提交准备：补充 .gradle-user-home/、__pycache__/、*.py[cod] 忽略规则；构建产物、安装包、验收证据、缓存和本地工具目录沿用忽略规则。保留设计源图、字体许可证及图标原始归档；未发现常见私钥或访问令牌格式。
- 验证：提交前重新执行 UI 契约、安装器契约和 0.2.17 版本检查，全部通过；既有 Android 单元测试/真机验收及 Windows 包完整性校验沿用上方对应任务记录。最终完整 Windows UI 回归和实际安装矩阵仍受对应记录的未验证项限制，不将本次提交视为正式签名发布验收。
- 差异检查：暂存 732 个文件，未纳入安装产物或缓存；标准 diff --check 报告既有许可证/源码中的 7 处尾空格或末尾空行，保留原文件，不为提交任务改写许可证或无关源码；排除这两类空白后的检查通过。
- 决策：从当前工作分支生成一次包含完整累计成果的提交，推送目标为 origin 的默认主分支 feature/settings-ui-v1；推送结果及提交号在任务交付中报告。无长期方向变化，不更新路线图。
